---
title: Semantica di riferimento con i tipi valore
description: "Informazioni sulle funzionalità del linguaggio che riducono al minimo la copia delle strutture in modo sicuro"
author: billwagner
ms.author: wiwagn
ms.date: 11/10/2017
ms.topic: article
ms.prod: .net
ms.technology: devlang-csharp
ms.devlang: csharp
ms.custom: mvc
ms.openlocfilehash: 0c6e44a3e1a1458f4211b66b6d1ef5b4b30cd7c1
ms.sourcegitcommit: 5177d6ae2e9baf026f07ee0631556700a5a193f7
ms.translationtype: HT
ms.contentlocale: it-IT
ms.lasthandoff: 11/28/2017
---
# <a name="reference-semantics-with-value-types"></a><span data-ttu-id="f565e-103">Semantica di riferimento con i tipi valore</span><span class="sxs-lookup"><span data-stu-id="f565e-103">Reference semantics with value types</span></span>

<span data-ttu-id="f565e-104">Un vantaggio dell'uso dei tipi valore è che spesso evitano le allocazioni heap.</span><span class="sxs-lookup"><span data-stu-id="f565e-104">An advantage to using value types is that they often avoid heap allocations.</span></span>
<span data-ttu-id="f565e-105">Lo svantaggio è invece che vengono copiati in base al valore.</span><span class="sxs-lookup"><span data-stu-id="f565e-105">The corresponding disadvantage is that they are copied by value.</span></span> <span data-ttu-id="f565e-106">Risulta quindi più difficile ottimizzare gli algoritmi che operano su grandi quantità di dati.</span><span class="sxs-lookup"><span data-stu-id="f565e-106">This tradeoff makes it harder to optimize algorithms that operate on large amounts of data.</span></span> <span data-ttu-id="f565e-107">Le nuove funzionalità del linguaggio C# 7.2 forniscono meccanismi che abilitano la semantica pass-by-reference con i tipi valore.</span><span class="sxs-lookup"><span data-stu-id="f565e-107">New language features in C# 7.2 provide mechanisms that enable pass-by-reference semantics with value types.</span></span> <span data-ttu-id="f565e-108">Se si usano queste funzionalità con criterio, è possibile ridurre al minimo sia le allocazioni che le operazioni di copia.</span><span class="sxs-lookup"><span data-stu-id="f565e-108">If you use these features wisely you can minimize both allocations and copy operations.</span></span> <span data-ttu-id="f565e-109">Questo articolo esamina le nuove funzionalità.</span><span class="sxs-lookup"><span data-stu-id="f565e-109">This article explores those new features.</span></span>

<span data-ttu-id="f565e-110">Gran parte del codice di esempio in questo articolo illustra le funzionalità aggiunte in C# 7.2.</span><span class="sxs-lookup"><span data-stu-id="f565e-110">Much of the sample code in this article demonstrates features added in C# 7.2.</span></span> <span data-ttu-id="f565e-111">Per usare tali funzionalità, è necessario configurare il progetto per usare C# 7.2 o versione successiva nel progetto.</span><span class="sxs-lookup"><span data-stu-id="f565e-111">In order to use those features, you have to configure your project to use C# 7.2 or later in your project.</span></span> <span data-ttu-id="f565e-112">È possibile usare Visual Studio per selezionarlo.</span><span class="sxs-lookup"><span data-stu-id="f565e-112">You can use Visual Studio to select it.</span></span> <span data-ttu-id="f565e-113">Per ogni progetto, scegliere **Progetto** dal menu, quindi **Proprietà**.</span><span class="sxs-lookup"><span data-stu-id="f565e-113">For each project, select **Project** from the menu, then **Properties**.</span></span> <span data-ttu-id="f565e-114">Nella scheda **Compilazione** scegliere **Avanzate**.</span><span class="sxs-lookup"><span data-stu-id="f565e-114">Select the **Build** tab and click **Advanced**.</span></span> <span data-ttu-id="f565e-115">Da qui è possibile configurare la versione del linguaggio.</span><span class="sxs-lookup"><span data-stu-id="f565e-115">From there, you can configure the language version.</span></span> <span data-ttu-id="f565e-116">Scegliere "7.2" o "più recente".</span><span class="sxs-lookup"><span data-stu-id="f565e-116">Choose either "7.2", or "latest".</span></span>  <span data-ttu-id="f565e-117">Oppure è possibile modificare il file con estensione *csproj* e aggiungere il nodo seguente:</span><span class="sxs-lookup"><span data-stu-id="f565e-117">Or you can edit the *csproj* file and add the following node:</span></span>

```XML
  <PropertyGroup>
    <LangVersion>7.2</LangVersion>
  </PropertyGroup>
```

<span data-ttu-id="f565e-118">È possibile usare "7.2" o "latest" come valore.</span><span class="sxs-lookup"><span data-stu-id="f565e-118">You can use either "7.2" or "latest" for the value.</span></span>

## <a name="specifying-in-parameters"></a><span data-ttu-id="f565e-119">Specifica dei parametri `in`</span><span class="sxs-lookup"><span data-stu-id="f565e-119">Specifying `in` parameters</span></span>

<span data-ttu-id="f565e-120">In C# 7.2 è stata aggiunta la parola chiave `in` a complemento delle parole chiave `ref` e `out` esistenti quando si scrive un metodo che passa gli argomenti per riferimento.</span><span class="sxs-lookup"><span data-stu-id="f565e-120">C# 7.2 adds the `in` keyword to complement the existing `ref` and `out` keywords when you write a method that passes arguments by reference.</span></span> <span data-ttu-id="f565e-121">La parola chiave `in` specifica che si sta passando il parametro per riferimento e che il metodo chiamato non modifica il valore passato.</span><span class="sxs-lookup"><span data-stu-id="f565e-121">The `in` keyword specifies that you are passing the parameter by reference and the called method does not modify the value passed to it.</span></span> 

<span data-ttu-id="f565e-122">Questa aggiunta fornisce un vocabolario completo per esprimere le finalità di progettazione.</span><span class="sxs-lookup"><span data-stu-id="f565e-122">This addition provides a full vocabulary to express your design intent.</span></span> <span data-ttu-id="f565e-123">I tipi valore vengono copiati quando sono passati a un metodo denominato se non si specifica nessuno dei modificatori seguenti.</span><span class="sxs-lookup"><span data-stu-id="f565e-123">Value types are copied when passed to a called method when you do not specify any of the following modifiers.</span></span> <span data-ttu-id="f565e-124">Ogni modificatore specifica che un tipo valore viene passato per riferimento, evitando la copia.</span><span class="sxs-lookup"><span data-stu-id="f565e-124">Each of these modifiers specify that a value type is passed by reference, avoiding the copy.</span></span> <span data-ttu-id="f565e-125">Ogni modificatore esprime una finalità diversa:</span><span class="sxs-lookup"><span data-stu-id="f565e-125">Each modifier expresses a different intent:</span></span>

- <span data-ttu-id="f565e-126">`out`: questo metodo imposta il valore dell'argomento usato come parametro.</span><span class="sxs-lookup"><span data-stu-id="f565e-126">`out`: This method sets the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="f565e-127">`ref`: questo metodo può impostare il valore dell'argomento usato come parametro.</span><span class="sxs-lookup"><span data-stu-id="f565e-127">`ref`: This method may set the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="f565e-128">`in`: questo metodo non modifica il valore dell'argomento usato come parametro.</span><span class="sxs-lookup"><span data-stu-id="f565e-128">`in`: This method does not modify the value of the argument used as this parameter.</span></span>

<span data-ttu-id="f565e-129">Quando si aggiunge il modificatore `in` per passare un argomento per riferimento, si dichiara che la finalità della progettazione è quella di passare gli argomenti per riferimento per evitare operazioni di copia non necessarie,</span><span class="sxs-lookup"><span data-stu-id="f565e-129">When you add the `in` modifier to pass an argument by reference, you declare your design intent is to pass arguments by reference to avoid unnecessary copying.</span></span> <span data-ttu-id="f565e-130">non quella di modificare l'oggetto usato come argomento.</span><span class="sxs-lookup"><span data-stu-id="f565e-130">You do not intend to modify the object used as that argument.</span></span> <span data-ttu-id="f565e-131">Il codice seguente illustra un esempio di metodo che calcola la distanza tra due punti nello spazio 3D.</span><span class="sxs-lookup"><span data-stu-id="f565e-131">The following code shows an example of a method that calculates the distance between two points in 3D space.</span></span> 

[!code-csharp[InArgument](../../samples/csharp/reference-semantics/Program.cs#InArgument "Specifying an In argument")]

<span data-ttu-id="f565e-132">Gli argomenti sono due strutture, contenenti ognuna tre valori double.</span><span class="sxs-lookup"><span data-stu-id="f565e-132">The arguments are two structures that each contain three doubles.</span></span> <span data-ttu-id="f565e-133">Un valore double è pari a 8 byte, quindi ogni argomento è pari a 24 byte.</span><span class="sxs-lookup"><span data-stu-id="f565e-133">A double is 8 bytes, so each argument is 24 bytes.</span></span> <span data-ttu-id="f565e-134">Specificando il modificatore `in`, si passa un riferimento a 4 byte o 8 byte a tali argomenti, a seconda dell'architettura del computer.</span><span class="sxs-lookup"><span data-stu-id="f565e-134">By specifying the `in` modifier, you pass 4-byte or 8-byte reference to those arguments, depending on the architecture of the machine.</span></span> <span data-ttu-id="f565e-135">La differenza a livello di dimensioni è piccola, ma può aumentare rapidamente quando l'applicazione chiama questo metodo in un ciclo ridotto usando più valori diversi.</span><span class="sxs-lookup"><span data-stu-id="f565e-135">The difference in size is small, but it can quickly add up when your application calls this method in a tight loop using many different values.</span></span>
 
<span data-ttu-id="f565e-136">Il modificatore `in` integra `out` e `ref` anche in altri modi.</span><span class="sxs-lookup"><span data-stu-id="f565e-136">The `in` modifier complements `out` and `ref` in other ways as well.</span></span> <span data-ttu-id="f565e-137">Non è possibile creare overload di un metodo che si distinguono solo per la presenza di `in`, `out` o `ref`.</span><span class="sxs-lookup"><span data-stu-id="f565e-137">You cannot create overloads of a method that differ only in the presence of `in`, `out` or `ref`.</span></span> <span data-ttu-id="f565e-138">Queste nuove regole estendono lo stesso comportamento da sempre definito per i parametri `out` e `ref`.</span><span class="sxs-lookup"><span data-stu-id="f565e-138">These new rules extend the same behavior that had always been defined for `out` and `ref` parameters.</span></span>

<span data-ttu-id="f565e-139">Il modificatore `in` può essere applicato a tutti i membri che accettano parametri: metodi, delegati, espressioni lambda, funzioni locali, indicizzatori, operatori.</span><span class="sxs-lookup"><span data-stu-id="f565e-139">The `in` modifier may be applied to any member that takes parameters: methods, delegates, lambdas, local functions, indexers, operators.</span></span>

<span data-ttu-id="f565e-140">Diversamente dagli argomenti `ref` e `out`, è possibile usare valori letterali o costanti per l'argomento in un parametro `in`.</span><span class="sxs-lookup"><span data-stu-id="f565e-140">Unlike `ref` and `out` arguments, you may use literal values or constants for the argument to an `in` parameter.</span></span> <span data-ttu-id="f565e-141">Inoltre, diversamente da un parametro `ref` o `out`, non è necessario applicare il modificatore `in` nel sito di chiamata.</span><span class="sxs-lookup"><span data-stu-id="f565e-141">Also, unlike a `ref` or `out` parameter, you don't need to apply the `in` modifier at the call site.</span></span> <span data-ttu-id="f565e-142">Il codice seguente illustra due esempi di chiamata al metodo `CalculateDistance`.</span><span class="sxs-lookup"><span data-stu-id="f565e-142">The following code shows you two examples of calling the `CalculateDistance` method.</span></span> <span data-ttu-id="f565e-143">Il primo usa due variabili locali passate per riferimento.</span><span class="sxs-lookup"><span data-stu-id="f565e-143">The first uses two local variables passed by reference.</span></span> <span data-ttu-id="f565e-144">Il secondo include una variabile temporanea creata durante la chiamata al metodo.</span><span class="sxs-lookup"><span data-stu-id="f565e-144">The second includes a temporary variable created as part of the method call.</span></span> 

[!code-csharp[UseInArgument](../../samples/csharp/reference-semantics/Program.cs#UseInArgument "Specifying an In argument")]

<span data-ttu-id="f565e-145">Il compilatore assicura che venga applicata la natura di sola lettura di un argomento `in` in diversi modi.</span><span class="sxs-lookup"><span data-stu-id="f565e-145">There are several ways in which the compiler ensures that the read-only nature of an `in` argument is enforced.</span></span>  <span data-ttu-id="f565e-146">Prima di tutto il metodo chiamato non può essere direttamente assegnato a un parametro `in`.</span><span class="sxs-lookup"><span data-stu-id="f565e-146">First of all, the called method can't directly assign to an `in` parameter.</span></span> <span data-ttu-id="f565e-147">Non può essere direttamente assegnato ai campi di un parametro `in`.</span><span class="sxs-lookup"><span data-stu-id="f565e-147">It can't directly assign to any field of an `in` parameter.</span></span> <span data-ttu-id="f565e-148">Non è neppure possibile passare un parametro `in` ai metodi che richiedono il modificatore `ref` o `out`.</span><span class="sxs-lookup"><span data-stu-id="f565e-148">In addition, you cannot pass an `in` parameter to any method demanding the `ref` or `out` modifier.</span></span>
<span data-ttu-id="f565e-149">Il compilatore fa in modo che l'argomento `in` sia una variabile di sola lettura.</span><span class="sxs-lookup"><span data-stu-id="f565e-149">The compiler enforces that the `in` argument is a readonly variable.</span></span> <span data-ttu-id="f565e-150">È possibile chiamare qualsiasi metodo di istanza che usa la semantica pass-by-value.</span><span class="sxs-lookup"><span data-stu-id="f565e-150">You can call any instance method that uses pass-by-value semantics.</span></span> <span data-ttu-id="f565e-151">In tali istanze viene creata una copia del parametro `in`.</span><span class="sxs-lookup"><span data-stu-id="f565e-151">In those instances, a copy of the `in` parameter is created.</span></span> <span data-ttu-id="f565e-152">Poiché il compilatore può creare una variabile temporanea per qualsiasi parametro `in`, è anche possibile specificare i valori predefiniti per qualsiasi parametro `in`.</span><span class="sxs-lookup"><span data-stu-id="f565e-152">Because the compiler can create a temporary variable for any `in` parameter, you can also specify default values for any `in` parameter.</span></span> <span data-ttu-id="f565e-153">Il codice seguente adotta tale comportamento per specificare l'origine (punto 0,0) come valore predefinito per il secondo punto:</span><span class="sxs-lookup"><span data-stu-id="f565e-153">The follow code uses that to specify the origin (point 0,0) as the default value for the second point:</span></span>

[!code-csharp[InArgumentDefault](../../samples/csharp/reference-semantics/Program.cs#InArgumentDefault "Specifying defaults for an in parameter")]

<span data-ttu-id="f565e-154">La designazione del parametro `in` può anche essere usata con i tipi riferimento o i valori numerici predefiniti.</span><span class="sxs-lookup"><span data-stu-id="f565e-154">The `in` parameter designation can also be used with reference types or built in numeric values.</span></span> <span data-ttu-id="f565e-155">Gli eventuali vantaggi in entrambi i casi sono tuttavia minimi.</span><span class="sxs-lookup"><span data-stu-id="f565e-155">However, the benefits in both cases are minimal, if any.</span></span>

## <a name="ref-readonly-returns"></a><span data-ttu-id="f565e-156">Valori restituiti `ref readonly`</span><span class="sxs-lookup"><span data-stu-id="f565e-156">`ref readonly` returns</span></span>

<span data-ttu-id="f565e-157">È anche possibile restituire un tipo valore per riferimento, ma non consentire al chiamante di modificare tale valore.</span><span class="sxs-lookup"><span data-stu-id="f565e-157">You may also want to return a value type by reference, but disallow the caller from modifying that value.</span></span> <span data-ttu-id="f565e-158">Usare il modificatore `ref readonly` per esprimere tale finalità di progettazione.</span><span class="sxs-lookup"><span data-stu-id="f565e-158">Use the `ref readonly` modifier to express that design intent.</span></span> <span data-ttu-id="f565e-159">Notifica ai lettori che si restituirà un riferimento ai dati esistenti, ma non si consentirà la modifica.</span><span class="sxs-lookup"><span data-stu-id="f565e-159">It notifies readers that you are returning a reference to existing data, but not allowing modification.</span></span> 

<span data-ttu-id="f565e-160">Il compilatore fa in modo che il chiamante non possa modificare il riferimento.</span><span class="sxs-lookup"><span data-stu-id="f565e-160">The compiler enforces that the caller cannot modify the reference.</span></span> <span data-ttu-id="f565e-161">I tentativi di assegnazione diretta al valore generano un errore in fase di compilazione.</span><span class="sxs-lookup"><span data-stu-id="f565e-161">Attempts to assign to the value directly generate a compile-time error.</span></span> <span data-ttu-id="f565e-162">Il compilatore non può tuttavia sapere se un metodo del membro modifica lo stato dello struct.</span><span class="sxs-lookup"><span data-stu-id="f565e-162">However, the compiler cannot know if any member method modifies the state of the struct.</span></span>
<span data-ttu-id="f565e-163">Per assicurarsi che l'oggetto non venga modificato, il compilatore crea una copia e chiama i riferimenti al membro usando tale copia.</span><span class="sxs-lookup"><span data-stu-id="f565e-163">To ensure that the object is not modified, the compiler creates a copy and calls member references using that copy.</span></span> <span data-ttu-id="f565e-164">Tutte le modifiche vengono apportate a tale copia difensiva.</span><span class="sxs-lookup"><span data-stu-id="f565e-164">Any modifications are to that defensive copy.</span></span> 

<span data-ttu-id="f565e-165">È probabile che la libreria che usa `Point3D` usi spesso l'origine in tutto il codice.</span><span class="sxs-lookup"><span data-stu-id="f565e-165">It's likely that the library using `Point3D` would often use the origin throughout the code.</span></span> <span data-ttu-id="f565e-166">Ogni istanza crea un nuovo oggetto nello stack.</span><span class="sxs-lookup"><span data-stu-id="f565e-166">Every instance creates a new object on the stack.</span></span> <span data-ttu-id="f565e-167">Può essere vantaggioso creare una costante e usarla per riferimento,</span><span class="sxs-lookup"><span data-stu-id="f565e-167">It may be advantageous to create a constant and return it by reference.</span></span> <span data-ttu-id="f565e-168">ma, se si restituisce un riferimento alla risorsa di archiviazione interna, è possibile fare in modo che il chiamante non possa modificare la risorsa di archiviazione di riferimento.</span><span class="sxs-lookup"><span data-stu-id="f565e-168">But, if you return a reference to internal storage, you may want to enforce that the caller cannot modify the referenced storage.</span></span> <span data-ttu-id="f565e-169">Il codice seguente definisce una proprietà di sola lettura che restituisce `readonly ref` a un oggetto `Point3D` che specifica l'origine.</span><span class="sxs-lookup"><span data-stu-id="f565e-169">The following code defines a read-only property that returns a `readonly ref` to a `Point3D` that specifies the origin.</span></span>

[!code-csharp[OriginReference](../../samples/csharp/reference-semantics/Point3D.cs#OriginReference "Creating a readonly Origin reference")]

<span data-ttu-id="f565e-170">Per creare una copia di un valore restituito ref readonly, è sufficiente assegnarlo a una variabile non dichiarata con il modificatore `ref readonly`.</span><span class="sxs-lookup"><span data-stu-id="f565e-170">Creating a copy of a ref readonly return is easy: Just assign it to a variable not declared with the `ref readonly` modifier.</span></span> <span data-ttu-id="f565e-171">Il compilatore genera il codice per copiare l'oggetto durante l'assegnazione.</span><span class="sxs-lookup"><span data-stu-id="f565e-171">The compiler generates code to copy the object as part of the assignment.</span></span> 

<span data-ttu-id="f565e-172">Quando si assegna una variabile a `ref readonly return`, è possibile specificare una variabile `ref readonly` o una copia per valore del riferimento di sola lettura:</span><span class="sxs-lookup"><span data-stu-id="f565e-172">When you assign a variable to a `ref readonly return`, you can specify either a `ref readonly` variable, or a by-value copy of the readonly reference:</span></span>

[!code-csharp[AssignRefReadonly](../../samples/csharp/reference-semantics/Program.cs#AssignRefReadonly "Assigning a ref readonly")]

<span data-ttu-id="f565e-173">La prima assegnazione del codice precedente crea una copia della costante `Origin` e assegna tale copia.</span><span class="sxs-lookup"><span data-stu-id="f565e-173">The first assignment in the preceding code makes a copy of the `Origin` constant and assigns that copy.</span></span> <span data-ttu-id="f565e-174">La seconda assegna un riferimento.</span><span class="sxs-lookup"><span data-stu-id="f565e-174">The second assigns a reference.</span></span> <span data-ttu-id="f565e-175">Si noti che il modificatore `readonly` deve fare parte della dichiarazione della variabile.</span><span class="sxs-lookup"><span data-stu-id="f565e-175">Notice that the `readonly` modifier must be part of the declaration of the variable.</span></span> <span data-ttu-id="f565e-176">Il riferimento a cui viene fatto riferimento non può essere modificato.</span><span class="sxs-lookup"><span data-stu-id="f565e-176">The reference to which it refers cannot be modified.</span></span> <span data-ttu-id="f565e-177">I tentativi di eseguire questa operazione generano un errore in fase di compilazione.</span><span class="sxs-lookup"><span data-stu-id="f565e-177">Attempts to do so result in a compile-time error.</span></span>

## <a name="readonly-struct-type"></a><span data-ttu-id="f565e-178">Tipo `readonly struct`</span><span class="sxs-lookup"><span data-stu-id="f565e-178">`readonly struct` type</span></span>

<span data-ttu-id="f565e-179">Applicare `ref readonly` agli usi a traffico elevato di uno struct può essere sufficiente.</span><span class="sxs-lookup"><span data-stu-id="f565e-179">Applying `ref readonly` to high-traffic uses of a struct may be sufficient.</span></span>
<span data-ttu-id="f565e-180">In altri casi, è possibile creare uno struct non modificabile.</span><span class="sxs-lookup"><span data-stu-id="f565e-180">Other times, you may want to create an immutable struct.</span></span> <span data-ttu-id="f565e-181">È quindi sempre possibile eseguire il passaggio per riferimento di sola lettura.</span><span class="sxs-lookup"><span data-stu-id="f565e-181">Then you can always pass by readonly reference.</span></span> <span data-ttu-id="f565e-182">Tale procedura rimuove le copie difensive che vengono eseguite quando si accede ai metodi di uno struct usato come parametro `in`.</span><span class="sxs-lookup"><span data-stu-id="f565e-182">That practice removes the defensive copies that take place when you access methods of a struct used as an `in` parameter.</span></span>

<span data-ttu-id="f565e-183">A tale scopo, è possibile creare un tipo `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="f565e-183">You can do that by creating a `readonly struct` type.</span></span> <span data-ttu-id="f565e-184">È possibile aggiungere il modificatore `readonly` a una dichiarazione di struct.</span><span class="sxs-lookup"><span data-stu-id="f565e-184">You can add the `readonly` modifier to a struct declaration.</span></span> <span data-ttu-id="f565e-185">Il compilatore fa in modo che tutti i membri di istanza dello struct siano `readonly`. `struct` deve essere non modificabile.</span><span class="sxs-lookup"><span data-stu-id="f565e-185">The compiler enforces that all instance members of the struct are `readonly`; the `struct` must be immutable.</span></span>

<span data-ttu-id="f565e-186">Esistono altre ottimizzazioni per `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="f565e-186">There are other optimizations for a `readonly struct`.</span></span> <span data-ttu-id="f565e-187">È possibile usare il modificatore `in` in ogni posizione in cui `readonly struct` è un argomento.</span><span class="sxs-lookup"><span data-stu-id="f565e-187">You can use the `in` modifier at every location where a `readonly struct` is an argument.</span></span> <span data-ttu-id="f565e-188">È anche possibile restituire `readonly struct` come `ref return` quando si restituisce un oggetto la cui durata si estende oltre l'ambito del metodo che restituisce l'oggetto.</span><span class="sxs-lookup"><span data-stu-id="f565e-188">In addition, you can return a `readonly struct` as a `ref return` when you are returning an object whose lifetime extends beyond the scope of the method returning the object.</span></span>

<span data-ttu-id="f565e-189">Il compilatore genera infine un codice più efficiente quando si chiamano i membri di `readonly struct`: il riferimento `this`, invece di una copia del ricevitore, è sempre un parametro `in` passato per riferimento al metodo dei membri.</span><span class="sxs-lookup"><span data-stu-id="f565e-189">Finally, the compiler generates more efficient code when you call members of a `readonly struct`: The `this` reference, instead of a copy of the receiver, is always an `in` parameter passed by reference to the member method.</span></span> <span data-ttu-id="f565e-190">Questa ottimizzazione consente un risparmio a livello di copie quando si usa `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="f565e-190">This optimization saves more copying when you use a `readonly struct`.</span></span> <span data-ttu-id="f565e-191">`Point3D` è un candidato ideale per questa modifica.</span><span class="sxs-lookup"><span data-stu-id="f565e-191">The `Point3D` is a great candidate for this change.</span></span> <span data-ttu-id="f565e-192">Nel codice seguente viene illustrata una struttura `ReadonlyPoint3D` aggiornata:</span><span class="sxs-lookup"><span data-stu-id="f565e-192">The following code shows an updated `ReadonlyPoint3D` structure:</span></span>

[!code-csharp[ReadonlyOnlyPoint3D](../../samples/csharp/reference-semantics/Point3D.cs#ReadonlyOnlyPoint3D "Defining an immutable structure")]

## <a name="ref-struct-type"></a><span data-ttu-id="f565e-193">Tipo `ref struct`</span><span class="sxs-lookup"><span data-stu-id="f565e-193">`ref struct` type</span></span>

<span data-ttu-id="f565e-194">Un'altra funzionalità del linguaggio correlata è la possibilità di dichiarare un tipo valore che deve essere allocato nello stack.</span><span class="sxs-lookup"><span data-stu-id="f565e-194">Another related language feature is the ability to declare a value type that must be stack allocated.</span></span> <span data-ttu-id="f565e-195">In altre parole, questi tipi non possono mai essere creati nell'heap come membro di un'altra classe.</span><span class="sxs-lookup"><span data-stu-id="f565e-195">In other words, these types can never be created on the heap as a member of another class.</span></span> <span data-ttu-id="f565e-196">La principale motivazione di questa funzionalità sono stati <xref:System.Span%601> e le strutture correlate.</span><span class="sxs-lookup"><span data-stu-id="f565e-196">The primary motivation for this feature was <xref:System.Span%601> and related structures.</span></span> <span data-ttu-id="f565e-197"><xref:System.Span%601> può contenere un puntatore gestito come uno dei membri, mentre l'altro è la lunghezza dell'intervallo.</span><span class="sxs-lookup"><span data-stu-id="f565e-197"><xref:System.Span%601> may contain a managed pointer as one of its members, the other being the length of the span.</span></span> <span data-ttu-id="f565e-198">In realtà l'implementazione è leggermente diversa perché C# non supporta i puntatori alla memoria gestita al di fuori di un contesto non sicuro.</span><span class="sxs-lookup"><span data-stu-id="f565e-198">It's actually implemented a bit differently because C# doesn't support pointers to managed memory outside of an unsafe context.</span></span> <span data-ttu-id="f565e-199">Le operazioni di scrittura che modificano il puntatore e la lunghezza non sono atomiche.</span><span class="sxs-lookup"><span data-stu-id="f565e-199">Any write that changes the pointer and the length is not atomic.</span></span> <span data-ttu-id="f565e-200">Per questo <xref:System.Span%601> sarà soggetto a errori non compresi nell'intervallo o ad altre violazioni dell'indipendenza dai tipi se non è vincolato a un singolo stack frame.</span><span class="sxs-lookup"><span data-stu-id="f565e-200">That means a <xref:System.Span%601> would be subject to out of range errors or other type safety violations were it not constrained to a single stack frame.</span></span> <span data-ttu-id="f565e-201">Inoltre l'inserimento di un puntatore gestito nell'heap GC provoca in genere un arresto anomalo del sistema in fase JIT.</span><span class="sxs-lookup"><span data-stu-id="f565e-201">In addition, putting a managed pointer on the GC heap typically crashes at JIT time.</span></span>

<span data-ttu-id="f565e-202">Possono esistere requisiti simili quando si usa la memoria creata con [`stackalloc`](language-reference/keywords/stackalloc.md) o quando si usa la memoria delle API di interoperabilità.</span><span class="sxs-lookup"><span data-stu-id="f565e-202">You may have similar requirements working with memory created using [`stackalloc`](language-reference/keywords/stackalloc.md) or when using memory from interop APIs.</span></span> <span data-ttu-id="f565e-203">È possibile definire i tipi `ref struct` in base a tali esigenze.</span><span class="sxs-lookup"><span data-stu-id="f565e-203">You can define your own `ref struct` types for those needs.</span></span> <span data-ttu-id="f565e-204">In questo articolo vengono illustrati esempi che usano `Span<T>` per semplicità.</span><span class="sxs-lookup"><span data-stu-id="f565e-204">In this article, you see examples using `Span<T>` for simplicity.</span></span>

<span data-ttu-id="f565e-205">La dichiarazione `ref struct` dichiara che uno struct di questo tipo deve essere nello stack.</span><span class="sxs-lookup"><span data-stu-id="f565e-205">The `ref struct` declaration declares that a struct of this type must be on the stack.</span></span> <span data-ttu-id="f565e-206">Le regole del linguaggio garantiscono l'uso sicuro di questi tipi.</span><span class="sxs-lookup"><span data-stu-id="f565e-206">The language rules ensure the safe use of these types.</span></span> <span data-ttu-id="f565e-207">Gli altri tipi dichiarati come `ref struct` includono <xref:System.ReadOnlySpan%601>.</span><span class="sxs-lookup"><span data-stu-id="f565e-207">Other types declared as `ref struct` include <xref:System.ReadOnlySpan%601>.</span></span> 

<span data-ttu-id="f565e-208">L'obiettivo di mantenere un tipo `ref struct` come variabile allocata nello stack comporta diverse regole che il compilatore applica per tutti i tipi `ref struct`.</span><span class="sxs-lookup"><span data-stu-id="f565e-208">The goal of keeping a `ref struct` type as a stack-allocated variable introduces several rules that the compiler enforces for all `ref struct` types.</span></span>

- <span data-ttu-id="f565e-209">Non è possibile eseguire il boxing di `ref struct`.</span><span class="sxs-lookup"><span data-stu-id="f565e-209">You can't box a `ref struct`.</span></span> <span data-ttu-id="f565e-210">Non è possibile assegnare un tipo `ref struct` a una variabile di tipo `object`, `dynamic` o qualsiasi tipo di interfaccia.</span><span class="sxs-lookup"><span data-stu-id="f565e-210">You cannot assign a `ref struct` type to a variable of type `object`, `dynamic`, or any interface type.</span></span>
- <span data-ttu-id="f565e-211">Non è possibile dichiarare `ref struct` come membro di una classe o di un normale struct.</span><span class="sxs-lookup"><span data-stu-id="f565e-211">You can't declare a `ref struct` as a member of a class or a normal struct.</span></span>
- <span data-ttu-id="f565e-212">Non è possibile dichiarare variabili locali che sono tipi `ref struct` nei metodi asincroni.</span><span class="sxs-lookup"><span data-stu-id="f565e-212">You cannot declare local variables that are `ref struct` types in async methods.</span></span> <span data-ttu-id="f565e-213">È possibile dichiararle nei metodi sincroni che restituiscono tipi `Task`, `Task<T>` o simili a Task.</span><span class="sxs-lookup"><span data-stu-id="f565e-213">You can declare them in synchronous methods that return `Task`, `Task<T>` or Task-like types.</span></span>
- <span data-ttu-id="f565e-214">Non è possibile dichiarare variabili locali `ref struct` negli iteratori.</span><span class="sxs-lookup"><span data-stu-id="f565e-214">You cannot declare `ref struct` local variables in iterators.</span></span>
- <span data-ttu-id="f565e-215">Non è possibile acquisire variabili `ref struct` in espressioni lambda o funzioni locali.</span><span class="sxs-lookup"><span data-stu-id="f565e-215">You cannot capture `ref struct` variables in lambda expressions or local functions.</span></span>

<span data-ttu-id="f565e-216">Queste restrizioni garantiscono che non si usi accidentalmente `ref struct` in modo tale che possa essere alzato di livello nell'heap gestito.</span><span class="sxs-lookup"><span data-stu-id="f565e-216">These restrictions ensure that you do not accidentally use a `ref struct` in a manner that could promote it to the managed heap.</span></span>

## <a name="conclusions"></a><span data-ttu-id="f565e-217">Conclusioni</span><span class="sxs-lookup"><span data-stu-id="f565e-217">Conclusions</span></span>

<span data-ttu-id="f565e-218">Questi miglioramenti del linguaggio C# sono progettati per gli algoritmi con prestazioni critiche in cui le allocazioni della memoria possono essere strategiche per raggiungere le prestazioni necessarie.</span><span class="sxs-lookup"><span data-stu-id="f565e-218">These enhancements to the C# language are designed for performance critical algorithms where memory allocations can be critical to achieving the necessary performance.</span></span> <span data-ttu-id="f565e-219">È possibile che non si usino spesso queste funzionalità nella scrittura del codice.</span><span class="sxs-lookup"><span data-stu-id="f565e-219">You may find that you don't often use these features in the code you write.</span></span> <span data-ttu-id="f565e-220">Questi miglioramenti sono stati tuttavia adottati in diverse posizioni di .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="f565e-220">However, these enhancements have been adopted in many locations in the .NET Framework.</span></span> <span data-ttu-id="f565e-221">Il sempre maggior numero di API che useranno queste funzionalità renderà evidente il miglioramento delle prestazioni delle applicazioni.</span><span class="sxs-lookup"><span data-stu-id="f565e-221">As more and more APIs make use of these features, you'll see the performance of your own applications improve.</span></span>
