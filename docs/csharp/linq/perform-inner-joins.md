---
title: Eseguire inner join
description: Come eseguire degli inner join.
keywords: .NET, .NET Core, C#
author: BillWagner
manager: wpickett
ms.author: wiwagn
ms.date: 12/1/2016
ms.topic: article
ms.prod: .net
ms.technology: devlang-csharp
ms.assetid: 45bceed6-f549-4114-a9b1-b44feb497742
ms.openlocfilehash: fdf75c0b7195742bdce70566ebb3880bb0565f31
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: HT
ms.contentlocale: it-IT
ms.lasthandoff: 11/21/2017
---
# <a name="perform-inner-joins"></a><span data-ttu-id="59dd2-104">Eseguire inner join</span><span class="sxs-lookup"><span data-stu-id="59dd2-104">Perform inner joins</span></span>

<span data-ttu-id="59dd2-105">In termini di database relazionale, un *inner join* produce un set di risultati in cui ogni elemento della prima raccolta viene visualizzato una volta per ogni elemento corrispondente nella seconda raccolta.</span><span class="sxs-lookup"><span data-stu-id="59dd2-105">In relational database terms, an *inner join* produces a result set in which each element of the first collection appears one time for every matching element in the second collection.</span></span> <span data-ttu-id="59dd2-106">Se un elemento nella prima raccolta non ha corrispondenti, non viene visualizzato nel set di risultati.</span><span class="sxs-lookup"><span data-stu-id="59dd2-106">If an element in the first collection has no matching elements, it does not appear in the result set.</span></span> <span data-ttu-id="59dd2-107">Il metodo <xref:System.Linq.Enumerable.Join%2A>, chiamato dalla clausola `join` in C#, implementa un inner join.</span><span class="sxs-lookup"><span data-stu-id="59dd2-107">The <xref:System.Linq.Enumerable.Join%2A> method, which is called by the `join` clause in C#, implements an inner join.</span></span>  
  
 <span data-ttu-id="59dd2-108">In questo argomento viene illustrato come eseguire quattro varianti di un inner join:</span><span class="sxs-lookup"><span data-stu-id="59dd2-108">This topic shows you how to perform four variations of an inner join:</span></span>  
  
-   <span data-ttu-id="59dd2-109">Un inner join semplice che correla gli elementi di due origini dati in base a una chiave semplice.</span><span class="sxs-lookup"><span data-stu-id="59dd2-109">A simple inner join that correlates elements from two data sources based on a simple key.</span></span>  
  
-   <span data-ttu-id="59dd2-110">Un inner join che correla gli elementi di due origini dati in base a una chiave *composta*.</span><span class="sxs-lookup"><span data-stu-id="59dd2-110">An inner join that correlates elements from two data sources based on a *composite* key.</span></span> <span data-ttu-id="59dd2-111">Una chiave composta, che è una chiave costituita da più di un valore, consente di correlare gli elementi in base a più di una proprietà.</span><span class="sxs-lookup"><span data-stu-id="59dd2-111">A composite key, which is a key that consists of more than one value, enables you to correlate elements based on more than one property.</span></span>  
  
-   <span data-ttu-id="59dd2-112">Un *join multiplo* in cui le operazioni di join successive vengono aggiunte l'una all'altra.</span><span class="sxs-lookup"><span data-stu-id="59dd2-112">A *multiple join* in which successive join operations are appended to each other.</span></span>  
  
-   <span data-ttu-id="59dd2-113">Un inner join che viene implementato usando un group join.</span><span class="sxs-lookup"><span data-stu-id="59dd2-113">An inner join that is implemented by using a group join.</span></span>  
  
## <a name="example"></a><span data-ttu-id="59dd2-114">Esempio</span><span class="sxs-lookup"><span data-stu-id="59dd2-114">Example</span></span>  
  
## <a name="simple-key-join-example"></a><span data-ttu-id="59dd2-115">Esempio di join con chiave semplice</span><span class="sxs-lookup"><span data-stu-id="59dd2-115">Simple key join example</span></span>  
 <span data-ttu-id="59dd2-116">Nell'esempio seguente vengono create due raccolte che contengono oggetti di due tipi definiti dall'utente, `Person` e `Pet`.</span><span class="sxs-lookup"><span data-stu-id="59dd2-116">The following example creates two collections that contain objects of two user-defined types, `Person` and `Pet`.</span></span> <span data-ttu-id="59dd2-117">La query usa la clausola `join` in C# per la corrispondenza degli oggetti `Person` con gli oggetti `Pet` per il cui il valore di `Owner` è `Person`.</span><span class="sxs-lookup"><span data-stu-id="59dd2-117">The query uses the `join` clause in C# to match `Person` objects with `Pet` objects whose `Owner` is that `Person`.</span></span> <span data-ttu-id="59dd2-118">La clausola `select` in C# definisce l'aspetto degli oggetti risultanti.</span><span class="sxs-lookup"><span data-stu-id="59dd2-118">The `select` clause in C# defines how the resulting objects will look.</span></span> <span data-ttu-id="59dd2-119">In questo esempio gli oggetti risultanti sono tipi anonimi costituiti dal nome del proprietario (owner) e dal nome dell'animale domestico (pet).</span><span class="sxs-lookup"><span data-stu-id="59dd2-119">In this example the resulting objects are anonymous types that consist of the owner's first name and the pet's name.</span></span>  
  
 [!code-csharp[CsLINQProgJoining#1](../../../samples/snippets/csharp/concepts/linq/how-to-perform-inner-joins_1.cs)]  
  
 <span data-ttu-id="59dd2-120">Notare che l'oggetto `Person` il cui valore `LastName` è "Huff" non viene visualizzato nel set di risultati perché non esiste un oggetto `Pet` con `Pet.Owner` uguale a `Person`.</span><span class="sxs-lookup"><span data-stu-id="59dd2-120">Note that the `Person` object whose `LastName` is "Huff" does not appear in the result set because there is no `Pet` object that has `Pet.Owner` equal to that `Person`.</span></span>  
  
## <a name="example"></a><span data-ttu-id="59dd2-121">Esempio</span><span class="sxs-lookup"><span data-stu-id="59dd2-121">Example</span></span>  
  
## <a name="composite-key-join-example"></a><span data-ttu-id="59dd2-122">Esempio di join con chiave composta</span><span class="sxs-lookup"><span data-stu-id="59dd2-122">Composite key join example</span></span>  
 <span data-ttu-id="59dd2-123">Anziché correlare gli elementi in base a una sola proprietà, è possibile usare una chiave composta per confrontare gli elementi in base a più proprietà.</span><span class="sxs-lookup"><span data-stu-id="59dd2-123">Instead of correlating elements based on just one property, you can use a composite key to compare elements based on multiple properties.</span></span> <span data-ttu-id="59dd2-124">Per eseguire questa operazione, specificare la funzione del selettore di chiave per ogni raccolta in modo da restituire un tipo anonimo che include le proprietà da confrontare.</span><span class="sxs-lookup"><span data-stu-id="59dd2-124">To do this, specify the key selector function for each collection to return an anonymous type that consists of the properties you want to compare.</span></span> <span data-ttu-id="59dd2-125">Se si applicano etichette alle proprietà, l'etichetta deve essere la stessa in ogni tipo anonimo della chiave.</span><span class="sxs-lookup"><span data-stu-id="59dd2-125">If you label the properties, they must have the same label in each key's anonymous type.</span></span> <span data-ttu-id="59dd2-126">Le proprietà devono inoltre apparire nello stesso ordine.</span><span class="sxs-lookup"><span data-stu-id="59dd2-126">The properties must also appear in the same order.</span></span>  
  
 <span data-ttu-id="59dd2-127">L'esempio seguente usa un elenco di oggetti `Employee` e un elenco di oggetti `Student` per determinare quali dipendenti sono anche studenti.</span><span class="sxs-lookup"><span data-stu-id="59dd2-127">The following example uses a list of `Employee` objects and a list of `Student` objects to determine which employees are also students.</span></span> <span data-ttu-id="59dd2-128">Entrambi questi tipi presentano una proprietà `FirstName` e una proprietà `LastName` di tipo <xref:System.String>.</span><span class="sxs-lookup"><span data-stu-id="59dd2-128">Both of these types have a `FirstName` and a `LastName` property of type <xref:System.String>.</span></span> <span data-ttu-id="59dd2-129">Le funzioni che creano le chiavi di join dagli elementi di ogni elenco restituiscono un tipo anonimo costituito dalle proprietà `FirstName` e `LastName` di ogni elemento.</span><span class="sxs-lookup"><span data-stu-id="59dd2-129">The functions that create the join keys from each list's elements return an anonymous type that consists of the `FirstName` and `LastName` properties of each element.</span></span> <span data-ttu-id="59dd2-130">L'operazione di join confronta le chiavi composte per verificarne l'uguaglianza e restituisce coppie di oggetti da ogni elenco in cui sia il nome che il cognome corrispondono.</span><span class="sxs-lookup"><span data-stu-id="59dd2-130">The join operation compares these composite keys for equality and returns pairs of objects from each list where both the first name and the last name match.</span></span>  
  
 [!code-csharp[CsLINQProgJoining#2](../../../samples/snippets/csharp/concepts/linq/how-to-perform-inner-joins_2.cs)]  
  
## <a name="example"></a><span data-ttu-id="59dd2-131">Esempio</span><span class="sxs-lookup"><span data-stu-id="59dd2-131">Example</span></span>  
  
## <a name="multiple-join-example"></a><span data-ttu-id="59dd2-132">Esempio di join multiplo</span><span class="sxs-lookup"><span data-stu-id="59dd2-132">Multiple join example</span></span>  
 <span data-ttu-id="59dd2-133">È possibile collegare tra loro qualsiasi numero di operazioni di join per eseguire un join multiplo.</span><span class="sxs-lookup"><span data-stu-id="59dd2-133">Any number of join operations can be appended to each other to perform a multiple join.</span></span> <span data-ttu-id="59dd2-134">Ogni clausola `join` in C# consente di correlare un'origine dati specificata con i risultati del join precedente.</span><span class="sxs-lookup"><span data-stu-id="59dd2-134">Each `join` clause in C# correlates a specified data source with the results of the previous join.</span></span>  
  
 <span data-ttu-id="59dd2-135">Il seguente esempio crea tre raccolte: un elenco di oggetti `Person`, un elenco di oggetti `Cat` e un elenco di oggetti `Dog`.</span><span class="sxs-lookup"><span data-stu-id="59dd2-135">The following example creates three collections: a list of `Person` objects, a list of `Cat` objects, and a list of `Dog` objects.</span></span>  
  
 <span data-ttu-id="59dd2-136">La prima clausola `join` in C# associa le persone e i gatti in base a un oggetto `Person` corrispondente a `Cat.Owner`.</span><span class="sxs-lookup"><span data-stu-id="59dd2-136">The first `join` clause in C# matches people and cats based on a `Person` object matching `Cat.Owner`.</span></span> <span data-ttu-id="59dd2-137">Restituisce una sequenza di tipi anonimi che contengono l'oggetto `Person` e `Cat.Name`.</span><span class="sxs-lookup"><span data-stu-id="59dd2-137">It returns a sequence of anonymous types that contain the `Person` object and `Cat.Name`.</span></span>  
  
 <span data-ttu-id="59dd2-138">La seconda clausola `join` in C# consente di correlare i tipi anonimi restituiti dal primo join con gli oggetti `Dog` dell'elenco di cani, in base a una chiave costituita dalla proprietà `Owner` del tipo `Person` e dalla prima lettera del nome dell'animale.</span><span class="sxs-lookup"><span data-stu-id="59dd2-138">The second `join` clause in C# correlates the anonymous types returned by the first join with `Dog` objects in the supplied list of dogs, based on a composite key that consists of the `Owner` property of type `Person`, and the first letter of the animal's name.</span></span> <span data-ttu-id="59dd2-139">Restituisce una sequenza di tipi anonimi che contengono le proprietà `Cat.Name` e `Dog.Name` di ogni coppia corrispondente.</span><span class="sxs-lookup"><span data-stu-id="59dd2-139">It returns a sequence of anonymous types that contain the `Cat.Name` and `Dog.Name` properties from each matching pair.</span></span> <span data-ttu-id="59dd2-140">Poiché si tratta di un inner join, vengono restituiti solo gli oggetti della prima origine dati per cui esiste una corrispondenza nella seconda origine dati.</span><span class="sxs-lookup"><span data-stu-id="59dd2-140">Because this is an inner join, only those objects from the first data source that have a match in the second data source are returned.</span></span>  
  
 [!code-csharp[CsLINQProgJoining#3](../../../samples/snippets/csharp/concepts/linq/how-to-perform-inner-joins_3.cs)]  
  
## <a name="example"></a><span data-ttu-id="59dd2-141">Esempio</span><span class="sxs-lookup"><span data-stu-id="59dd2-141">Example</span></span>  
  
## <a name="inner-join-by-using-grouped-join-example"></a><span data-ttu-id="59dd2-142">Inner join usando l'esempio con join raggruppati</span><span class="sxs-lookup"><span data-stu-id="59dd2-142">Inner join by using grouped join example</span></span>  
 <span data-ttu-id="59dd2-143">Nell'esempio seguente viene illustrato come implementare un inner join usando un group join.</span><span class="sxs-lookup"><span data-stu-id="59dd2-143">The following example shows you how to implement an inner join by using a group join.</span></span>  
  
 <span data-ttu-id="59dd2-144">In `query1` l'elenco di oggetti `Person` viene collegato con un group join all'elenco di oggetti `Pet` in base all'oggetto `Person` corrispondente alla proprietà `Pet.Owner`.</span><span class="sxs-lookup"><span data-stu-id="59dd2-144">In `query1`, the list of `Person` objects is group-joined to the list of `Pet` objects based on the `Person` matching the `Pet.Owner` property.</span></span> <span data-ttu-id="59dd2-145">Il group join crea una raccolta di gruppi intermedi, dove ogni gruppo è costituito da un oggetto `Person` e una sequenza di oggetti `Pet` corrispondenti.</span><span class="sxs-lookup"><span data-stu-id="59dd2-145">The group join creates a collection of intermediate groups, where each group consists of a `Person` object and a sequence of matching `Pet` objects.</span></span>  
  
 <span data-ttu-id="59dd2-146">Aggiungendo una seconda clausola `from` alla query, questa sequenza di sequenze viene combinata (o appiattita) in un'unica sequenza più lunga.</span><span class="sxs-lookup"><span data-stu-id="59dd2-146">By adding a second `from` clause to the query, this sequence of sequences is combined (or flattened) into one longer sequence.</span></span> <span data-ttu-id="59dd2-147">Il tipo degli elementi della sequenza finale viene specificato dalla clausola `select`.</span><span class="sxs-lookup"><span data-stu-id="59dd2-147">The type of the elements of the final sequence is specified by the `select` clause.</span></span> <span data-ttu-id="59dd2-148">In questo esempio il tipo è un tipo anonimo costituito dalle proprietà `Person.FirstName` e `Pet.Name` per ogni coppia corrispondente.</span><span class="sxs-lookup"><span data-stu-id="59dd2-148">In this example, that type is an anonymous type that consists of the `Person.FirstName` and `Pet.Name` properties for each matching pair.</span></span>  
  
 <span data-ttu-id="59dd2-149">Il risultato di `query1` è equivalente al set di risultati che si ottiene usando la clausola `join` senza la clausola `into` per eseguire un inner join.</span><span class="sxs-lookup"><span data-stu-id="59dd2-149">The result of `query1` is equivalent to the result set that would have been obtained by using the `join` clause without the `into` clause to perform an inner join.</span></span> <span data-ttu-id="59dd2-150">La variabile `query2` dimostra questa query equivalente.</span><span class="sxs-lookup"><span data-stu-id="59dd2-150">The `query2` variable demonstrates this equivalent query.</span></span>  
  
 [!code-csharp[CsLINQProgJoining#4](../../../samples/snippets/csharp/concepts/linq/how-to-perform-inner-joins_4.cs)]  
  
## <a name="see-also"></a><span data-ttu-id="59dd2-151">Vedere anche</span><span class="sxs-lookup"><span data-stu-id="59dd2-151">See also</span></span>  
 <xref:System.Linq.Enumerable.Join%2A>  
 <xref:System.Linq.Enumerable.GroupJoin%2A>  
 [<span data-ttu-id="59dd2-152">Eseguire join raggruppati</span><span class="sxs-lookup"><span data-stu-id="59dd2-152">Perform grouped joins</span></span>](perform-grouped-joins.md)  
 [<span data-ttu-id="59dd2-153">Eseguire left outer join</span><span class="sxs-lookup"><span data-stu-id="59dd2-153">Perform left outer joins</span></span>](perform-left-outer-joins.md)  
 [<span data-ttu-id="59dd2-154">Tipi anonimi</span><span class="sxs-lookup"><span data-stu-id="59dd2-154">Anonymous types</span></span>](../programming-guide/classes-and-structs/anonymous-types.md)  
 
