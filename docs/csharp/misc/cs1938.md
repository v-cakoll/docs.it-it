---
title: Errore del compilatore CS1938
ms.date: 07/20/2015
ms.prod: .net
ms.technology:
- devlang-csharp
ms.topic: article
f1_keywords:
- CS1938
helpviewer_keywords:
- CS1938
ms.assetid: fc8de996-f7a1-46e8-b07b-aea520b391b9
caps.latest.revision: 
author: BillWagner
ms.author: wiwagn
ms.openlocfilehash: a0ec730a377e8f9c362973ef3194f2e279a14a50
ms.sourcegitcommit: 83dd5ec003e788ccb3e33f3412a7af39ae347646
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 03/15/2018
---
# <a name="compiler-error-cs1938"></a><span data-ttu-id="d7a2f-102">Errore del compilatore CS1938</span><span class="sxs-lookup"><span data-stu-id="d7a2f-102">Compiler Error CS1938</span></span>
<span data-ttu-id="d7a2f-103">Il nome 'name' non si trova nell'ambito a destra di 'equals'.</span><span class="sxs-lookup"><span data-stu-id="d7a2f-103">The name 'name' is not in scope on the right side of 'equals'.</span></span> <span data-ttu-id="d7a2f-104">Provare a invertire le espressioni ai lati di 'equals'.</span><span class="sxs-lookup"><span data-stu-id="d7a2f-104">Consider swapping the expressions on either side of 'equals'.</span></span>  
  
 <span data-ttu-id="d7a2f-105">La parola chiave `equals` è un operatore speciale che viene usato in una clausola `join` per determinare l'uguaglianza tra due espressioni.</span><span class="sxs-lookup"><span data-stu-id="d7a2f-105">The `equals` keyword is a special operator that is used in a `join` clause to determine equality between two expressions.</span></span> <span data-ttu-id="d7a2f-106">La variabile di intervallo per la sequenza di origine sul lato sinistro è nell'ambito sul lato sinistro di equals e la variabile di intervallo per l'origine sul lato destro è nell'ambito solo sul lato sinistro di equals.</span><span class="sxs-lookup"><span data-stu-id="d7a2f-106">The range variable for the left side source sequence is in scope on the left side of equals, and the range variable for the right side source is only in scope on the left side of equals.</span></span> <span data-ttu-id="d7a2f-107">È possibile verificarlo con IntelliSense nell'esempio di codice seguente.</span><span class="sxs-lookup"><span data-stu-id="d7a2f-107">You can verify this by experimenting with IntelliSense in the following code example.</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="d7a2f-108">Per correggere l'errore</span><span class="sxs-lookup"><span data-stu-id="d7a2f-108">To correct this error</span></span>  
  
1.  <span data-ttu-id="d7a2f-109">Scambiare la posizione delle due variabili di intervallo come illustrato nella riga di commento nell'esempio seguente:</span><span class="sxs-lookup"><span data-stu-id="d7a2f-109">Swap the position of the two range variables as shown in the commented line in the following example:</span></span>  
  
## <a name="example"></a><span data-ttu-id="d7a2f-110">Esempio</span><span class="sxs-lookup"><span data-stu-id="d7a2f-110">Example</span></span>  
 <span data-ttu-id="d7a2f-111">Il codice seguente genera l'errore CS1938:</span><span class="sxs-lookup"><span data-stu-id="d7a2f-111">The following code generates CS1938:</span></span>  
  
```csharp  
// cs1938.cs  
using System.Linq;  
class Test  
{  
    static void Main()  
    {  
        int[] sourceA = { 1, 2, 3, 4, 5 };  
        int[] sourceB = { 3, 4, 5, 6, 7 };  
  
        var query = from a in sourceA  
                    join b in sourceB on b equals a // CS1938  
                    // Try the following line instead.  
                    // join b in sourceB on a equals b  
                    select new { a, b };  
    }  
}  
```  
  
## <a name="see-also"></a><span data-ttu-id="d7a2f-112">Vedere anche</span><span class="sxs-lookup"><span data-stu-id="d7a2f-112">See Also</span></span>  
 [<span data-ttu-id="d7a2f-113">Clausola join</span><span class="sxs-lookup"><span data-stu-id="d7a2f-113">join clause</span></span>](../../csharp/language-reference/keywords/join-clause.md)
