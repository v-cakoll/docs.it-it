---
title: Tipi riferimento nullable
description: Questo articolo offre una panoramica dei tipi riferimento nullable, aggiunti in C# 8. Si apprenderà come la funzionalità offra sicurezza contro le eccezioni dei riferimenti Null, per progetti nuovi ed esistenti.
ms.date: 02/19/2019
ms.openlocfilehash: 9ce9efb890f0eff5a6c6747f96c143a4d093dbfb
ms.sourcegitcommit: 160a88c8087b0e63606e6e35f9bd57fa5f69c168
ms.translationtype: HT
ms.contentlocale: it-IT
ms.lasthandoff: 03/09/2019
ms.locfileid: "57725025"
---
# <a name="nullable-reference-types"></a><span data-ttu-id="022f4-104">Tipi riferimento nullable</span><span class="sxs-lookup"><span data-stu-id="022f4-104">Nullable reference types</span></span>

<span data-ttu-id="022f4-105">In C# 8.0 sono stati introdotti i **tipi riferimento nullable** e i **tipi riferimento non nullable** che consentono di creare istruzioni importanti sulle proprietà delle variabili dei tipi riferimento:</span><span class="sxs-lookup"><span data-stu-id="022f4-105">C# 8.0 introduces **nullable reference types** and **non-nullable reference types** that enable you to make important statements about the properties for reference type variables:</span></span>

- <span data-ttu-id="022f4-106">**Non si presume che un riferimento sia Null**.</span><span class="sxs-lookup"><span data-stu-id="022f4-106">**A reference is not supposed to be null**.</span></span> <span data-ttu-id="022f4-107">Quando le variabili non devono essere Null, il compilatore applica regole che garantiscono che sia sicuro dereferenziare queste variabili senza prima controllare che non siano Null:</span><span class="sxs-lookup"><span data-stu-id="022f4-107">When variables aren't supposed to be null, the compiler enforces rules that ensure it is safe to dereference these variables without first checking that it isn't null:</span></span>
  - <span data-ttu-id="022f4-108">La variabile deve essere inizializzata come valore non Null.</span><span class="sxs-lookup"><span data-stu-id="022f4-108">The variable must be initialized to a non-null value.</span></span>
  - <span data-ttu-id="022f4-109">Alla variabile non può mai essere assegnato il valore `null`.</span><span class="sxs-lookup"><span data-stu-id="022f4-109">The variable can never be assigned the value `null`.</span></span>
- <span data-ttu-id="022f4-110">**Un riferimento potrebbe essere Null**.</span><span class="sxs-lookup"><span data-stu-id="022f4-110">**A reference may be null**.</span></span> <span data-ttu-id="022f4-111">Quando le variabili possono essere Null, il compilatore applica regole diverse per assicurarsi che sia stata verificata correttamente la presenza di un riferimento Null:</span><span class="sxs-lookup"><span data-stu-id="022f4-111">When variables may be null, the compiler enforces different rules to ensure that you've correctly checked for a null reference:</span></span>
  - <span data-ttu-id="022f4-112">La variabile può essere dereferenziata solo quando il compilatore può garantire che il valore non sia Null.</span><span class="sxs-lookup"><span data-stu-id="022f4-112">The variable may only be dereferenced when the compiler can guarantee that the value isn't null.</span></span>
  - <span data-ttu-id="022f4-113">Queste variabili possono essere inizializzate con il valore predefinito `null` e possono ricevere l'assegnazione del valore `null` in un altro codice.</span><span class="sxs-lookup"><span data-stu-id="022f4-113">These variables may be initialized with the default `null` value and may be assigned the value `null` in other code.</span></span>

<span data-ttu-id="022f4-114">Questa nuova funzionalità offre vantaggi significativi rispetto alla gestione delle variabili di riferimento nelle versioni precedenti di C# in cui la finalità della progettazione non poteva essere determinata dalla dichiarazione di variabile.</span><span class="sxs-lookup"><span data-stu-id="022f4-114">This new feature provides significant benefits over the handling of reference variables in earlier versions of C# where the design intent couldn't be determined from the variable declaration.</span></span> <span data-ttu-id="022f4-115">Il compilatore non offriva sicurezza contro le eccezioni dei riferimenti Null per i tipi riferimento:</span><span class="sxs-lookup"><span data-stu-id="022f4-115">The compiler didn't provide safety against null reference exceptions for reference types:</span></span>

- <span data-ttu-id="022f4-116">**Un riferimento può essere Null**.</span><span class="sxs-lookup"><span data-stu-id="022f4-116">**A reference can be null**.</span></span> <span data-ttu-id="022f4-117">Non vengono generati avvisi quando un tipo riferimento viene inizializzato come Null o successivamente assegnato a Null.</span><span class="sxs-lookup"><span data-stu-id="022f4-117">No warnings are issued when a reference type is initialized to null, or later assigned to null.</span></span>
- <span data-ttu-id="022f4-118">**Si suppone che un riferimento sia non Null**.</span><span class="sxs-lookup"><span data-stu-id="022f4-118">**A reference is assumed to be not null**.</span></span> <span data-ttu-id="022f4-119">Il compilatore non genera avvisi quando i tipi riferimento sono dereferenziati.</span><span class="sxs-lookup"><span data-stu-id="022f4-119">The compiler doesn't issue any warnings when reference types are dereferenced.</span></span> <span data-ttu-id="022f4-120">Con i riferimenti nullable, il compilatore genera avvisi quando si dereferenzia una variabile che potrebbe essere Null.</span><span class="sxs-lookup"><span data-stu-id="022f4-120">(With nullable references,  the compiler issues warnings whenever you dereference a variable that may be null).</span></span>

<span data-ttu-id="022f4-121">Con l'aggiunta di tipi riferimento nullable, è possibile dichiarare in modo più chiaro la finalità.</span><span class="sxs-lookup"><span data-stu-id="022f4-121">With the addition of nullable reference types, you can declare your intent more clearly.</span></span> <span data-ttu-id="022f4-122">Il valore `null` è il modo corretto di indicare che una variabile non fa riferimento a un valore.</span><span class="sxs-lookup"><span data-stu-id="022f4-122">The `null` value is the correct way to represent that a variable doesn't refer to a value.</span></span> <span data-ttu-id="022f4-123">Non usare questa funzionalità per rimuovere tutti i valori `null` dal codice.</span><span class="sxs-lookup"><span data-stu-id="022f4-123">Don't use this feature to remove all `null` values from your code.</span></span> <span data-ttu-id="022f4-124">È invece consigliabile dichiarare la finalità al compilatore e agli altri sviluppatori che leggono il codice.</span><span class="sxs-lookup"><span data-stu-id="022f4-124">Rather, you should declare your intent to the compiler and other developers that read your code.</span></span> <span data-ttu-id="022f4-125">Dichiarando la finalità, il compilatore informa l'utente quando scrive codice non coerente con tale finalità.</span><span class="sxs-lookup"><span data-stu-id="022f4-125">By declaring your intent, the compiler informs you when you write code that is inconsistent with that intent.</span></span>

<span data-ttu-id="022f4-126">Viene inserita una nota in un **tipo riferimento nullable** usando la stessa sintassi dei [tipi valore nullable](programming-guide/nullable-types/index.md): viene aggiunto `?` al tipo della variabile.</span><span class="sxs-lookup"><span data-stu-id="022f4-126">A **nullable reference type** is noted using the same syntax as [nullable value types](programming-guide/nullable-types/index.md): a `?` is appended to the type of the variable.</span></span> <span data-ttu-id="022f4-127">La dichiarazione della variabile seguente, ad esempio, rappresenta una variabile di stringa nullable, `name`:</span><span class="sxs-lookup"><span data-stu-id="022f4-127">For example, the following variable declaration represents a nullable string variable, `name`:</span></span>

```csharp
string? name;
```

<span data-ttu-id="022f4-128">Le variabili in cui `?` non viene aggiunto al nome del tipo sono **tipi riferimento non nullable**,</span><span class="sxs-lookup"><span data-stu-id="022f4-128">Any variable where the `?` is not appended to the type name is a **non-nullable reference type**.</span></span> <span data-ttu-id="022f4-129">incluse tutte le variabili dei tipi riferimento nel codice esistente quando questa funzionalità è abilitata.</span><span class="sxs-lookup"><span data-stu-id="022f4-129">That includes all reference type variables in existing code when you have enabled this feature.</span></span>

<span data-ttu-id="022f4-130">Il compilatore usa l'analisi statica per determinare se un riferimento nullable è notoriamente non null.</span><span class="sxs-lookup"><span data-stu-id="022f4-130">The compiler uses static analysis to determine if a nullable reference is known to be non-null.</span></span> <span data-ttu-id="022f4-131">Il compilatore genera un avviso se si dereferenzia un riferimento nullable quando potrebbe essere null.</span><span class="sxs-lookup"><span data-stu-id="022f4-131">The compiler warns you if you dereference a nullable reference when it may be null.</span></span> <span data-ttu-id="022f4-132">È possibile eseguire l'override di questo comportamento usando l'**operatore null-forgiving** (`!`) che segue un nome di variabile.</span><span class="sxs-lookup"><span data-stu-id="022f4-132">You can override this behavior by using the **null-forgiving operator** (`!`) following a variable name.</span></span> <span data-ttu-id="022f4-133">Se ad esempio si è certi che la variabile `name` non sia Null, ma il compilatore genera un avviso, è possibile scrivere il codice seguente per eseguire l'override dell'analisi del compilatore:</span><span class="sxs-lookup"><span data-stu-id="022f4-133">For example, if you know the `name` variable isn't null but the compiler issues a warning, you can write the following code to override the compiler's analysis:</span></span>

```csharp
name!.Length;
```

<span data-ttu-id="022f4-134">Per informazioni dettagliate su questo operatore, vedere la proposta di specifica sulla [bozza di tipi riferimento nullable](../../_csharplang/proposals/csharp-8.0/nullable-reference-types-specification.md#the-null-forgiving-operator) in GitHub.</span><span class="sxs-lookup"><span data-stu-id="022f4-134">You can read details about this operator in the [draft nullable reference types](../../_csharplang/proposals/csharp-8.0/nullable-reference-types-specification.md#the-null-forgiving-operator) specification proposal on GitHub.</span></span>

## <a name="nullability-of-types"></a><span data-ttu-id="022f4-135">Supporto dei valori Null dei tipi</span><span class="sxs-lookup"><span data-stu-id="022f4-135">Nullability of types</span></span>

<span data-ttu-id="022f4-136">Qualsiasi tipo riferimento può avere una delle quattro tipologie di *supporto dei valori Null*, che descrive quando vengono generati gli avvisi:</span><span class="sxs-lookup"><span data-stu-id="022f4-136">Any reference type can have one of four *nullabilities*, which describes when warnings are generated:</span></span>

- <span data-ttu-id="022f4-137">*Non nullable*: Null non può essere assegnato alle variabili di questo tipo.</span><span class="sxs-lookup"><span data-stu-id="022f4-137">*Nonnullable*: Null can't be assigned to variables of this type.</span></span> <span data-ttu-id="022f4-138">Non è necessario verificare la presenza di valori Null nelle variabili di questo tipo prima della dereferenziazione.</span><span class="sxs-lookup"><span data-stu-id="022f4-138">Variables of this type don't need to be null-checked before dereferencing.</span></span>
- <span data-ttu-id="022f4-139">*Nullable*: Null può essere assegnato alle variabili di questo tipo.</span><span class="sxs-lookup"><span data-stu-id="022f4-139">*Nullable*: Null can be assigned to variables of this type.</span></span> <span data-ttu-id="022f4-140">Se si dereferenziano le variabili di questo tipo senza prima verificare la presenza di valori `null`, viene generato un avviso.</span><span class="sxs-lookup"><span data-stu-id="022f4-140">Dereferencing variables of this type without first checking for `null` causes a warning.</span></span>
- <span data-ttu-id="022f4-141">*Indipendente dai valori*: si tratta dello stato precedente a C# 8.</span><span class="sxs-lookup"><span data-stu-id="022f4-141">*Oblivious*: This is the pre-C# 8 state.</span></span> <span data-ttu-id="022f4-142">Le variabili di questo tipo possono essere dereferenziate o assegnate senza avvisi.</span><span class="sxs-lookup"><span data-stu-id="022f4-142">Variables of this type can be dereferenced or assigned without warnings.</span></span>
- <span data-ttu-id="022f4-143">*Sconosciuto*: in genere viene usato per i parametri di tipo in cui non sono previsti vincoli che indicano al compilatore che il tipo deve essere *nullable* o *non nullable*.</span><span class="sxs-lookup"><span data-stu-id="022f4-143">*Unknown*: This is generally for type parameters where constraints don't tell the compiler that the type must be *nullable* or *nonnullable*.</span></span>

<span data-ttu-id="022f4-144">Il supporto dei valori Null di un tipo in una dichiarazione di variabile viene controllato dal *contesto nullable* in cui viene dichiarata la variabile.</span><span class="sxs-lookup"><span data-stu-id="022f4-144">The nullability of a type in a variable declaration is controlled by the *nullable context* in which the variable is declared.</span></span>

## <a name="nullable-contexts"></a><span data-ttu-id="022f4-145">Contesti nullable</span><span class="sxs-lookup"><span data-stu-id="022f4-145">Nullable contexts</span></span>

<span data-ttu-id="022f4-146">I contesti nullable consentono il controllo con granularità fine di come il compilatore interpreta le variabili dei tipi riferimento.</span><span class="sxs-lookup"><span data-stu-id="022f4-146">Nullable contexts enable fine-grained control for how the compiler interprets reference type variables.</span></span> <span data-ttu-id="022f4-147">Il **contesto dell'annotazione nullable** di una determinata riga di origine è `enabled` o `disabled`.</span><span class="sxs-lookup"><span data-stu-id="022f4-147">The **nullable annotation context** of any given source line is `enabled` or `disabled`.</span></span> <span data-ttu-id="022f4-148">Nel compilatore delle versioni precedenti a C# 8 la compilazione di tutto il codice viene eseguita in un contesto nullable `disabled`: Qualsiasi tipo riferimento potrebbe essere null.</span><span class="sxs-lookup"><span data-stu-id="022f4-148">You can think of the pre-C# 8 compiler as compiling all your code in a `disabled` nullable context: Any reference type may be null.</span></span> <span data-ttu-id="022f4-149">Il **contesto degli avvisi nullable** potrebbe essere impostato su `enabled`, `disabled` o `safeonly`.</span><span class="sxs-lookup"><span data-stu-id="022f4-149">The **nullable warnings context** may be set to `enabled`, `disabled`, or `safeonly`.</span></span> <span data-ttu-id="022f4-150">Il contesto degli avvisi nullable specifica gli avvisi generati dal compilatore usando l'analisi del flusso.</span><span class="sxs-lookup"><span data-stu-id="022f4-150">The nullable warnings context specifies the warnings generated by the compiler using its flow analysis.</span></span>

<span data-ttu-id="022f4-151">Il contesto dell'annotazione nullable e il contesto dell'avviso nullable possono essere impostati per un progetto usando l'elemento `NullableContextOptions` nel file `csproj`.</span><span class="sxs-lookup"><span data-stu-id="022f4-151">The nullable annotation context and nullable warning context can be set for a project using the `NullableContextOptions` element in your `csproj` file.</span></span> <span data-ttu-id="022f4-152">Questo elemento configura come il compilatore interpreta il supporto dei valori Null dei tipi e quali avvisi vengono generati.</span><span class="sxs-lookup"><span data-stu-id="022f4-152">This element configures how the compiler interprets the nullability of types and what warnings are generated.</span></span> <span data-ttu-id="022f4-153">Le impostazioni valide sono:</span><span class="sxs-lookup"><span data-stu-id="022f4-153">Valid settings are:</span></span>

- <span data-ttu-id="022f4-154">`enable`: il contesto dell'annotazione nullable è **enabled**.</span><span class="sxs-lookup"><span data-stu-id="022f4-154">`enable`: The nullable annotation context is **enabled**.</span></span> <span data-ttu-id="022f4-155">Il contesto dell'avviso nullable è **enabled**.</span><span class="sxs-lookup"><span data-stu-id="022f4-155">The nullable warning context is **enabled**.</span></span>
  - <span data-ttu-id="022f4-156">Le variabili di un tipo riferimento, ad esempio `string`, sono non nullable.</span><span class="sxs-lookup"><span data-stu-id="022f4-156">Variables of a reference type, `string` for example, are non-nullable.</span></span>  <span data-ttu-id="022f4-157">Tutti gli avvisi relativi al supporto dei valori Null sono abilitati.</span><span class="sxs-lookup"><span data-stu-id="022f4-157">All nullability warnings are enabled.</span></span>
- <span data-ttu-id="022f4-158">`disable`: il contesto dell'annotazione nullable è **disabled**.</span><span class="sxs-lookup"><span data-stu-id="022f4-158">`disable`: The nullable annotation context is **disabled**.</span></span> <span data-ttu-id="022f4-159">Il contesto dell'avviso nullable è **disabled**.</span><span class="sxs-lookup"><span data-stu-id="022f4-159">The nullable warning context is **disabled**.</span></span>
  - <span data-ttu-id="022f4-160">Le variabili di un tipo riferimento sono indipendenti dai valori, come nelle versioni precedenti di C#.</span><span class="sxs-lookup"><span data-stu-id="022f4-160">Variables of a reference type are oblivious, just like earlier versions of C#.</span></span> <span data-ttu-id="022f4-161">Tutti gli avvisi relativi al supporto dei valori Null sono disabilitati.</span><span class="sxs-lookup"><span data-stu-id="022f4-161">All nullability warnings are disabled.</span></span>
- <span data-ttu-id="022f4-162">`safeonly`: il contesto dell'annotazione nullable è **enabled**.</span><span class="sxs-lookup"><span data-stu-id="022f4-162">`safeonly`: The nullable annotation context is **enabled**.</span></span> <span data-ttu-id="022f4-163">Il contesto dell'avviso nullable è **safeonly**.</span><span class="sxs-lookup"><span data-stu-id="022f4-163">The nullable warning context is **safeonly**.</span></span>
  - <span data-ttu-id="022f4-164">Le variabili di un tipo riferimento sono non nullable.</span><span class="sxs-lookup"><span data-stu-id="022f4-164">Variables of a reference type are nonnullable.</span></span> <span data-ttu-id="022f4-165">Tutti gli avvisi relativi al supporto dei valori Null per la sicurezza sono abilitati.</span><span class="sxs-lookup"><span data-stu-id="022f4-165">All safety nullability warnings are enabled.</span></span>
- <span data-ttu-id="022f4-166">`warnings`: il contesto dell'annotazione nullable è **disabled**.</span><span class="sxs-lookup"><span data-stu-id="022f4-166">`warnings`: The nullable annotation context is **disabled**.</span></span> <span data-ttu-id="022f4-167">Il contesto dell'avviso nullable è **enabled**.</span><span class="sxs-lookup"><span data-stu-id="022f4-167">The nullable warning context is **enabled**.</span></span>
  - <span data-ttu-id="022f4-168">Le variabili di un tipo riferimento sono indipendenti dai valori.</span><span class="sxs-lookup"><span data-stu-id="022f4-168">Variables of a reference type are oblivious.</span></span> <span data-ttu-id="022f4-169">Tutti gli avvisi relativi al supporto dei valori Null sono abilitati.</span><span class="sxs-lookup"><span data-stu-id="022f4-169">All nullability warnings are enabled.</span></span>
- <span data-ttu-id="022f4-170">`safeonlywarnings`: il contesto dell'annotazione nullable è **disabled**.</span><span class="sxs-lookup"><span data-stu-id="022f4-170">`safeonlywarnings`: The nullable annotation context is **disabled**.</span></span> <span data-ttu-id="022f4-171">Il contesto dell'avviso nullable è **safeonly**.</span><span class="sxs-lookup"><span data-stu-id="022f4-171">The nullable warning context is **safeonly**.</span></span>
  - <span data-ttu-id="022f4-172">Le variabili di un tipo riferimento sono indipendenti dai valori.</span><span class="sxs-lookup"><span data-stu-id="022f4-172">Variables of a reference type are oblivious.</span></span> <span data-ttu-id="022f4-173">Tutti gli avvisi relativi al supporto dei valori Null per la sicurezza sono abilitati.</span><span class="sxs-lookup"><span data-stu-id="022f4-173">All safety nullability warnings are enabled.</span></span>

<span data-ttu-id="022f4-174">È anche possibile usare le direttive per impostare questi stessi contesti ovunque nel progetto:</span><span class="sxs-lookup"><span data-stu-id="022f4-174">You can also use directives to set these same contexts anywhere in your project:</span></span>

- <span data-ttu-id="022f4-175">`#nullable enable`: imposta il contesto dell'annotazione nullable e il contesto dell'avviso nullable su **enabled**.</span><span class="sxs-lookup"><span data-stu-id="022f4-175">`#nullable enable`: Sets the nullable annotation context and nullable warning context to **enabled**.</span></span>
- <span data-ttu-id="022f4-176">`#nullable disable`: imposta il contesto dell'annotazione nullable e il contesto dell'avviso nullable su **disabled**.</span><span class="sxs-lookup"><span data-stu-id="022f4-176">`#nullable disable`: Sets the nullable annotation context and nullable warning context to **disabled**.</span></span>
- <span data-ttu-id="022f4-177">`#nullable safeonly`: impostare il contesto dell'annotazione nullable su **enabled** e il contesto dell'avviso su **safeonly**.</span><span class="sxs-lookup"><span data-stu-id="022f4-177">`#nullable safeonly`: Set the nullable annotation context to **enabled**, and the warning context to **safeonly**.</span></span>
- <span data-ttu-id="022f4-178">`#nullable restore`: ripristina le impostazioni di progetto per il contesto dell'annotazione nullable e il contesto dell'avviso nullable.</span><span class="sxs-lookup"><span data-stu-id="022f4-178">`#nullable restore`: Restores the nullable annotation context and nullable warning context to the project settings.</span></span>
- <span data-ttu-id="022f4-179">`#pragma warning disable nullable`: impostare il contesto dell'avviso nullable su **disabled**.</span><span class="sxs-lookup"><span data-stu-id="022f4-179">`#pragma warning disable nullable`: Set the nullable warning context to **disabled**.</span></span>
- <span data-ttu-id="022f4-180">`#pragma warning enable nullable`: impostare il contesto dell'avviso nullable su **enabled**.</span><span class="sxs-lookup"><span data-stu-id="022f4-180">`#pragma warning enable nullable`: Set the nullable warning context to **enabled**.</span></span>
- <span data-ttu-id="022f4-181">`#pragma warning restore nullable`: ripristina le impostazioni di progetto per il contesto dell'avviso nullable.</span><span class="sxs-lookup"><span data-stu-id="022f4-181">`#pragma warning restore nullable`: Restores the nullable warning context to the project settings.</span></span>
- <span data-ttu-id="022f4-182">`#pragma warning safeonly nullable`: imposta il contesto dell'avviso nullable su **safeonly**.</span><span class="sxs-lookup"><span data-stu-id="022f4-182">`#pragma warning safeonly nullable`: Sets the nullable warning context to **safeonly**.</span></span>

<span data-ttu-id="022f4-183">I contesti dell'annotazione e dell'avviso nullable predefiniti sono `disabled`.</span><span class="sxs-lookup"><span data-stu-id="022f4-183">The default nullable annotation and warning contexts are `disabled`.</span></span> <span data-ttu-id="022f4-184">Tale decisione significa che il codice esistente viene compilato senza modifiche e senza generare nuovi avvisi.</span><span class="sxs-lookup"><span data-stu-id="022f4-184">That decision means that your existing code compiles without changes and without generating any new warnings.</span></span>

<span data-ttu-id="022f4-185">Le differenze tra i contesti degli avvisi nullable `enabled` e `safeonly` sono gli avvisi relativi all'assegnazione di un riferimento nullable a un riferimento non nullable.</span><span class="sxs-lookup"><span data-stu-id="022f4-185">The differences between the `enabled` and `safeonly` nullable warning contexts are warnings for assigning a nullable reference to a non-nullable reference.</span></span> <span data-ttu-id="022f4-186">L'assegnazione seguente genera un avviso in un contesto di avviso `enabled`, ma non in un contesto di avviso `safeonly`.</span><span class="sxs-lookup"><span data-stu-id="022f4-186">The following assignment generates a warning in an `enabled` warning context, but not a `safeonly` warning context.</span></span> <span data-ttu-id="022f4-187">La seconda riga, tuttavia, in cui `s` è dereferenziato, genera un avviso in un contesto `safeonly`:</span><span class="sxs-lookup"><span data-stu-id="022f4-187">However, the second line, where `s` is dereferenced, generates a warning in a `safeonly` context:</span></span>

```csharp
string s = null; // warning when nullable warning context is enabled.
var txt = s.ToString(); // warning when nullable warnings context is safeonly, or enabled.
```

### <a name="nullable-annotation-context"></a><span data-ttu-id="022f4-188">Contesto dell'annotazione nullable</span><span class="sxs-lookup"><span data-stu-id="022f4-188">Nullable annotation context</span></span>

<span data-ttu-id="022f4-189">Il compilatore usa le regole seguenti in un contesto di annotazione nullable disabilitato:</span><span class="sxs-lookup"><span data-stu-id="022f4-189">The compiler uses the following rules in a disabled nullable annotation context:</span></span>

- <span data-ttu-id="022f4-190">Non è possibile dichiarare riferimenti nullable in un contesto disabilitato.</span><span class="sxs-lookup"><span data-stu-id="022f4-190">You can't declare nullable references in a disabled context.</span></span>
- <span data-ttu-id="022f4-191">Tutte le variabili di riferimento possono essere assegnate a null.</span><span class="sxs-lookup"><span data-stu-id="022f4-191">All reference variables may be assigned to null.</span></span>
- <span data-ttu-id="022f4-192">Non vengono generati avvisi quando una variabile di un tipo riferimento viene dereferenziata.</span><span class="sxs-lookup"><span data-stu-id="022f4-192">No warnings are generated when a variable of a reference type is dereferenced.</span></span>
- <span data-ttu-id="022f4-193">L'operatore null-forgiving non può essere usato in un contesto disabilitato.</span><span class="sxs-lookup"><span data-stu-id="022f4-193">The null-forgiving operator may not be used in a disabled context.</span></span>

<span data-ttu-id="022f4-194">Il comportamento è lo stesso delle versioni precedenti di C#.</span><span class="sxs-lookup"><span data-stu-id="022f4-194">The behavior is the same as previous versions of C#.</span></span>

<span data-ttu-id="022f4-195">Il compilatore usa le regole seguenti in un contesto di annotazione nullable abilitato:</span><span class="sxs-lookup"><span data-stu-id="022f4-195">The compiler uses the following rules in an enabled nullable annotation context:</span></span>

- <span data-ttu-id="022f4-196">Qualsiasi variabile di un tipo riferimento è un **riferimento non nullable**.</span><span class="sxs-lookup"><span data-stu-id="022f4-196">Any variable of a reference type is a **non-nullable reference**.</span></span>
- <span data-ttu-id="022f4-197">Qualsiasi riferimento non nullable può essere dereferenziato in modo sicuro.</span><span class="sxs-lookup"><span data-stu-id="022f4-197">Any non-nullable reference may be dereferenced safely.</span></span>
- <span data-ttu-id="022f4-198">Qualsiasi tipo riferimento nullable (indicato da `?` dopo il tipo nella dichiarazione della variabile) può essere null.</span><span class="sxs-lookup"><span data-stu-id="022f4-198">Any nullable reference type (noted by `?` after the type in the variable declaration) may be null.</span></span> <span data-ttu-id="022f4-199">L'analisi statica determina se il valore è non null quando viene dereferenziato.</span><span class="sxs-lookup"><span data-stu-id="022f4-199">Static analysis determines if the value is known to be non-null when it is dereferenced.</span></span> <span data-ttu-id="022f4-200">In caso contrario, il compilatore genera un avviso.</span><span class="sxs-lookup"><span data-stu-id="022f4-200">If not, the compiler warns you.</span></span>
- <span data-ttu-id="022f4-201">È possibile usare l'operatore null-forgiving per dichiarare che un riferimento nullable non è Null.</span><span class="sxs-lookup"><span data-stu-id="022f4-201">You can use the null-forgiving operator to declare that a nullable reference isn't null.</span></span>

<span data-ttu-id="022f4-202">In un contesto di annotazione nullable abilitato il carattere `?` aggiunto al tipo riferimento dichiara un **tipo riferimento nullable**.</span><span class="sxs-lookup"><span data-stu-id="022f4-202">In an enabled nullable annotation context, the `?` character appended to a reference type declares a **nullable reference type**.</span></span> <span data-ttu-id="022f4-203">L'**operatore null forgiveness** (`!`) può essere aggiunto a un'espressione per dichiarare che l'espressione non è Null.</span><span class="sxs-lookup"><span data-stu-id="022f4-203">The **null forgiveness operator** (`!`) may be appended to an expression to declare that the expression isn't null.</span></span>

## <a name="nullable-warning-context"></a><span data-ttu-id="022f4-204">Contesto dell'avviso nullable</span><span class="sxs-lookup"><span data-stu-id="022f4-204">Nullable warning context</span></span>

<span data-ttu-id="022f4-205">Il contesto dell'avviso nullable è diverso dal contesto dell'annotazione nullable.</span><span class="sxs-lookup"><span data-stu-id="022f4-205">The nullable warning context is distinct from the nullable annotation context.</span></span> <span data-ttu-id="022f4-206">Gli avvisi possono essere abilitati anche quando le nuove annotazioni sono disabilitate.</span><span class="sxs-lookup"><span data-stu-id="022f4-206">Warnings can be enabled even when the new annotations are disabled.</span></span> <span data-ttu-id="022f4-207">Il compilatore usa l'analisi statica del flusso per determinare lo **stato null** di qualsiasi riferimento.</span><span class="sxs-lookup"><span data-stu-id="022f4-207">The compiler uses static flow analysis to determine the **null state** of any reference.</span></span> <span data-ttu-id="022f4-208">Lo stato null è **not null** o **maybe null** quando il *contesto dell'avviso nullable* non è **disabled**.</span><span class="sxs-lookup"><span data-stu-id="022f4-208">The null state is either **not null** or **maybe null** when the *nullable warning context* isn't **disabled**.</span></span> <span data-ttu-id="022f4-209">Se si dereferenzia un riferimento quando il compilatore ha determinato che è **maybe null**, il compilatore genera un avviso.</span><span class="sxs-lookup"><span data-stu-id="022f4-209">If you dereference a reference when the compiler has determined it's **maybe null**, the compiler warns you.</span></span> <span data-ttu-id="022f4-210">Lo stato di un riferimento è **maybe null** a meno che il compilatore non riesca a determinare una di due condizioni:</span><span class="sxs-lookup"><span data-stu-id="022f4-210">The state of a reference is **maybe null** unless the compiler can determine one of two conditions:</span></span>

1. <span data-ttu-id="022f4-211">La variabile è stata assegnata in modo definito a un valore non null.</span><span class="sxs-lookup"><span data-stu-id="022f4-211">The variable has been definitely assigned to a non-null value.</span></span>
1. <span data-ttu-id="022f4-212">La variabile o l'espressione è stata confrontata con null prima di dereferenziarla.</span><span class="sxs-lookup"><span data-stu-id="022f4-212">The variable or expression has been checked against null before de-referencing it.</span></span>

<span data-ttu-id="022f4-213">Il compilatore genera avvisi ogni volta che si dereferenzia una variabile o un'espressione in uno stato **maybe null** quando il contesto dell'avviso nullable è `enabled` o `safeonly`.</span><span class="sxs-lookup"><span data-stu-id="022f4-213">The compiler generates warnings whenever you dereference a variable or expression in a **maybe null** state when the nullable warning context is `enabled` or `safeonly`.</span></span> <span data-ttu-id="022f4-214">Vengono generati avvisi anche quando un'espressione o una variabile **maybe null** è assegnata a un tipo riferimento non nullable quando il contesto dell'annotazione nullable è `enabled`.</span><span class="sxs-lookup"><span data-stu-id="022f4-214">Furthermore, warnings are generated when a **maybe null** variable or expression is assigned to a nonnullable reference type when the nullable annotation context is `enabled`.</span></span>

## <a name="learn-more"></a><span data-ttu-id="022f4-215">Altre informazioni</span><span class="sxs-lookup"><span data-stu-id="022f4-215">Learn more</span></span>

- <span data-ttu-id="022f4-216">[Draft Nullable reference specification](https://github.com/dotnet/csharplang/blob/master/proposals/csharp-8.0/nullable-reference-types-specification.md) (Bozza della specifica del riferimento nullable)</span><span class="sxs-lookup"><span data-stu-id="022f4-216">[Draft Nullable reference specification](https://github.com/dotnet/csharplang/blob/master/proposals/csharp-8.0/nullable-reference-types-specification.md)</span></span>
- [<span data-ttu-id="022f4-217">Introduzione all'esercitazione sui riferimenti nullable</span><span class="sxs-lookup"><span data-stu-id="022f4-217">Intro to nullable references tutorial</span></span>](tutorials/nullable-reference-types.md)
- [<span data-ttu-id="022f4-218">Eseguire la migrazione di una base di codice esistente a riferimenti nullable</span><span class="sxs-lookup"><span data-stu-id="022f4-218">Migrate an existing codebase to nullable references</span></span>](tutorials/upgrade-to-nullable-references.md)
