---
title: Novità di C# 7.0 - Guida a C#
description: Panoramica delle nuove funzionalità incluse nella prossima versione 7 del linguaggio C#.
keywords: C#, .NET, .NET Core, funzionalità più recenti, novità
author: BillWagner
ms.author: wiwagn
ms.date: 12/21/2016
ms.topic: article
ms.prod: .net
ms.technology: devlang-csharp
ms.devlang: csharp
ms.assetid: fd41596d-d0c2-4816-b94d-c4d00a5d0243
ms.openlocfilehash: 1951c60ee11d0d5c4856f5f92eee8ba690b11f8d
ms.sourcegitcommit: 86adcc06e35390f13c1e372c36d2e044f1fc31ef
ms.translationtype: HT
ms.contentlocale: it-IT
ms.lasthandoff: 04/26/2018
---
# <a name="whats-new-in-c-70"></a><span data-ttu-id="ae926-104">Novità di C# 7.0</span><span class="sxs-lookup"><span data-stu-id="ae926-104">What's new in C# 7.0</span></span>

<span data-ttu-id="ae926-105">C# 7.0 aggiunge diverse nuove funzionalità al linguaggio C#:</span><span class="sxs-lookup"><span data-stu-id="ae926-105">C# 7.0 adds a number of new features to the C# language:</span></span>
* [<span data-ttu-id="ae926-106">Variabili `out`</span><span class="sxs-lookup"><span data-stu-id="ae926-106">`out` variables</span></span>](#out-variables)
    - <span data-ttu-id="ae926-107">È possibile dichiarare valori `out` inline come argomenti al metodo in cui vengono usati.</span><span class="sxs-lookup"><span data-stu-id="ae926-107">You can declare `out` values inline as arguments to the method where they are used.</span></span>
* [<span data-ttu-id="ae926-108">Tuple</span><span class="sxs-lookup"><span data-stu-id="ae926-108">Tuples</span></span>](#tuples)
    - <span data-ttu-id="ae926-109">È possibile creare tipi leggeri e senza nome che contengono più campi pubblici.</span><span class="sxs-lookup"><span data-stu-id="ae926-109">You can create lightweight, unnamed types that contain multiple public fields.</span></span> <span data-ttu-id="ae926-110">I compilatori e gli strumenti dell'IDE comprendono la semantica di questi tipi.</span><span class="sxs-lookup"><span data-stu-id="ae926-110">Compilers and IDE tools understand the semantics of these types.</span></span>
* [<span data-ttu-id="ae926-111">Variabili discard</span><span class="sxs-lookup"><span data-stu-id="ae926-111">Discards</span></span>](#discards)
    - <span data-ttu-id="ae926-112">Le variabili discard sono variabili temporanee di sola scrittura usate nelle assegnazioni quando non si è interessati al valore assegnato.</span><span class="sxs-lookup"><span data-stu-id="ae926-112">Discards are temporary, write-only variables used in assignments when you don't care about the value assigned.</span></span> <span data-ttu-id="ae926-113">Sono particolarmente utili per la decostruzione di tuple e tipi definiti dall'utente, nonché per la chiamata di metodi con i parametri `out`.</span><span class="sxs-lookup"><span data-stu-id="ae926-113">They are particularly useful when deconstructing tuples and user-defined types, as well as when calling methods with `out` parameters.</span></span>
* [<span data-ttu-id="ae926-114">Criteri di ricerca</span><span class="sxs-lookup"><span data-stu-id="ae926-114">Pattern Matching</span></span>](#pattern-matching)
    - <span data-ttu-id="ae926-115">È possibile creare una logica di salto condizionato basata su tipi e valori arbitrari dei membri di tali tipi.</span><span class="sxs-lookup"><span data-stu-id="ae926-115">You can create branching logic based on arbitrary types and values of the members of those types.</span></span>
* [<span data-ttu-id="ae926-116">Variabili locali e valori restituiti `ref`</span><span class="sxs-lookup"><span data-stu-id="ae926-116">`ref` locals and returns</span></span>](#ref-locals-and-returns)
    - <span data-ttu-id="ae926-117">Gli argomenti del metodo e le variabili locali possono essere riferimenti ad altre opzioni di memorizzazione.</span><span class="sxs-lookup"><span data-stu-id="ae926-117">Method arguments and local variables can be references to other storage.</span></span>
* [<span data-ttu-id="ae926-118">Funzioni locali</span><span class="sxs-lookup"><span data-stu-id="ae926-118">Local Functions</span></span>](#local-functions)
    - <span data-ttu-id="ae926-119">È possibile annidare funzioni all'interno di altre funzioni per limitarne l'ambito e visibilità.</span><span class="sxs-lookup"><span data-stu-id="ae926-119">You can nest functions inside other functions to limit their scope and visibility.</span></span>
* [<span data-ttu-id="ae926-120">Più membri con corpo di espressione</span><span class="sxs-lookup"><span data-stu-id="ae926-120">More expression-bodied members</span></span>](#more-expression-bodied-members)
    - <span data-ttu-id="ae926-121">L'elenco dei membri che possono essere creati con le espressioni è cresciuto.</span><span class="sxs-lookup"><span data-stu-id="ae926-121">The list of members that can be authored using expressions has grown.</span></span>
* [<span data-ttu-id="ae926-122">Espressioni `throw`</span><span class="sxs-lookup"><span data-stu-id="ae926-122">`throw` Expressions</span></span>](#throw-expressions)
    - <span data-ttu-id="ae926-123">È possibile generare eccezioni in costrutti di codice che in precedenza non erano consentiti poiché `throw` era un'istruzione.</span><span class="sxs-lookup"><span data-stu-id="ae926-123">You can throw exceptions in code constructs that previously were not allowed because `throw` was a statement.</span></span> 
* [<span data-ttu-id="ae926-124">Tipi restituiti asincroni generalizzati</span><span class="sxs-lookup"><span data-stu-id="ae926-124">Generalized async return types</span></span>](#generalized-async-return-types)
    - <span data-ttu-id="ae926-125">I metodi dichiarati con il modificatore `async` possono restituire altri tipi oltre a `Task` e `Task<T>`.</span><span class="sxs-lookup"><span data-stu-id="ae926-125">Methods declared with the `async` modifier can return other types in addition to `Task` and `Task<T>`.</span></span>
* [<span data-ttu-id="ae926-126">Miglioramenti della sintassi dei valori letterali numerici</span><span class="sxs-lookup"><span data-stu-id="ae926-126">Numeric literal syntax improvements</span></span>](#numeric-literal-syntax-improvements)
    - <span data-ttu-id="ae926-127">Nuovi token migliorano la leggibilità delle costanti numeriche.</span><span class="sxs-lookup"><span data-stu-id="ae926-127">New tokens improve readability for numeric constants.</span></span>

<span data-ttu-id="ae926-128">Tutte le funzionalità sono descritte nelle sezioni rimanenti di questo argomento.</span><span class="sxs-lookup"><span data-stu-id="ae926-128">The remainder of this topic discusses each of the features.</span></span> <span data-ttu-id="ae926-129">Per ogni funzionalità verranno illustrati i concetti di base</span><span class="sxs-lookup"><span data-stu-id="ae926-129">For each feature, you'll learn the reasoning behind it.</span></span> <span data-ttu-id="ae926-130">e si apprenderà la sintassi.</span><span class="sxs-lookup"><span data-stu-id="ae926-130">You'll learn the syntax.</span></span> <span data-ttu-id="ae926-131">Si noterà che alcuni scenari di esempio in cui viene usata la nuova funzionalità aumentano la produttività dello sviluppatore.</span><span class="sxs-lookup"><span data-stu-id="ae926-131">You'll see some sample scenarios where using the new feature will make you more productive as a developer.</span></span> 

## <a name="out-variables"></a><span data-ttu-id="ae926-132">Variabili `out`</span><span class="sxs-lookup"><span data-stu-id="ae926-132">`out` variables</span></span>

<span data-ttu-id="ae926-133">La sintassi esistente che supporta i parametri `out` è stata migliorata in questa versione.</span><span class="sxs-lookup"><span data-stu-id="ae926-133">The existing syntax that supports `out` parameters has been improved in this version.</span></span>  

<span data-ttu-id="ae926-134">In precedenza era necessario separare la dichiarazione della variabile out e la relativa inizializzazione in due istruzioni diverse:</span><span class="sxs-lookup"><span data-stu-id="ae926-134">Previously, you would need to separate the declaration of the out variable and its initialization into two different statements:</span></span>

[!code-csharp[OutVariableOldStyle](../../../samples/snippets/csharp/new-in-7/program.cs#03_OutVariableOldStyle "classic out variable declaration")]

<span data-ttu-id="ae926-135">Ora è possibile dichiarare le variabili `out` nell'elenco di argomenti di una chiamata al metodo, anziché scrivere un'istruzione di dichiarazione separata:</span><span class="sxs-lookup"><span data-stu-id="ae926-135">You can now declare `out` variables in the argument list of a method call, rather than writing a separate declaration statement:</span></span>

[!code-csharp[OutVariableDeclarations](../../../samples/snippets/csharp/new-in-7/program.cs#01_OutVariableDeclarations "Out variable declarations")]

<span data-ttu-id="ae926-136">Si consiglia di specificare il tipo della variabile `out` per maggiore chiarezza, come illustrato in precedenza.</span><span class="sxs-lookup"><span data-stu-id="ae926-136">You may want to specify the type of the `out` variable for clarity, as shown above.</span></span> <span data-ttu-id="ae926-137">Il linguaggio tuttavia supporta l'uso di una variabile locale tipizzata in modo implicito:</span><span class="sxs-lookup"><span data-stu-id="ae926-137">However, the language does support using an implicitly typed local variable:</span></span>

[!code-csharp[OutVarVariableDeclarations](../../../samples/snippets/csharp/new-in-7/program.cs#02_OutVarVariableDeclarations "Implicitly typed Out variable")]

* <span data-ttu-id="ae926-138">Il codice è più facile da leggere.</span><span class="sxs-lookup"><span data-stu-id="ae926-138">The code is easier to read.</span></span> 
    - <span data-ttu-id="ae926-139">Si dichiara la variabile out nel punto in cui viene usata, non in una riga precedente.</span><span class="sxs-lookup"><span data-stu-id="ae926-139">You declare the out variable where you use it, not on another line above.</span></span>
* <span data-ttu-id="ae926-140">Non è necessario assegnare un valore iniziale.</span><span class="sxs-lookup"><span data-stu-id="ae926-140">No need to assign an initial value.</span></span>
    - <span data-ttu-id="ae926-141">Dichiarando la variabile `out` nel punto in cui viene usata in una chiamata al metodo, non è possibile usarla accidentalmente prima che venga assegnata.</span><span class="sxs-lookup"><span data-stu-id="ae926-141">By declaring the `out` variable where it is used in a method call, you can't accidentally use it before it is assigned.</span></span>

<span data-ttu-id="ae926-142">L'uso più comune di questa funzionalità è il modello `Try`.</span><span class="sxs-lookup"><span data-stu-id="ae926-142">The most common use for this feature will be the `Try` pattern.</span></span> <span data-ttu-id="ae926-143">In questo modello un metodo restituisce un valore `bool` che indica l'esito positivo o negativo e una variabile `out` che indica il risultato se il metodo ha esito positivo.</span><span class="sxs-lookup"><span data-stu-id="ae926-143">In this pattern, a method returns a `bool` indicating success or failure and an `out` variable that provides the result if the method succeeds.</span></span>

<span data-ttu-id="ae926-144">Quando si usa la dichiarazione di variabile `out` la variabile dichiarata "fuoriesce" nell'ambito esterno dell'istruzione if.</span><span class="sxs-lookup"><span data-stu-id="ae926-144">When using the `out` variable declaration, the declared variable "leaks" into the outer scope of the if statement.</span></span> <span data-ttu-id="ae926-145">In questo modo è possibile usare la variabile in un secondo momento:</span><span class="sxs-lookup"><span data-stu-id="ae926-145">This allows you to use the variable afterwards:</span></span>

```csharp
if (!int.TryParse(input, out int result))
{    
    return null;
}

return result;
```

## <a name="tuples"></a><span data-ttu-id="ae926-146">Tuple</span><span class="sxs-lookup"><span data-stu-id="ae926-146">Tuples</span></span>

> [!NOTE]
> <span data-ttu-id="ae926-147">Le nuove funzionalità delle tuple richiedono i tipi <xref:System.ValueTuple>.</span><span class="sxs-lookup"><span data-stu-id="ae926-147">The new tuples features require the <xref:System.ValueTuple> types.</span></span>
> <span data-ttu-id="ae926-148">È necessario aggiungere il pacchetto NuGet [ `System.ValueTuple` ](https://www.nuget.org/packages/System.ValueTuple/) per usarlo nelle piattaforme che non includono i tipi.</span><span class="sxs-lookup"><span data-stu-id="ae926-148">You must add the NuGet package [`System.ValueTuple`](https://www.nuget.org/packages/System.ValueTuple/) in order to use it on platforms that do not include the types.</span></span>
>
> <span data-ttu-id="ae926-149">È simile ad altre funzionalità del linguaggio basate sui tipi resi disponibili nel framework.</span><span class="sxs-lookup"><span data-stu-id="ae926-149">This is similar to other language features that rely on types delivered in the framework.</span></span> <span data-ttu-id="ae926-150">Alcuni esempi sono `async` e `await`, basati sull'interfaccia `INotifyCompletion`, e LINQ, basato su `IEnumerable<T>`.</span><span class="sxs-lookup"><span data-stu-id="ae926-150">Example include `async` and `await` relying on the `INotifyCompletion` interface, and LINQ relying on `IEnumerable<T>`.</span></span> <span data-ttu-id="ae926-151">Tuttavia, il meccanismo di distribuzione sta cambiando perché .NET sta diventando più indipendente dalla piattaforma.</span><span class="sxs-lookup"><span data-stu-id="ae926-151">However, the delivery mechanism is changing as .NET is becoming more platform independent.</span></span> <span data-ttu-id="ae926-152">.NET Framework potrebbe non essere sempre distribuito secondo la stessa cadenza del compilatore del linguaggio.</span><span class="sxs-lookup"><span data-stu-id="ae926-152">The .NET Framework may not always ship on the same cadence as the language compiler.</span></span> <span data-ttu-id="ae926-153">Quando nuove funzionalità del linguaggio si basano su nuovi tipi, tali tipi saranno disponibili come pacchetti NuGet al momento della distribuzione delle funzionalità del linguaggio.</span><span class="sxs-lookup"><span data-stu-id="ae926-153">When new language features rely on new types, those types will be available as NuGet packages when the language features ship.</span></span> <span data-ttu-id="ae926-154">Una volta che questi nuovi tipi vengono aggiunti all'API standard .NET e distribuiti come parte del framework, il requisito del pacchetto NuGet viene rimosso.</span><span class="sxs-lookup"><span data-stu-id="ae926-154">As these new types get added to the .NET Standard API and delivered as part of the framework, the NuGet package requirement will be removed.</span></span>

<span data-ttu-id="ae926-155">C# offre una sintassi completa per le classi e gli struct usati per spiegare la finalità della progettazione.</span><span class="sxs-lookup"><span data-stu-id="ae926-155">C# provides a rich syntax for classes and structs that is used to explain your design intent.</span></span> <span data-ttu-id="ae926-156">In alcuni casi tale sintassi richiede ulteriore lavoro con vantaggi minimi.</span><span class="sxs-lookup"><span data-stu-id="ae926-156">But sometimes that rich syntax requires extra work with minimal benefit.</span></span> <span data-ttu-id="ae926-157">Spesso è possibile scrivere metodi che richiedono una struttura semplice contenente più di un elemento dati.</span><span class="sxs-lookup"><span data-stu-id="ae926-157">You may often write methods that need a simple structure containing more than one data element.</span></span> <span data-ttu-id="ae926-158">Per supportare questi scenari, sono state aggiunte *tuple* a C#.</span><span class="sxs-lookup"><span data-stu-id="ae926-158">To support these scenarios *tuples* were added to C#.</span></span> <span data-ttu-id="ae926-159">Le tuple sono strutture di dati leggere che contengono più campi per rappresentare i membri dati.</span><span class="sxs-lookup"><span data-stu-id="ae926-159">Tuples are lightweight data structures that contain multiple fields to represent the data members.</span></span>
<span data-ttu-id="ae926-160">I campi non vengono convalidati e non è possibile definire i propri metodi</span><span class="sxs-lookup"><span data-stu-id="ae926-160">The fields are not validated, and you cannot define your own methods</span></span>

> [!NOTE]
> <span data-ttu-id="ae926-161">Le tuple erano già disponibili prima di C# 7.0, ma in modo poco efficiente e senza supporto del linguaggio.</span><span class="sxs-lookup"><span data-stu-id="ae926-161">Tuples were available before C# 7.0, but they were inefficient and had no language support.</span></span>
> <span data-ttu-id="ae926-162">Questo significava poter fare riferimento agli elementi delle tuple solo come `Item1`, `Item2` e così via.</span><span class="sxs-lookup"><span data-stu-id="ae926-162">This meant that tuple elements could only be referenced as `Item1`, `Item2` and so on.</span></span> <span data-ttu-id="ae926-163">C# 7.0 introduce il supporto del linguaggio per le tuple, che consente nomi semantici per i campi di una tupla con tipi di tupla nuovi e più efficienti.</span><span class="sxs-lookup"><span data-stu-id="ae926-163">C# 7.0 introduces language support for tuples, which enables semantic names for the fields of a tuple using new, more efficient tuple types.</span></span>

<span data-ttu-id="ae926-164">È possibile creare una tupla assegnando ogni membro a un valore:</span><span class="sxs-lookup"><span data-stu-id="ae926-164">You can create a tuple by assigning each member to a value:</span></span>

[!code-csharp[UnnamedTuple](../../../samples/snippets/csharp/new-in-7/program.cs#04_UnnamedTuple "Unnamed tuple")]

<span data-ttu-id="ae926-165">L'assegnazione consente di creare una tupla i cui membri sono `Item1` e `Item2`, che è possibile usare in modo analogo a <xref:System.Tuple>. È possibile modificare la sintassi per creare una tupla che fornisce nomi semantici per ognuno dei membri della tupla:</span><span class="sxs-lookup"><span data-stu-id="ae926-165">That assignment creates a tuple whose members are `Item1` and `Item2`, which you can use in the same way as <xref:System.Tuple> You can change the syntax to create a tuple that provides semantic names to each of the members of the tuple:</span></span>

[!code-csharp[NamedTuple](../../../samples/snippets/csharp/new-in-7/program.cs#05_NamedTuple "Named tuple")]

<span data-ttu-id="ae926-166">La tupla `namedLetters` contiene campi detti `Alpha` e `Beta`.</span><span class="sxs-lookup"><span data-stu-id="ae926-166">The `namedLetters` tuple contains fields referred to as `Alpha` and `Beta`.</span></span> <span data-ttu-id="ae926-167">Tali nomi esistono solo in fase di compilazione e non vengono mantenuti, ad esempio, quando si esamina la tupla tramite reflection in fase di esecuzione.</span><span class="sxs-lookup"><span data-stu-id="ae926-167">Those names exist only at compile time and are not preserved for example when inspecting the tuple using reflection at runtime.</span></span>

<span data-ttu-id="ae926-168">In un'assegnazione di tupla è anche possibile specificare i nomi dei campi sul lato destro dell'assegnazione:</span><span class="sxs-lookup"><span data-stu-id="ae926-168">In a tuple assignment, you can also specify the names of the fields on the right-hand side of the assignment:</span></span>

[!code-csharp[ImplicitNamedTuple](../../../samples/snippets/csharp/new-in-7/program.cs#06_ImplicitNamedTuple "Implicitly named tuple")]

<span data-ttu-id="ae926-169">È possibile specificare nomi per i campi sia a sinistra che a destra dell'assegnazione:</span><span class="sxs-lookup"><span data-stu-id="ae926-169">You can specify names for the fields on both the left and right-hand side of the assignment:</span></span>

[!code-csharp[NamedTupleConflict](../../../samples/snippets/csharp/new-in-7/program.cs#07_NamedTupleConflict "Named tuple conflict")]

<span data-ttu-id="ae926-170">La riga precedente genera un avviso, `CS8123`, che indica che i nomi sul lato destro dell'assegnazione, `Alpha` e `Beta`, vengono ignorati perché sono in conflitto con i nomi sul lato sinistro, `First` e `Second`.</span><span class="sxs-lookup"><span data-stu-id="ae926-170">The line above generates a warning, `CS8123`, telling you that the names on the right side of the assignment, `Alpha` and `Beta` are ignored because they conflict with the names on the left side, `First` and `Second`.</span></span>

<span data-ttu-id="ae926-171">Gli esempi precedenti illustrano la sintassi di base per dichiarare le tuple.</span><span class="sxs-lookup"><span data-stu-id="ae926-171">The examples above show the basic syntax to declare tuples.</span></span> <span data-ttu-id="ae926-172">Le tuple sono particolarmente utili come tipi restituiti per i metodi `private` e `internal`.</span><span class="sxs-lookup"><span data-stu-id="ae926-172">Tuples are most useful as return types for `private` and `internal` methods.</span></span> <span data-ttu-id="ae926-173">Offrono una sintassi semplice per tali metodi per restituire più valori discreti: si salva il lavoro di creazione di una `class` o uno `struct` che definisce il tipo restituito.</span><span class="sxs-lookup"><span data-stu-id="ae926-173">Tuples provide a simple syntax for those methods to return multiple discrete values: You save the work of authoring a `class` or a `struct` that defines the type returned.</span></span> <span data-ttu-id="ae926-174">Non è necessario creare un nuovo tipo.</span><span class="sxs-lookup"><span data-stu-id="ae926-174">There is no need for creating a new type.</span></span>

<span data-ttu-id="ae926-175">La creazione di una tupla è più efficiente e più produttiva.</span><span class="sxs-lookup"><span data-stu-id="ae926-175">Creating a tuple is more efficient and more productive.</span></span>
<span data-ttu-id="ae926-176">È una sintassi più semplice e leggera per definire una struttura dei dati che contiene più di un valore.</span><span class="sxs-lookup"><span data-stu-id="ae926-176">It is a simpler, lightweight syntax to define a data structure that carries more than one value.</span></span> <span data-ttu-id="ae926-177">Il metodo di esempio riportato di seguito restituisce i valori minimo e massimo trovati in una sequenza di interi:</span><span class="sxs-lookup"><span data-stu-id="ae926-177">The example method below returns the minimum and maximum values found in a sequence of integers:</span></span>

[!code-csharp[TupleReturningMethod](../../../samples/snippets/csharp/new-in-7/program.cs#08_TupleReturningMethod "Tuple returning method")]

<span data-ttu-id="ae926-178">Usare le tuple in questo modo offre diversi vantaggi:</span><span class="sxs-lookup"><span data-stu-id="ae926-178">Using tuples in this way offers several advantages:</span></span>

* <span data-ttu-id="ae926-179">Si salva il lavoro di creazione di una `class` o uno `struct` che definisce il tipo restituito.</span><span class="sxs-lookup"><span data-stu-id="ae926-179">You save the work of authoring a `class` or a `struct` that defines the type returned.</span></span> 
* <span data-ttu-id="ae926-180">Non è necessario creare un nuovo tipo.</span><span class="sxs-lookup"><span data-stu-id="ae926-180">You do not need to create new type.</span></span>
* <span data-ttu-id="ae926-181">L'ottimizzazione del linguaggio consente di evitare la chiamata ai metodi <xref:System.Tuple.Create``1(``0)>.</span><span class="sxs-lookup"><span data-stu-id="ae926-181">The language enhancements removes the need to call the <xref:System.Tuple.Create``1(``0)> methods.</span></span>

<span data-ttu-id="ae926-182">La dichiarazione per il metodo specifica i nomi per i campi della tupla restituita.</span><span class="sxs-lookup"><span data-stu-id="ae926-182">The declaration for the method provides the names for the fields of the tuple that is returned.</span></span> <span data-ttu-id="ae926-183">Quando si chiama il metodo, il valore restituito è una tupla i cui campi sono `Max` e `Min`:</span><span class="sxs-lookup"><span data-stu-id="ae926-183">When you call the method, the return value is a tuple whose fields are `Max` and `Min`:</span></span>

[!code-csharp[CallingTupleMethod](../../../samples/snippets/csharp/new-in-7/program.cs#09_CallingTupleMethod "Calling a tuple returning method")]

<span data-ttu-id="ae926-184">Può rendersi necessario decomprimere i membri di una tupla che sono stati restituiti da un metodo.</span><span class="sxs-lookup"><span data-stu-id="ae926-184">There may be times when you want to unpackage the members of a tuple that were returned from a method.</span></span>  <span data-ttu-id="ae926-185">In questo caso è possibile dichiarare variabili separate per ogni valore nella tupla.</span><span class="sxs-lookup"><span data-stu-id="ae926-185">You can do that by declaring separate variables for each of the values in the tuple.</span></span> <span data-ttu-id="ae926-186">Questa operazione è detta *decostruzione* della tupla:</span><span class="sxs-lookup"><span data-stu-id="ae926-186">This is called *deconstructing* the tuple:</span></span>

[!code-csharp[CallingWithDeconstructor](../../../samples/snippets/csharp/new-in-7/program.cs#10_CallingWithDeconstructor "Deconstructing a tuple")]

<span data-ttu-id="ae926-187">È anche possibile specificare una decostruzione simile per qualsiasi tipo in .NET.</span><span class="sxs-lookup"><span data-stu-id="ae926-187">You can also provide a similar deconstruction for any type in .NET.</span></span> <span data-ttu-id="ae926-188">L'operazione si esegue scrivendo un metodo `Deconstruct` come membro della classe.</span><span class="sxs-lookup"><span data-stu-id="ae926-188">This is done by writing a `Deconstruct` method as a member of the class.</span></span> <span data-ttu-id="ae926-189">Tale metodo `Deconstruct` specifica un set di argomenti `out` per ognuna delle proprietà da estrarre.</span><span class="sxs-lookup"><span data-stu-id="ae926-189">That `Deconstruct` method provides a set of `out` arguments for each of the properties you want to extract.</span></span> <span data-ttu-id="ae926-190">Considerare questa classe `Point` che specifica un metodo di decostruzione che estrae le coordinate `X` e `Y`:</span><span class="sxs-lookup"><span data-stu-id="ae926-190">Consider this `Point` class that provides a deconstructor method that extracts the `X` and `Y` coordinates:</span></span>

[!code-csharp[PointWithDeconstruction](../../../samples/snippets/csharp/new-in-7/point.cs#11_PointWithDeconstruction "Point with deconstruction method")]
 
<span data-ttu-id="ae926-191">È possibile estrarre i singoli campi assegnando una tupla a un oggetto `Point`:</span><span class="sxs-lookup"><span data-stu-id="ae926-191">You can extract the individual fields by assigning a tuple to a `Point`:</span></span>

[!code-csharp[DeconstructPoint](../../../samples/snippets/csharp/new-in-7/program.cs#12_DeconstructPoint "Deconstruct a point")]

<span data-ttu-id="ae926-192">Non si è vincolati dai nomi definiti nel metodo `Deconstruct`.</span><span class="sxs-lookup"><span data-stu-id="ae926-192">You are not bound by the names defined in the `Deconstruct` method.</span></span> <span data-ttu-id="ae926-193">È possibile rinominare le variabili di estrazione come parte dell'assegnazione:</span><span class="sxs-lookup"><span data-stu-id="ae926-193">You can rename the extract variables as part of the assignment:</span></span>  

[!code-csharp[DeconstructNames](../../../samples/snippets/csharp/new-in-7/program.cs#13_DeconstructNames "Deconstruct with new names")]

<span data-ttu-id="ae926-194">Informazioni più approfondite sulle tuple sono disponibili nell'[argomento relativo ai tipi di tuple](../tuples.md).</span><span class="sxs-lookup"><span data-stu-id="ae926-194">You can learn more in depth about tuples in the [tuples topic](../tuples.md).</span></span>

## <a name="discards"></a><span data-ttu-id="ae926-195">Variabili discard</span><span class="sxs-lookup"><span data-stu-id="ae926-195">Discards</span></span>

<span data-ttu-id="ae926-196">Spesso durante lo decostruzione di una tupla o la chiamata di un metodo con parametri `out`, si è costretti a definire una variabile con un valore non significativo e che non si intende usare.</span><span class="sxs-lookup"><span data-stu-id="ae926-196">Often when deconstructing a tuple or calling a method with `out` parameters, you're forced to define a variable whose value you don't care about and don't intend to use.</span></span> <span data-ttu-id="ae926-197">C# aggiunge il supporto delle *variabili discard* per gestire questo scenario.</span><span class="sxs-lookup"><span data-stu-id="ae926-197">C# adds support for *discards* to handle this scenario.</span></span> <span data-ttu-id="ae926-198">Una variabile discard è una variabile di sola scrittura il cui nome è `_` (carattere di sottolineatura). È possibile assegnare tutti i valori che non occorre mantenere alla singola variabile.</span><span class="sxs-lookup"><span data-stu-id="ae926-198">A discard is a write-only variable whose name is `_` (the underscore character); you can assign all of the values that you intend to discard to the single variable.</span></span> <span data-ttu-id="ae926-199">Una variabile discard è simile a una variabile non assegnata. Con l'eccezione dell'istruzione di assegnazione, la variabile discard non può essere usata nel codice.</span><span class="sxs-lookup"><span data-stu-id="ae926-199">A discard is like an unassigned variable; apart from the assignment statement, the discard can't be used in code.</span></span>

<span data-ttu-id="ae926-200">Le variabili discard sono supportate negli scenari seguenti:</span><span class="sxs-lookup"><span data-stu-id="ae926-200">Discards are supported in the following scenarios:</span></span>

* <span data-ttu-id="ae926-201">Per la decostruzione di tuple o tipi definiti dall'utente.</span><span class="sxs-lookup"><span data-stu-id="ae926-201">When deconstructing tuples or user-defined types.</span></span>

* <span data-ttu-id="ae926-202">Per la chiamata di metodi con parametri [out](../language-reference/keywords/out-parameter-modifier.md).</span><span class="sxs-lookup"><span data-stu-id="ae926-202">When calling methods with [out](../language-reference/keywords/out-parameter-modifier.md) parameters.</span></span>

* <span data-ttu-id="ae926-203">In operazioni con criteri di ricerca con le istruzioni [is](../language-reference/keywords/is.md) e [switch](../language-reference/keywords/switch.md).</span><span class="sxs-lookup"><span data-stu-id="ae926-203">In a pattern matching operation with the [is](../language-reference/keywords/is.md) and [switch](../language-reference/keywords/switch.md) statements.</span></span>

* <span data-ttu-id="ae926-204">Come identificatore autonomo quando si vuole identificare in modo esplicito il valore di un'assegnazione come variabile discard.</span><span class="sxs-lookup"><span data-stu-id="ae926-204">As a standalone identifier when you want to explicitly identify the value of an assignment as a discard.</span></span>

<span data-ttu-id="ae926-205">L'esempio seguente definisce un metodo `QueryCityDataForYears` che restituisce una tupla con 6 elementi che contiene i dati di una città per due anni diversi.</span><span class="sxs-lookup"><span data-stu-id="ae926-205">The following example defines a `QueryCityDataForYears` method that returns a 6-tuple that contains a data for a city for two different years.</span></span> <span data-ttu-id="ae926-206">La chiamata del metodo nell'esempio è interessata solo ai due valori di popolazione restituiti dal metodo e quindi gestisce i valori rimanenti nella tupla come variabili discard quando decostruisce la tupla.</span><span class="sxs-lookup"><span data-stu-id="ae926-206">The method call in the example is concerned only with the two population values returned by the method and so treats the remaining values in the tuple as discards when it deconstructs the tuple.</span></span>

[!code-csharp[Tuple-discard](../../../samples/snippets/csharp/programming-guide/deconstructing-tuples/discard-tuple1.cs)]

<span data-ttu-id="ae926-207">Per altre informazioni, vedere [Variabili discard](../discards.md).</span><span class="sxs-lookup"><span data-stu-id="ae926-207">For more information, see [Discards](../discards.md).</span></span>
 
## <a name="pattern-matching"></a><span data-ttu-id="ae926-208">Criteri di ricerca</span><span class="sxs-lookup"><span data-stu-id="ae926-208">Pattern matching</span></span>

<span data-ttu-id="ae926-209">L'uso dei *criteri di ricerca* consente di implementare l'invio dei metodi per le proprietà diverse dal tipo di un oggetto.</span><span class="sxs-lookup"><span data-stu-id="ae926-209">*Pattern matching* is a feature that allows you to implement method dispatch on properties other than the type of an object.</span></span> <span data-ttu-id="ae926-210">Probabilmente si ha già familiarità con l'invio dei metodi basato sul tipo di un oggetto.</span><span class="sxs-lookup"><span data-stu-id="ae926-210">You're probably already familiar with method dispatch based on the type of an object.</span></span> <span data-ttu-id="ae926-211">Nella programmazione orientata agli oggetti i metodi virtuali e di override offrono la sintassi del linguaggio per implementare l'invio dei metodi basato sul tipo di un oggetto.</span><span class="sxs-lookup"><span data-stu-id="ae926-211">In Object Oriented programming, virtual and override methods provide language syntax to implement method dispatching based on an object's type.</span></span> <span data-ttu-id="ae926-212">Le classi di base e derivate consentono diverse implementazioni.</span><span class="sxs-lookup"><span data-stu-id="ae926-212">Base and Derived classes provide different implementations.</span></span> <span data-ttu-id="ae926-213">Le espressioni di criteri di ricerca estendono questo concetto in modo che sia possibile implementare facilmente modelli di invio simili per i tipi e gli elementi di dati che non sono correlati attraverso una gerarchia di ereditarietà.</span><span class="sxs-lookup"><span data-stu-id="ae926-213">Pattern matching expressions extend this concept so that you can easily implement similar dispatch patterns for types and data elements that are not related through an inheritance hierarchy.</span></span> 

<span data-ttu-id="ae926-214">I criteri di ricerca supportano le espressioni `is` e le espressioni `switch`.</span><span class="sxs-lookup"><span data-stu-id="ae926-214">Pattern matching supports `is` expressions and `switch` expressions.</span></span> <span data-ttu-id="ae926-215">Ognuno consente di esaminare un oggetto e le relative proprietà per determinare se l'oggetto soddisfa il criterio ricercato.</span><span class="sxs-lookup"><span data-stu-id="ae926-215">Each enables inspecting an object and its properties to determine if that object satisfies the sought pattern.</span></span> <span data-ttu-id="ae926-216">Usare la parola chiave `when` per specificare regole aggiuntive per il modello.</span><span class="sxs-lookup"><span data-stu-id="ae926-216">You use the `when` keyword to specify additional rules to the pattern.</span></span>

### <a name="is-expression"></a><span data-ttu-id="ae926-217">Espressione `is`</span><span class="sxs-lookup"><span data-stu-id="ae926-217">`is` expression</span></span>

<span data-ttu-id="ae926-218">L'espressione con criterio `is` estende il noto operatore `is` per eseguire query su un oggetto oltre il relativo tipo.</span><span class="sxs-lookup"><span data-stu-id="ae926-218">The `is` pattern expression extends the familiar `is` operator to query an object beyond its type.</span></span>

<span data-ttu-id="ae926-219">Iniziamo con uno scenario semplice</span><span class="sxs-lookup"><span data-stu-id="ae926-219">Let's start with a simple scenario.</span></span> <span data-ttu-id="ae926-220">a cui verranno aggiunte delle funzionalità per spiegare in che modo le espressioni di criteri di ricerca semplificano gli algoritmi usati con i tipi non correlati.</span><span class="sxs-lookup"><span data-stu-id="ae926-220">We'll add capabilities to this scenario that demonstrate how pattern matching expressions make algorithms that work with unrelated types easy.</span></span> <span data-ttu-id="ae926-221">Si inizierà con un metodo che calcola la somma di un numero di tirate di dadi:</span><span class="sxs-lookup"><span data-stu-id="ae926-221">We'll start with a method that computes the sum of a number of die rolls:</span></span>

[!code-csharp[SumDieRolls](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#14_SumDieRolls "Sum die rolls")]

<span data-ttu-id="ae926-222">È possibile vedere rapidamente che è necessario trovare la somma delle tirate di dadi dove alcune delle tirate sono fatte con più dadi.</span><span class="sxs-lookup"><span data-stu-id="ae926-222">You might quickly find that you need to find the sum of die rolls where some of the rolls are made with multiple dice (dice is the plural of die).</span></span> <span data-ttu-id="ae926-223">Parte della sequenza di input può essere costituita da più risultati anziché un singolo numero:</span><span class="sxs-lookup"><span data-stu-id="ae926-223">Part of the input sequence may be multiple results instead of a single number:</span></span>

[!code-csharp[SumDieRollsWithGroups](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#15_SumDieRollsWithGroups "Sum die rolls with groups")]

<span data-ttu-id="ae926-224">L'espressione con il criterio `is` funziona piuttosto bene in questo scenario.</span><span class="sxs-lookup"><span data-stu-id="ae926-224">The `is` pattern expression works quite well in this scenario.</span></span> <span data-ttu-id="ae926-225">Come parte del controllo del tipo, si scrive un'inizializzazione delle variabili.</span><span class="sxs-lookup"><span data-stu-id="ae926-225">As part of checking the type, you write a variable initialization.</span></span> <span data-ttu-id="ae926-226">In questo modo viene creata una nuova variabile del tipo di runtime convalidato.</span><span class="sxs-lookup"><span data-stu-id="ae926-226">This creates a new variable of the validated runtime type.</span></span>

<span data-ttu-id="ae926-227">Man mano che questi scenari vengono estesi, si può scoprire che si stanno compilando più istruzioni `if` e `else if`.</span><span class="sxs-lookup"><span data-stu-id="ae926-227">As you keep extending these scenarios, you may find that you build more `if` and `else if` statements.</span></span> <span data-ttu-id="ae926-228">Quando la situazione diventa difficile da gestire, probabilmente si vorrà passare alle espressioni con il criterio `switch`.</span><span class="sxs-lookup"><span data-stu-id="ae926-228">Once that becomes unwieldy, you'll likely want to switch to `switch` pattern expressions.</span></span>

### <a name="switch-statement-updates"></a><span data-ttu-id="ae926-229">Aggiornamenti dell'istruzione `switch`</span><span class="sxs-lookup"><span data-stu-id="ae926-229">`switch` statement updates</span></span>

<span data-ttu-id="ae926-230">L'*espressione di ricerca* ha una sintassi familiare, basata sull'istruzione `switch` che fa già parte del linguaggio C#.</span><span class="sxs-lookup"><span data-stu-id="ae926-230">The *match expression* has a familiar syntax, based on the `switch` statement already part of the C# language.</span></span> <span data-ttu-id="ae926-231">Supponiamo di convertire il codice esistente in modo da usare un'espressione di ricerca prima di aggiungere nuovi casi:</span><span class="sxs-lookup"><span data-stu-id="ae926-231">Let's translate the existing code to use a match expression before adding new cases:</span></span> 

[!code-csharp[SumUsingSwitch](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#16_SumUsingSwitch "Sum using switch")]

<span data-ttu-id="ae926-232">Le espressioni di ricerca hanno una sintassi leggermente diversa dalle espressioni `is`, in cui si dichiara il tipo e la variabile all'inizio dell'espressione `case`.</span><span class="sxs-lookup"><span data-stu-id="ae926-232">The match expressions have a slightly different syntax than the `is` expressions, where you declare the type and variable at the beginning of the `case` expression.</span></span>

<span data-ttu-id="ae926-233">Le espressioni di ricerca supportano anche le costanti.</span><span class="sxs-lookup"><span data-stu-id="ae926-233">The match expressions also support constants.</span></span> <span data-ttu-id="ae926-234">Ciò consente di risparmiare tempo eseguendo il factoring dei casi semplici:</span><span class="sxs-lookup"><span data-stu-id="ae926-234">This can save time by factoring out simple cases:</span></span>

[!code-csharp[SwitchWithConstants](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#17_SwitchWithConstants "Switch with constants")]

<span data-ttu-id="ae926-235">Il codice riportato sopra aggiunge casi per `0` come caso speciale di `int` e `null` come caso speciale quando non vi è alcun input.</span><span class="sxs-lookup"><span data-stu-id="ae926-235">The code above adds cases for `0` as a special case of `int`, and `null` as a special case when there is no input.</span></span> <span data-ttu-id="ae926-236">Questo esempio dimostra un nuovo importante aspetto delle espressioni con criterio switch: l'ordine delle espressioni `case` ora viene tenuto in considerazione.</span><span class="sxs-lookup"><span data-stu-id="ae926-236">This demonstrates one important new feature in switch pattern expressions: the order of the `case` expressions now matters.</span></span> <span data-ttu-id="ae926-237">Il caso `0` deve essere visualizzato prima del caso `int` generale.</span><span class="sxs-lookup"><span data-stu-id="ae926-237">The `0` case must appear before the general `int` case.</span></span> <span data-ttu-id="ae926-238">In caso contrario, il primo criterio di corrispondenza sarebbe il caso `int`, anche se il valore è `0`.</span><span class="sxs-lookup"><span data-stu-id="ae926-238">Otherwise, the first pattern to match would be the `int` case, even when the value is `0`.</span></span> <span data-ttu-id="ae926-239">Se le espressioni di ricerca vengono accidentalmente ordinate in modo tale che un caso successivo sia già stato gestito, il compilatore applica un contrassegno e genera un errore.</span><span class="sxs-lookup"><span data-stu-id="ae926-239">If you accidentally order match expressions such that a later case has already been handled, the compiler will flag that and generate an error.</span></span>

<span data-ttu-id="ae926-240">Questo stesso comportamento abilita il caso speciale per una sequenza di input vuota.</span><span class="sxs-lookup"><span data-stu-id="ae926-240">This same behavior enables the special case for an empty input sequence.</span></span>
<span data-ttu-id="ae926-241">Come si può vedere, il caso per un oggetto `IEnumerable` con elementi deve apparire prima del caso `IEnumerable` generale.</span><span class="sxs-lookup"><span data-stu-id="ae926-241">You can see that the case for an `IEnumerable` item that has elements must appear before the general `IEnumerable` case.</span></span>

<span data-ttu-id="ae926-242">Questa versione ha inoltre aggiunto un caso `default`.</span><span class="sxs-lookup"><span data-stu-id="ae926-242">This version has also added a `default` case.</span></span> <span data-ttu-id="ae926-243">Il caso `default` viene sempre valutato per ultimo, indipendentemente dall'ordine in cui appare nell'origine.</span><span class="sxs-lookup"><span data-stu-id="ae926-243">The `default` case is always evaluated last, regardless of the order it appears in the source.</span></span> <span data-ttu-id="ae926-244">Per questo motivo, la convenzione è inserire il caso `default` per ultimo.</span><span class="sxs-lookup"><span data-stu-id="ae926-244">For that reason, convention is to put the `default` case last.</span></span>

<span data-ttu-id="ae926-245">Infine, si aggiungerà un ultimo `case` per un nuovo stile di dado.</span><span class="sxs-lookup"><span data-stu-id="ae926-245">Finally, let's add one last `case` for a new style of die.</span></span> <span data-ttu-id="ae926-246">Alcuni giochi usano dadi percentili per rappresentare intervalli di numeri più ampi.</span><span class="sxs-lookup"><span data-stu-id="ae926-246">Some games use percentile dice to represent larger ranges of numbers.</span></span> 

> [!NOTE]
> <span data-ttu-id="ae926-247">Due dadi percentili a 10 facce possono rappresentare qualsiasi numero da 0 a 99.</span><span class="sxs-lookup"><span data-stu-id="ae926-247">Two 10-sided percentile dice can represent every number from 0 through 99.</span></span> <span data-ttu-id="ae926-248">Un dado ha le facce etichettate con `00`, `10`, `20`,... `90`.</span><span class="sxs-lookup"><span data-stu-id="ae926-248">One die has sides labelled `00`, `10`, `20`, ... `90`.</span></span> <span data-ttu-id="ae926-249">L'altro dado ha le facce etichettate con `0`, `1`, `2`, ... `9`.</span><span class="sxs-lookup"><span data-stu-id="ae926-249">The other die has sides labeled `0`, `1`, `2`, ... `9`.</span></span> <span data-ttu-id="ae926-250">Sommare i valori dei due dadi e si potrà avere qualsiasi numero compreso tra 0 e 99.</span><span class="sxs-lookup"><span data-stu-id="ae926-250">Add the two die values together and you can get every number from 0 through 99.</span></span>

<span data-ttu-id="ae926-251">Per aggiungere questo tipo di dado alla raccolta, per prima cosa definire un tipo per rappresentare il dado percentile.</span><span class="sxs-lookup"><span data-stu-id="ae926-251">To add this kind of die to your collection, first define a type to represent the percentile dice.</span></span> <span data-ttu-id="ae926-252">La proprietà `TensDigit` archivia i valori `0`, `10`, `20` fino a `90`:</span><span class="sxs-lookup"><span data-stu-id="ae926-252">The `TensDigit` property stores values `0`, `10`, `20`, up to `90`:</span></span>

[!code-csharp[18_PercentileDice](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#18_PercentileDice "Percentile Die type")]

<span data-ttu-id="ae926-253">Aggiungere quindi un'espressione di ricerca `case` per il nuovo tipo:</span><span class="sxs-lookup"><span data-stu-id="ae926-253">Then, add a `case` match expression for the new type:</span></span>

[!code-csharp[SwitchWithNewTypes](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#19_SwitchWithNewTypes "Include Percentile Die type")]

<span data-ttu-id="ae926-254">La nuova sintassi per le espressioni di criteri di ricerca, più chiara e concisa, semplifica la creazione degli algoritmi di invio basati sul tipo di un oggetto o su altre proprietà.</span><span class="sxs-lookup"><span data-stu-id="ae926-254">The new syntax for pattern matching expressions makes it easier to create dispatch algorithms based on an object's type, or other properties, using a clear and concise syntax.</span></span> <span data-ttu-id="ae926-255">Le espressioni di criteri di ricerca abilitano questi costrutti per i tipi di dati non correlati per ereditarietà.</span><span class="sxs-lookup"><span data-stu-id="ae926-255">Pattern matching expressions enable these constructs on data types that are unrelated by inheritance.</span></span>

<span data-ttu-id="ae926-256">Altre informazioni sui criteri di ricerca sono disponibili nell'argomento dedicato ai [criteri di ricerca in C#](../pattern-matching.md).</span><span class="sxs-lookup"><span data-stu-id="ae926-256">You can learn more about pattern matching in the topic dedicated to [pattern matching in C#](../pattern-matching.md).</span></span>

## <a name="ref-locals-and-returns"></a><span data-ttu-id="ae926-257">Variabili locali e valori restituiti per riferimento</span><span class="sxs-lookup"><span data-stu-id="ae926-257">Ref locals and returns</span></span>

<span data-ttu-id="ae926-258">Questa funzionalità abilita algoritmi che usano e restituiscono riferimenti a variabili definite altrove.</span><span class="sxs-lookup"><span data-stu-id="ae926-258">This feature enables algorithms that use and return references to variables defined elsewhere.</span></span> <span data-ttu-id="ae926-259">Un esempio è lavorare con matrici di grandi dimensioni e trovare un'unica posizione con determinate caratteristiche.</span><span class="sxs-lookup"><span data-stu-id="ae926-259">One example is working with large matrices, and finding a single location with certain characteristics.</span></span> <span data-ttu-id="ae926-260">Un metodo restituirebbe due indici per una singola posizione nella matrice:</span><span class="sxs-lookup"><span data-stu-id="ae926-260">One method would return the two indices for a single location in the matrix:</span></span>

[!code-csharp[FindReturningIndices](../../../samples/snippets/csharp/new-in-7/MatrixSearch.cs#20_FindReturningIndices "Find returning indices")]

<span data-ttu-id="ae926-261">Esistono molti problemi con questo codice.</span><span class="sxs-lookup"><span data-stu-id="ae926-261">There are many issues with this code.</span></span> <span data-ttu-id="ae926-262">Prima di tutto, è un metodo pubblico che restituisce una tupla.</span><span class="sxs-lookup"><span data-stu-id="ae926-262">First of all, it's a public method that's returning a tuple.</span></span> <span data-ttu-id="ae926-263">Il linguaggio la supporta, ma sono preferibili i tipi definiti dall'utente (classi o struct) per le API pubbliche.</span><span class="sxs-lookup"><span data-stu-id="ae926-263">The language supports this, but user defined types (either classes or structs) are preferred for public APIs.</span></span>

<span data-ttu-id="ae926-264">In secondo luogo, questo metodo restituisce gli indici all'elemento nella matrice.</span><span class="sxs-lookup"><span data-stu-id="ae926-264">Second, this method is returning the indices to the item in the matrix.</span></span>
<span data-ttu-id="ae926-265">Ciò induce i chiamanti a scrivere codice che usa tali indici per dereferenziare la matrice e modificare un singolo elemento:</span><span class="sxs-lookup"><span data-stu-id="ae926-265">That leads callers to write code that uses those indices to dereference the matrix and modify a single element:</span></span>

[!code-csharp[UpdateItemFromIndices](../../../samples/snippets/csharp/new-in-7/program.cs#21_UpdateItemFromIndices "Update Item From Indices")]

<span data-ttu-id="ae926-266">È più opportuno scrivere un metodo che restituisce un *riferimento* all'elemento della matrice che si vuole modificare.</span><span class="sxs-lookup"><span data-stu-id="ae926-266">You'd rather write a method that returns a *reference* to the element of the matrix that you want to change.</span></span> <span data-ttu-id="ae926-267">Nelle versioni precedenti questa operazione era possibile solo usando codice unsafe e restituendo un puntatore a un oggetto `int`.</span><span class="sxs-lookup"><span data-stu-id="ae926-267">You could only accomplish this by using unsafe code and returning a pointer to an `int` in previous versions.</span></span>

<span data-ttu-id="ae926-268">Verrà ora analizzata una serie di modifiche per illustrare la funzionalità delle variabili locali ref e la procedura di creazione di un metodo che restituisce un riferimento all'archiviazione interna.</span><span class="sxs-lookup"><span data-stu-id="ae926-268">Let's walk through a series of changes to demonstrate the ref local feature and show how to create a method that returns a reference to internal storage.</span></span>
<span data-ttu-id="ae926-269">Si apprenderanno inoltre le regole per i valori restituiti ref e le variabili locali ref che consentono di evitare usi impropri della funzionalità.</span><span class="sxs-lookup"><span data-stu-id="ae926-269">Along the way, you'll learn the rules of the ref return and ref local feature that protects you from accidentally misusing it.</span></span>

<span data-ttu-id="ae926-270">Iniziare modificando la dichiarazione di metodo `Find` in modo che restituisca un oggetto `ref int` anziché una tupla.</span><span class="sxs-lookup"><span data-stu-id="ae926-270">Start by modifying the `Find` method declaration so that it returns a `ref int` instead of a tuple.</span></span> <span data-ttu-id="ae926-271">Quindi, modificare l'istruzione return in modo che restituisca il valore memorizzato nella matrice anziché i due indici:</span><span class="sxs-lookup"><span data-stu-id="ae926-271">Then, modify the return statement so it returns the value stored in the matrix instead of the two indices:</span></span>

```csharp
// Note that this won't compile. 
// Method declaration indicates ref return,
// but return statement specifies a value return.
public static ref int Find2(int[,] matrix, Func<int, bool> predicate)
{
    for (int i = 0; i < matrix.GetLength(0); i++)
        for (int j = 0; j < matrix.GetLength(1); j++)
            if (predicate(matrix[i, j]))
                return matrix[i, j];
    throw new InvalidOperationException("Not found");
}
```

<span data-ttu-id="ae926-272">Quando si dichiara che un metodo restituisce una variabile `ref` è inoltre necessario aggiungere la parola chiave `ref` a ogni istruzione return.</span><span class="sxs-lookup"><span data-stu-id="ae926-272">When you declare that a method returns a `ref` variable, you must also add the `ref` keyword to each return statement.</span></span> <span data-ttu-id="ae926-273">Ciò indica la restituzione per riferimento e consente agli sviluppatori che leggono il codice di ricordare in seguito che il metodo restituisce per riferimento:</span><span class="sxs-lookup"><span data-stu-id="ae926-273">That indicates return by reference, and helps developers reading the code later remember that the method returns by reference:</span></span>

[!code-csharp[FindReturningRef](../../../samples/snippets/csharp/new-in-7/MatrixSearch.cs#22_FindReturningRef "Find returning by reference")]

<span data-ttu-id="ae926-274">Ora che il metodo restituisce un riferimento al valore integer della matrice, è necessario modificare il punto in cui viene chiamato.</span><span class="sxs-lookup"><span data-stu-id="ae926-274">Now that the method returns a reference to the integer value in the matrix, you need to modify where it's called.</span></span>  <span data-ttu-id="ae926-275">La dichiarazione `var` significa che `valItem` è ora un oggetto `int` anziché una tupla:</span><span class="sxs-lookup"><span data-stu-id="ae926-275">The `var` declaration means that `valItem` is now an `int` rather than a tuple:</span></span>

[!code-csharp[AssignRefReturnToValue](../../../samples/snippets/csharp/new-in-7/program.cs#23_AssignRefReturnToValue "Assign ref return to value")]

<span data-ttu-id="ae926-276">La seconda istruzione `WriteLine` nell'esempio precedente stampa il valore `42`, non `24`.</span><span class="sxs-lookup"><span data-stu-id="ae926-276">The second `WriteLine` statement in the example above prints out the value `42`, not `24`.</span></span> <span data-ttu-id="ae926-277">La variabile `valItem` è `int`, non `ref int`.</span><span class="sxs-lookup"><span data-stu-id="ae926-277">The variable `valItem` is an `int`, not a `ref int`.</span></span> <span data-ttu-id="ae926-278">La parola chiave `var` consente al compilatore di specificare il tipo, ma non di aggiungere in modo implicito il modificatore `ref`.</span><span class="sxs-lookup"><span data-stu-id="ae926-278">The `var` keyword enables the compiler to specify the type, but will not implicitly add the `ref` modifier.</span></span> <span data-ttu-id="ae926-279">Il valore a cui fa riferimento `ref return` viene invece *copiato* nella variabile sul lato sinistro dell'assegnazione.</span><span class="sxs-lookup"><span data-stu-id="ae926-279">Instead, the value referred to by the `ref return` is *copied* to the variable on the left-hand side of the assignment.</span></span> <span data-ttu-id="ae926-280">La variabile non è una variabile locale `ref`.</span><span class="sxs-lookup"><span data-stu-id="ae926-280">The variable is not a `ref` local.</span></span>

<span data-ttu-id="ae926-281">Per ottenere il risultato desiderato, è necessario aggiungere il modificatore `ref` alla dichiarazione di variabile locale per fare in modo che la variabile sia un riferimento quando il valore restituito è un riferimento:</span><span class="sxs-lookup"><span data-stu-id="ae926-281">In order to get the result you want, you need to add the `ref` modifier to the local variable declaration to make the variable a reference when the return value is a reference:</span></span>

[!code-csharp[AssignRefReturn](../../../samples/snippets/csharp/new-in-7/program.cs#24_AssignRefReturn "Assign ref return")]

<span data-ttu-id="ae926-282">A questo punto, la seconda istruzione `WriteLine` nell'esempio precedente consente di stampare il valore `24`, che indica che la memorizzazione nella matrice è stata modificata.</span><span class="sxs-lookup"><span data-stu-id="ae926-282">Now, the second `WriteLine` statement in the example above will print out the value `24`, indicating that the storage in the matrix has been modified.</span></span> <span data-ttu-id="ae926-283">La variabile locale è stata dichiarata con il modificatore `ref` e accetterà un valore restituito `ref`.</span><span class="sxs-lookup"><span data-stu-id="ae926-283">The local variable has been declared with the `ref` modifier, and it will take a `ref` return.</span></span> <span data-ttu-id="ae926-284">È necessario inizializzare una variabile `ref` quando viene dichiarata, non è possibile separare la dichiarazione e l'inizializzazione.</span><span class="sxs-lookup"><span data-stu-id="ae926-284">You must initialize a `ref` variable when it is declared, you cannot split the declaration and the initialization.</span></span>

<span data-ttu-id="ae926-285">Il linguaggio C# usa altre tre regole per evitare usi impropri delle variabili locali e dei valori restituiti `ref`:</span><span class="sxs-lookup"><span data-stu-id="ae926-285">The C# language has three other rules that protect you from misusing the `ref` locals and returns:</span></span>

* <span data-ttu-id="ae926-286">Non è possibile assegnare un valore restituito del metodo standard a una variabile locale `ref`.</span><span class="sxs-lookup"><span data-stu-id="ae926-286">You cannot assign a standard method return value to a `ref` local variable.</span></span>
    - <span data-ttu-id="ae926-287">Questo non consente istruzioni come `ref int i = sequence.Count();`</span><span class="sxs-lookup"><span data-stu-id="ae926-287">That disallows statements like `ref int i = sequence.Count();`</span></span>
* <span data-ttu-id="ae926-288">Non è possibile restituire un oggetto `ref` a una variabile la cui durata non si estende oltre l'esecuzione del metodo.</span><span class="sxs-lookup"><span data-stu-id="ae926-288">You cannot return a `ref` to a variable whose lifetime does not extend beyond the execution of the method.</span></span>
    - <span data-ttu-id="ae926-289">Ciò significa che non è possibile restituire un riferimento a una variabile locale o a una variabile con ambito simile.</span><span class="sxs-lookup"><span data-stu-id="ae926-289">That means you cannot return a reference to a local variable or a variable with a similar scope.</span></span>
* <span data-ttu-id="ae926-290">Non è possibile usare variabili locali e valori restituiti `ref` con i metodi asincroni.</span><span class="sxs-lookup"><span data-stu-id="ae926-290">`ref` locals and returns can't be used with async methods.</span></span>
    - <span data-ttu-id="ae926-291">Il compilatore non può stabilire se la variabile a cui si fa riferimento è stata impostata sul valore finale quando il metodo asincrono restituisce il controllo.</span><span class="sxs-lookup"><span data-stu-id="ae926-291">The compiler can't know if the referenced variable has been set to its final value when the async method returns.</span></span>

<span data-ttu-id="ae926-292">L'aggiunta di variabili locali e valori restituiti ref abilita algoritmi più efficienti, evitando la copia dei valori o l'esecuzione ripetuta di operazioni di dereferenziazione.</span><span class="sxs-lookup"><span data-stu-id="ae926-292">The addition of ref locals and ref returns enable algorithms that are more efficient by avoiding copying values, or performing dereferencing operations multiple times.</span></span> 

## <a name="local-functions"></a><span data-ttu-id="ae926-293">Funzioni locali</span><span class="sxs-lookup"><span data-stu-id="ae926-293">Local functions</span></span>

<span data-ttu-id="ae926-294">Molte progettazioni per le classi includono metodi che vengono chiamati da un solo percorso.</span><span class="sxs-lookup"><span data-stu-id="ae926-294">Many designs for classes include methods that are called from only one location.</span></span> <span data-ttu-id="ae926-295">Si tratta di metodi privati aggiuntivi che rendono più circoscritto e mirato ogni metodo.</span><span class="sxs-lookup"><span data-stu-id="ae926-295">These additional private methods keep each method small and focused.</span></span> <span data-ttu-id="ae926-296">Tuttavia, possono complicare la comprensione di una classe quando viene letta la prima volta.</span><span class="sxs-lookup"><span data-stu-id="ae926-296">However, they can make it harder to understand a class when reading it the first time.</span></span> <span data-ttu-id="ae926-297">Questi metodi devono essere compresi fuori dal contesto del singolo percorso chiamante.</span><span class="sxs-lookup"><span data-stu-id="ae926-297">These methods must be understood outside of the context of the single calling location.</span></span>

<span data-ttu-id="ae926-298">In queste progettazioni le *funzioni locali* consentono di dichiarare metodi all'interno del contesto di un altro metodo.</span><span class="sxs-lookup"><span data-stu-id="ae926-298">For those designs, *local functions* enable you to declare methods inside the context of another method.</span></span> <span data-ttu-id="ae926-299">In questo modo è più semplice per i lettori della classe verificare se il metodo locale viene chiamato solo dal contesto in cui è dichiarato.</span><span class="sxs-lookup"><span data-stu-id="ae926-299">This makes it easier for readers of the class to see that the local method is only called from the context in which is it declared.</span></span>

<span data-ttu-id="ae926-300">Esistono due casi di utilizzo molto comuni per le funzioni locali: i metodi iterator pubblici e i metodi async pubblici.</span><span class="sxs-lookup"><span data-stu-id="ae926-300">There are two very common use cases for local functions: public iterator methods and public async methods.</span></span> <span data-ttu-id="ae926-301">Entrambi i tipi di metodi generano codice che segnala gli errori in ritardo rispetto a quanto previsto dai programmatori.</span><span class="sxs-lookup"><span data-stu-id="ae926-301">Both types of methods generate code that reports errors later than programmers might expect.</span></span> <span data-ttu-id="ae926-302">Nel caso dei metodi iterator tutte le eccezioni vengono riscontrate solo quando si chiama il codice che enumera la sequenza restituita.</span><span class="sxs-lookup"><span data-stu-id="ae926-302">In the case of iterator methods, any exceptions are observed only when calling code that enumerates the returned sequence.</span></span> <span data-ttu-id="ae926-303">Nel caso dei metodi async tutte le eccezioni vengono riscontrate solo quando è atteso l'oggetto restituito `Task`.</span><span class="sxs-lookup"><span data-stu-id="ae926-303">In the case of async methods, any exceptions are only observed when the returned `Task` is awaited.</span></span>

<span data-ttu-id="ae926-304">Iniziamo con un metodo iterator:</span><span class="sxs-lookup"><span data-stu-id="ae926-304">Let's start with an iterator method:</span></span>

[!code-csharp[IteratorMethod](../../../samples/snippets/csharp/new-in-7/Iterator.cs#25_IteratorMethod "Iterator method")]

<span data-ttu-id="ae926-305">Esaminare il codice seguente che chiama il metodo iterator in modo non corretto:</span><span class="sxs-lookup"><span data-stu-id="ae926-305">Examine the code below that calls the iterator method incorrectly:</span></span>

[!code-csharp[CallIteratorMethod](../../../samples/snippets/csharp/new-in-7/program.cs#26_CallIteratorMethod "Call iterator method")]

<span data-ttu-id="ae926-306">L'eccezione viene generata quando viene eseguita l'iterazione di `resultSet`, non quando viene creato `resultSet`.</span><span class="sxs-lookup"><span data-stu-id="ae926-306">The exception is thrown when `resultSet` is iterated, not when `resultSet` is created.</span></span>
<span data-ttu-id="ae926-307">In questo esempio di dimensioni ridotte la maggior parte degli sviluppatori può diagnosticare rapidamente il problema.</span><span class="sxs-lookup"><span data-stu-id="ae926-307">In this contained example, most developers could quickly diagnose the problem.</span></span> <span data-ttu-id="ae926-308">Tuttavia, nelle codebase più grandi il codice che crea un iteratore spesso non è così vicino al codice che enumera i risultati.</span><span class="sxs-lookup"><span data-stu-id="ae926-308">However, in larger codebases, the code that creates an iterator often isn't as close to the code that enumerates the result.</span></span> <span data-ttu-id="ae926-309">È possibile eseguire il refactoring del codice in modo che il metodo pubblico convalidi tutti gli argomenti e un metodo privato generi l'enumerazione:</span><span class="sxs-lookup"><span data-stu-id="ae926-309">You can refactor the code so that the public method validates all arguments, and a private method generates the enumeration:</span></span>

[!code-csharp[IteratorMethodRefactored](../../../samples/snippets/csharp/new-in-7/Iterator.cs#27_IteratorMethodRefactored "Iterator method refactored")]

<span data-ttu-id="ae926-310">La versione sottoposta a refactoring genera immediatamente eccezioni perché il metodo pubblico non è un metodo iterator. Solo il metodo privato usa la sintassi `yield return`.</span><span class="sxs-lookup"><span data-stu-id="ae926-310">This refactored version will throw exceptions immediately because the public method is not an iterator method; only the private method uses the `yield return` syntax.</span></span> <span data-ttu-id="ae926-311">Tuttavia, esistono potenziali problemi con questo refactoring.</span><span class="sxs-lookup"><span data-stu-id="ae926-311">However, there are potential problems with this refactoring.</span></span> <span data-ttu-id="ae926-312">Il metodo privato deve essere chiamato solo dal metodo di interfaccia pubblico, perché in caso contrario viene ignorata la convalida di tutti gli argomenti.</span><span class="sxs-lookup"><span data-stu-id="ae926-312">The private method should only be called from the public interface method, because otherwise all argument validation is skipped.</span></span>
<span data-ttu-id="ae926-313">I lettori della classe devono individuare questo fatto leggendo l'intera classe e cercando eventuali altri riferimenti al metodo `alphabetSubsetImplementation`.</span><span class="sxs-lookup"><span data-stu-id="ae926-313">Readers of the class must discover this fact by reading the entire class and searching for any other references to the `alphabetSubsetImplementation` method.</span></span>

<span data-ttu-id="ae926-314">È possibile rendere più chiara la finalità della progettazione dichiarando `alphabetSubsetImplementation` come funzione locale all'interno del metodo API pubblico:</span><span class="sxs-lookup"><span data-stu-id="ae926-314">You can make that design intent more clear by declaring the `alphabetSubsetImplementation` as a local function inside the public API method:</span></span>

[!code-csharp[22_IteratorMethodLocal](../../../samples/snippets/csharp/new-in-7/Iterator.cs#28_IteratorMethodLocal "Iterator method with local function")]

<span data-ttu-id="ae926-315">La versione precedente indica chiaramente che viene fatto riferimento al metodo locale solo nel contesto del metodo esterno.</span><span class="sxs-lookup"><span data-stu-id="ae926-315">The version above makes it clear that the local method is referenced only in the context of the outer method.</span></span> <span data-ttu-id="ae926-316">Le regole per le funzioni locali garantiscono inoltre che uno sviluppatore non possa chiamare accidentalmente la funzione locale da un'altra posizione nella classe accidentalmente e ignorare la convalida degli argomenti.</span><span class="sxs-lookup"><span data-stu-id="ae926-316">The rules for local functions also ensure that a developer can't accidentally call the local function from another location in the class and bypass the argument validation.</span></span>

<span data-ttu-id="ae926-317">La stessa tecnica può essere usata con i metodi `async` per garantire che le eccezioni risultanti dalla convalida degli argomenti vengano generate prima che inizino le attività asincrone:</span><span class="sxs-lookup"><span data-stu-id="ae926-317">The same technique can be employed with `async` methods to ensure that exceptions arising from argument validation are thrown before the asynchronous work begins:</span></span>

[!code-csharp[TaskExample](../../../samples/snippets/csharp/new-in-7/AsyncWork.cs#29_TaskExample "Task returning method with local function")]

> [!NOTE]
> <span data-ttu-id="ae926-318">Alcune delle progettazioni supportate dalle funzioni locali possono essere eseguite anche usando le *espressioni lambda*.</span><span class="sxs-lookup"><span data-stu-id="ae926-318">Some of the designs that are supported by local functions could also be accomplished using *lambda expressions*.</span></span> <span data-ttu-id="ae926-319">Per chi è interessato sono disponibili [altre informazioni sulle differenze](../local-functions-vs-lambdas.md)</span><span class="sxs-lookup"><span data-stu-id="ae926-319">Those interested can [read more about the differences](../local-functions-vs-lambdas.md)</span></span>

## <a name="more-expression-bodied-members"></a><span data-ttu-id="ae926-320">Più membri con corpo di espressione</span><span class="sxs-lookup"><span data-stu-id="ae926-320">More expression-bodied members</span></span>

<span data-ttu-id="ae926-321">In C# 6 sono stati introdotti i [membri con corpo di espressione](csharp-6.md#expression-bodied-function-members) per le funzioni membro e le proprietà di sola lettura.</span><span class="sxs-lookup"><span data-stu-id="ae926-321">C# 6 introduced [expression-bodied members](csharp-6.md#expression-bodied-function-members) for member functions, and read-only properties.</span></span> <span data-ttu-id="ae926-322">C# 7.0 amplia la gamma di membri consentiti che possono essere implementati come espressioni.</span><span class="sxs-lookup"><span data-stu-id="ae926-322">C# 7.0 expands the allowed members that can be implemented as expressions.</span></span> <span data-ttu-id="ae926-323">In C# 7.0 è possibile implementare *costruttori*, *finalizzatori* e funzioni di accesso `get` e `set` per *proprietà* e *indicizzatori*.</span><span class="sxs-lookup"><span data-stu-id="ae926-323">In C# 7.0, you can implement *constructors*, *finalizers*, and `get` and `set` accessors on *properties* and *indexers*.</span></span> <span data-ttu-id="ae926-324">Nel codice riportato di seguito vengono illustrati esempi di ogni tipo di membro:</span><span class="sxs-lookup"><span data-stu-id="ae926-324">The following code shows examples of each:</span></span>

[!code-csharp[ExpressionBodiedMembers](../../../samples/snippets/csharp/new-in-7/expressionmembers.cs#36_ExpressionBodiedEverything "new expression-bodied members")]

> [!NOTE]
> <span data-ttu-id="ae926-325">In questo esempio non è necessario un finalizzatore, ma viene incluso per illustrare la sintassi.</span><span class="sxs-lookup"><span data-stu-id="ae926-325">This example does not need a finalizer, but it is shown to demonstrate the syntax.</span></span> <span data-ttu-id="ae926-326">Non implementare un finalizzatore nella classe a meno che non sia necessario per rilasciare risorse non gestite.</span><span class="sxs-lookup"><span data-stu-id="ae926-326">You should not implement a finalizer in your class unless it is necessary to  release unmanaged resources.</span></span> <span data-ttu-id="ae926-327">È inoltre consigliabile usare la classe <xref:System.Runtime.InteropServices.SafeHandle> anziché gestire direttamente le risorse non gestite.</span><span class="sxs-lookup"><span data-stu-id="ae926-327">You should also consider using the <xref:System.Runtime.InteropServices.SafeHandle> class instead of managing unmanaged resources directly.</span></span>

<span data-ttu-id="ae926-328">Le nuove posizioni per i membri con corpo di espressione rappresentano un importante punto cardine per il linguaggio C#: queste funzionalità sono state implementate dai membri della community che lavoravano al progetto open source [Roslyn](https://github.com/dotnet/Roslyn).</span><span class="sxs-lookup"><span data-stu-id="ae926-328">These new locations for expression-bodied members represent an important milestone for the C# language: These features were implemented by community members working on the open-source [Roslyn](https://github.com/dotnet/Roslyn) project.</span></span>

## <a name="throw-expressions"></a><span data-ttu-id="ae926-329">Espressioni throw</span><span class="sxs-lookup"><span data-stu-id="ae926-329">Throw expressions</span></span>

<span data-ttu-id="ae926-330">In C# `throw` è sempre stata un'istruzione.</span><span class="sxs-lookup"><span data-stu-id="ae926-330">In C#, `throw` has always been a statement.</span></span> <span data-ttu-id="ae926-331">Poiché `throw` è un'istruzione, non un'espressione, vi sono costrutti di C# in cui non è possibile usarla.</span><span class="sxs-lookup"><span data-stu-id="ae926-331">Because `throw` is a statement, not an expression, there were C# constructs where you could not use it.</span></span> <span data-ttu-id="ae926-332">Sono incluse le espressioni condizionali, le espressioni Null ridondanti e alcune espressioni lambda.</span><span class="sxs-lookup"><span data-stu-id="ae926-332">These included conditional expressions, null coalescing expressions, and some lambda expressions.</span></span> <span data-ttu-id="ae926-333">L'aggiunta di membri con corpo di espressione consente di aggiungere più posizioni in cui le espressioni `throw` possono risultare utili.</span><span class="sxs-lookup"><span data-stu-id="ae926-333">The addition of expression-bodied members adds more locations where `throw` expressions would be useful.</span></span> <span data-ttu-id="ae926-334">Per fare in modo che sia possibile scrivere uno di questi costrutti, C# 7.0 introduce le *espressioni throw*.</span><span class="sxs-lookup"><span data-stu-id="ae926-334">So that you can write any of these constructs, C# 7.0 introduces *throw expressions*.</span></span>

<span data-ttu-id="ae926-335">La sintassi è la stessa che è sempre stata usata per le istruzioni `throw`.</span><span class="sxs-lookup"><span data-stu-id="ae926-335">The syntax is the same as you've always used for `throw` statements.</span></span> <span data-ttu-id="ae926-336">L'unica differenza è che ora è possibile l'inserimento in nuove posizioni, ad esempio in un'espressione condizionale:</span><span class="sxs-lookup"><span data-stu-id="ae926-336">The only difference is that now you can place them in new locations, such as in a conditional expression:</span></span>

[!code-csharp[Throw_ExpressionExample](../../../samples/snippets/csharp/new-in-7/throwexpressions.cs#37_Throw_ExpressionExample "conditional throw expressions")]

<span data-ttu-id="ae926-337">Questa funzionalità consente l'uso di espressioni throw nelle espressioni di inizializzazione:</span><span class="sxs-lookup"><span data-stu-id="ae926-337">This features enables using throw expressions in initialization expressions:</span></span>

[!code-csharp[ThrowInInitialization](../../../samples/snippets/csharp/new-in-7/throwexpressions.cs#38_ThrowInInitialization "conditional throw expressions")]

<span data-ttu-id="ae926-338">In precedenza le inizializzazioni dovevano avvenire in un costruttore, con le istruzioni throw nel corpo del costruttore:</span><span class="sxs-lookup"><span data-stu-id="ae926-338">Previously, those initializations would need to be in a constructor, with the throw statements in the body of the constructor:</span></span>


[!code-csharp[ThrowInConstructor](../../../samples/snippets/csharp/new-in-7/throwexpressions.cs#39_ThrowInConstructor "throw statements")]

> [!NOTE]
> <span data-ttu-id="ae926-339">Entrambi i costrutti precedenti causano la generazione di eccezioni durante la costruzione di un oggetto.</span><span class="sxs-lookup"><span data-stu-id="ae926-339">Both of the preceding constructs will cause exceptions to be thrown during the construction of an object.</span></span> <span data-ttu-id="ae926-340">Spesso sono situazioni difficili da risolvere.</span><span class="sxs-lookup"><span data-stu-id="ae926-340">Those are often difficult to recover from.</span></span>
> <span data-ttu-id="ae926-341">Per questo motivo, non sono consigliate le progettazioni che generano eccezioni durante la costruzione.</span><span class="sxs-lookup"><span data-stu-id="ae926-341">For that reason, designs that throw exceptions during construction are discouraged.</span></span>

## <a name="generalized-async-return-types"></a><span data-ttu-id="ae926-342">Tipi restituiti asincroni generalizzati</span><span class="sxs-lookup"><span data-stu-id="ae926-342">Generalized async return types</span></span>

<span data-ttu-id="ae926-343">La restituzione di un oggetto `Task` dai metodi asincroni può introdurre colli di bottiglia delle prestazioni in determinati percorsi.</span><span class="sxs-lookup"><span data-stu-id="ae926-343">Returning a `Task` object from async methods can introduce performance bottlenecks in certain paths.</span></span> <span data-ttu-id="ae926-344">`Task` è un tipo di riferimento, quindi usarlo significa allocare un oggetto.</span><span class="sxs-lookup"><span data-stu-id="ae926-344">`Task` is a reference type, so using it means allocating an object.</span></span> <span data-ttu-id="ae926-345">Nei casi in cui un metodo dichiarato con il modificatore `async` restituisce un risultato memorizzato nella cache o viene completato in modo sincrono, le allocazioni aggiuntive possono diventare impegnative in termini di tempo nelle sezioni di codice critiche per le prestazioni.</span><span class="sxs-lookup"><span data-stu-id="ae926-345">In cases where a method declared with the `async` modifier returns a cached result, or completes synchronously, the extra allocations can become a significant time cost in performance critical sections of code.</span></span> <span data-ttu-id="ae926-346">Possono diventare molto onerose se si verificano in cicli ridotti.</span><span class="sxs-lookup"><span data-stu-id="ae926-346">It can become very costly if those allocations occur in tight loops.</span></span>

<span data-ttu-id="ae926-347">La nuova funzionalità del linguaggio consente ai metodi asincroni di restituire altri tipi oltre a `Task`, `Task<T>` e `void`.</span><span class="sxs-lookup"><span data-stu-id="ae926-347">The new language feature means that async methods may return other types in addition to `Task`, `Task<T>` and `void`.</span></span> <span data-ttu-id="ae926-348">Il tipo restituito deve comunque essere conforme al modello asincrono ovvero deve essere accessibile un metodo `GetAwaiter`.</span><span class="sxs-lookup"><span data-stu-id="ae926-348">The returned type must still satisfy the async pattern, meaning a `GetAwaiter` method must be accessible.</span></span> <span data-ttu-id="ae926-349">Per fare un esempio concreto, il tipo `ValueTask` è stato aggiunto a .NET Framework per consentire l'uso di questa nuova funzionalità del linguaggio:</span><span class="sxs-lookup"><span data-stu-id="ae926-349">As one concrete example, the `ValueTask` type has been added to the .NET framework to make use of this new language feature:</span></span> 

[!code-csharp[UsingValueTask](../../../samples/snippets/csharp/new-in-7/AsyncWork.cs#30_UsingValueTask "Using ValueTask")]

> [!NOTE]
> <span data-ttu-id="ae926-350">È necessario aggiungere il pacchetto NuGet [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) per usare il tipo <xref:System.Threading.Tasks.ValueTask%601>.</span><span class="sxs-lookup"><span data-stu-id="ae926-350">You need to add the NuGet package [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) in order to use the <xref:System.Threading.Tasks.ValueTask%601> type.</span></span>

<span data-ttu-id="ae926-351">Una semplice ottimizzazione sarebbe usare `ValueTask` in posizioni in cui prima sarebbe stato usato `Task`.</span><span class="sxs-lookup"><span data-stu-id="ae926-351">A simple optimization would be to use `ValueTask` in places where `Task` would be used before.</span></span> <span data-ttu-id="ae926-352">Tuttavia, se si vogliono eseguire manualmente altre ottimizzazioni, è possibile memorizzare nella cache i risultati delle attività asincrone e usarli di nuovo nelle chiamate successive.</span><span class="sxs-lookup"><span data-stu-id="ae926-352">However, if you want to perform extra optimizations by hand, you can cache results from async work and reuse the result in subsequent calls.</span></span> <span data-ttu-id="ae926-353">Lo struct `ValueTask` include un costruttore con un parametro `Task` in modo che sia possibile costruire un oggetto `ValueTask` dal valore restituito di qualsiasi metodo asincrono esistente:</span><span class="sxs-lookup"><span data-stu-id="ae926-353">The `ValueTask` struct has a constructor with a `Task` parameter so that you can construct a `ValueTask` from the return value of any existing async method:</span></span>

[!code-csharp[AsyncOptimizedValueTask](../../../samples/snippets/csharp/new-in-7/AsyncWork.cs#31_AsyncOptimizedValueTask "Return async result or cached value")]
 
<span data-ttu-id="ae926-354">Come si raccomanda sempre a proposito delle prestazioni, è necessario creare un benchmark di entrambe le versioni prima di apportare modifiche su larga scala al codice.</span><span class="sxs-lookup"><span data-stu-id="ae926-354">As with all performance recommendations, you should benchmark both versions before making large scale changes to your code.</span></span>

## <a name="numeric-literal-syntax-improvements"></a><span data-ttu-id="ae926-355">Miglioramenti della sintassi dei valori letterali numerici</span><span class="sxs-lookup"><span data-stu-id="ae926-355">Numeric literal syntax improvements</span></span>

<span data-ttu-id="ae926-356">La lettura non corretta delle costanti numeriche può rendere più difficile la comprensione del codice quando viene letto per la prima volta.</span><span class="sxs-lookup"><span data-stu-id="ae926-356">Misreading numeric constants can make it harder to understand code when reading it for the first time.</span></span> <span data-ttu-id="ae926-357">Ciò si verifica spesso quando questi numeri vengono usati come maschere di bit o altri elementi simbolici anziché come valori numerici.</span><span class="sxs-lookup"><span data-stu-id="ae926-357">This often occurs when those numbers are used as bit masks or other symbolic rather than numeric values.</span></span> <span data-ttu-id="ae926-358">C# 7.0 include due nuove funzionalità per semplificare la scrittura dei numeri in modo che siano più leggibili per l'uso previsto: *valori letterali binari* e *separatori di cifre*.</span><span class="sxs-lookup"><span data-stu-id="ae926-358">C# 7.0 includes two new features to make it easier to write numbers in the most readable fashion for the intended use: *binary literals*, and *digit separators*.</span></span>

<span data-ttu-id="ae926-359">Ogni volta che si creano maschere di bit o che una rappresentazione binaria di un numero rende il codice più leggibile, scrivere il numero in formato binario:</span><span class="sxs-lookup"><span data-stu-id="ae926-359">For those times when you are creating bit masks, or whenever a binary representation of a number makes the most readable code, write that number in binary:</span></span>

[!code-csharp[BinaryConstants](../../../samples/snippets/csharp/new-in-7/Program.cs#32_BinaryConstants "Binary constants")]

<span data-ttu-id="ae926-360">`0b` all'inizio della costante indica che il numero viene scritto come numero binario.</span><span class="sxs-lookup"><span data-stu-id="ae926-360">The `0b` at the beginning of the constant indicates that the number is written as a binary number.</span></span>

<span data-ttu-id="ae926-361">I numeri binari possono essere molto lunghi, quindi spesso è più semplice visualizzare gli schemi di bit introducendo il carattere `_` come separatore di cifre:</span><span class="sxs-lookup"><span data-stu-id="ae926-361">Binary numbers can get very long, so it's often easier to see the bit patterns by introducing the `_` as a digit separator:</span></span>

[!code-csharp[ThousandSeparators](../../../samples/snippets/csharp/new-in-7/Program.cs#33_ThousandSeparators "Thousands separators")]

<span data-ttu-id="ae926-362">Il separatore di cifre può apparire ovunque nella costante.</span><span class="sxs-lookup"><span data-stu-id="ae926-362">The digit separator can appear anywhere in the constant.</span></span> <span data-ttu-id="ae926-363">Per i numeri in base 10, è più comune usarlo come separatore delle migliaia:</span><span class="sxs-lookup"><span data-stu-id="ae926-363">For base 10 numbers, it would be common to use it as a thousands separator:</span></span>

[!code-csharp[LargeIntegers](../../../samples/snippets/csharp/new-in-7/Program.cs#34_LargeIntegers "Large integer")]

<span data-ttu-id="ae926-364">Il separatore di cifre può essere usato anche con i tipi `decimal`, `float` e `double`:</span><span class="sxs-lookup"><span data-stu-id="ae926-364">The digit separator can be used with `decimal`, `float` and `double` types as well:</span></span>

[!code-csharp[OtherConstants](../../../samples/snippets/csharp/new-in-7/Program.cs#35_OtherConstants "non-integral constants")]

<span data-ttu-id="ae926-365">Nel loro insieme, è possibile dichiarare le costanti numeriche ottimizzando la leggibilità.</span><span class="sxs-lookup"><span data-stu-id="ae926-365">Taken together, you can declare numeric constants with much more readability.</span></span>
