---
title: Citazioni di codice (F#)
description: 'Informazioni su F # citazioni di codice, una funzionalità del linguaggio che consente di generare e utilizzare le espressioni di codice F # a livello di codice.'
ms.date: 05/16/2016
ms.openlocfilehash: a6fab0364cadef1f45276267a59c694140b24a9c
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 05/04/2018
ms.locfileid: "33564534"
---
# <a name="code-quotations"></a><span data-ttu-id="b71c9-103">Citazioni di codice</span><span class="sxs-lookup"><span data-stu-id="b71c9-103">Code Quotations</span></span>

> [!NOTE]
<span data-ttu-id="b71c9-104">Il collegamento al riferimento per l'API porta a MSDN.</span><span class="sxs-lookup"><span data-stu-id="b71c9-104">The API reference link will take you to MSDN.</span></span>  <span data-ttu-id="b71c9-105">Il riferimento all'API in Microsoft Docs (docs.microsoft.com) non è completo.</span><span class="sxs-lookup"><span data-stu-id="b71c9-105">The docs.microsoft.com API reference is not complete.</span></span>

<span data-ttu-id="b71c9-106">Questo argomento viene descritto *quotation di codice*, una funzionalità del linguaggio che consente di generare e utilizzare le espressioni di codice F # a livello di codice.</span><span class="sxs-lookup"><span data-stu-id="b71c9-106">This topic describes *code quotations*, a language feature that enables you to generate and work with F# code expressions programmatically.</span></span> <span data-ttu-id="b71c9-107">Questa funzionalità consente di generare un albero sintattico astratto che rappresenta il codice F #.</span><span class="sxs-lookup"><span data-stu-id="b71c9-107">This feature lets you generate an abstract syntax tree that represents F# code.</span></span> <span data-ttu-id="b71c9-108">L'albero sintattico astratto può quindi essere attraversato ed elaborato in base alle esigenze dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="b71c9-108">The abstract syntax tree can then be traversed and processed according to the needs of your application.</span></span> <span data-ttu-id="b71c9-109">Ad esempio, è possibile utilizzare la struttura ad albero per generare codice F # o generare il codice in un altro linguaggio.</span><span class="sxs-lookup"><span data-stu-id="b71c9-109">For example, you can use the tree to generate F# code or generate code in some other language.</span></span>


## <a name="quoted-expressions"></a><span data-ttu-id="b71c9-110">Espressioni tra virgolette</span><span class="sxs-lookup"><span data-stu-id="b71c9-110">Quoted Expressions</span></span>
<span data-ttu-id="b71c9-111">Oggetto *espressione in quotation* è un'espressione F # nel codice che è delimitato in modo tale da non compilata come parte del programma, ma viene invece compilata in un oggetto che rappresenta un'espressione F #.</span><span class="sxs-lookup"><span data-stu-id="b71c9-111">A *quoted expression* is an F# expression in your code that is delimited in such a way that it is not compiled as part of your program, but instead is compiled into an object that represents an F# expression.</span></span> <span data-ttu-id="b71c9-112">È possibile contrassegnare un'espressione tra virgolette in uno dei due modi: con informazioni sul tipo o senza informazioni sul tipo.</span><span class="sxs-lookup"><span data-stu-id="b71c9-112">You can mark a quoted expression in one of two ways: either with type information or without type information.</span></span> <span data-ttu-id="b71c9-113">Se si desidera includere informazioni sul tipo, si utilizzano i simboli `<@` e `@>` per delimitare l'espressione tra virgolette.</span><span class="sxs-lookup"><span data-stu-id="b71c9-113">If you want to include type information, you use the symbols `<@` and `@>` to delimit the quoted expression.</span></span> <span data-ttu-id="b71c9-114">Se non si richiedono informazioni sul tipo, si utilizzano i simboli `<@@` e `@@>`.</span><span class="sxs-lookup"><span data-stu-id="b71c9-114">If you do not need type information, you use the symbols `<@@` and `@@>`.</span></span> <span data-ttu-id="b71c9-115">Il codice seguente illustra le offerte di DataSet tipizzate e non.</span><span class="sxs-lookup"><span data-stu-id="b71c9-115">The following code shows typed and untyped quotations.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet501.fs)]

<span data-ttu-id="b71c9-116">Attraversare un albero delle espressioni di grandi dimensioni è più veloce se non si include informazioni sul tipo.</span><span class="sxs-lookup"><span data-stu-id="b71c9-116">Traversing a large expression tree is faster if you do not include type information.</span></span> <span data-ttu-id="b71c9-117">Il tipo risulta dell'espressione tra virgolette con i simboli tipizzati è `Expr<'T>`, dove il parametro di tipo presenta il tipo dell'espressione, come determinato dall'algoritmo di inferenza del tipo del compilatore F #.</span><span class="sxs-lookup"><span data-stu-id="b71c9-117">The resulting type of an expression quoted with the typed symbols is `Expr<'T>`, where the type parameter has the type of the expression as determined by the F# compiler's type inference algorithm.</span></span> <span data-ttu-id="b71c9-118">Quando si usa citazioni di codice senza informazioni sul tipo, il tipo dell'espressione tra virgolette è il tipo non generico [Expr](https://msdn.microsoft.com/library/ed6a2caf-69d4-45c2-ab97-e9b3be9bce65).</span><span class="sxs-lookup"><span data-stu-id="b71c9-118">When you use code quotations without type information, the type of the quoted expression is the non-generic type [Expr](https://msdn.microsoft.com/library/ed6a2caf-69d4-45c2-ab97-e9b3be9bce65).</span></span> <span data-ttu-id="b71c9-119">È possibile chiamare il [Raw](https://msdn.microsoft.com/library/47fb94f1-e77f-4c68-aabc-2b0ba40d59c2) proprietà tipizzata `Expr` classe per ottenere il dataset non `Expr` oggetto.</span><span class="sxs-lookup"><span data-stu-id="b71c9-119">You can call the [Raw](https://msdn.microsoft.com/library/47fb94f1-e77f-4c68-aabc-2b0ba40d59c2) property on the typed `Expr` class to obtain the untyped `Expr` object.</span></span>

<span data-ttu-id="b71c9-120">Esistono diversi metodi statici che consentono di generare oggetti F # espressione livello di programmazione la `Expr` classe senza l'utilizzo di espressioni in quotation.</span><span class="sxs-lookup"><span data-stu-id="b71c9-120">There are a variety of static methods that allow you to generate F# expression objects programmatically in the `Expr` class without using quoted expressions.</span></span>

<span data-ttu-id="b71c9-121">Si noti che una quotation di codice deve includere un'espressione completa.</span><span class="sxs-lookup"><span data-stu-id="b71c9-121">Note that a code quotation must include a complete expression.</span></span> <span data-ttu-id="b71c9-122">Per un `let` associazione, ad esempio, è necessario sia la definizione del nome dell'associazione e un'espressione aggiuntiva che utilizza l'associazione.</span><span class="sxs-lookup"><span data-stu-id="b71c9-122">For a `let` binding, for example, you need both the definition of the bound name and an additional expression that uses the binding.</span></span> <span data-ttu-id="b71c9-123">Nella sintassi dettagliata, si tratta di un'espressione che segue il `in` (parola chiave).</span><span class="sxs-lookup"><span data-stu-id="b71c9-123">In verbose syntax, this is an expression that follows the `in` keyword.</span></span> <span data-ttu-id="b71c9-124">Al livello superiore in un modulo, questa è l'espressione successiva nel modulo, ma in un'offerta, è necessario in modo esplicito.</span><span class="sxs-lookup"><span data-stu-id="b71c9-124">At the top-level in a module, this is just the next expression in the module, but in a quotation, it is explicitly required.</span></span>

<span data-ttu-id="b71c9-125">Pertanto, l'espressione seguente non è valido.</span><span class="sxs-lookup"><span data-stu-id="b71c9-125">Therefore, the following expression is not valid.</span></span>

```fsharp
// Not valid:
// <@ let f x = x + 1 @>
```

<span data-ttu-id="b71c9-126">Ma le espressioni seguenti sono valide.</span><span class="sxs-lookup"><span data-stu-id="b71c9-126">But the following expressions are valid.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet502.fs)]

<span data-ttu-id="b71c9-127">Per utilizzare citazioni di codice, è necessario aggiungere una dichiarazione di importazione (tramite il `open` parola chiave) che consente di aprire il [Quotations](https://msdn.microsoft.com/library/e9ce8a3a-e00c-4190-bad5-cce52ee089b2) dello spazio dei nomi.</span><span class="sxs-lookup"><span data-stu-id="b71c9-127">To use code quotations, you must add an import declaration (by using the `open` keyword) that opens the [Microsoft.FSharp.Quotations](https://msdn.microsoft.com/library/e9ce8a3a-e00c-4190-bad5-cce52ee089b2) namespace.</span></span>

<span data-ttu-id="b71c9-128">PowerPacks F # offre supporto per la valutazione e l'esecuzione di oggetti di espressione F #.</span><span class="sxs-lookup"><span data-stu-id="b71c9-128">The F# PowerPack provides support for evaluating and executing F# expression objects.</span></span>


## <a name="expr-type"></a><span data-ttu-id="b71c9-129">Tipo di expr</span><span class="sxs-lookup"><span data-stu-id="b71c9-129">Expr Type</span></span>
<span data-ttu-id="b71c9-130">Un'istanza di `Expr` tipo rappresenta un'espressione F #.</span><span class="sxs-lookup"><span data-stu-id="b71c9-130">An instance of the `Expr` type represents an F# expression.</span></span> <span data-ttu-id="b71c9-131">Sia il tipo generico e non generica `Expr` tipi sono documentati nella documentazione della libreria F #.</span><span class="sxs-lookup"><span data-stu-id="b71c9-131">Both the generic and the non-generic `Expr` types are documented in the F# library documentation.</span></span> <span data-ttu-id="b71c9-132">Per ulteriori informazioni, vedere [Quotations Namespace](https://msdn.microsoft.com/visualfsharpdocs/conceptual/microsoft.fsharp.quotations-namespace-%5bfsharp%5d) e [classe Quotations. expr](https://msdn.microsoft.com/visualfsharpdocs/conceptual/quotations.expr-class-%5bfsharp%5d).</span><span class="sxs-lookup"><span data-stu-id="b71c9-132">For more information, see [Microsoft.FSharp.Quotations Namespace](https://msdn.microsoft.com/visualfsharpdocs/conceptual/microsoft.fsharp.quotations-namespace-%5bfsharp%5d) and [Quotations.Expr Class](https://msdn.microsoft.com/visualfsharpdocs/conceptual/quotations.expr-class-%5bfsharp%5d).</span></span>


## <a name="splicing-operators"></a><span data-ttu-id="b71c9-133">Operatori di splicing</span><span class="sxs-lookup"><span data-stu-id="b71c9-133">Splicing Operators</span></span>
<span data-ttu-id="b71c9-134">Splicing consente di combinare citazioni di codice letterale con espressioni che è stato creato a livello di codice o da un'altra offerta di codice.</span><span class="sxs-lookup"><span data-stu-id="b71c9-134">Splicing enables you to combine literal code quotations with expressions that you have created programmatically or from another code quotation.</span></span> <span data-ttu-id="b71c9-135">Il `%` e `%%` operatori consentono di aggiungere un oggetto di espressione F # in una quotation di codice.</span><span class="sxs-lookup"><span data-stu-id="b71c9-135">The `%` and `%%` operators enable you to add an F# expression object into a code quotation.</span></span> <span data-ttu-id="b71c9-136">Utilizzare il `%` operatore per inserire un oggetto di espressione tipizzata in una quotation tipizzata, ma utilizzare il `%%` operatore per inserire un oggetto di espressione non tipizzata in una quotation non tipizzato.</span><span class="sxs-lookup"><span data-stu-id="b71c9-136">You use the `%` operator to insert a typed expression object into a typed quotation; you use the `%%` operator to insert an untyped expression object into an untyped quotation.</span></span> <span data-ttu-id="b71c9-137">Entrambi gli operatori sono gli operatori di prefisso unario.</span><span class="sxs-lookup"><span data-stu-id="b71c9-137">Both operators are unary prefix operators.</span></span> <span data-ttu-id="b71c9-138">In questo modo se `expr` è un'espressione non tipizzata di tipo `Expr`, il codice seguente è valido.</span><span class="sxs-lookup"><span data-stu-id="b71c9-138">Thus if `expr` is an untyped expression of type `Expr`, the following code is valid.</span></span>

```fsharp
<@@ 1 + %%expr @@>
```

<span data-ttu-id="b71c9-139">E se `expr` è una quotation tipizzata di tipo `Expr<int>`, il codice seguente è valido.</span><span class="sxs-lookup"><span data-stu-id="b71c9-139">And if `expr` is a typed quotation of type `Expr<int>`, the following code is valid.</span></span>

```fsharp
<@ 1 + %expr @>
```

## <a name="example"></a><span data-ttu-id="b71c9-140">Esempio</span><span class="sxs-lookup"><span data-stu-id="b71c9-140">Example</span></span>

### <a name="description"></a><span data-ttu-id="b71c9-141">Descrizione</span><span class="sxs-lookup"><span data-stu-id="b71c9-141">Description</span></span>
<span data-ttu-id="b71c9-142">Nell'esempio seguente viene illustrato l'utilizzo di citazioni di codice per inserire codice F # in un oggetto di espressione e quindi stampare il codice F # che rappresenta l'espressione.</span><span class="sxs-lookup"><span data-stu-id="b71c9-142">The following example illustrates the use of code quotations to put F# code into an expression object and then print the F# code that represents the expression.</span></span> <span data-ttu-id="b71c9-143">Una funzione `println` è definito che contiene una funzione ricorsiva `print` che visualizza un oggetto di espressione F # (di tipo `Expr`) in un formato descrittivo.</span><span class="sxs-lookup"><span data-stu-id="b71c9-143">A function `println` is defined that contains a recursive function `print` that displays an F# expression object (of type `Expr`) in a friendly format.</span></span> <span data-ttu-id="b71c9-144">Esistono diversi criteri attivi nel [Microsoft.FSharp.Quotations.Patterns](https://msdn.microsoft.com/library/093944a9-c752-403a-8983-5fcd5dbf92a4) e [Microsoft.FSharp.Quotations.DerivedPatterns](https://msdn.microsoft.com/library/d2434a6e-ae7b-4f3d-b567-c162938bc9cd) moduli che possono essere utilizzati per analizzare gli oggetti di espressione.</span><span class="sxs-lookup"><span data-stu-id="b71c9-144">There are several active patterns in the [Microsoft.FSharp.Quotations.Patterns](https://msdn.microsoft.com/library/093944a9-c752-403a-8983-5fcd5dbf92a4) and [Microsoft.FSharp.Quotations.DerivedPatterns](https://msdn.microsoft.com/library/d2434a6e-ae7b-4f3d-b567-c162938bc9cd) modules that can be used to analyze expression objects.</span></span> <span data-ttu-id="b71c9-145">In questo esempio non include tutti i possibili modelli che potrebbero essere visualizzati in un'espressione F #.</span><span class="sxs-lookup"><span data-stu-id="b71c9-145">This example does not include all the possible patterns that might appear in an F# expression.</span></span> <span data-ttu-id="b71c9-146">Qualsiasi criterio non riconosciuto attiva una corrispondenza per il carattere jolly (`_`) e viene eseguito il rendering tramite il `ToString` (metodo), che, nel `Expr` digitare, consente di individuare il criterio attivo da aggiungere all'espressione corrispondente.</span><span class="sxs-lookup"><span data-stu-id="b71c9-146">Any unrecognized pattern triggers a match to the wildcard pattern (`_`) and is rendered by using the `ToString` method, which, on the `Expr` type, lets you know the active pattern to add to your match expression.</span></span>


### <a name="code"></a><span data-ttu-id="b71c9-147">Codice</span><span class="sxs-lookup"><span data-stu-id="b71c9-147">Code</span></span>
[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet601.fs)]
    
### <a name="output"></a><span data-ttu-id="b71c9-148">Output</span><span class="sxs-lookup"><span data-stu-id="b71c9-148">Output</span></span>

```fsharp
fun (x:System.Int32) -> x + 1
a + 1
let f = fun (x:System.Int32) -> x + 10 in f 10
```

## <a name="example"></a><span data-ttu-id="b71c9-149">Esempio</span><span class="sxs-lookup"><span data-stu-id="b71c9-149">Example</span></span>

### <a name="description"></a><span data-ttu-id="b71c9-150">Descrizione</span><span class="sxs-lookup"><span data-stu-id="b71c9-150">Description</span></span>
<span data-ttu-id="b71c9-151">È inoltre possibile utilizzare i tre criteri attivi nel [ExprShape (modulo)](https://msdn.microsoft.com/library/7685150e-2432-4d39-9338-57292eff18de) attraversare gli alberi delle espressioni con un numero inferiore di criteri attivo.</span><span class="sxs-lookup"><span data-stu-id="b71c9-151">You can also use the three active patterns in the [ExprShape module](https://msdn.microsoft.com/library/7685150e-2432-4d39-9338-57292eff18de) to traverse expression trees with fewer active patterns.</span></span> <span data-ttu-id="b71c9-152">Questi criteri attivi possono essere utili quando si desidera attraversare una struttura ad albero, ma non è necessario tutte le informazioni nella maggior parte dei nodi.</span><span class="sxs-lookup"><span data-stu-id="b71c9-152">These active patterns can be useful when you want to traverse a tree but you do not need all the information in most of the nodes.</span></span> <span data-ttu-id="b71c9-153">Quando si utilizzano questi criteri, qualsiasi espressione F # corrisponde a uno dei tre criteri seguenti: `ShapeVar` se l'espressione è una variabile, `ShapeLambda` se l'espressione è un'espressione lambda, o `ShapeCombination` se l'espressione è diversa.</span><span class="sxs-lookup"><span data-stu-id="b71c9-153">When you use these patterns, any F# expression matches one of the following three patterns: `ShapeVar` if the expression is a variable, `ShapeLambda` if the expression is a lambda expression, or `ShapeCombination` if the expression is anything else.</span></span> <span data-ttu-id="b71c9-154">Se si passano attraverso un albero delle espressioni usando i criteri attivi come nell'esempio di codice precedente, è necessario utilizzare molti più criteri per gestire tutti i possibili tipi F # espressione e il codice sarà più complesso.</span><span class="sxs-lookup"><span data-stu-id="b71c9-154">If you traverse an expression tree by using the active patterns as in the previous code example, you have to use many more patterns to handle all possible F# expression types, and your code will be more complex.</span></span> <span data-ttu-id="b71c9-155">Per altre informazioni, vedere [ExprShape.ShapeVar&#124;ShapeLambda&#124;ShapeCombination (modello attivo)](https://msdn.microsoft.com/visualfsharpdocs/conceptual/exprshape.shapevarhshapelambdahshapecombination-active-pattern-%5bfsharp%5d).</span><span class="sxs-lookup"><span data-stu-id="b71c9-155">For more information, see [ExprShape.ShapeVar&#124;ShapeLambda&#124;ShapeCombination Active Pattern](https://msdn.microsoft.com/visualfsharpdocs/conceptual/exprshape.shapevarhshapelambdahshapecombination-active-pattern-%5bfsharp%5d).</span></span>

<span data-ttu-id="b71c9-156">Esempio di codice seguente è utilizzabile come base per attraversamenti più complessi.</span><span class="sxs-lookup"><span data-stu-id="b71c9-156">The following code example can be used as a basis for more complex traversals.</span></span> <span data-ttu-id="b71c9-157">In questo codice, viene creato un albero delle espressioni per un'espressione che include una chiamata di funzione `add`.</span><span class="sxs-lookup"><span data-stu-id="b71c9-157">In this code, an expression tree is created for an expression that involves a function call, `add`.</span></span> <span data-ttu-id="b71c9-158">Il [SpecificCall](https://msdn.microsoft.com/library/05a77b21-20fe-4b9a-8e07-aa999538198d) (modello attivo) viene utilizzato per rilevare qualsiasi chiamata a `add` nell'albero delle espressioni.</span><span class="sxs-lookup"><span data-stu-id="b71c9-158">The [SpecificCall](https://msdn.microsoft.com/library/05a77b21-20fe-4b9a-8e07-aa999538198d) active pattern is used to detect any call to `add` in the expression tree.</span></span> <span data-ttu-id="b71c9-159">Questo criterio attivo assegna gli argomenti della chiamata al `exprList` valore.</span><span class="sxs-lookup"><span data-stu-id="b71c9-159">This active pattern assigns the arguments of the call to the `exprList` value.</span></span> <span data-ttu-id="b71c9-160">In questo caso, sono disponibili solo due, pertanto vengono estratti e la funzione viene chiamata in modo ricorsivo sugli argomenti.</span><span class="sxs-lookup"><span data-stu-id="b71c9-160">In this case, there are only two, so these are pulled out and the function is called recursively on the arguments.</span></span> <span data-ttu-id="b71c9-161">I risultati vengono inseriti in una quotation di codice che rappresenta una chiamata a `mul` usando l'operatore di splicing (`%%`).</span><span class="sxs-lookup"><span data-stu-id="b71c9-161">The results are inserted into a code quotation that represents a call to `mul` by using the splice operator (`%%`).</span></span> <span data-ttu-id="b71c9-162">Il `println` funzione dell'esempio precedente viene utilizzata per visualizzare i risultati.</span><span class="sxs-lookup"><span data-stu-id="b71c9-162">The `println` function from the previous example is used to display the results.</span></span>

<span data-ttu-id="b71c9-163">Il codice in altri rami (modello attivo) rigenera semplicemente dell'albero delle espressioni stesso, pertanto l'unica modifica nell'espressione risultante è la modifica da `add` a `mul`.</span><span class="sxs-lookup"><span data-stu-id="b71c9-163">The code in the other active pattern branches just regenerates the same expression tree, so the only change in the resulting expression is the change from `add` to `mul`.</span></span>


### <a name="code"></a><span data-ttu-id="b71c9-164">Codice</span><span class="sxs-lookup"><span data-stu-id="b71c9-164">Code</span></span>
[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet701.fs)]
    
### <a name="output"></a><span data-ttu-id="b71c9-165">Output</span><span class="sxs-lookup"><span data-stu-id="b71c9-165">Output</span></span>

```fsharp
1 + Module1.add(2,Module1.add(3,4))
1 + Module1.mul(2,Module1.mul(3,4))
```

## <a name="see-also"></a><span data-ttu-id="b71c9-166">Vedere anche</span><span class="sxs-lookup"><span data-stu-id="b71c9-166">See Also</span></span>
[<span data-ttu-id="b71c9-167">Riferimenti per il linguaggio F#</span><span class="sxs-lookup"><span data-stu-id="b71c9-167">F# Language Reference</span></span>](index.md)

