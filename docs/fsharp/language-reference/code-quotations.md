---
title: Citazioni di codice (F#)
description: "Informazioni su F # citazioni di codice, una funzionalità del linguaggio che consente di generare e utilizzare le espressioni di codice F # a livello di codice."
keywords: visual f#, f#, programmazione funzionale
author: cartermp
ms.author: phcart
ms.date: 05/16/2016
ms.topic: language-reference
ms.prod: .net
ms.technology: devlang-fsharp
ms.devlang: fsharp
ms.assetid: 4559e659-2b04-48bd-8a0b-8527920eec95
ms.openlocfilehash: f7a08013bc6487b570a62576bb01ca2dd65ce8b1
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 10/18/2017
---
# <a name="code-quotations"></a><span data-ttu-id="9d4de-104">Citazioni di codice</span><span class="sxs-lookup"><span data-stu-id="9d4de-104">Code Quotations</span></span>

> [!NOTE]
<span data-ttu-id="9d4de-105">Il collegamento al riferimento per l'API porta a MSDN.</span><span class="sxs-lookup"><span data-stu-id="9d4de-105">The API reference link will take you to MSDN.</span></span>  <span data-ttu-id="9d4de-106">Il riferimento all'API in Microsoft Docs (docs.microsoft.com) non è completo.</span><span class="sxs-lookup"><span data-stu-id="9d4de-106">The docs.microsoft.com API reference is not complete.</span></span>

<span data-ttu-id="9d4de-107">Questo argomento viene descritto *quotation di codice*, una funzionalità del linguaggio che consente di generare e utilizzare le espressioni di codice F # a livello di codice.</span><span class="sxs-lookup"><span data-stu-id="9d4de-107">This topic describes *code quotations*, a language feature that enables you to generate and work with F# code expressions programmatically.</span></span> <span data-ttu-id="9d4de-108">Questa funzionalità consente di generare un albero sintattico astratto che rappresenta il codice F #.</span><span class="sxs-lookup"><span data-stu-id="9d4de-108">This feature lets you generate an abstract syntax tree that represents F# code.</span></span> <span data-ttu-id="9d4de-109">L'albero sintattico astratto può quindi essere attraversato ed elaborato in base alle esigenze dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="9d4de-109">The abstract syntax tree can then be traversed and processed according to the needs of your application.</span></span> <span data-ttu-id="9d4de-110">Ad esempio, è possibile utilizzare la struttura ad albero per generare codice F # o generare il codice in un altro linguaggio.</span><span class="sxs-lookup"><span data-stu-id="9d4de-110">For example, you can use the tree to generate F# code or generate code in some other language.</span></span>


## <a name="quoted-expressions"></a><span data-ttu-id="9d4de-111">Espressioni tra virgolette</span><span class="sxs-lookup"><span data-stu-id="9d4de-111">Quoted Expressions</span></span>
<span data-ttu-id="9d4de-112">Oggetto *espressione in quotation* è un'espressione F # nel codice che è delimitato in modo tale da non compilata come parte del programma, ma viene invece compilata in un oggetto che rappresenta un'espressione F #.</span><span class="sxs-lookup"><span data-stu-id="9d4de-112">A *quoted expression* is an F# expression in your code that is delimited in such a way that it is not compiled as part of your program, but instead is compiled into an object that represents an F# expression.</span></span> <span data-ttu-id="9d4de-113">È possibile contrassegnare un'espressione tra virgolette in uno dei due modi: con informazioni sul tipo o senza informazioni sul tipo.</span><span class="sxs-lookup"><span data-stu-id="9d4de-113">You can mark a quoted expression in one of two ways: either with type information or without type information.</span></span> <span data-ttu-id="9d4de-114">Se si desidera includere informazioni sul tipo, si utilizzano i simboli `<@` e `@>` per delimitare l'espressione tra virgolette.</span><span class="sxs-lookup"><span data-stu-id="9d4de-114">If you want to include type information, you use the symbols `<@` and `@>` to delimit the quoted expression.</span></span> <span data-ttu-id="9d4de-115">Se non si richiedono informazioni sul tipo, si utilizzano i simboli `<@@` e `@@>`.</span><span class="sxs-lookup"><span data-stu-id="9d4de-115">If you do not need type information, you use the symbols `<@@` and `@@>`.</span></span> <span data-ttu-id="9d4de-116">Il codice seguente illustra le offerte di DataSet tipizzate e non.</span><span class="sxs-lookup"><span data-stu-id="9d4de-116">The following code shows typed and untyped quotations.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet501.fs)]

<span data-ttu-id="9d4de-117">Attraversare un albero delle espressioni di grandi dimensioni è più veloce se non si include informazioni sul tipo.</span><span class="sxs-lookup"><span data-stu-id="9d4de-117">Traversing a large expression tree is faster if you do not include type information.</span></span> <span data-ttu-id="9d4de-118">Il tipo risulta dell'espressione tra virgolette con i simboli tipizzati è `Expr<'T>`, dove il parametro di tipo presenta il tipo dell'espressione, come determinato dall'algoritmo di inferenza del tipo del compilatore F #.</span><span class="sxs-lookup"><span data-stu-id="9d4de-118">The resulting type of an expression quoted with the typed symbols is `Expr<'T>`, where the type parameter has the type of the expression as determined by the F# compiler's type inference algorithm.</span></span> <span data-ttu-id="9d4de-119">Quando si usa citazioni di codice senza informazioni sul tipo, il tipo dell'espressione tra virgolette è il tipo non generico [Expr](https://msdn.microsoft.com/library/ed6a2caf-69d4-45c2-ab97-e9b3be9bce65).</span><span class="sxs-lookup"><span data-stu-id="9d4de-119">When you use code quotations without type information, the type of the quoted expression is the non-generic type [Expr](https://msdn.microsoft.com/library/ed6a2caf-69d4-45c2-ab97-e9b3be9bce65).</span></span> <span data-ttu-id="9d4de-120">È possibile chiamare il [Raw](https://msdn.microsoft.com/library/47fb94f1-e77f-4c68-aabc-2b0ba40d59c2) proprietà tipizzata `Expr` classe per ottenere il dataset non `Expr` oggetto.</span><span class="sxs-lookup"><span data-stu-id="9d4de-120">You can call the [Raw](https://msdn.microsoft.com/library/47fb94f1-e77f-4c68-aabc-2b0ba40d59c2) property on the typed `Expr` class to obtain the untyped `Expr` object.</span></span>

<span data-ttu-id="9d4de-121">Esistono diversi metodi statici che consentono di generare oggetti F # espressione livello di programmazione la `Expr` classe senza l'utilizzo di espressioni in quotation.</span><span class="sxs-lookup"><span data-stu-id="9d4de-121">There are a variety of static methods that allow you to generate F# expression objects programmatically in the `Expr` class without using quoted expressions.</span></span>

<span data-ttu-id="9d4de-122">Si noti che una quotation di codice deve includere un'espressione completa.</span><span class="sxs-lookup"><span data-stu-id="9d4de-122">Note that a code quotation must include a complete expression.</span></span> <span data-ttu-id="9d4de-123">Per un `let` associazione, ad esempio, è necessario sia la definizione del nome dell'associazione e un'espressione aggiuntiva che utilizza l'associazione.</span><span class="sxs-lookup"><span data-stu-id="9d4de-123">For a `let` binding, for example, you need both the definition of the bound name and an additional expression that uses the binding.</span></span> <span data-ttu-id="9d4de-124">Nella sintassi dettagliata, si tratta di un'espressione che segue il `in` (parola chiave).</span><span class="sxs-lookup"><span data-stu-id="9d4de-124">In verbose syntax, this is an expression that follows the `in` keyword.</span></span> <span data-ttu-id="9d4de-125">Al livello superiore in un modulo, questa è l'espressione successiva nel modulo, ma in un'offerta, è necessario in modo esplicito.</span><span class="sxs-lookup"><span data-stu-id="9d4de-125">At the top-level in a module, this is just the next expression in the module, but in a quotation, it is explicitly required.</span></span>

<span data-ttu-id="9d4de-126">Pertanto, l'espressione seguente non è valido.</span><span class="sxs-lookup"><span data-stu-id="9d4de-126">Therefore, the following expression is not valid.</span></span>

```fsharp
// Not valid:
// <@ let f x = x + 1 @>
```

<span data-ttu-id="9d4de-127">Ma le espressioni seguenti sono valide.</span><span class="sxs-lookup"><span data-stu-id="9d4de-127">But the following expressions are valid.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet502.fs)]

<span data-ttu-id="9d4de-128">Per utilizzare citazioni di codice, è necessario aggiungere una dichiarazione di importazione (tramite il `open` parola chiave) che consente di aprire il [Quotations](https://msdn.microsoft.com/library/e9ce8a3a-e00c-4190-bad5-cce52ee089b2) dello spazio dei nomi.</span><span class="sxs-lookup"><span data-stu-id="9d4de-128">To use code quotations, you must add an import declaration (by using the `open` keyword) that opens the [Microsoft.FSharp.Quotations](https://msdn.microsoft.com/library/e9ce8a3a-e00c-4190-bad5-cce52ee089b2) namespace.</span></span>

<span data-ttu-id="9d4de-129">PowerPacks F # offre supporto per la valutazione e l'esecuzione di oggetti di espressione F #.</span><span class="sxs-lookup"><span data-stu-id="9d4de-129">The F# PowerPack provides support for evaluating and executing F# expression objects.</span></span>


## <a name="expr-type"></a><span data-ttu-id="9d4de-130">Tipo di expr</span><span class="sxs-lookup"><span data-stu-id="9d4de-130">Expr Type</span></span>
<span data-ttu-id="9d4de-131">Un'istanza di `Expr` tipo rappresenta un'espressione F #.</span><span class="sxs-lookup"><span data-stu-id="9d4de-131">An instance of the `Expr` type represents an F# expression.</span></span> <span data-ttu-id="9d4de-132">Sia il tipo generico e non generica `Expr` tipi sono documentati nella documentazione della libreria F #.</span><span class="sxs-lookup"><span data-stu-id="9d4de-132">Both the generic and the non-generic `Expr` types are documented in the F# library documentation.</span></span> <span data-ttu-id="9d4de-133">Per ulteriori informazioni, vedere [Quotations Namespace](https://msdn.microsoft.com/visualfsharpdocs/conceptual/microsoft.fsharp.quotations-namespace-%5bfsharp%5d) e [classe Quotations. expr](https://msdn.microsoft.com/visualfsharpdocs/conceptual/quotations.expr-class-%5bfsharp%5d).</span><span class="sxs-lookup"><span data-stu-id="9d4de-133">For more information, see [Microsoft.FSharp.Quotations Namespace](https://msdn.microsoft.com/visualfsharpdocs/conceptual/microsoft.fsharp.quotations-namespace-%5bfsharp%5d) and [Quotations.Expr Class](https://msdn.microsoft.com/visualfsharpdocs/conceptual/quotations.expr-class-%5bfsharp%5d).</span></span>


## <a name="splicing-operators"></a><span data-ttu-id="9d4de-134">Operatori di splicing</span><span class="sxs-lookup"><span data-stu-id="9d4de-134">Splicing Operators</span></span>
<span data-ttu-id="9d4de-135">Splicing consente di combinare citazioni di codice letterale con espressioni che è stato creato a livello di codice o da un'altra offerta di codice.</span><span class="sxs-lookup"><span data-stu-id="9d4de-135">Splicing enables you to combine literal code quotations with expressions that you have created programmatically or from another code quotation.</span></span> <span data-ttu-id="9d4de-136">Il `%` e `%%` operatori consentono di aggiungere un oggetto di espressione F # in una quotation di codice.</span><span class="sxs-lookup"><span data-stu-id="9d4de-136">The `%` and `%%` operators enable you to add an F# expression object into a code quotation.</span></span> <span data-ttu-id="9d4de-137">Utilizzare il `%` operatore per inserire un oggetto di espressione tipizzata in una quotation tipizzata, ma utilizzare il `%%` operatore per inserire un oggetto di espressione non tipizzata in una quotation non tipizzato.</span><span class="sxs-lookup"><span data-stu-id="9d4de-137">You use the `%` operator to insert a typed expression object into a typed quotation; you use the `%%` operator to insert an untyped expression object into an untyped quotation.</span></span> <span data-ttu-id="9d4de-138">Entrambi gli operatori sono gli operatori di prefisso unario.</span><span class="sxs-lookup"><span data-stu-id="9d4de-138">Both operators are unary prefix operators.</span></span> <span data-ttu-id="9d4de-139">In questo modo se `expr` è un'espressione non tipizzata di tipo `Expr`, il codice seguente è valido.</span><span class="sxs-lookup"><span data-stu-id="9d4de-139">Thus if `expr` is an untyped expression of type `Expr`, the following code is valid.</span></span>

```fsharp
<@@ 1 + %%expr @@>
```

<span data-ttu-id="9d4de-140">E se `expr` è una quotation tipizzata di tipo `Expr<int>`, il codice seguente è valido.</span><span class="sxs-lookup"><span data-stu-id="9d4de-140">And if `expr` is a typed quotation of type `Expr<int>`, the following code is valid.</span></span>

```fsharp
<@ 1 + %expr @>
```

## <a name="example"></a><span data-ttu-id="9d4de-141">Esempio</span><span class="sxs-lookup"><span data-stu-id="9d4de-141">Example</span></span>

### <a name="description"></a><span data-ttu-id="9d4de-142">Descrizione</span><span class="sxs-lookup"><span data-stu-id="9d4de-142">Description</span></span>
<span data-ttu-id="9d4de-143">Nell'esempio seguente viene illustrato l'utilizzo di citazioni di codice per inserire codice F # in un oggetto di espressione e quindi stampare il codice F # che rappresenta l'espressione.</span><span class="sxs-lookup"><span data-stu-id="9d4de-143">The following example illustrates the use of code quotations to put F# code into an expression object and then print the F# code that represents the expression.</span></span> <span data-ttu-id="9d4de-144">Una funzione `println` è definito che contiene una funzione ricorsiva `print` che visualizza un oggetto di espressione F # (di tipo `Expr`) in un formato descrittivo.</span><span class="sxs-lookup"><span data-stu-id="9d4de-144">A function `println` is defined that contains a recursive function `print` that displays an F# expression object (of type `Expr`) in a friendly format.</span></span> <span data-ttu-id="9d4de-145">Esistono diversi criteri attivi nel [Microsoft.FSharp.Quotations.Patterns](https://msdn.microsoft.com/library/093944a9-c752-403a-8983-5fcd5dbf92a4) e [Microsoft.FSharp.Quotations.DerivedPatterns](https://msdn.microsoft.com/library/d2434a6e-ae7b-4f3d-b567-c162938bc9cd) moduli che possono essere utilizzati per analizzare gli oggetti di espressione.</span><span class="sxs-lookup"><span data-stu-id="9d4de-145">There are several active patterns in the [Microsoft.FSharp.Quotations.Patterns](https://msdn.microsoft.com/library/093944a9-c752-403a-8983-5fcd5dbf92a4) and [Microsoft.FSharp.Quotations.DerivedPatterns](https://msdn.microsoft.com/library/d2434a6e-ae7b-4f3d-b567-c162938bc9cd) modules that can be used to analyze expression objects.</span></span> <span data-ttu-id="9d4de-146">In questo esempio non include tutti i possibili modelli che potrebbero essere visualizzati in un'espressione F #.</span><span class="sxs-lookup"><span data-stu-id="9d4de-146">This example does not include all the possible patterns that might appear in an F# expression.</span></span> <span data-ttu-id="9d4de-147">Qualsiasi criterio non riconosciuto attiva una corrispondenza per il carattere jolly (`_`) e viene eseguito il rendering tramite il `ToString` (metodo), che, nel `Expr` digitare, consente di individuare il criterio attivo da aggiungere all'espressione corrispondente.</span><span class="sxs-lookup"><span data-stu-id="9d4de-147">Any unrecognized pattern triggers a match to the wildcard pattern (`_`) and is rendered by using the `ToString` method, which, on the `Expr` type, lets you know the active pattern to add to your match expression.</span></span>


### <a name="code"></a><span data-ttu-id="9d4de-148">Codice</span><span class="sxs-lookup"><span data-stu-id="9d4de-148">Code</span></span>
[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet601.fs)]
    
### <a name="output"></a><span data-ttu-id="9d4de-149">Output</span><span class="sxs-lookup"><span data-stu-id="9d4de-149">Output</span></span>

```fsharp
fun (x:System.Int32) -> x + 1
a + 1
let f = fun (x:System.Int32) -> x + 10 in f 10
```

## <a name="example"></a><span data-ttu-id="9d4de-150">Esempio</span><span class="sxs-lookup"><span data-stu-id="9d4de-150">Example</span></span>

### <a name="description"></a><span data-ttu-id="9d4de-151">Descrizione</span><span class="sxs-lookup"><span data-stu-id="9d4de-151">Description</span></span>
<span data-ttu-id="9d4de-152">È inoltre possibile utilizzare i tre criteri attivi nel [ExprShape (modulo)](https://msdn.microsoft.com/library/7685150e-2432-4d39-9338-57292eff18de) attraversare gli alberi delle espressioni con un numero inferiore di criteri attivo.</span><span class="sxs-lookup"><span data-stu-id="9d4de-152">You can also use the three active patterns in the [ExprShape module](https://msdn.microsoft.com/library/7685150e-2432-4d39-9338-57292eff18de) to traverse expression trees with fewer active patterns.</span></span> <span data-ttu-id="9d4de-153">Questi criteri attivi possono essere utili quando si desidera attraversare una struttura ad albero, ma non è necessario tutte le informazioni nella maggior parte dei nodi.</span><span class="sxs-lookup"><span data-stu-id="9d4de-153">These active patterns can be useful when you want to traverse a tree but you do not need all the information in most of the nodes.</span></span> <span data-ttu-id="9d4de-154">Quando si utilizzano questi criteri, qualsiasi espressione F # corrisponde a uno dei tre criteri seguenti: `ShapeVar` se l'espressione è una variabile, `ShapeLambda` se l'espressione è un'espressione lambda, o `ShapeCombination` se l'espressione è diversa.</span><span class="sxs-lookup"><span data-stu-id="9d4de-154">When you use these patterns, any F# expression matches one of the following three patterns: `ShapeVar` if the expression is a variable, `ShapeLambda` if the expression is a lambda expression, or `ShapeCombination` if the expression is anything else.</span></span> <span data-ttu-id="9d4de-155">Se si passano attraverso un albero delle espressioni usando i criteri attivi come nell'esempio di codice precedente, è necessario utilizzare molti più criteri per gestire tutti i possibili tipi F # espressione e il codice sarà più complesso.</span><span class="sxs-lookup"><span data-stu-id="9d4de-155">If you traverse an expression tree by using the active patterns as in the previous code example, you have to use many more patterns to handle all possible F# expression types, and your code will be more complex.</span></span> <span data-ttu-id="9d4de-156">Per ulteriori informazioni, vedere [ExprShape.ShapeVar &#124; ShapeLambda &#124; Criterio attivo ShapeCombination](https://msdn.microsoft.com/visualfsharpdocs/conceptual/exprshape.shapevarhshapelambdahshapecombination-active-pattern-%5bfsharp%5d).</span><span class="sxs-lookup"><span data-stu-id="9d4de-156">For more information, see [ExprShape.ShapeVar&#124;ShapeLambda&#124;ShapeCombination Active Pattern](https://msdn.microsoft.com/visualfsharpdocs/conceptual/exprshape.shapevarhshapelambdahshapecombination-active-pattern-%5bfsharp%5d).</span></span>

<span data-ttu-id="9d4de-157">Esempio di codice seguente è utilizzabile come base per attraversamenti più complessi.</span><span class="sxs-lookup"><span data-stu-id="9d4de-157">The following code example can be used as a basis for more complex traversals.</span></span> <span data-ttu-id="9d4de-158">In questo codice, viene creato un albero delle espressioni per un'espressione che include una chiamata di funzione `add`.</span><span class="sxs-lookup"><span data-stu-id="9d4de-158">In this code, an expression tree is created for an expression that involves a function call, `add`.</span></span> <span data-ttu-id="9d4de-159">Il [SpecificCall](https://msdn.microsoft.com/library/05a77b21-20fe-4b9a-8e07-aa999538198d) (modello attivo) viene utilizzato per rilevare qualsiasi chiamata a `add` nell'albero delle espressioni.</span><span class="sxs-lookup"><span data-stu-id="9d4de-159">The [SpecificCall](https://msdn.microsoft.com/library/05a77b21-20fe-4b9a-8e07-aa999538198d) active pattern is used to detect any call to `add` in the expression tree.</span></span> <span data-ttu-id="9d4de-160">Questo criterio attivo assegna gli argomenti della chiamata al `exprList` valore.</span><span class="sxs-lookup"><span data-stu-id="9d4de-160">This active pattern assigns the arguments of the call to the `exprList` value.</span></span> <span data-ttu-id="9d4de-161">In questo caso, sono disponibili solo due, pertanto vengono estratti e la funzione viene chiamata in modo ricorsivo sugli argomenti.</span><span class="sxs-lookup"><span data-stu-id="9d4de-161">In this case, there are only two, so these are pulled out and the function is called recursively on the arguments.</span></span> <span data-ttu-id="9d4de-162">I risultati vengono inseriti in una quotation di codice che rappresenta una chiamata a `mul` usando l'operatore di splicing (`%%`).</span><span class="sxs-lookup"><span data-stu-id="9d4de-162">The results are inserted into a code quotation that represents a call to `mul` by using the splice operator (`%%`).</span></span> <span data-ttu-id="9d4de-163">Il `println` funzione dell'esempio precedente viene utilizzata per visualizzare i risultati.</span><span class="sxs-lookup"><span data-stu-id="9d4de-163">The `println` function from the previous example is used to display the results.</span></span>

<span data-ttu-id="9d4de-164">Il codice in altri rami (modello attivo) rigenera semplicemente dell'albero delle espressioni stesso, pertanto l'unica modifica nell'espressione risultante è la modifica da `add` a `mul`.</span><span class="sxs-lookup"><span data-stu-id="9d4de-164">The code in the other active pattern branches just regenerates the same expression tree, so the only change in the resulting expression is the change from `add` to `mul`.</span></span>


### <a name="code"></a><span data-ttu-id="9d4de-165">Codice</span><span class="sxs-lookup"><span data-stu-id="9d4de-165">Code</span></span>
[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet701.fs)]
    
### <a name="output"></a><span data-ttu-id="9d4de-166">Output</span><span class="sxs-lookup"><span data-stu-id="9d4de-166">Output</span></span>

```fsharp
1 + Module1.add(2,Module1.add(3,4))
1 + Module1.mul(2,Module1.mul(3,4))
```

## <a name="see-also"></a><span data-ttu-id="9d4de-167">Vedere anche</span><span class="sxs-lookup"><span data-stu-id="9d4de-167">See Also</span></span>
[<span data-ttu-id="9d4de-168">Riferimenti per il linguaggio F#</span><span class="sxs-lookup"><span data-stu-id="9d4de-168">F# Language Reference</span></span>](index.md)

