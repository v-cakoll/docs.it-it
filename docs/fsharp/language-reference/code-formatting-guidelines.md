---
title: Linee guida per la formattazione del codice (F#)
description: 'Ulteriori linee guida di formattazione rientro di codice per il linguaggio di programmazione per migliorare la leggibilità, estetica, standardizzazione e compilazione F #.'
author: cartermp
ms.author: phcart
ms.date: 05/16/2016
ms.topic: language-reference
ms.prod: dotnet-fsharp
ms.devlang: fsharp
ms.openlocfilehash: a16378b42e7d2acd44dcfe0af7c68a55e4be41d1
ms.sourcegitcommit: 03ee570f6f528a7d23a4221dcb26a9498edbdf8c
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 04/28/2018
---
# <a name="code-formatting-guidelines"></a><span data-ttu-id="e3b30-103">Linee guida per la formattazione del codice</span><span class="sxs-lookup"><span data-stu-id="e3b30-103">Code Formatting Guidelines</span></span>

<span data-ttu-id="e3b30-104">In questo argomento vengono riepilogate le linee guida rientro di codice per F #.</span><span class="sxs-lookup"><span data-stu-id="e3b30-104">This topic summarizes code indentation guidelines for F#.</span></span> <span data-ttu-id="e3b30-105">Poiché il linguaggio F # è sensibile alle interruzioni di riga e il rientro, non è semplicemente un problema di migliorare la leggibilità, estetica o problema relativo alla codifica standardizzazione per formattare correttamente il codice.</span><span class="sxs-lookup"><span data-stu-id="e3b30-105">Because the F# language is sensitive to line breaks and indentation, it is not just a readability issue, aesthetic issue, or coding standardization issue to format your code correctly.</span></span> <span data-ttu-id="e3b30-106">È necessario formattare il codice in modo corretto per la compilazione corretta.</span><span class="sxs-lookup"><span data-stu-id="e3b30-106">You must format your code correctly for it to compile correctly.</span></span>


## <a name="general-rules-for-indentation"></a><span data-ttu-id="e3b30-107">Regole generali per il rientro</span><span class="sxs-lookup"><span data-stu-id="e3b30-107">General Rules for Indentation</span></span>
<span data-ttu-id="e3b30-108">Quando è necessario il rientro, è necessario utilizzare spazi e non tabulazioni.</span><span class="sxs-lookup"><span data-stu-id="e3b30-108">When indentation is required, you must use spaces, not tabs.</span></span> <span data-ttu-id="e3b30-109">È necessario almeno uno spazio.</span><span class="sxs-lookup"><span data-stu-id="e3b30-109">At least one space is required.</span></span> <span data-ttu-id="e3b30-110">L'organizzazione può creare standard di codifica per specificare il numero di spazi da utilizzare per il rientro. tre o quattro spazi di rientro a ogni livello in cui si verifica il rientro è tipico.</span><span class="sxs-lookup"><span data-stu-id="e3b30-110">Your organization can create coding standards to specify the number of spaces to use for indentation; three or four spaces of indentation at each level where indentation occurs is typical.</span></span> <span data-ttu-id="e3b30-111">È possibile configurare Visual Studio in base agli standard di rientro dell'organizzazione, modificando le opzioni nel `Options` nella finestra di dialogo è disponibile il `Tools` menu.</span><span class="sxs-lookup"><span data-stu-id="e3b30-111">You can configure Visual Studio to match your organization's indentation standards by changing the options in the `Options` dialog box, which is available from the `Tools` menu.</span></span> <span data-ttu-id="e3b30-112">Nel `Text Editor` nodo espandere `F#` e quindi fare clic su `Tabs`.</span><span class="sxs-lookup"><span data-stu-id="e3b30-112">In the `Text Editor` node, expand `F#` and then click `Tabs`.</span></span> <span data-ttu-id="e3b30-113">Per una descrizione delle opzioni disponibili, vedere [opzioni, Editor di testo, tutti i linguaggi, schede](https://msdn.microsoft.com/library/7sffa753.aspx).</span><span class="sxs-lookup"><span data-stu-id="e3b30-113">For a description of the available options, see [Options, Text Editor, All Languages, Tabs](https://msdn.microsoft.com/library/7sffa753.aspx).</span></span>

<span data-ttu-id="e3b30-114">In generale, quando il compilatore analizza il codice, gestisce uno stack interno che indica il livello di nidificazione corrente.</span><span class="sxs-lookup"><span data-stu-id="e3b30-114">In general, when the compiler parses your code, it maintains an internal stack that indicates the current level of nesting.</span></span> <span data-ttu-id="e3b30-115">Quando il codice viene rientrato, un nuovo livello di nidificazione viene creato o inserito nello stack interno.</span><span class="sxs-lookup"><span data-stu-id="e3b30-115">When code is indented, a new level of nesting is created, or pushed onto this internal stack.</span></span> <span data-ttu-id="e3b30-116">Al termine di un costrutto viene estratto il livello.</span><span class="sxs-lookup"><span data-stu-id="e3b30-116">When a construct ends, the level is popped.</span></span> <span data-ttu-id="e3b30-117">Rientro è un modo per segnalare la fine di un livello e pop stack interno, ma anche determinati token comportano il livello da estrarre, ad esempio il `end` (parola chiave), o una parentesi graffa di chiusura o parentesi.</span><span class="sxs-lookup"><span data-stu-id="e3b30-117">Indentation is one way to signal the end of a level and pop the internal stack, but certain tokens also cause the level to be popped, such as the `end` keyword, or a closing brace or parenthesis.</span></span>

<span data-ttu-id="e3b30-118">Codice in un costrutto su più righe, ad esempio una definizione di tipo, definizione di funzione, `try...with` costrutto, costrutti di ciclo e, devono essere rientrate rispetto alla riga di apertura del costrutto.</span><span class="sxs-lookup"><span data-stu-id="e3b30-118">Code in a multiline construct, such as a type definition, function definition, `try...with` construct, and looping constructs, must be indented relative to the opening line of the construct.</span></span> <span data-ttu-id="e3b30-119">La prima riga rientrata definisce una posizione di colonna per il codice successivo nello stesso costrutto.</span><span class="sxs-lookup"><span data-stu-id="e3b30-119">The first indented line establishes a column position for subsequent code in the same construct.</span></span> <span data-ttu-id="e3b30-120">Il livello di rientro viene chiamato un *contesto*.</span><span class="sxs-lookup"><span data-stu-id="e3b30-120">The indentation level is called a *context*.</span></span> <span data-ttu-id="e3b30-121">Posizione della colonna imposta una minima della colonna, detta un *riga di offside*, per le successive righe di codice che sono nello stesso contesto.</span><span class="sxs-lookup"><span data-stu-id="e3b30-121">The column position sets a minimum column, referred to as an *offside line*, for subsequent lines of code that are in the same context.</span></span> <span data-ttu-id="e3b30-122">Quando viene rilevata una riga di codice rientro minore rispetto a questa posizione colonna stabilita, il compilatore presuppone che il contesto è stata completata e che il codice si trovi al livello successivo verso l'alto, nel contesto precedente.</span><span class="sxs-lookup"><span data-stu-id="e3b30-122">When a line of code is encountered that is indented less than this established column position, the compiler assumes that the context has ended and that you are now coding at the next level up, in the previous context.</span></span> <span data-ttu-id="e3b30-123">Il termine *offside* viene utilizzato per descrivere la condizione in cui una riga di codice attiva la fine di un costrutto in quanto il rientro non è sufficiente.</span><span class="sxs-lookup"><span data-stu-id="e3b30-123">The term *offside* is used to describe the condition in which a line of code triggers the end of a construct because it is not indented far enough.</span></span> <span data-ttu-id="e3b30-124">In altre parole, codice a sinistra di una riga di offside è offside.</span><span class="sxs-lookup"><span data-stu-id="e3b30-124">In other words, code to the left of an offside line is offside.</span></span> <span data-ttu-id="e3b30-125">Nel codice rientrato correttamente, si sfruttare la regola di offside per definire la fine dei costrutti.</span><span class="sxs-lookup"><span data-stu-id="e3b30-125">In correctly indented code, you take advantage of the offside rule in order to delineate the end of constructs.</span></span> <span data-ttu-id="e3b30-126">Se si utilizza il rientro in modo non corretto, una condizione di offside può causare al compilatore di generare un avviso o può comportare l'errata interpretazione del codice.</span><span class="sxs-lookup"><span data-stu-id="e3b30-126">If you use indentation improperly, an offside condition can cause the compiler to issue a warning or can lead to the incorrect interpretation of your code.</span></span>

<span data-ttu-id="e3b30-127">Righe di offside vengono determinate come segue.</span><span class="sxs-lookup"><span data-stu-id="e3b30-127">Offside lines are determined as follows.</span></span>


- <span data-ttu-id="e3b30-128">Un `=` token associato a un `let` introduce una riga di offside in corrispondenza della colonna del primo token dopo il `=` sign.</span><span class="sxs-lookup"><span data-stu-id="e3b30-128">An `=` token associated with a `let` introduces an offside line at the column of the first token after the `=` sign.</span></span>


- <span data-ttu-id="e3b30-129">In un `if...then...else` espressione, la posizione colonna del primo token dopo il `then` parola chiave o `else` parola chiave introduce una riga di offside.</span><span class="sxs-lookup"><span data-stu-id="e3b30-129">In an `if...then...else` expression, the column position of the first token after the `then` keyword or the `else` keyword introduces an offside line.</span></span>


- <span data-ttu-id="e3b30-130">In un `try...with` espressione, il primo token dopo `try` introduce una riga di offside.</span><span class="sxs-lookup"><span data-stu-id="e3b30-130">In a `try...with` expression, the first token after `try` introduces an offside line.</span></span>


- <span data-ttu-id="e3b30-131">In un `match` espressione, il primo token dopo `with` e il primo token dopo ogni `->` introducono righe di offside.</span><span class="sxs-lookup"><span data-stu-id="e3b30-131">In a `match` expression, the first token after `with` and the first token after each `->` introduce offside lines.</span></span>


- <span data-ttu-id="e3b30-132">Il primo token dopo `with` in un tipo di estensione introduce una riga di offside.</span><span class="sxs-lookup"><span data-stu-id="e3b30-132">The first token after `with` in a type extension introduces an offside line.</span></span>


- <span data-ttu-id="e3b30-133">Il primo token dopo una parentesi o parentesi graffa di apertura o dopo il `begin` (parola chiave), introduce una riga di offside.</span><span class="sxs-lookup"><span data-stu-id="e3b30-133">The first token after an opening brace or parenthesis, or after the `begin` keyword, introduces an offside line.</span></span>


- <span data-ttu-id="e3b30-134">Il primo carattere nelle parole chiave `let`, `if`, e `module` introducono righe di offside.</span><span class="sxs-lookup"><span data-stu-id="e3b30-134">The first character in the keywords `let`, `if`, and `module` introduce offside lines.</span></span>


<span data-ttu-id="e3b30-135">Gli esempi di codice seguenti illustrano le regole di rientro.</span><span class="sxs-lookup"><span data-stu-id="e3b30-135">The following code examples illustrate the indentation rules.</span></span> <span data-ttu-id="e3b30-136">In questo caso, le istruzioni print si basano su rientro per la loro associazione con il contesto appropriato.</span><span class="sxs-lookup"><span data-stu-id="e3b30-136">Here, the print statements rely on indentation to associate them with the appropriate context.</span></span> <span data-ttu-id="e3b30-137">Ogni volta che viene spostato il rientro, il contesto viene estratto e restituisce il contesto precedente.</span><span class="sxs-lookup"><span data-stu-id="e3b30-137">Every time the indentation shifts, the context is popped and returns to the previous context.</span></span> <span data-ttu-id="e3b30-138">Pertanto, in cui viene stampato uno spazio alla fine di ogni iterazione. "Completata".</span><span class="sxs-lookup"><span data-stu-id="e3b30-138">Therefore, a space is printed at the end of each iteration; "Done!"</span></span> <span data-ttu-id="e3b30-139">Poiché il rientro di offside stabilisce che non è parte del ciclo, viene stampato solo una volta.</span><span class="sxs-lookup"><span data-stu-id="e3b30-139">is only printed one time because the offside indentation establishes that it is not part of the loop.</span></span> <span data-ttu-id="e3b30-140">La stampa della stringa "Il contesto di primo livello" non fa parte della funzione.</span><span class="sxs-lookup"><span data-stu-id="e3b30-140">The printing of the string "Top-level context" is not part of the function.</span></span> <span data-ttu-id="e3b30-141">Pertanto, verrà stampato prima, durante l'inizializzazione statica, prima di chiamare la funzione.</span><span class="sxs-lookup"><span data-stu-id="e3b30-141">Therefore, it is printed first, during the static initialization, before the function is called.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet1.fs)]

<span data-ttu-id="e3b30-142">L'output è indicato di seguito.</span><span class="sxs-lookup"><span data-stu-id="e3b30-142">The output is as follows.</span></span>

```
Top-level context

(Negative number) Zero 1 2 3 Done!
```

<span data-ttu-id="e3b30-143">Quando si interrompono le righe lunghe, la continuazione di riga deve essere rientrata un costrutto che lo contiene.</span><span class="sxs-lookup"><span data-stu-id="e3b30-143">When you break long lines, the continuation of the line must be indented farther than the enclosing construct.</span></span> <span data-ttu-id="e3b30-144">Ad esempio, gli argomenti della funzione devono essere impostato un rientro il primo carattere del nome della funzione, come illustrato nel codice seguente.</span><span class="sxs-lookup"><span data-stu-id="e3b30-144">For example, function arguments must be indented farther than the first character of the function name, as shown in the following code.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet2.fs)]

<span data-ttu-id="e3b30-145">Esistono eccezioni a queste regole, come descritto nella sezione successiva.</span><span class="sxs-lookup"><span data-stu-id="e3b30-145">There are exceptions to these rules, as described in the next section.</span></span>


## <a name="indentation-in-modules"></a><span data-ttu-id="e3b30-146">Rientro nei moduli</span><span class="sxs-lookup"><span data-stu-id="e3b30-146">Indentation in Modules</span></span>
<span data-ttu-id="e3b30-147">Il codice in un modulo locale deve essere rientrato rispetto al modulo, ma codice in un modulo di livello superiore non deve essere rientrato.</span><span class="sxs-lookup"><span data-stu-id="e3b30-147">Code in a local module must be indented relative to the module, but code in a top-level module does not have to be indented.</span></span> <span data-ttu-id="e3b30-148">Elementi Namespace non è necessario impostare un rientro.</span><span class="sxs-lookup"><span data-stu-id="e3b30-148">Namespace elements do not have to be indented.</span></span>

<span data-ttu-id="e3b30-149">Gli esempi di codice seguenti illustrano questo.</span><span class="sxs-lookup"><span data-stu-id="e3b30-149">The following code examples illustrate this.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet3.fs)]
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet4.fs)]

<span data-ttu-id="e3b30-150">Per ulteriori informazioni, vedere [moduli](modules.md).</span><span class="sxs-lookup"><span data-stu-id="e3b30-150">For more information, see [Modules](modules.md).</span></span>


## <a name="exceptions-to-the-basic-indentation-rules"></a><span data-ttu-id="e3b30-151">Eccezioni alle regole di base di rientro</span><span class="sxs-lookup"><span data-stu-id="e3b30-151">Exceptions to the Basic Indentation Rules</span></span>
<span data-ttu-id="e3b30-152">La regola generale, come descritto nella sezione precedente, è che il codice nei costrutti su più righe deve essere rientrato relativo il rientro della prima riga del costrutto, e che la fine del costrutto è determinata da quando si verifica la prima riga di offside.</span><span class="sxs-lookup"><span data-stu-id="e3b30-152">The general rule, as described in the previous section, is that code in multiline constructs must be indented relative to the indentation of the first line of the construct, and that the end of the construct is determined by when the first offside line occurs.</span></span> <span data-ttu-id="e3b30-153">Un'eccezione alla regola relativa alla fine di contesti fatto che alcuni costrutti, ad esempio il `try...with` espressione, il `if...then...else` espressione e l'utilizzo di `and` sintassi per la dichiarazione di funzioni ricorsive o tipi, che più parti.</span><span class="sxs-lookup"><span data-stu-id="e3b30-153">An exception to the rule about when contexts end is that some constructs, such as the `try...with` expression, the `if...then...else` expression, and the use of `and` syntax for declaring mutually recursive functions or types, have multiple parts.</span></span> <span data-ttu-id="e3b30-154">Ad esempio si impostano i rientri, le parti `then` e `else` in un `if...then...else` espressione, allo stesso livello del token di inizio dell'espressione, ma invece di indicare la fine del contesto, che rappresenta la parte successiva dello stesso contesto.</span><span class="sxs-lookup"><span data-stu-id="e3b30-154">You indent the later parts, such as `then` and `else` in an `if...then...else` expression, at the same level as the token that starts the expression, but instead of indicating an end to the context, it represents the next part of the same context.</span></span> <span data-ttu-id="e3b30-155">Pertanto, un `if...then...else` espressione può essere scritta come nell'esempio di codice seguente.</span><span class="sxs-lookup"><span data-stu-id="e3b30-155">Therefore, an `if...then...else` expression can be written as in the following code example.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet5.fs)]

<span data-ttu-id="e3b30-156">L'eccezione alla regola di offside si applica solo al `then` e `else` parole chiave.</span><span class="sxs-lookup"><span data-stu-id="e3b30-156">The exception to the offside rule applies only to the `then` and `else` keywords.</span></span> <span data-ttu-id="e3b30-157">Pertanto, sebbene non sia un errore per il rientro di `then` e `else` , inoltre, non riusciti per il rientro alle righe di codice in un `then` blocco genera un avviso.</span><span class="sxs-lookup"><span data-stu-id="e3b30-157">Therefore, although it is not an error to indent the `then` and `else` further, failing to indent the lines of code in a `then` block produces a warning.</span></span> <span data-ttu-id="e3b30-158">Come illustrato nelle seguenti righe di codice.</span><span class="sxs-lookup"><span data-stu-id="e3b30-158">This is illustrated in the following lines of code.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet6.fs)]
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet7.fs)]

<span data-ttu-id="e3b30-159">Per il codice in un `else` blocco, una regola speciale aggiuntiva viene applicato.</span><span class="sxs-lookup"><span data-stu-id="e3b30-159">For code in an `else` block, an additional special rule applies.</span></span> <span data-ttu-id="e3b30-160">Nell'esempio precedente l'avviso si verifica solo nel codice nel `then` blocco, non sul codice di `else` blocco.</span><span class="sxs-lookup"><span data-stu-id="e3b30-160">The warning in the previous example occurs only on the code in the `then` block, not on the code in the `else` block.</span></span> <span data-ttu-id="e3b30-161">Ciò consente di scrivere codice che verifica la presenza di diverse condizioni all'inizio di una funzione senza forzare il resto del codice per la funzione, che potrebbe essere in un `else` blocco, il rientro.</span><span class="sxs-lookup"><span data-stu-id="e3b30-161">This allows you to write code that checks for various conditions at the beginning of a function without forcing the rest of the code for the function, which might be in an `else` block, to be indented.</span></span> <span data-ttu-id="e3b30-162">Di conseguenza, è possibile scrivere quanto segue senza generare un avviso.</span><span class="sxs-lookup"><span data-stu-id="e3b30-162">Thus, you can write the following without producing a warning.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet8.fs)]

<span data-ttu-id="e3b30-163">Un'altra eccezione alla regola che fine dei contesti quando una riga non viene rientrata come una riga precedente viene ad esempio, gli operatori infissi `+` e `|>`.</span><span class="sxs-lookup"><span data-stu-id="e3b30-163">Another exception to the rule that contexts end when a line is not indented as far as a previous line is for infix operators, such as `+` and `|>`.</span></span> <span data-ttu-id="e3b30-164">Le righe che iniziano con gli operatori infissi possono iniziare `(1 + oplength)` colonne prima della posizione normale senza attivare la fine del contesto, in cui `oplength` è il numero di caratteri che costituiscono l'operatore.</span><span class="sxs-lookup"><span data-stu-id="e3b30-164">Lines that start with infix operators are permitted to begin `(1 + oplength)` columns before the normal position without triggering an end to the context, where `oplength` is the number of characters that make up the operator.</span></span> <span data-ttu-id="e3b30-165">In questo modo il primo token dopo l'operatore per la compatibilità con la riga precedente.</span><span class="sxs-lookup"><span data-stu-id="e3b30-165">This causes the first token after the operator to align with the previous line.</span></span>

<span data-ttu-id="e3b30-166">Ad esempio, nel codice seguente, il `+` simbolo può essere rientrate due colonne minore rispetto alla riga precedente.</span><span class="sxs-lookup"><span data-stu-id="e3b30-166">For example, in the following code, the `+` symbol is permitted to be indented two columns less than the previous line.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet9.fs)]

<span data-ttu-id="e3b30-167">Sebbene rientro aumenta in genere quando il livello di nidificazione diventa maggiore, vi sono alcuni costrutti in cui il compilatore consente di reimpostare il rientro in una posizione inferiore di colonna.</span><span class="sxs-lookup"><span data-stu-id="e3b30-167">Although indentation usually increases as the level of nesting becomes higher, there are several constructs in which the compiler allows you to reset the indentation to a lower column position.</span></span>

<span data-ttu-id="e3b30-168">I costrutti che permettono la reimpostazione della posizione di colonna sono i seguenti:</span><span class="sxs-lookup"><span data-stu-id="e3b30-168">The constructs that permit a reset of column position are as follows:</span></span>


- <span data-ttu-id="e3b30-169">Corpi delle funzioni anonime.</span><span class="sxs-lookup"><span data-stu-id="e3b30-169">Bodies of anonymous functions.</span></span> <span data-ttu-id="e3b30-170">Nel codice seguente, l'espressione di stampa inizia in una posizione di colonna più a sinistra rispetto di `fun` (parola chiave).</span><span class="sxs-lookup"><span data-stu-id="e3b30-170">In the following code, the print expression starts at a column position that is farther to the left than the `fun` keyword.</span></span> <span data-ttu-id="e3b30-171">Tuttavia, la riga non deve iniziare in una colonna a sinistra dell'inizio del livello di rientro precedente (ovvero, a sinistra del `L` in `List`).</span><span class="sxs-lookup"><span data-stu-id="e3b30-171">However, the line must not start at a column to the left of the start of the previous indentation level (that is, to the left of the `L` in `List`).</span></span>
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet10.fs)]

- <span data-ttu-id="e3b30-172">Costrutti racchiusi tra parentesi o dalla `begin` e `end` in un `then` o `else` blocco di un `if...then...else` espressione, purché il rientro non minore di posizione della colonna di `if` (parola chiave).</span><span class="sxs-lookup"><span data-stu-id="e3b30-172">Constructs enclosed by parentheses or by `begin` and `end` in a `then` or `else` block of an `if...then...else` expression, provided the indentation is no less than the column position of the `if` keyword.</span></span> <span data-ttu-id="e3b30-173">Questa eccezione consente di uno stile di codifica in cui una parentesi di apertura o `begin` viene utilizzato alla fine di una riga dopo `then` o `else`.</span><span class="sxs-lookup"><span data-stu-id="e3b30-173">This exception allows for a coding style in which an opening parenthesis or `begin` is used at the end of a line after `then` or `else`.</span></span>


- <span data-ttu-id="e3b30-174">Corpi di moduli, classi, interfacce e strutture delimitati da `begin...end`, `{...}`, `class...end`, o `interface...end`.</span><span class="sxs-lookup"><span data-stu-id="e3b30-174">Bodies of modules, classes, interfaces, and structures delimited by `begin...end`, `{...}`, `class...end`, or `interface...end`.</span></span> <span data-ttu-id="e3b30-175">In questo modo per uno stile in cui la parola chiave di apertura di una definizione di tipo può essere sulla stessa riga come nome del tipo senza forzare il corpo intero far rientrare un la parola chiave di apertura.</span><span class="sxs-lookup"><span data-stu-id="e3b30-175">This allows for a style in which the opening keyword of a type definition can be on the same line as the type name without forcing the whole body to be indented farther than the opening keyword.</span></span>
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet13.fs)]


## <a name="see-also"></a><span data-ttu-id="e3b30-176">Vedere anche</span><span class="sxs-lookup"><span data-stu-id="e3b30-176">See Also</span></span>
[<span data-ttu-id="e3b30-177">Riferimenti per il linguaggio F#</span><span class="sxs-lookup"><span data-stu-id="e3b30-177">F# Language Reference</span></span>](index.md)
