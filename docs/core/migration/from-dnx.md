---
title: Migrazione da DNX all'interfaccia della riga di comando .NET Core
description: Eseguire la migrazione dagli strumenti DNX agli strumenti dell'interfaccia della riga di comando di .NET Core.
ms.date: 06/20/2016
ms.openlocfilehash: 31317f110ae1e8586b78becd757d0a8ff07f1459
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 03/14/2020
ms.locfileid: "77503827"
---
# <a name="migrating-from-dnx-to-net-core-cli-projectjson"></a><span data-ttu-id="bfc22-103">Migrazione da DNX all'interfaccia della riga di comando di .NET Core (project.json)</span><span class="sxs-lookup"><span data-stu-id="bfc22-103">Migrating from DNX to .NET Core CLI (project.json)</span></span>

## <a name="overview"></a><span data-ttu-id="bfc22-104">Panoramica</span><span class="sxs-lookup"><span data-stu-id="bfc22-104">Overview</span></span>
<span data-ttu-id="bfc22-105">La versione RC1 di .NET Core e ASP.NET Core 1.0 Microsoft hanno presentato gli strumenti DNX.</span><span class="sxs-lookup"><span data-stu-id="bfc22-105">The RC1 release of .NET Core and ASP.NET Core 1.0 introduced DNX tooling.</span></span> <span data-ttu-id="bfc22-106">La versione RC2 di .NET Core e ASP.NET Core 1.0 hanno eseguito il passaggio da DNX all'interfaccia della riga di comando di .NET Core.</span><span class="sxs-lookup"><span data-stu-id="bfc22-106">The RC2 release of .NET Core and ASP.NET Core 1.0 moved from DNX to the .NET Core CLI.</span></span>

<span data-ttu-id="bfc22-107">Di seguito è riportato un breve riepilogo delle caratteristiche di DNX.</span><span class="sxs-lookup"><span data-stu-id="bfc22-107">As a slight refresher, let's recap what DNX was about.</span></span> <span data-ttu-id="bfc22-108">DNX è un runtime e un set di strumenti usati per creare applicazioni .NET Core e, più specificamente, ASP.NET Core 1.0.</span><span class="sxs-lookup"><span data-stu-id="bfc22-108">DNX was a runtime and a toolset used to build .NET Core and, more specifically, ASP.NET Core 1.0 applications.</span></span> <span data-ttu-id="bfc22-109">È costituito da tre componenti principali:</span><span class="sxs-lookup"><span data-stu-id="bfc22-109">It consisted of 3 main pieces:</span></span>

1. <span data-ttu-id="bfc22-110">DNVM: uno script di installazione per l'acquisizione di DNX</span><span class="sxs-lookup"><span data-stu-id="bfc22-110">DNVM - an install script for obtaining DNX</span></span>
2. <span data-ttu-id="bfc22-111">DNX (Dotnet Execution Runtime): il runtime che esegue il codice</span><span class="sxs-lookup"><span data-stu-id="bfc22-111">DNX (Dotnet Execution Runtime) - the runtime that executes your code</span></span>
3. <span data-ttu-id="bfc22-112">DNU (Dotnet Developer Utility): strumenti per la gestione delle dipendenze e per la creazione e la pubblicazione di applicazioni</span><span class="sxs-lookup"><span data-stu-id="bfc22-112">DNU (Dotnet Developer Utility) - tooling for managing dependencies, building and publishing your applications</span></span>

<span data-ttu-id="bfc22-113">Con l'introduzione dell'interfaccia della riga di comando, tutti gli elementi sopra elencati sono parte di un unico set di strumenti.</span><span class="sxs-lookup"><span data-stu-id="bfc22-113">With the introduction of the CLI, all of the above are now part of a single toolset.</span></span> <span data-ttu-id="bfc22-114">Tuttavia, poiché DNX era disponibile durante il periodo di commercializzazione della versione RC1, è possibile che gli utenti dispongano di progetti creati con DNX di cui vogliono eseguire la migrazione ai nuovi strumenti dell'interfaccia della riga di comando.</span><span class="sxs-lookup"><span data-stu-id="bfc22-114">However, since DNX was available in RC1 timeframe, you might have projects that were built using it that you would want to move off to the new CLI tooling.</span></span>

<span data-ttu-id="bfc22-115">Questa guida fornisce le istruzioni di base per la migrazione dei progetti da DNX all'interfaccia della riga di comando di .NET Core.</span><span class="sxs-lookup"><span data-stu-id="bfc22-115">This migration guide will cover the essentials on how to migrate projects off of DNX and onto .NET Core CLI.</span></span> <span data-ttu-id="bfc22-116">Se si sta iniziando un progetto direttamente in .NET Core, è possibile ignorare questo documento.</span><span class="sxs-lookup"><span data-stu-id="bfc22-116">If you are just starting a project on .NET Core from scratch, you can freely skip this document.</span></span>

## <a name="main-changes-in-the-tooling"></a><span data-ttu-id="bfc22-117">Principali modifiche apportate agli strumenti</span><span class="sxs-lookup"><span data-stu-id="bfc22-117">Main changes in the tooling</span></span>
<span data-ttu-id="bfc22-118">Riguardo agli strumenti sono state apportate alcune modifiche di carattere generale che devono essere descritte per prime.</span><span class="sxs-lookup"><span data-stu-id="bfc22-118">There are some general changes in the tooling that should be outlined first.</span></span>

### <a name="no-more-dnvm"></a><span data-ttu-id="bfc22-119">DNVM non più disponibile</span><span class="sxs-lookup"><span data-stu-id="bfc22-119">No more DNVM</span></span>
<span data-ttu-id="bfc22-120">DNVM, acronimo di *DotNet Version Manager*, era uno script bash/PowerShell usato per installare un'istanza di DNX nel computer in uso.</span><span class="sxs-lookup"><span data-stu-id="bfc22-120">DNVM, short for *DotNet Version Manager* was a bash/PowerShell script used to install a DNX on your machine.</span></span> <span data-ttu-id="bfc22-121">DNVM consentiva agli utenti di ottenere il DNX necessario dal feed specificato o dai feed predefiniti. Permetteva inoltre di contrassegnare un determinato DNX come "attivo", inserendolo in $PATH per la sessione in oggetto.</span><span class="sxs-lookup"><span data-stu-id="bfc22-121">It helped users get the DNX they need from the feed they specified (or default ones) as well as mark a certain DNX "active", which would put it on the $PATH for the given session.</span></span> <span data-ttu-id="bfc22-122">Questo consentiva di usare i diversi strumenti.</span><span class="sxs-lookup"><span data-stu-id="bfc22-122">This would allow you to use the various tools.</span></span>

<span data-ttu-id="bfc22-123">DNVM non è più disponibile perché il relativo set di funzionalità è stato reso ridondante dalle modifiche in arrivo nell'interfaccia della riga di comando di .NET Core.</span><span class="sxs-lookup"><span data-stu-id="bfc22-123">DNVM was discontinued because its feature set was made redundant by changes coming in the .NET Core CLI.</span></span>

<span data-ttu-id="bfc22-124">L'interfaccia della riga di comando è disponibile in due modi principali:</span><span class="sxs-lookup"><span data-stu-id="bfc22-124">The CLI comes packaged in two main ways:</span></span>

1. <span data-ttu-id="bfc22-125">Programmi di installazione nativi per una determinata piattaforma</span><span class="sxs-lookup"><span data-stu-id="bfc22-125">Native installers for a given platform</span></span>
2. <span data-ttu-id="bfc22-126">Script di installazione per altre situazioni, ad esempio server CI</span><span class="sxs-lookup"><span data-stu-id="bfc22-126">Install script for other situations (like CI servers)</span></span>

<span data-ttu-id="bfc22-127">Tenendo conto di quanto affermato sopra, le funzionalità di installazione DNVM non sono necessarie.</span><span class="sxs-lookup"><span data-stu-id="bfc22-127">Given this, the DNVM install features are not needed.</span></span> <span data-ttu-id="bfc22-128">Qual è tuttavia la situazione per le funzionalità di selezione del runtime?</span><span class="sxs-lookup"><span data-stu-id="bfc22-128">But what about the runtime selection features?</span></span>

<span data-ttu-id="bfc22-129">Si fa riferimento a un runtime in `project.json` aggiungendo alle dipendenze un pacchetto di una determinata versione.</span><span class="sxs-lookup"><span data-stu-id="bfc22-129">You reference a runtime in your `project.json` by adding a package of a certain version to your dependencies.</span></span> <span data-ttu-id="bfc22-130">Con questa modifica, l'applicazione sarà in grado di usare i nuovi componenti di runtime.</span><span class="sxs-lookup"><span data-stu-id="bfc22-130">With this change, your application will be able to use the new runtime bits.</span></span> <span data-ttu-id="bfc22-131">L'installazione di questi componenti nel computer in uso è analoga all'installazione con l'interfaccia della riga di comando: è possibile installare il runtime tramite uno dei programmi di installazione nativi oppure tramite lo script di installazione.</span><span class="sxs-lookup"><span data-stu-id="bfc22-131">Getting these bits to your machine is the same as with the CLI: you install the runtime via one of the native installers it supports or via its install script.</span></span>

### <a name="different-commands"></a><span data-ttu-id="bfc22-132">Comandi differenti</span><span class="sxs-lookup"><span data-stu-id="bfc22-132">Different commands</span></span>
<span data-ttu-id="bfc22-133">Se si usa DNX, sono disponibili i comandi di uno dei tre componenti (DNX, DNU o DNVM).</span><span class="sxs-lookup"><span data-stu-id="bfc22-133">If you were using DNX, you used some commands from one of its three parts (DNX, DNU or DNVM).</span></span> <span data-ttu-id="bfc22-134">Con l'interfaccia della riga di comando, alcuni di questi comandi risultano diversi, alcuni non sono disponibili e altri infine sono uguali ma con una semantica leggermente differente.</span><span class="sxs-lookup"><span data-stu-id="bfc22-134">With the CLI, some of these commands change, some are not available and some are the same but have slightly different semantics.</span></span>

<span data-ttu-id="bfc22-135">La tabella seguente mostra il mapping tra i comandi DNX/DNU e i corrispondenti comandi dell'interfaccia della riga di comando.</span><span class="sxs-lookup"><span data-stu-id="bfc22-135">The table below shows the mapping between the DNX/DNU commands and their CLI counterparts.</span></span>

| <span data-ttu-id="bfc22-136">Comando DNX</span><span class="sxs-lookup"><span data-stu-id="bfc22-136">DNX command</span></span>                    | <span data-ttu-id="bfc22-137">Comando dell'interfaccia della riga di comando</span><span class="sxs-lookup"><span data-stu-id="bfc22-137">CLI command</span></span>    | <span data-ttu-id="bfc22-138">Descrizione</span><span class="sxs-lookup"><span data-stu-id="bfc22-138">Description</span></span>                                                                                                     |
|--------------------------------|----------------|-----------------------------------------------------------------------------------------------------------------|
| <span data-ttu-id="bfc22-139">dnx run</span><span class="sxs-lookup"><span data-stu-id="bfc22-139">dnx run</span></span>                        | `dotnet run`     | <span data-ttu-id="bfc22-140">Esegue il codice dall'origine.</span><span class="sxs-lookup"><span data-stu-id="bfc22-140">Run code from source.</span></span>                                                                                           |
| <span data-ttu-id="bfc22-141">dnu build</span><span class="sxs-lookup"><span data-stu-id="bfc22-141">dnu build</span></span>                      | `dotnet build`   | <span data-ttu-id="bfc22-142">Crea un file binario IL (Intermediate Language) del codice.</span><span class="sxs-lookup"><span data-stu-id="bfc22-142">Build an IL binary of your code.</span></span>                                                                                |
| <span data-ttu-id="bfc22-143">dnu pack</span><span class="sxs-lookup"><span data-stu-id="bfc22-143">dnu pack</span></span>                       | `dotnet pack`    | <span data-ttu-id="bfc22-144">Crea un pacchetto NuGet del codice.</span><span class="sxs-lookup"><span data-stu-id="bfc22-144">Package up a NuGet package of your code.</span></span>                                                                        |
| <span data-ttu-id="bfc22-145">dnx \[command] (ad esempio, "dnx web")</span><span class="sxs-lookup"><span data-stu-id="bfc22-145">dnx \[command] (for example, "dnx web")</span></span> | <span data-ttu-id="bfc22-146">N/D\*</span><span class="sxs-lookup"><span data-stu-id="bfc22-146">N/A\*</span></span>          | <span data-ttu-id="bfc22-147">In DNX esegue un comando in base a quanto definito nel file project.json.</span><span class="sxs-lookup"><span data-stu-id="bfc22-147">In DNX world, run a command as defined in the project.json.</span></span>                                                     |
| <span data-ttu-id="bfc22-148">dnu install</span><span class="sxs-lookup"><span data-stu-id="bfc22-148">dnu install</span></span>                    | <span data-ttu-id="bfc22-149">N/D\*</span><span class="sxs-lookup"><span data-stu-id="bfc22-149">N/A\*</span></span>          | <span data-ttu-id="bfc22-150">In DNX installa un pacchetto come dipendenza.</span><span class="sxs-lookup"><span data-stu-id="bfc22-150">In the DNX world, install a package as a dependency.</span></span>                                                            |
| <span data-ttu-id="bfc22-151">dnu restore</span><span class="sxs-lookup"><span data-stu-id="bfc22-151">dnu restore</span></span>                    | `dotnet restore` | <span data-ttu-id="bfc22-152">Ripristina le dipendenze specificate nel file project.json.</span><span class="sxs-lookup"><span data-stu-id="bfc22-152">Restore dependencies specified in your project.json.</span></span> <span data-ttu-id="bfc22-153">([vedere la nota](#dotnet-restore-note))</span><span class="sxs-lookup"><span data-stu-id="bfc22-153">([see note](#dotnet-restore-note))</span></span>                                                            |
| <span data-ttu-id="bfc22-154">dnu publish</span><span class="sxs-lookup"><span data-stu-id="bfc22-154">dnu publish</span></span>                    | `dotnet publish` | <span data-ttu-id="bfc22-155">Pubblica l'applicazione per la distribuzione in una delle tre forme possibili: portabile, portabile con nativo e autonoma.</span><span class="sxs-lookup"><span data-stu-id="bfc22-155">Publish your application for deployment in one of the three forms (portable, portable with native and standalone).</span></span> |
| <span data-ttu-id="bfc22-156">dnu wrap</span><span class="sxs-lookup"><span data-stu-id="bfc22-156">dnu wrap</span></span>                       | <span data-ttu-id="bfc22-157">N/D\*</span><span class="sxs-lookup"><span data-stu-id="bfc22-157">N/A\*</span></span>          | <span data-ttu-id="bfc22-158">In DNX esegue il wrapping di un file project.json in csproj.</span><span class="sxs-lookup"><span data-stu-id="bfc22-158">In DNX world, wrap a project.json in csproj.</span></span>                                                                    |
| <span data-ttu-id="bfc22-159">dnu commands</span><span class="sxs-lookup"><span data-stu-id="bfc22-159">dnu commands</span></span>                   | <span data-ttu-id="bfc22-160">N/D\*</span><span class="sxs-lookup"><span data-stu-id="bfc22-160">N/A\*</span></span>          | <span data-ttu-id="bfc22-161">In DNX gestisce i comandi installati a livello globale.</span><span class="sxs-lookup"><span data-stu-id="bfc22-161">In DNX world, manage the globally installed commands.</span></span>                                                           |

<span data-ttu-id="bfc22-162">(\*) Queste funzionalità non sono supportate nell'interfaccia della riga di comando in base alla progettazione.</span><span class="sxs-lookup"><span data-stu-id="bfc22-162">(\*) - these features are not supported in the CLI by design.</span></span>

## <a name="dnx-features-that-are-not-supported"></a><span data-ttu-id="bfc22-163">Funzionalità DNX non supportate</span><span class="sxs-lookup"><span data-stu-id="bfc22-163">DNX features that are not supported</span></span>
<span data-ttu-id="bfc22-164">Come mostrato dalla tabella sopra riportata, esistono alcune funzionalità di DNX che, almeno per il momento, si è deciso di non supportare nell'interfaccia della riga di comando.</span><span class="sxs-lookup"><span data-stu-id="bfc22-164">As the table above shows, there are features from the DNX world that we decided not to support in the CLI, at least for the time being.</span></span> <span data-ttu-id="bfc22-165">Questa sezione indica le più importanti di queste funzionalità e descrive i motivi per cui non sono supportate. Illustra inoltre le soluzioni alternative da adottare nel caso in cui sia comunque necessario usare queste funzionalità.</span><span class="sxs-lookup"><span data-stu-id="bfc22-165">This section will go through the most important ones and outline the rationale behind not supporting them as well as workarounds if you do need them.</span></span>

### <a name="global-commands"></a><span data-ttu-id="bfc22-166">Comandi globali</span><span class="sxs-lookup"><span data-stu-id="bfc22-166">Global commands</span></span>
<span data-ttu-id="bfc22-167">DNU integrava un concetto denominato "comandi globali".</span><span class="sxs-lookup"><span data-stu-id="bfc22-167">DNU came with a concept called "global commands".</span></span> <span data-ttu-id="bfc22-168">Si trattava essenzialmente di applicazioni console create come pacchetti NuGet con uno script della shell che richiamava il DNX specificato per l'esecuzione dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="bfc22-168">These were, essentially, console applications packaged up as NuGet packages with a shell script that would invoke the DNX you specified to run the application.</span></span>

<span data-ttu-id="bfc22-169">L'interfaccia della riga di comando non supporta questo concetto.</span><span class="sxs-lookup"><span data-stu-id="bfc22-169">The CLI does not support this concept.</span></span> <span data-ttu-id="bfc22-170">Supporta tuttavia l'aggiunta di comandi per progetto che è possibile richiamare usando la comune sintassi `dotnet <command>`.</span><span class="sxs-lookup"><span data-stu-id="bfc22-170">It does, however, support the concept of adding per-project commands that can be invoked using the familiar `dotnet <command>` syntax.</span></span>

### <a name="installing-dependencies"></a><span data-ttu-id="bfc22-171">Installazione delle dipendenze</span><span class="sxs-lookup"><span data-stu-id="bfc22-171">Installing dependencies</span></span>
<span data-ttu-id="bfc22-172">A partire dalla versione 1, l'interfaccia `install` della riga di comando di .NET Core non dispone di un comando per l'installazione delle dipendenze.</span><span class="sxs-lookup"><span data-stu-id="bfc22-172">As of v1, the .NET Core CLI doesn't have an `install` command for installing dependencies.</span></span> <span data-ttu-id="bfc22-173">Per installare un pacchetto da NuGet, è necessario aggiungerlo come dipendenza al file `project.json` e quindi eseguire `dotnet restore` ([vedere la nota](#dotnet-restore-note)).</span><span class="sxs-lookup"><span data-stu-id="bfc22-173">In order to install a package from NuGet, you would need to add it as a dependency to your `project.json` file and then run `dotnet restore` ([see note](#dotnet-restore-note)).</span></span>

### <a name="running-your-code"></a><span data-ttu-id="bfc22-174">Esecuzione del codice</span><span class="sxs-lookup"><span data-stu-id="bfc22-174">Running your code</span></span>
<span data-ttu-id="bfc22-175">È possibile eseguire il codice in due modi:</span><span class="sxs-lookup"><span data-stu-id="bfc22-175">There are two main ways to run your code.</span></span> <span data-ttu-id="bfc22-176">Dal sorgente, con `dotnet run`.</span><span class="sxs-lookup"><span data-stu-id="bfc22-176">One is from source, with `dotnet run`.</span></span> <span data-ttu-id="bfc22-177">A differenza di `dnx run`, questo comando non esegue alcuna compilazione in memoria,</span><span class="sxs-lookup"><span data-stu-id="bfc22-177">Unlike `dnx run`, this will not do any in-memory compilation.</span></span> <span data-ttu-id="bfc22-178">ma richiama `dotnet build` per compilare il codice e quindi eseguire il file binario compilato.</span><span class="sxs-lookup"><span data-stu-id="bfc22-178">It will actually invoke `dotnet build` to build your code and then run the built binary.</span></span>

<span data-ttu-id="bfc22-179">Un altro modo consiste nell'usare `dotnet` per eseguire il codice.</span><span class="sxs-lookup"><span data-stu-id="bfc22-179">Another way is using the `dotnet` itself to run your code.</span></span> <span data-ttu-id="bfc22-180">A tale scopo, fornire un percorso all'assembly: `dotnet path/to/an/assembly.dll`.</span><span class="sxs-lookup"><span data-stu-id="bfc22-180">This is done by providing a path to your assembly: `dotnet path/to/an/assembly.dll`.</span></span>

## <a name="migrating-your-dnx-project-to-net-core-cli"></a><span data-ttu-id="bfc22-181">Migrazione di un progetto DNX all'interfaccia della riga di comando di .NET Core</span><span class="sxs-lookup"><span data-stu-id="bfc22-181">Migrating your DNX project to .NET Core CLI</span></span>
<span data-ttu-id="bfc22-182">Oltre all'uso di nuovi comandi per la gestione del codice, la migrazione da DNX consente anche le tre operazioni seguenti:</span><span class="sxs-lookup"><span data-stu-id="bfc22-182">In addition to using new commands when working with your code, there are three major things left in migrating from DNX:</span></span>

1. <span data-ttu-id="bfc22-183">Migrazione del file `global.json` se tale file è in grado di usare l'interfaccia della riga di comando.</span><span class="sxs-lookup"><span data-stu-id="bfc22-183">Migrate the `global.json` file if you have it to be able to use CLI.</span></span>
2. <span data-ttu-id="bfc22-184">Migrazione del file di progetto stesso (`project.json`) agli strumenti dell'interfaccia della riga di comando.</span><span class="sxs-lookup"><span data-stu-id="bfc22-184">Migrating the project file (`project.json`) itself to the CLI tooling.</span></span>
3. <span data-ttu-id="bfc22-185">Migrazione di qualsiasi API DNX alla corrispondente controparte BCL.</span><span class="sxs-lookup"><span data-stu-id="bfc22-185">Migrating off of any DNX APIs to their BCL counterparts.</span></span>

### <a name="changing-the-globaljson-file"></a><span data-ttu-id="bfc22-186">Modifica del file global.json</span><span class="sxs-lookup"><span data-stu-id="bfc22-186">Changing the global.json file</span></span>
<span data-ttu-id="bfc22-187">Il file `global.json` funge da file di soluzione sia per progetti RC1 che per progetti RC2 (o versioni successive).</span><span class="sxs-lookup"><span data-stu-id="bfc22-187">The `global.json` file acts like a solution file for both the RC1 and RC2 (or later) projects.</span></span> <span data-ttu-id="bfc22-188">Affinché l'interfaccia della riga di comando di .NET Core (così come `"sdk": { "version" }` Visual Studio) distingua tra RC1 e versioni successive, usano la proprietà per fare la distinzione quale progetto è RC1 o versione successiva.</span><span class="sxs-lookup"><span data-stu-id="bfc22-188">In order for the .NET Core CLI (as well as Visual Studio) to differentiate between RC1 and later versions, they use the `"sdk": { "version" }` property to make the distinction which project is RC1 or later.</span></span> <span data-ttu-id="bfc22-189">Se il file `global.json` non dispone affatto di questo nodo, presuppone che si tratti della versione più recente.</span><span class="sxs-lookup"><span data-stu-id="bfc22-189">If `global.json` doesn't have this node at all, it is assumed to be the latest.</span></span>

<span data-ttu-id="bfc22-190">Per aggiornare il file `global.json`, rimuovere la proprietà o impostarla sulla versione esatta degli strumenti che si intende usare, in questo caso **1.0.0-preview2-003121**:</span><span class="sxs-lookup"><span data-stu-id="bfc22-190">In order to update the `global.json` file, either remove the property or set it to the exact version of the tools that you wish to use, in this case **1.0.0-preview2-003121**:</span></span>

```json
{
    "sdk": {
        "version": "1.0.0-preview2-003121"
    }
}
```

### <a name="migrating-the-project-file"></a><span data-ttu-id="bfc22-191">Migrazione del file di progetto</span><span class="sxs-lookup"><span data-stu-id="bfc22-191">Migrating the project file</span></span>

<span data-ttu-id="bfc22-192">L'interfaccia della riga di comando e DNX usano entrambi lo stesso sistema di progetto basato sul file `project.json`.</span><span class="sxs-lookup"><span data-stu-id="bfc22-192">The CLI and DNX both use the same basic project system based on `project.json` file.</span></span> <span data-ttu-id="bfc22-193">La sintassi e la semantica del file di progetto sono molto simili, a parte alcune differenze dipendenti dallo scenario.</span><span class="sxs-lookup"><span data-stu-id="bfc22-193">The syntax and the semantics of the project file are pretty much the same, with small differences based on the scenarios.</span></span> <span data-ttu-id="bfc22-194">Sono presenti anche alcune modifiche dello schema, visibili nel [file di schema](http://json.schemastore.org/project).</span><span class="sxs-lookup"><span data-stu-id="bfc22-194">There are also some changes to the schema which you can see in the [schema file](http://json.schemastore.org/project).</span></span>

<span data-ttu-id="bfc22-195">Se si sta creando un'applicazione console, è necessario aggiungere al file di progetto il frammento seguente:</span><span class="sxs-lookup"><span data-stu-id="bfc22-195">If you are building a console application, you need to add the following snippet to your project file:</span></span>

```json
"buildOptions": {
    "emitEntryPoint": true
}
```

<span data-ttu-id="bfc22-196">Questo frammento indica a `dotnet build` di creare un punto di ingresso per l'applicazione, consentendo così l'esecuzione del codice.</span><span class="sxs-lookup"><span data-stu-id="bfc22-196">This instructs `dotnet build` to emit an entry point for your application, effectively making your code runnable.</span></span> <span data-ttu-id="bfc22-197">Se si sta creando una libreria di classi, ignorare semplicemente questa sezione.</span><span class="sxs-lookup"><span data-stu-id="bfc22-197">If you are building a class library, simply omit the above section.</span></span> <span data-ttu-id="bfc22-198">Naturalmente, dopo aver aggiunto al file `project.json` il frammento sopra riportato, è necessario aggiungere un punto di ingresso statico.</span><span class="sxs-lookup"><span data-stu-id="bfc22-198">Of course, once you add the above snippet to your `project.json` file, you need to add a static entry point.</span></span> <span data-ttu-id="bfc22-199">Con la sospensione di DNX, i servizi DI forniti da quest'ultimo non sono più disponibili e il punto di ingresso statico deve essere un punto di ingresso .NET di base: `static void Main()`.</span><span class="sxs-lookup"><span data-stu-id="bfc22-199">With the move off DNX, the DI services it provided are no longer available and thus this needs to be a basic .NET entry point: `static void Main()`.</span></span>

<span data-ttu-id="bfc22-200">Se nel file `project.json` è presente una sezione "commands", è possibile rimuoverla.</span><span class="sxs-lookup"><span data-stu-id="bfc22-200">If you have a "commands" section in your `project.json`, you can remove it.</span></span> <span data-ttu-id="bfc22-201">Alcuni comandi usati per rappresentare comandi DNU, ad esempio comandi dell'interfaccia della riga di comando di Entity Framework, vengono trasferiti nell'interfaccia della riga di comando come estensioni per progetto.</span><span class="sxs-lookup"><span data-stu-id="bfc22-201">Some of the commands that used to exist as DNU commands, such as Entity Framework CLI commands, are being ported to be per-project extensions to the CLI.</span></span> <span data-ttu-id="bfc22-202">Se sono stati creati comandi personalizzati che vengono usati nei progetti, è necessario sostituirli con estensioni dell'interfaccia della riga di comando.</span><span class="sxs-lookup"><span data-stu-id="bfc22-202">If you built your own commands that you are using in your projects, you need to replace them with CLI extensions.</span></span> <span data-ttu-id="bfc22-203">In questo caso, il nodo `commands` di `project.json` deve essere sostituito dal nodo `tools` e deve elencare le dipendenze degli strumenti.</span><span class="sxs-lookup"><span data-stu-id="bfc22-203">In this case, the `commands` node in `project.json` needs to be replaced by the `tools` node and it needs to list the tools dependencies.</span></span>

<span data-ttu-id="bfc22-204">Al termine di queste operazioni, è necessario decidere il tipo di portabilità per l'app.</span><span class="sxs-lookup"><span data-stu-id="bfc22-204">After these things are done, you need to decide which type of portability you wish for you app.</span></span> <span data-ttu-id="bfc22-205">Con .NET Core, Microsoft ha reso disponibile un'ampia gamma di opzioni di portabilità.</span><span class="sxs-lookup"><span data-stu-id="bfc22-205">With .NET Core, we have invested into providing a spectrum of portability options that you can choose from.</span></span> <span data-ttu-id="bfc22-206">È possibile, ad esempio, creare un'applicazione completamente *portabile* o un'applicazione *autonoma*.</span><span class="sxs-lookup"><span data-stu-id="bfc22-206">For instance, you may want to have a fully *portable* application or you may want to have a *self-contained* application.</span></span> <span data-ttu-id="bfc22-207">Il funzionamento di un'applicazione portabile è simile a quello delle applicazioni .NET Framework: è necessario un componente condiviso (.NET Core) da eseguire nel computer di destinazione.</span><span class="sxs-lookup"><span data-stu-id="bfc22-207">The portable application option is more like .NET Framework applications work: it needs a shared component to execute it on the target machine (.NET Core).</span></span> <span data-ttu-id="bfc22-208">L'applicazione autonoma non richiede l'installazione di .NET Core nel computer di destinazione, ma è necessario creare un'applicazione per ogni sistema operativo che si vuole supportare.</span><span class="sxs-lookup"><span data-stu-id="bfc22-208">The self-contained application doesn't require .NET Core to be installed on the target, but you have to produce one application for each OS you wish to support.</span></span> <span data-ttu-id="bfc22-209">Questi e altri tipi di portabilità sono discussi nel documento [Application portability type](../deploying/index.md) (Tipo di portabilità delle applicazioni).</span><span class="sxs-lookup"><span data-stu-id="bfc22-209">These portability types and more are discussed in the [application portability type](../deploying/index.md) document.</span></span>

<span data-ttu-id="bfc22-210">Una volta deciso il tipo di portabilità, è necessario modificare i framework di destinazione.</span><span class="sxs-lookup"><span data-stu-id="bfc22-210">Once you make a call on what type of portability you want, you need to change your targeted framework(s).</span></span> <span data-ttu-id="bfc22-211">Se si stanno creando applicazioni per .NET Core, è probabile che come framework di destinazione venga usato `dnxcore50`.</span><span class="sxs-lookup"><span data-stu-id="bfc22-211">If you were writing applications for .NET Core, you were most likely using `dnxcore50` as  your targeted framework.</span></span> <span data-ttu-id="bfc22-212">Con l'interfaccia della riga di comando e le modifiche apportate dal nuovo [.NET Standard](../../standard/net-standard.md), è necessario che il framework sia uno dei seguenti:</span><span class="sxs-lookup"><span data-stu-id="bfc22-212">With the CLI and the changes that the new [.NET Standard](../../standard/net-standard.md) brought, the framework needs to be one of the following:</span></span>

1. <span data-ttu-id="bfc22-213">`netcoreapp1.0`: se si stanno creando applicazioni in .NET Core (incluse applicazioni ASP.NET Core)</span><span class="sxs-lookup"><span data-stu-id="bfc22-213">`netcoreapp1.0` - if you are writing applications on .NET Core (including ASP.NET Core applications)</span></span>
2. <span data-ttu-id="bfc22-214">`netstandard1.6`: se si stanno creando librerie di classi per .NET Core</span><span class="sxs-lookup"><span data-stu-id="bfc22-214">`netstandard1.6` - if you are writing class libraries for .NET Core</span></span>

<span data-ttu-id="bfc22-215">Se si usano altre destinazioni `dnx`, ad esempio `dnx451`, sarà necessario modificare anche tali destinazioni.</span><span class="sxs-lookup"><span data-stu-id="bfc22-215">If you are using other `dnx` targets, like `dnx451` you will need to change those as well.</span></span> <span data-ttu-id="bfc22-216">`dnx451` deve essere modificato in `net451`.</span><span class="sxs-lookup"><span data-stu-id="bfc22-216">`dnx451` should be changed to `net451`.</span></span>
<span data-ttu-id="bfc22-217">Per altre informazioni, vedere l'argomento [.NET Standard](../../standard/net-standard.md).</span><span class="sxs-lookup"><span data-stu-id="bfc22-217">Please refer to the [.NET Standard](../../standard/net-standard.md) topic for more information.</span></span>

<span data-ttu-id="bfc22-218">Il file `project.json` è ora praticamente pronto.</span><span class="sxs-lookup"><span data-stu-id="bfc22-218">Your `project.json` is now mostly ready.</span></span> <span data-ttu-id="bfc22-219">È necessario scorrere l'elenco delle dipendenze e aggiornarle alle versioni più recenti, in particolare se si usano dipendenze ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="bfc22-219">You need to go through your dependencies list and update the dependencies to their newer versions, especially if you are using ASP.NET Core dependencies.</span></span> <span data-ttu-id="bfc22-220">Se si usano pacchetti separati per le API BCL, è possibile usare il pacchetto di runtime, come spiegato nel documento [Application portability type](../deploying/index.md) (Tipo di portabilità delle applicazioni).</span><span class="sxs-lookup"><span data-stu-id="bfc22-220">If you were using separate packages for BCL APIs, you can use the runtime package as explained in the [application portability type](../deploying/index.md) document.</span></span>

<span data-ttu-id="bfc22-221">Al termine, è possibile tentare il ripristino con `dotnet restore` ([vedere la nota](#dotnet-restore-note)).</span><span class="sxs-lookup"><span data-stu-id="bfc22-221">Once you are ready, you can try restoring with `dotnet restore` ([see note](#dotnet-restore-note)).</span></span> <span data-ttu-id="bfc22-222">A seconda della versione delle dipendenze, si potrebbero verificare errori se NuGet non è in grado di risolvere le dipendenze stesse per uno dei framework di destinazione sopra indicati.</span><span class="sxs-lookup"><span data-stu-id="bfc22-222">Depending on the version of your dependencies, you may encounter errors if NuGet cannot resolve the dependencies for one of the targeted frameworks above.</span></span> <span data-ttu-id="bfc22-223">Si tratta di un problema momentaneo: con il passare del tempo saranno sempre di più i pacchetti che includono il supporto per questi framework.</span><span class="sxs-lookup"><span data-stu-id="bfc22-223">This is a "point-in-time" problem; as time progresses, more and more packages will include support for these frameworks.</span></span> <span data-ttu-id="bfc22-224">Per ora, se si verifica questo problema, è possibile usare l'istruzione `imports` all'interno del nodo `framework` per indicare a NuGet che può ripristinare i pacchetti che hanno come destinazione il framework indicato nell'istruzione "imports".</span><span class="sxs-lookup"><span data-stu-id="bfc22-224">For now, if you run into this, you can use the `imports` statement within the `framework` node to specify to NuGet that it can restore the packages targeting the framework within the "imports" statement.</span></span>
<span data-ttu-id="bfc22-225">I messaggi di errore di ripristino visualizzati in questo caso dovrebbero fornire informazioni sufficienti per stabilire quali framework è necessario importare.</span><span class="sxs-lookup"><span data-stu-id="bfc22-225">The restoring errors you get in this case should provide enough information to tell you which frameworks you need to import.</span></span> <span data-ttu-id="bfc22-226">In caso di dubbi o se si esegue questa operazione per la prima volta, come indicazione generale specificare `dnxcore50` e `portable-net45+win8` nell'istruzione `imports`.</span><span class="sxs-lookup"><span data-stu-id="bfc22-226">If you are slightly lost or new to this, in general, specifying `dnxcore50` and `portable-net45+win8` in the `imports` statement should do the trick.</span></span> <span data-ttu-id="bfc22-227">Il frammento JSON seguente mostra un esempio di questo scenario:</span><span class="sxs-lookup"><span data-stu-id="bfc22-227">The JSON snippet below shows how this looks like:</span></span>

```json
    "frameworks": {
        "netcoreapp1.0": {
            "imports": ["dnxcore50", "portable-net45+win8"]
        }
    }
```

<span data-ttu-id="bfc22-228">L'esecuzione di `dotnet build` mostrerà eventuali errori di compilazione, che tuttavia non dovrebbero essere numerosi.</span><span class="sxs-lookup"><span data-stu-id="bfc22-228">Running `dotnet build` will show any eventual build errors, though there shouldn't be too many of them.</span></span> <span data-ttu-id="bfc22-229">Dopo aver compilato ed eseguito correttamente il codice, è possibile testarlo con Test Runner.</span><span class="sxs-lookup"><span data-stu-id="bfc22-229">After your code is building and running properly, you can test it out with the runner.</span></span> <span data-ttu-id="bfc22-230">Eseguire `dotnet <path-to-your-assembly>` e osservare l'esecuzione.</span><span class="sxs-lookup"><span data-stu-id="bfc22-230">Execute `dotnet <path-to-your-assembly>` and see it run.</span></span>

<a name="dotnet-restore-note"></a>

[!INCLUDE[DotNet Restore Note](~/includes/dotnet-restore-note.md)]
