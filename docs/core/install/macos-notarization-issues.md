---
title: Uso dell'autenticazione di macOS Catalina
description: Come gestire i problemi di autenticazione e autenticazione con macOS quando si installa il runtime, l'SDK e le app di .NET Core compilati con .NET Core.
author: adegeo
ms.author: adegeo
ms.date: 02/14/2020
ms.openlocfilehash: cd3886b2e772a182156d212aefb9705a3fb5e17c
ms.sourcegitcommit: dc2feef0794cf41dbac1451a13b8183258566c0e
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 06/24/2020
ms.locfileid: "85324616"
---
# <a name="macos-catalina-notarization-and-the-impact-on-net-core-downloads-and-projects"></a><span data-ttu-id="2b3d9-103">Autenticazione di macOS Catalina e l'effetto sui download e sui progetti di .NET Core</span><span class="sxs-lookup"><span data-stu-id="2b3d9-103">macOS Catalina Notarization and the impact on .NET Core downloads and projects</span></span>

<span data-ttu-id="2b3d9-104">A partire da macOS Catalina (versione 10,15), tutto il software creato dopo il 1 ° giugno 2019 e distribuito con ID sviluppatore deve essere autenticato.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-104">Beginning with macOS Catalina (version 10.15), all software built after June 1, 2019, and distributed with Developer ID, must be notarized.</span></span> <span data-ttu-id="2b3d9-105">Questo requisito si applica al runtime di .NET Core, alla .NET Core SDK e al software creato con .NET Core.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-105">This requirement applies to the .NET Core runtime, .NET Core SDK, and software created with .NET Core.</span></span> <span data-ttu-id="2b3d9-106">Questo articolo descrive gli scenari comuni che si possono incontrare con l'autenticazione di .NET Core e macOS.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-106">This article describes the common scenarios you may face with .NET Core and macOS notarization.</span></span>

## <a name="installing-net-core"></a><span data-ttu-id="2b3d9-107">Installazione di .NET Core</span><span class="sxs-lookup"><span data-stu-id="2b3d9-107">Installing .NET Core</span></span>

<span data-ttu-id="2b3d9-108">I programmi di installazione per .NET Core (Runtime e SDK) versioni 3,1, 3,0 e 2,1 sono stati autenticati dal 18 febbraio 2020.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-108">The installers for .NET Core (both runtime and SDK) versions 3.1, 3.0, and 2.1, have been notarized since February 18, 2020.</span></span> <span data-ttu-id="2b3d9-109">Le versioni precedenti rilasciate non vengono autenticate.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-109">Prior released versions aren't notarized.</span></span> <span data-ttu-id="2b3d9-110">È possibile installare manualmente una versione non autenticata di .NET Core scaricando prima il programma di installazione e quindi usando il `sudo installer` comando.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-110">You can manually install a non-notarized version of .NET Core by first downloading the installer, and then using the `sudo installer` command.</span></span> <span data-ttu-id="2b3d9-111">Per altre informazioni, vedere [scaricare e installare manualmente per MacOS](sdk.md?pivots=os-macos#download-and-manually-install).</span><span class="sxs-lookup"><span data-stu-id="2b3d9-111">For more information, see [Download and manually install for macOS](sdk.md?pivots=os-macos#download-and-manually-install).</span></span>

<span data-ttu-id="2b3d9-112">A partire dalle versioni seguenti, i programmi di installazione di .NET Core sono autenticati:</span><span class="sxs-lookup"><span data-stu-id="2b3d9-112">Beginning with the following versions, the .NET Core installers are notarized:</span></span>

- <span data-ttu-id="2b3d9-113">Runtime di .NET Core</span><span class="sxs-lookup"><span data-stu-id="2b3d9-113">.NET Core Runtime</span></span>
  - <span data-ttu-id="2b3d9-114">2.1.16</span><span class="sxs-lookup"><span data-stu-id="2b3d9-114">2.1.16</span></span>
  - <span data-ttu-id="2b3d9-115">3.0.3</span><span class="sxs-lookup"><span data-stu-id="2b3d9-115">3.0.3</span></span>
  - <span data-ttu-id="2b3d9-116">3.1.2</span><span class="sxs-lookup"><span data-stu-id="2b3d9-116">3.1.2</span></span>

- <span data-ttu-id="2b3d9-117">.NET Core SDK</span><span class="sxs-lookup"><span data-stu-id="2b3d9-117">.NET Core SDK</span></span>
  - <span data-ttu-id="2b3d9-118">2.1.512</span><span class="sxs-lookup"><span data-stu-id="2b3d9-118">2.1.512</span></span>
  - <span data-ttu-id="2b3d9-119">3.0.103</span><span class="sxs-lookup"><span data-stu-id="2b3d9-119">3.0.103</span></span>
  - <span data-ttu-id="2b3d9-120">3.1.102</span><span class="sxs-lookup"><span data-stu-id="2b3d9-120">3.1.102</span></span>

## <a name="apphost-is-disabled-by-default"></a><span data-ttu-id="2b3d9-121">appHost è disabilitato per impostazione predefinita</span><span class="sxs-lookup"><span data-stu-id="2b3d9-121">appHost is disabled by default</span></span>

<span data-ttu-id="2b3d9-122">Per impostazione predefinita, le versioni non autenticate di .NET Core SDK 3,0 e versioni successive producono un eseguibile nativo di Mach-O (noto come **APPHOST**) quando il progetto viene compilato, pubblicato o eseguito.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-122">By default, non-notarized versions of the .NET Core SDK 3.0 and above produce a native Mach-O executable (known as the **appHost**) when your project compiles, publishes, or is run.</span></span> <span data-ttu-id="2b3d9-123">Questo eseguibile è un modo pratico per eseguire l'app.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-123">This executable is a convenient way to run your app.</span></span> <span data-ttu-id="2b3d9-124">In caso contrario, l'app deve essere avviata eseguendo `dotnet <filename.dll>` .</span><span class="sxs-lookup"><span data-stu-id="2b3d9-124">Otherwise, your app must be started by running `dotnet <filename.dll>`.</span></span> <span data-ttu-id="2b3d9-125">Quando appHost è abilitato, il `dotnet run` comando viene richiamato nel contesto di APPHOST.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-125">When the appHost is enabled, the `dotnet run` command is invoked in the context of the appHost.</span></span> <span data-ttu-id="2b3d9-126">Per ulteriori informazioni, vedere [contesto di APPHOST](#context-of-the-apphost).</span><span class="sxs-lookup"><span data-stu-id="2b3d9-126">For more information, see [Context of the appHost](#context-of-the-apphost).</span></span>

<span data-ttu-id="2b3d9-127">A partire dalle versioni autenticate di .NET Core SDK 3,0 e versioni successive, l'eseguibile appHost non viene generato per impostazione predefinita.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-127">Starting with the notarized versions of the .NET Core SDK 3.0 and above, the appHost executable isn't generated by default.</span></span> <span data-ttu-id="2b3d9-128">È possibile attivare la generazione appHost con l' [`UseAppHost`](../project-sdk/msbuild-props.md#useapphost) impostazione booleana nel file di progetto.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-128">You can turn on appHost generation with the [`UseAppHost`](../project-sdk/msbuild-props.md#useapphost) boolean setting in the project file.</span></span> <span data-ttu-id="2b3d9-129">È anche possibile impostare appHost con il `-p:UseAppHost` parametro nella riga di comando per il `dotnet` comando specifico eseguito:</span><span class="sxs-lookup"><span data-stu-id="2b3d9-129">You can also toggle the appHost with the `-p:UseAppHost` parameter on the command line for the specific `dotnet` command you run:</span></span>

- <span data-ttu-id="2b3d9-130">File di progetto</span><span class="sxs-lookup"><span data-stu-id="2b3d9-130">Project file</span></span>

  ```xml
  <PropertyGroup>
    <UseAppHost>true</UseAppHost>
  </PropertyGroup>
  ```

- <span data-ttu-id="2b3d9-131">Parametro della riga di comando</span><span class="sxs-lookup"><span data-stu-id="2b3d9-131">Command-line parameter</span></span>

  ```dotnetcli
  dotnet run -p:UseAppHost=true
  ```

<span data-ttu-id="2b3d9-132">Quando si pubblica l'app [autonoma](../deploying/index.md#publish-self-contained), viene sempre creato un APPHOST.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-132">An appHost is always created when you publish your app [self-contained](../deploying/index.md#publish-self-contained).</span></span>

<span data-ttu-id="2b3d9-133">Per ulteriori informazioni sull' `UseAppHost` impostazione, vedere [Proprietà MSBuild per Microsoft. NET. SDK](../project-sdk/msbuild-props.md#useapphost).</span><span class="sxs-lookup"><span data-stu-id="2b3d9-133">For more information about the `UseAppHost` setting, see [MSBuild properties for Microsoft.NET.Sdk](../project-sdk/msbuild-props.md#useapphost).</span></span>

### <a name="context-of-the-apphost"></a><span data-ttu-id="2b3d9-134">Contesto di appHost</span><span class="sxs-lookup"><span data-stu-id="2b3d9-134">Context of the appHost</span></span>

<span data-ttu-id="2b3d9-135">Quando il appHost è abilitato nel progetto e si usa il `dotnet run` comando per eseguire l'app, l'app viene richiamata nel contesto di APPHOST e non nell'host predefinito (l'host predefinito è il `dotnet` comando).</span><span class="sxs-lookup"><span data-stu-id="2b3d9-135">When the appHost is enabled in your project, and you use the `dotnet run` command to run your app, the app is invoked in the context of the appHost and not the default host (the default host is the `dotnet` command).</span></span> <span data-ttu-id="2b3d9-136">Se il appHost è disabilitato nel progetto, il `dotnet run` comando esegue l'applicazione nel contesto dell'host predefinito.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-136">If the appHost is disabled in your project, the `dotnet run` command runs your app in the context of the default host.</span></span> <span data-ttu-id="2b3d9-137">Anche se il appHost è disabilitato, la pubblicazione dell'applicazione come autonoma genera un eseguibile appHost e gli utenti usano tale eseguibile per eseguire l'app.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-137">Even if the appHost is disabled, publishing your app as self-contained generates an appHost executable, and users use that executable to run your app.</span></span> <span data-ttu-id="2b3d9-138">L'esecuzione dell'app con `dotnet <filename.dll>` richiama l'app con l'host predefinito, il runtime condiviso.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-138">Running your app with `dotnet <filename.dll>` invokes the app with the default host, the shared runtime.</span></span>

<span data-ttu-id="2b3d9-139">Quando viene richiamata un'app che usa il appHost, la partizione del certificato a cui accede l'app è diversa dall'host predefinito autenticato.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-139">When an app using the appHost is invoked, the certificate partition accessed by the app is different from the notarized default host.</span></span> <span data-ttu-id="2b3d9-140">Se l'app deve accedere ai certificati installati tramite l'host predefinito, usare il `dotnet run` comando per eseguire l'app dal file di progetto oppure usare il `dotnet <filename.dll>` comando per avviare direttamente l'app.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-140">If your app must access the certificates installed through the default host, use the `dotnet run` command to run your app from its project file, or use the `dotnet <filename.dll>` command to start the app directly.</span></span>

<span data-ttu-id="2b3d9-141">Altre informazioni su questo scenario sono disponibili nella sezione [ASP.NET Core e MacOS e sui certificati](#aspnet-core-and-macos-and-certificates) .</span><span class="sxs-lookup"><span data-stu-id="2b3d9-141">More information about this scenario is provided in the [ASP.NET Core and macOS and certificates](#aspnet-core-and-macos-and-certificates) section.</span></span>

## <a name="aspnet-core-and-macos-and-certificates"></a><span data-ttu-id="2b3d9-142">ASP.NET Core e macOS e certificati</span><span class="sxs-lookup"><span data-stu-id="2b3d9-142">ASP.NET Core and macOS and certificates</span></span>

<span data-ttu-id="2b3d9-143">.NET Core consente di gestire i certificati nel keychain di macOS con la <xref:System.Security.Cryptography.X509Certificates> classe.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-143">.NET Core provides the ability to manage certificates in the macOS Keychain with the <xref:System.Security.Cryptography.X509Certificates> class.</span></span> <span data-ttu-id="2b3d9-144">L'accesso al keychain di macOS usa l'identità delle applicazioni come chiave primaria per decidere quale partizione prendere in considerazione.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-144">Access to the macOS Keychain uses the applications identity as the primary key when deciding which partition to consider.</span></span> <span data-ttu-id="2b3d9-145">Ad esempio, le applicazioni non firmate archiviano i segreti nella partizione senza segno, ma le applicazioni firmate archiviano i segreti solo nelle partizioni a cui possono accedere.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-145">For example, unsigned applications store secrets in the unsigned partition, but signed applications store their secrets in partitions only they can access.</span></span> <span data-ttu-id="2b3d9-146">L'origine di esecuzione che richiama l'app decide quale partizione usare.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-146">The source of execution that invokes your app decides which partition to use.</span></span>

<span data-ttu-id="2b3d9-147">.NET Core fornisce tre origini di esecuzione: [APPHOST](#apphost-is-disabled-by-default), l'host predefinito (il `dotnet` comando) e un host personalizzato.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-147">.NET Core provides three sources of execution: [appHost](#apphost-is-disabled-by-default), default host (the `dotnet` command), and a custom host.</span></span> <span data-ttu-id="2b3d9-148">Ogni modello di esecuzione può avere identità diverse, con segno o senza segno e ha accesso a partizioni diverse all'interno del keychain.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-148">Each execution model may have different identities, either signed or unsigned, and has access to different partitions within the Keychain.</span></span> <span data-ttu-id="2b3d9-149">I certificati importati da una modalità potrebbero non essere accessibili da un altro.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-149">Certificates imported by one mode may not be accessible from another.</span></span> <span data-ttu-id="2b3d9-150">Ad esempio, le versioni autenticate di .NET Core hanno un host predefinito firmato.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-150">For example, the notarized versions of .NET Core have a default host that is signed.</span></span> <span data-ttu-id="2b3d9-151">I certificati vengono importati in una partizione sicura in base alla relativa identità.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-151">Certificates are imported into a secure partition based on its identity.</span></span> <span data-ttu-id="2b3d9-152">Questi certificati non sono accessibili da un appHost generato, perché appHost non è firmato.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-152">These certificates aren't accessible from a generated appHost, as the appHost is unsigned.</span></span>

<span data-ttu-id="2b3d9-153">Un altro esempio, per impostazione predefinita, ASP.NET Core importa un certificato SSL predefinito tramite l'host predefinito.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-153">Another example, by default, ASP.NET Core imports a default SSL certificate through the default host.</span></span> <span data-ttu-id="2b3d9-154">ASP.NET Core applicazioni che usano un appHost non avranno accesso a questo certificato e riceveranno un errore quando .NET Core rileva che il certificato non è accessibile.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-154">ASP.NET Core applications that use an appHost won't have access to this certificate and will receive an error when .NET Core detects the certificate isn't accessible.</span></span> <span data-ttu-id="2b3d9-155">Nel messaggio di errore vengono fornite istruzioni su come risolvere il problema.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-155">The error message provides instructions on how to fix this problem.</span></span>

<span data-ttu-id="2b3d9-156">Se è richiesta la condivisione del certificato, macOS fornisce le opzioni di configurazione con l' `security` utilità.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-156">If certificate sharing is required, macOS provides configuration options with the `security` utility.</span></span>

<span data-ttu-id="2b3d9-157">Per ulteriori informazioni su come risolvere i problemi relativi al certificato ASP.NET Core, vedere [enforce HTTPS in ASP.NET Core](/aspnet/core/security/enforcing-ssl?view=aspnetcore-3.1&tabs=visual-studio#troubleshoot-certificate-problems).</span><span class="sxs-lookup"><span data-stu-id="2b3d9-157">For more information on how to troubleshoot ASP.NET Core certificate issues, see [Enforce HTTPS in ASP.NET Core](/aspnet/core/security/enforcing-ssl?view=aspnetcore-3.1&tabs=visual-studio#troubleshoot-certificate-problems).</span></span>

## <a name="default-entitlements"></a><span data-ttu-id="2b3d9-158">Diritti predefiniti</span><span class="sxs-lookup"><span data-stu-id="2b3d9-158">Default entitlements</span></span>

<span data-ttu-id="2b3d9-159">L'host predefinito di .NET Core (il `dotnet` comando) ha un set di diritti predefiniti.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-159">.NET Core’s default host (the `dotnet` command) has a set of default entitlements.</span></span> <span data-ttu-id="2b3d9-160">Questi diritti sono necessari per il funzionamento corretto di .NET Core.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-160">These entitlements are required for proper operation of .NET Core.</span></span> <span data-ttu-id="2b3d9-161">È possibile che l'applicazione richieda diritti aggiuntivi, nel qual caso sarà necessario generare e usare un [APPHOST](#apphost-is-disabled-by-default) e quindi aggiungere i diritti necessari in locale.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-161">It's possible that your application may need additional entitlements, in which case you'll need to generate and use an [appHost](#apphost-is-disabled-by-default) and then add the necessary entitlements locally.</span></span>

<span data-ttu-id="2b3d9-162">Set predefinito di diritti per .NET Core:</span><span class="sxs-lookup"><span data-stu-id="2b3d9-162">Default set of entitlements for .NET Core:</span></span>

- `com.apple.security.cs.allow-jit`
- `com.apple.security.cs.allow-unsigned-executable-memory`
- `com.apple.security.cs.allow-dyld-environment-variables`
- `com.apple.security.cs.disable-library-validation`

## <a name="notarize-a-net-core-app"></a><span data-ttu-id="2b3d9-163">Autenticare un'app .NET Core</span><span class="sxs-lookup"><span data-stu-id="2b3d9-163">Notarize a .NET Core app</span></span>

<span data-ttu-id="2b3d9-164">Se si vuole che l'applicazione venga eseguita in macOS Catalina (versione 10,15) o successiva, è necessario autenticare l'app.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-164">If you want your application to run on macOS Catalina (version 10.15) or higher, you'll want to notarize your app.</span></span> <span data-ttu-id="2b3d9-165">I appHost inviati con l'applicazione per l'autenticazione devono essere usati con almeno gli stessi [diritti predefiniti](#default-entitlements) per .NET Core.</span><span class="sxs-lookup"><span data-stu-id="2b3d9-165">The appHost you submit with your application for notarization should be used with at least the same [default entitlements](#default-entitlements) for .NET Core.</span></span>

## <a name="next-steps"></a><span data-ttu-id="2b3d9-166">Passaggi successivi</span><span class="sxs-lookup"><span data-stu-id="2b3d9-166">Next steps</span></span>

- <span data-ttu-id="2b3d9-167">[Dipendenze e requisiti di .NET Core](dependencies.md).</span><span class="sxs-lookup"><span data-stu-id="2b3d9-167">[.NET Core dependencies and requirements](dependencies.md).</span></span>
- <span data-ttu-id="2b3d9-168">[Installare il .NET Core SDK](sdk.md).</span><span class="sxs-lookup"><span data-stu-id="2b3d9-168">[Install the .NET Core SDK](sdk.md).</span></span>
- [<span data-ttu-id="2b3d9-169">Installare il runtime di .NET Core</span><span class="sxs-lookup"><span data-stu-id="2b3d9-169">Install the .NET Core Runtime</span></span>](runtime.md)
