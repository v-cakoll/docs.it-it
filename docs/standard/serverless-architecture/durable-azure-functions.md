---
title: Funzioni di Azure durevole - App senza server
description: Funzioni di Azure durevole estendono il runtime di funzioni di Azure per abilitare flussi di lavoro con stati nel codice.
author: cecilphillip
ms.author: cephilli
ms.date: 06/26/2018
ms.openlocfilehash: f7ee74926d6658042120113b49dc763383881423
ms.sourcegitcommit: 8699383914c24a0df033393f55db3369db728a7b
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 05/15/2019
ms.locfileid: "65643408"
---
# <a name="durable-azure-functions"></a><span data-ttu-id="b4095-103">Funzioni Durable di Azure</span><span class="sxs-lookup"><span data-stu-id="b4095-103">Durable Azure functions</span></span>

<span data-ttu-id="b4095-104">Quando si creano applicazioni senza server con funzioni di Azure, le operazioni saranno in genere progettate per l'esecuzione in modalità senza stata.</span><span class="sxs-lookup"><span data-stu-id="b4095-104">When creating serverless applications with Azure Functions, your operations will typically be designed to run in a stateless manner.</span></span> <span data-ttu-id="b4095-105">Il motivo di tale scelta di progettazione è perché come aggiornare la piattaforma, diventa difficile sapere quali server in cui viene eseguito il codice.</span><span class="sxs-lookup"><span data-stu-id="b4095-105">The reason for this design choice is because as the platform scales, it becomes difficult to know what servers the code is running on.</span></span> <span data-ttu-id="b4095-106">Inoltre, diventa difficile sapere quante istanze sono attive in qualsiasi momento.</span><span class="sxs-lookup"><span data-stu-id="b4095-106">It also becomes difficult to know how many instances are active at any given point.</span></span> <span data-ttu-id="b4095-107">Tuttavia, esistono classi di applicazioni che richiedono lo stato corrente di un processo deve essere noto.</span><span class="sxs-lookup"><span data-stu-id="b4095-107">However, there are classes of applications that require the current state of a process to be known.</span></span> <span data-ttu-id="b4095-108">Prendere in considerazione il processo di invio di un ordine a uno store online.</span><span class="sxs-lookup"><span data-stu-id="b4095-108">Consider the process of submitting an order to an online store.</span></span> <span data-ttu-id="b4095-109">L'operazione di checkpoint può essere un flusso di lavoro costituito da più operazioni che è necessario conoscere lo stato del processo.</span><span class="sxs-lookup"><span data-stu-id="b4095-109">The checkout operation might be a workflow that is composed of multiple operations that need to know the state of the process.</span></span> <span data-ttu-id="b4095-110">Tali informazioni possono includere nell'inventario dei prodotti, se il cliente ha eventuali crediti nel proprio account, nonché i risultati dell'elaborazione della carta di credito.</span><span class="sxs-lookup"><span data-stu-id="b4095-110">Such information may include the product inventory, if the customer has any credits on their account, and also the results of processing the credit card.</span></span> <span data-ttu-id="b4095-111">Queste operazioni può essere facilmente i propri flussi di lavoro interni o persino servizi dai sistemi di terze parti.</span><span class="sxs-lookup"><span data-stu-id="b4095-111">These operations could easily be their own internal workflows or even services from third-party systems.</span></span>

<span data-ttu-id="b4095-112">Attualmente i vari modelli esistono fornire tale assistenza con il coordinamento di stato dell'applicazione tra i sistemi interni ed esterni.</span><span class="sxs-lookup"><span data-stu-id="b4095-112">Various patterns exist today that assist with the coordination of application state between internal and external systems.</span></span> <span data-ttu-id="b4095-113">È raro riscontrare soluzioni che si basano su sistemi centralizzati di accodamento, distribuito gli archivi chiave-valore o i database condivisi per gestire tale stato.</span><span class="sxs-lookup"><span data-stu-id="b4095-113">It's common to come across solutions that rely on centralized queuing systems, distributed key-value stores, or shared databases to manage that state.</span></span> <span data-ttu-id="b4095-114">Tuttavia, queste sono tutte le altre risorse che devono ora essere eseguito il provisioning e gestite.</span><span class="sxs-lookup"><span data-stu-id="b4095-114">However, these are all additional resources that now need to be provisioned and managed.</span></span> <span data-ttu-id="b4095-115">In un ambiente senza server, il codice può diventare scomodo tenta di coordinare con queste risorse manualmente.</span><span class="sxs-lookup"><span data-stu-id="b4095-115">In a serverless environment, your code could become cumbersome trying to coordinate with these resources manually.</span></span> <span data-ttu-id="b4095-116">Funzioni di Azure offre un'alternativa per la creazione di funzioni con state chiamate di funzioni permanenti.</span><span class="sxs-lookup"><span data-stu-id="b4095-116">Azure Functions offers an alternative for creating stateful functions called Durable Functions.</span></span>

<span data-ttu-id="b4095-117">Funzioni permanenti è un'estensione per il runtime di funzioni di Azure che consente la definizione dei flussi di lavoro con stati nel codice.</span><span class="sxs-lookup"><span data-stu-id="b4095-117">Durable Functions is an extension to the Azure Functions runtime that enables the definition of stateful workflows in code.</span></span> <span data-ttu-id="b4095-118">Per scomporre i flussi di lavoro nelle attività, l'estensione funzioni permanenti può gestire lo stato, si creano checkpoint lo stato di avanzamento e gestire la distribuzione delle chiamate di funzione tra server.</span><span class="sxs-lookup"><span data-stu-id="b4095-118">By breaking down workflows into activities, the Durable Functions extension can manage state, create progress checkpoints, and handle the distribution of function calls across servers.</span></span> <span data-ttu-id="b4095-119">In background, fa uso di un account di archiviazione di Azure per rendere persistente la cronologia delle esecuzioni, pianificare le funzioni di attività e recuperare le risposte.</span><span class="sxs-lookup"><span data-stu-id="b4095-119">In the background, it makes use of an Azure Storage account to persist execution history, schedule activity functions and retrieve responses.</span></span> <span data-ttu-id="b4095-120">Il codice senza server mai deve interagire con le informazioni persistenti nell'account di archiviazione e in genere non è un elemento con cui gli sviluppatori devono interagire.</span><span class="sxs-lookup"><span data-stu-id="b4095-120">Your serverless code should never interact with persisted information in that storage account, and is typically not something with which developers need to interact.</span></span>

## <a name="triggering-a-stateful-workflow"></a><span data-ttu-id="b4095-121">Attivare un flusso di lavoro con stato</span><span class="sxs-lookup"><span data-stu-id="b4095-121">Triggering a stateful workflow</span></span>

<span data-ttu-id="b4095-122">I flussi di lavoro con stati in funzioni permanenti può essere suddivisa in due componenti intrinseci. trigger di orchestrazione e l'attività.</span><span class="sxs-lookup"><span data-stu-id="b4095-122">Stateful workflows in Durable Functions can be broken down into two intrinsic components; orchestration and activity triggers.</span></span> <span data-ttu-id="b4095-123">Trigger e associazioni sono componenti di base utilizzati dalle funzioni di Azure per consentire le funzioni senza server ricevere una notifica quando viene avviato, ricevere input e restituiscono risultati.</span><span class="sxs-lookup"><span data-stu-id="b4095-123">Triggers and bindings are core components used by Azure Functions to enable your serverless functions to be notified when to start, receive input, and return results.</span></span>

### <a name="working-with-the-orchestration-client"></a><span data-ttu-id="b4095-124">Utilizzo del client di orchestrazione</span><span class="sxs-lookup"><span data-stu-id="b4095-124">Working with the Orchestration client</span></span>

<span data-ttu-id="b4095-125">Le orchestrazioni sono univoche rispetto agli altri stili di operazioni avviate in funzioni di Azure.</span><span class="sxs-lookup"><span data-stu-id="b4095-125">Orchestrations are unique when compared to other styles of triggered operations in Azure Functions.</span></span> <span data-ttu-id="b4095-126">Funzioni permanenti consente l'esecuzione di funzioni che potrebbero volerci ore o giorni per il completamento.</span><span class="sxs-lookup"><span data-stu-id="b4095-126">Durable Functions enables the execution of functions that may take hours or even days to complete.</span></span> <span data-ttu-id="b4095-127">Tale tipo di comportamento viene fornito con la necessità di poter controllare lo stato di un'orchestrazione in esecuzione, termina preventivamente o inviare notifiche di eventi esterni.</span><span class="sxs-lookup"><span data-stu-id="b4095-127">That type of behavior comes with the need to able to check the status of a running orchestration, preemptively terminate, or send notifications of external events.</span></span>

<span data-ttu-id="b4095-128">Per questi casi, l'estensione funzioni permanenti fornisce il `DurableOrchestrationClient` orchestrati di classe che consente di interagire con le funzioni.</span><span class="sxs-lookup"><span data-stu-id="b4095-128">For such cases, the Durable Functions extension provides the `DurableOrchestrationClient` class that allows you to interact with orchestrated functions.</span></span> <span data-ttu-id="b4095-129">Puoi accedere al client di orchestrazione utilizzando la `OrchestrationClientAttribute` associazione.</span><span class="sxs-lookup"><span data-stu-id="b4095-129">You get access to the orchestration client by using the `OrchestrationClientAttribute` binding.</span></span> <span data-ttu-id="b4095-130">In generale, è necessario includere questo attributo con un altro tipo di trigger, ad esempio un `HttpTrigger` o `ServiceBusTrigger`.</span><span class="sxs-lookup"><span data-stu-id="b4095-130">Generally, you would include this attribute with another trigger type, such as an `HttpTrigger` or `ServiceBusTrigger`.</span></span> <span data-ttu-id="b4095-131">Dopo che è stata attivata la funzione di origine, il client di orchestrazione è utilizzabile per avviare una funzione di orchestrazione.</span><span class="sxs-lookup"><span data-stu-id="b4095-131">Once the source function has been triggered, the orchestration client can be used to start an orchestrator function.</span></span>

```csharp
[FunctionName("KickOff")]
public static async Task<HttpResponseMessage> Run(
    [HttpTrigger(AuthorizationLevel.Function, "POST")]HttpRequestMessage req,
    [OrchestrationClient ] DurableOrchestrationClient<orchestrationClient>)
{
    OrderRequestData data = await req.Content.ReadAsAsync<OrderRequestData>();

    string instanceId = await orchestrationClient.StartNewAsync("PlaceOrder", data);

    return orchestrationClient.CreateCheckStatusResponse(req, instanceId);
}
```

### <a name="the-orchestrator-function"></a><span data-ttu-id="b4095-132">La funzione di orchestrazione</span><span class="sxs-lookup"><span data-stu-id="b4095-132">The orchestrator function</span></span>

<span data-ttu-id="b4095-133">Annotare una funzione con OrchestrationTriggerAttribute marchi di funzioni di Azure di tale funzione come una funzione di orchestrazione.</span><span class="sxs-lookup"><span data-stu-id="b4095-133">Annotating a function with the OrchestrationTriggerAttribute in Azure Functions marks that function as an orchestrator function.</span></span> <span data-ttu-id="b4095-134">È responsabile della gestione di varie attività che costituiscono il flusso di lavoro con stato.</span><span class="sxs-lookup"><span data-stu-id="b4095-134">It's responsible for managing the various activities that make up your stateful workflow.</span></span>

<span data-ttu-id="b4095-135">Le funzioni di orchestrazione sono in grado di rendere utilizzare diversa da quella di OrchestrationTriggerAttribute.</span><span class="sxs-lookup"><span data-stu-id="b4095-135">Orchestrator functions are unable to make use of bindings other than the OrchestrationTriggerAttribute.</span></span> <span data-ttu-id="b4095-136">Questo attributo utilizzabile solo con un tipo di parametro di DurableOrchestrationContext.</span><span class="sxs-lookup"><span data-stu-id="b4095-136">This attribute can only be used with a parameter type of DurableOrchestrationContext.</span></span> <span data-ttu-id="b4095-137">Nessun altro input può essere utilizzato perché non è supportata la deserializzazione dell'input nella firma della funzione.</span><span class="sxs-lookup"><span data-stu-id="b4095-137">No other inputs can be used since deserialization of inputs in the function signature isn't supported.</span></span> <span data-ttu-id="b4095-138">Per ottenere input fornito dal client di orchestrazione, il GetInput\<T\> necessario usare il metodo.</span><span class="sxs-lookup"><span data-stu-id="b4095-138">To get inputs provided by the orchestration client, the GetInput\<T\> method must be used.</span></span>

<span data-ttu-id="b4095-139">Inoltre, i tipi restituiti delle funzioni di orchestrazione devono essere void, Task o valore JSON serializzabile.</span><span class="sxs-lookup"><span data-stu-id="b4095-139">Also, the return types of orchestration functions must be either void, Task, or a JSON serializable value.</span></span>

> <span data-ttu-id="b4095-140">*La gestione del codice di errore è stato tralasciato, per motivi di brevità*</span><span class="sxs-lookup"><span data-stu-id="b4095-140">*Error handling code has been left out for brevity*</span></span>

```csharp
[FunctionName("PlaceOrder")]
public static async Task<string> PlaceOrder([OrchestrationTrigger] DurableOrchestrationContext context)
{
    OrderRequestData orderData = context.GetInput<OrderRequestData>();

    await context.CallActivityAsync<bool>("CheckAndReserveInventory", orderData);
    await context.CallActivityAsync<string>("ProcessPayment", orderData);

    string trackingNumber = await context.CallActivityAsync<string>("ScheduleShipping", orderData);
    await context.CallActivityAsync<string>("EmailCustomer", trackingNumber);

    return trackingNumber;
}
```

<span data-ttu-id="b4095-141">Più istanze di un'orchestrazione possono essere avviato e in esecuzione nello stesso momento.</span><span class="sxs-lookup"><span data-stu-id="b4095-141">Multiple instances of an orchestration can be started and running at the same time.</span></span> <span data-ttu-id="b4095-142">Chiama il `StartNewAsync` metodo sul `DurableOrchestrationClient` avvia una nuova istanza dell'orchestrazione.</span><span class="sxs-lookup"><span data-stu-id="b4095-142">Calling the `StartNewAsync` method on the `DurableOrchestrationClient` launches a new instance of the orchestration.</span></span> <span data-ttu-id="b4095-143">Il metodo restituisce un `Task<string>` che viene completato quando l'orchestrazione è stata avviata.</span><span class="sxs-lookup"><span data-stu-id="b4095-143">The method returns a `Task<string>` that completes when the orchestration has started.</span></span> <span data-ttu-id="b4095-144">Un'eccezione di tipo `TimeoutException` Ottiene generata se l'orchestrazione non è ancora stato avviato entro 30 secondi.</span><span class="sxs-lookup"><span data-stu-id="b4095-144">An exception of type `TimeoutException` gets thrown if the orchestration hasn't started within 30 seconds.</span></span>

<span data-ttu-id="b4095-145">Completate `Task<string>` da `StartNewAsync` deve contenere l'ID univoco dell'istanza di orchestrazione.</span><span class="sxs-lookup"><span data-stu-id="b4095-145">The completed `Task<string>` from `StartNewAsync` should contain the unique ID of the orchestration instance.</span></span> <span data-ttu-id="b4095-146">Questo ID istanza è utilizzabile per richiamare le operazioni nell'orchestrazione specifica.</span><span class="sxs-lookup"><span data-stu-id="b4095-146">This instance ID can be used to invoke operations on that specific orchestration.</span></span> <span data-ttu-id="b4095-147">L'orchestrazione è possibile eseguire una query per lo stato o inviare le notifiche degli eventi.</span><span class="sxs-lookup"><span data-stu-id="b4095-147">The orchestration can be queried for the status or sent event notifications.</span></span>

### <a name="the-activity-functions"></a><span data-ttu-id="b4095-148">Le funzioni di attività</span><span class="sxs-lookup"><span data-stu-id="b4095-148">The activity functions</span></span>

<span data-ttu-id="b4095-149">Funzioni di attività sono le operazioni discrete che vengono composte insieme all'interno di una funzione di orchestrazione per creare il flusso di lavoro.</span><span class="sxs-lookup"><span data-stu-id="b4095-149">Activity functions are the discrete operations that get composed together within an orchestration function to create the workflow.</span></span> <span data-ttu-id="b4095-150">Ecco dove la maggior parte del lavoro effettivo potrebbe essere applicata.</span><span class="sxs-lookup"><span data-stu-id="b4095-150">Here is where most of actual work would take place.</span></span> <span data-ttu-id="b4095-151">Che rappresentano la logica di business, lungo i processi in esecuzione e un puzzle di una soluzione più ampia.</span><span class="sxs-lookup"><span data-stu-id="b4095-151">They represent the business logic, long running processes, and the puzzle pieces to a larger solution.</span></span>

<span data-ttu-id="b4095-152">Il `ActivityTriggerAttribute` viene utilizzato per annotare un parametro della funzione di tipo `DurableActivityContext`.</span><span class="sxs-lookup"><span data-stu-id="b4095-152">The `ActivityTriggerAttribute` is used to annotate a function parameter of type `DurableActivityContext`.</span></span> <span data-ttu-id="b4095-153">Mediante l'annotazione informa il runtime che la funzione deve essere utilizzato come funzione di attività.</span><span class="sxs-lookup"><span data-stu-id="b4095-153">Using the annotation informs the runtime that the function is intended to be used as an activity function.</span></span> <span data-ttu-id="b4095-154">I valori di input per le funzioni di attività vengono recuperati usando la `GetInput<T>` metodo del `DurableActivityContext` parametro.</span><span class="sxs-lookup"><span data-stu-id="b4095-154">Input values to activity functions are retrieved using the `GetInput<T>` method of the `DurableActivityContext` parameter.</span></span>

<span data-ttu-id="b4095-155">Analogamente alle funzioni di orchestrazione, i tipi restituiti di funzioni di attività devono essere void, Task o valore JSON serializzabile.</span><span class="sxs-lookup"><span data-stu-id="b4095-155">Similar to orchestration functions, the return types of activity functions must be either void, Task, or a JSON serializable value.</span></span>

<span data-ttu-id="b4095-156">Eccezioni non gestite generate all'interno di funzioni di attività otterrà inviate alla funzione dell'agente di orchestrazione chiamante e presentate come una `TaskFailedException`.</span><span class="sxs-lookup"><span data-stu-id="b4095-156">Any unhandled exceptions that get thrown within activity functions will get sent up to the calling orchestrator function and presented as a `TaskFailedException`.</span></span> <span data-ttu-id="b4095-157">A questo punto, l'errore può essere rilevato e registrato nell'agente di orchestrazione e l'attività è possibile ritentare.</span><span class="sxs-lookup"><span data-stu-id="b4095-157">At this point, the error can be caught and logged in the orchestrator, and the activity can be retried.</span></span>

```csharp
[FunctionName("CheckAndReserveInventory")]
public static bool CheckAndReserveInventory([ActivityTrigger] DurableActivityContext context)
{
    OrderRequestData orderData = context.GetInput<OrderRequestData>();

    // Connect to inventory system and try to reserve items
    return true;
}
```

## <a name="recommended-resources"></a><span data-ttu-id="b4095-158">Risorse consigliate</span><span class="sxs-lookup"><span data-stu-id="b4095-158">Recommended resources</span></span>

* [<span data-ttu-id="b4095-159">Funzioni permanenti</span><span class="sxs-lookup"><span data-stu-id="b4095-159">Durable Functions</span></span>](https://docs.microsoft.com/azure/azure-functions/durable-functions-overview)
* [<span data-ttu-id="b4095-160">Associazioni per funzioni permanenti</span><span class="sxs-lookup"><span data-stu-id="b4095-160">Bindings for Durable Functions</span></span>](https://docs.microsoft.com/azure/azure-functions/durable-functions-bindings)
* [<span data-ttu-id="b4095-161">Gestire le istanze in funzioni permanenti</span><span class="sxs-lookup"><span data-stu-id="b4095-161">Manage instances in Durable Functions</span></span>](https://docs.microsoft.com/azure/azure-functions/durable-functions-instance-management)

>[!div class="step-by-step"]
><span data-ttu-id="b4095-162">[Precedente](event-grid.md)
>[Successivo](orchestration-patterns.md)</span><span class="sxs-lookup"><span data-stu-id="b4095-162">[Previous](event-grid.md)
[Next](orchestration-patterns.md)</span></span>
