---
title: Come verificare che le stringhe siano in formato di posta elettronica valido
ms.date: 12/10/2018
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- regular expressions, examples
- user input, examples
- Regex.IsMatch method
- regular expressions [.NET Framework], examples
- examples [Visual Basic], strings
- IsValidEmail
- validation, email strings
- input, checking
- strings [.NET Framework], examples [Visual Basic]
- email [.NET Framework], validating
- IsMatch method
ms.assetid: 7536af08-4e86-4953-98a1-a8298623df92
ms.openlocfilehash: 360ed985575358dd9603a55fc2d5d6c297621ec8
ms.sourcegitcommit: 33deec3e814238fb18a49b2a7e89278e27888291
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 06/02/2020
ms.locfileid: "84290422"
---
# <a name="how-to-verify-that-strings-are-in-valid-email-format"></a><span data-ttu-id="288cf-102">Come verificare che le stringhe siano in formato di posta elettronica valido</span><span class="sxs-lookup"><span data-stu-id="288cf-102">How to verify that strings are in valid email format</span></span>

<span data-ttu-id="288cf-103">L'esempio seguente usa un'espressione regolare per verificare la validità del formato di posta elettronica di una stringa.</span><span class="sxs-lookup"><span data-stu-id="288cf-103">The following example uses a regular expression to verify that a string is in valid email format.</span></span>

## <a name="example"></a><span data-ttu-id="288cf-104">Esempio</span><span class="sxs-lookup"><span data-stu-id="288cf-104">Example</span></span>

<span data-ttu-id="288cf-105">Nell'esempio viene definito un metodo `IsValidEmail` che restituisce `true` se nella stringa è presente un indirizzo di posta elettronica valido e `false` in caso contrario, ma non esegue alcuna altra azione.</span><span class="sxs-lookup"><span data-stu-id="288cf-105">The example defines an `IsValidEmail` method, which returns `true` if the string contains a valid email address and `false` if it does not, but takes no other action.</span></span>

<span data-ttu-id="288cf-106">Per verificare che l'indirizzo di posta elettronica sia valido, il metodo `IsValidEmail` chiama il metodo <xref:System.Text.RegularExpressions.Regex.Replace%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.MatchEvaluator%29?displayProperty=nameWithType> con il criterio di espressione regolare `(@)(.+)$` per separare il nome di dominio dall'indirizzo di posta elettronica.</span><span class="sxs-lookup"><span data-stu-id="288cf-106">To verify that the email address is valid, the `IsValidEmail` method calls the <xref:System.Text.RegularExpressions.Regex.Replace%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.MatchEvaluator%29?displayProperty=nameWithType> method with the `(@)(.+)$` regular expression pattern to separate the domain name from the email address.</span></span> <span data-ttu-id="288cf-107">Il terzo parametro è un delegato <xref:System.Text.RegularExpressions.MatchEvaluator> che rappresenta il metodo che elabora e sostituisce il testo corrispondente.</span><span class="sxs-lookup"><span data-stu-id="288cf-107">The third parameter is a <xref:System.Text.RegularExpressions.MatchEvaluator> delegate that represents the method that processes and replaces the matched text.</span></span> <span data-ttu-id="288cf-108">Il criterio di espressione regolare viene interpretato nel modo seguente.</span><span class="sxs-lookup"><span data-stu-id="288cf-108">The regular expression pattern is interpreted as follows.</span></span>

|<span data-ttu-id="288cf-109">Modello</span><span class="sxs-lookup"><span data-stu-id="288cf-109">Pattern</span></span>|<span data-ttu-id="288cf-110">Descrizione</span><span class="sxs-lookup"><span data-stu-id="288cf-110">Description</span></span>|
|-------------|-----------------|
|`(@)`|<span data-ttu-id="288cf-111">Trova la corrispondenza con il carattere @.</span><span class="sxs-lookup"><span data-stu-id="288cf-111">Match the @ character.</span></span> <span data-ttu-id="288cf-112">Equivale al primo gruppo di acquisizione.</span><span class="sxs-lookup"><span data-stu-id="288cf-112">This is the first capturing group.</span></span>|
|`(.+)`|<span data-ttu-id="288cf-113">Trova la corrispondenza con una o più occorrenze di qualsiasi carattere.</span><span class="sxs-lookup"><span data-stu-id="288cf-113">Match one or more occurrences of any character.</span></span> <span data-ttu-id="288cf-114">Equivale al secondo gruppo di acquisizione.</span><span class="sxs-lookup"><span data-stu-id="288cf-114">This is the second capturing group.</span></span>|
|`$`|<span data-ttu-id="288cf-115">Terminare la corrispondenza alla fine della stringa.</span><span class="sxs-lookup"><span data-stu-id="288cf-115">End the match at the end of the string.</span></span>|

<span data-ttu-id="288cf-116">Il nome di dominio con il carattere @ viene passato al metodo `DomainMapper` che usa la classe <xref:System.Globalization.IdnMapping> per convertire i caratteri Unicode che si trovano all'esterno dell'intervallo di caratteri US-ASCII a Punycode.</span><span class="sxs-lookup"><span data-stu-id="288cf-116">The domain name along with the @ character is passed to the `DomainMapper` method, which uses the <xref:System.Globalization.IdnMapping> class to translate Unicode characters that are outside the US-ASCII character range to Punycode.</span></span> <span data-ttu-id="288cf-117">Il metodo imposta inoltre il flag `invalid` su `True` se il metodo <xref:System.Globalization.IdnMapping.GetAscii%2A?displayProperty=nameWithType> rileva caratteri non validi nel nome di dominio.</span><span class="sxs-lookup"><span data-stu-id="288cf-117">The method also sets the `invalid` flag to `True` if the <xref:System.Globalization.IdnMapping.GetAscii%2A?displayProperty=nameWithType> method detects any invalid characters in the domain name.</span></span> <span data-ttu-id="288cf-118">Il metodo restituisce il nome di dominio Punycode preceduto dal simbolo @ al metodo `IsValidEmail` .</span><span class="sxs-lookup"><span data-stu-id="288cf-118">The method returns the Punycode domain name preceded by the @ symbol to the `IsValidEmail` method.</span></span>

<span data-ttu-id="288cf-119">Il metodo `IsValidEmail` chiama quindi il metodo <xref:System.Text.RegularExpressions.Regex.IsMatch%28System.String%2CSystem.String%29?displayProperty=nameWithType> per verificare che l'indirizzo sia conforme a un criterio di espressione regolare.</span><span class="sxs-lookup"><span data-stu-id="288cf-119">The `IsValidEmail` method then calls the <xref:System.Text.RegularExpressions.Regex.IsMatch%28System.String%2CSystem.String%29?displayProperty=nameWithType> method to verify that the address conforms to a regular expression pattern.</span></span>

<span data-ttu-id="288cf-120">Si noti che il metodo `IsValidEmail` non esegue l'autenticazione per convalidare l'indirizzo di posta elettronica</span><span class="sxs-lookup"><span data-stu-id="288cf-120">Note that the `IsValidEmail` method does not perform authentication to validate the email address.</span></span> <span data-ttu-id="288cf-121">e si limita a stabilire se il formato è valido per un indirizzo di posta elettronica.</span><span class="sxs-lookup"><span data-stu-id="288cf-121">It merely determines whether its format is valid for an email address.</span></span> <span data-ttu-id="288cf-122">Inoltre, il metodo `IsValidEmail` non verifica che il nome di dominio di primo livello sia un nome di dominio valido elencato nella pagina del [Database delle aree radice sul sito IANA](https://www.iana.org/domains/root/db), cosa che richiederebbe un'operazione di ricerca.</span><span class="sxs-lookup"><span data-stu-id="288cf-122">In addition, the `IsValidEmail` method does not verify that the top-level domain name is a valid domain name listed at the [IANA Root Zone Database](https://www.iana.org/domains/root/db), which would require a look-up operation.</span></span> <span data-ttu-id="288cf-123">L'espressione regolare verifica invece solo che il nome di dominio di primo livello sia costituito da un numero di caratteri ASCII compreso tra due e 24. Il nome deve iniziare e finire con caratteri alfanumerici e i caratteri rimanenti possono essere alfanumerici o un trattino (-).</span><span class="sxs-lookup"><span data-stu-id="288cf-123">Instead, the regular expression merely verifies that the top-level domain name consists of between two and twenty-four ASCII characters, with alphanumeric first and last characters and the remaining characters being either alphanumeric or a hyphen (-).</span></span>

[!code-csharp[RegularExpressions.Examples.Email#7](../../../samples/snippets/csharp/VS_Snippets_CLR/RegularExpressions.Examples.Email/cs/example4.cs#7)]
[!code-vb[RegularExpressions.Examples.Email#7](../../../samples/snippets/visualbasic/VS_Snippets_CLR/RegularExpressions.Examples.Email/vb/example4.vb#7)]

<span data-ttu-id="288cf-124">In questo esempio, il criterio di espressione regolare ``^(?(")(".+?(?<!\\)"@)|(([0-9a-z]((\.(?!\.))|[-!#\$%&'\*\+/=\?\^`\{\}\|~\w])*)(?<=[0-9a-z])@))(?(\[)(\[(\d{1,3}\.){3}\d{1,3}\])|(([0-9a-z][-0-9a-z]*[0-9a-z]*\.)+[a-z0-9][\-a-z0-9]{0,22}[a-z0-9]))$`` viene interpretato come illustrato nella legenda seguente.</span><span class="sxs-lookup"><span data-stu-id="288cf-124">In this example, the regular expression pattern ``^(?(")(".+?(?<!\\)"@)|(([0-9a-z]((\.(?!\.))|[-!#\$%&'\*\+/=\?\^`\{\}\|~\w])*)(?<=[0-9a-z])@))(?(\[)(\[(\d{1,3}\.){3}\d{1,3}\])|(([0-9a-z][-0-9a-z]*[0-9a-z]*\.)+[a-z0-9][\-a-z0-9]{0,22}[a-z0-9]))$`` is interpreted as shown in the following legend.</span></span> <span data-ttu-id="288cf-125">L'espressione regolare viene compilata usando il <xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase?displayProperty=nameWithType> flag.</span><span class="sxs-lookup"><span data-stu-id="288cf-125">The regular expression is compiled using the <xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase?displayProperty=nameWithType> flag.</span></span>

<span data-ttu-id="288cf-126">Pattern `^` : inizia la corrispondenza all'inizio della stringa.</span><span class="sxs-lookup"><span data-stu-id="288cf-126">Pattern `^`: Begin the match at the start of the string.</span></span>

<span data-ttu-id="288cf-127">Pattern `(?(")` : determinare se il primo carattere è una virgoletta.</span><span class="sxs-lookup"><span data-stu-id="288cf-127">Pattern `(?(")`: Determine whether the first character is a quotation mark.</span></span> <span data-ttu-id="288cf-128">`(?(")` è l'inizio di un costrutto di alternanza.</span><span class="sxs-lookup"><span data-stu-id="288cf-128">`(?(")` is the beginning of an alternation construct.</span></span>

<span data-ttu-id="288cf-129">Modello `(?(")(".+?(?<!\\)"@)` : se il primo carattere è una virgoletta, trovare la corrispondenza con una virgoletta iniziale seguita da almeno un'occorrenza di qualsiasi carattere, seguita da una virgoletta finale.</span><span class="sxs-lookup"><span data-stu-id="288cf-129">Pattern `(?(")(".+?(?<!\\)"@)`: If the first character is a quotation mark, match a beginning quotation mark followed by at least one occurrence of any character, followed by an ending quotation mark.</span></span> <span data-ttu-id="288cf-130">La virgoletta finale non deve essere preceduta da un carattere di barra rovesciata (\\).</span><span class="sxs-lookup"><span data-stu-id="288cf-130">The ending quotation mark must not be preceded by a backslash character (\\).</span></span> <span data-ttu-id="288cf-131">`(?<!` è l'inizio dell'asserzione lookbehind negativa di larghezza zero.</span><span class="sxs-lookup"><span data-stu-id="288cf-131">`(?<!` is the beginning of a zero-width negative lookbehind assertion.</span></span> <span data-ttu-id="288cf-132">La stringa dovrebbe terminare con il simbolo @.</span><span class="sxs-lookup"><span data-stu-id="288cf-132">The string should conclude with an at sign (@).</span></span>

<span data-ttu-id="288cf-133">Modello `|(([0-9a-z]` : se il primo carattere non è una virgoletta, trovare la corrispondenza con qualsiasi carattere alfabetico da a a z o da a a z (il confronto non fa distinzione tra maiuscole e minuscole) o qualsiasi carattere numerico compreso tra 0 e 9.</span><span class="sxs-lookup"><span data-stu-id="288cf-133">Pattern `|(([0-9a-z]`: If the first character is not a quotation mark, match any alphabetic character from a to z or A to Z (the comparison is case insensitive), or any numeric character from 0 to 9.</span></span>

<span data-ttu-id="288cf-134">Modello `(\.(?!\.))` : se il carattere successivo è un punto, trovarne la corrispondenza.</span><span class="sxs-lookup"><span data-stu-id="288cf-134">Pattern `(\.(?!\.))`: If the next character is a period, match it.</span></span> <span data-ttu-id="288cf-135">Se non è un punto, eseguire il look ahead del carattere successivo e continuare la ricerca della corrispondenza.</span><span class="sxs-lookup"><span data-stu-id="288cf-135">If it is not a period, look ahead to the next character and continue the match.</span></span> <span data-ttu-id="288cf-136">`(?!\.)` è un'asserzione lookahead negativa di larghezza zero che impedisce la comparsa di due punti consecutivi nella parte locale di un indirizzo di posta elettronica.</span><span class="sxs-lookup"><span data-stu-id="288cf-136">`(?!\.)` is a zero-width negative lookahead assertion that prevents two consecutive periods from appearing in the local part of an email address.</span></span>

<span data-ttu-id="288cf-137">Modello ``|[-!#\$%&'\*\+/=\?\^`\{\}\|~\w]`` : se il carattere successivo non è un punto, trovare la corrispondenza con qualsiasi carattere alfanumerico o con uno dei caratteri seguenti:-! # $% &' \* +/=? ^ \` {} | ~</span><span class="sxs-lookup"><span data-stu-id="288cf-137">Pattern ``|[-!#\$%&'\*\+/=\?\^`\{\}\|~\w]``: If the next character is not a period, match any word character or one of the following characters: -!#$%&'\*+/=?^\`{}|~</span></span>

<span data-ttu-id="288cf-138">Pattern ``((\.(?!\.))|[-!#\$%&'\*\+/=\?\^`\{\}\|~\w])*`` : corrisponde al modello di alternanza (un punto seguito da un numero di caratteri diverso da un periodo o da uno di essi) zero o più volte.</span><span class="sxs-lookup"><span data-stu-id="288cf-138">Pattern ``((\.(?!\.))|[-!#\$%&'\*\+/=\?\^`\{\}\|~\w])*``: Match the alternation pattern (a period followed by a non-period, or one of a number of characters) zero or more times.</span></span>

<span data-ttu-id="288cf-139">Pattern `@` : trovare la corrispondenza con il carattere @.</span><span class="sxs-lookup"><span data-stu-id="288cf-139">Pattern `@`: Match the @ character.</span></span>

<span data-ttu-id="288cf-140">Pattern `(?<=[0-9a-z])` : continua la ricerca della corrispondenza se il carattere che precede il carattere @ è compreso tra a e z, da a a z o da 0 a 9.</span><span class="sxs-lookup"><span data-stu-id="288cf-140">Pattern `(?<=[0-9a-z])`: Continue the match if the character that precedes the @ character is A through Z, a through z, or 0 through 9.</span></span> <span data-ttu-id="288cf-141">Questo modello definisce un'asserzione lookbehind positiva di larghezza zero.</span><span class="sxs-lookup"><span data-stu-id="288cf-141">This pattern defines a zero-width positive lookbehind assertion.</span></span>

<span data-ttu-id="288cf-142">Pattern `(?(\[)` : controllare se il carattere che segue @ è una parentesi di apertura.</span><span class="sxs-lookup"><span data-stu-id="288cf-142">Pattern `(?(\[)`: Check whether the character that follows @ is an opening bracket.</span></span>

<span data-ttu-id="288cf-143">Modello `(\[(\d{1,3}\.){3}\d{1,3}\])` : se è una parentesi di apertura, trovare la corrispondenza con la parentesi di apertura seguita da un indirizzo IP (quattro set da una a tre cifre, con ogni set separato da un punto) e una parentesi di chiusura.</span><span class="sxs-lookup"><span data-stu-id="288cf-143">Pattern `(\[(\d{1,3}\.){3}\d{1,3}\])`: If it is an opening bracket, match the opening bracket followed by an IP address (four sets of one to three digits, with each set separated by a period) and a closing bracket.</span></span>

<span data-ttu-id="288cf-144">Modello `|(([0-9a-z][-0-9a-z]*[0-9a-z]*\.)+` : se il carattere che segue @ non è una parentesi di apertura, trova la corrispondenza di un carattere alfanumerico con un valore a-z, a-z o 0-9, seguito da zero o più occorrenze di un trattino, seguite da zero o da un carattere alfanumerico con un valore a-z, a-z o 0-9, seguito da un punto.</span><span class="sxs-lookup"><span data-stu-id="288cf-144">Pattern `|(([0-9a-z][-0-9a-z]*[0-9a-z]*\.)+`: If the character that follows @ is not an opening bracket, match one alphanumeric character with a value of A-Z, a-z, or 0-9, followed by zero or more occurrences of a hyphen, followed by zero or one alphanumeric character with a value of A-Z, a-z, or 0-9, followed by a period.</span></span> <span data-ttu-id="288cf-145">Questo modello può essere ripetuto una o più volte e deve essere seguito dal nome di dominio di primo livello.</span><span class="sxs-lookup"><span data-stu-id="288cf-145">This pattern can be repeated one or more times, and must be followed by the top-level domain name.</span></span>

<span data-ttu-id="288cf-146">Modello `[a-z0-9][\-a-z0-9]{0,22}[a-z0-9]))` : il nome di dominio di primo livello deve iniziare e terminare con un carattere alfanumerico (a-z, a-z e 0-9).</span><span class="sxs-lookup"><span data-stu-id="288cf-146">Pattern `[a-z0-9][\-a-z0-9]{0,22}[a-z0-9]))`: The top-level domain name must begin and end with an alphanumeric character (a-z, A-Z, and 0-9).</span></span> <span data-ttu-id="288cf-147">Può anche includere da zero a 22 caratteri ASCII, che possono essere caratteri alfanumerici o trattini.</span><span class="sxs-lookup"><span data-stu-id="288cf-147">It can also include from zero to 22 ASCII characters that are either alphanumeric or hyphens.</span></span>

<span data-ttu-id="288cf-148">Pattern `$` : termina la corrispondenza alla fine della stringa.</span><span class="sxs-lookup"><span data-stu-id="288cf-148">Pattern `$`: End the match at the end of the string.</span></span>

## <a name="compile-the-code"></a><span data-ttu-id="288cf-149">Compilare il codice</span><span class="sxs-lookup"><span data-stu-id="288cf-149">Compile the code</span></span>

<span data-ttu-id="288cf-150">I metodi `IsValidEmail` e `DomainMapper` possono essere inclusi in una libreria di metodi di utilità di espressioni regolari oppure possono essere inclusi come metodi di istanza o statici privati nella classe dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="288cf-150">The `IsValidEmail` and `DomainMapper` methods can be included in a library of regular expression utility methods, or they can be included as private static or instance methods in the application class.</span></span>

<span data-ttu-id="288cf-151">È anche possibile usare il metodo <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A?displayProperty=nameWithType> per includere questa espressione regolare in una libreria di espressioni regolari.</span><span class="sxs-lookup"><span data-stu-id="288cf-151">You can also use the <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A?displayProperty=nameWithType> method to include this regular expression in a regular expression library.</span></span>

<span data-ttu-id="288cf-152">Se vengono usati in una libreria di espressioni regolari, è possibile chiamarli usando il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="288cf-152">If they are used in a regular expression library, you can call them by using code such as the following:</span></span>

[!code-csharp[RegularExpressions.Examples.Email#8](../../../samples/snippets/csharp/VS_Snippets_CLR/RegularExpressions.Examples.Email/cs/example4.cs#8)]
[!code-vb[RegularExpressions.Examples.Email#8](../../../samples/snippets/visualbasic/VS_Snippets_CLR/RegularExpressions.Examples.Email/vb/example4.vb#8)]

## <a name="see-also"></a><span data-ttu-id="288cf-153">Vedere anche</span><span class="sxs-lookup"><span data-stu-id="288cf-153">See also</span></span>

- [<span data-ttu-id="288cf-154">Espressioni regolari di .NET Framework</span><span class="sxs-lookup"><span data-stu-id="288cf-154">.NET Framework Regular Expressions</span></span>](regular-expressions.md)
