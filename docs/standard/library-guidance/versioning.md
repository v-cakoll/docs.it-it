---
title: Controllo delle versioni e librerie .NET
description: Procedure consigliate per il controllo delle versioni delle librerie .NET.
ms.date: 12/10/2018
ms.openlocfilehash: a274410714791e2790da0e3deb2a595390ee9389
ms.sourcegitcommit: de17a7a0a37042f0d4406f5ae5393531caeb25ba
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 01/24/2020
ms.locfileid: "76745024"
---
# <a name="versioning"></a><span data-ttu-id="b7af3-103">Versionamento</span><span class="sxs-lookup"><span data-stu-id="b7af3-103">Versioning</span></span>

<span data-ttu-id="b7af3-104">Una libreria software è raramente completa nella versione 1.0.</span><span class="sxs-lookup"><span data-stu-id="b7af3-104">A software library is rarely complete in version 1.0.</span></span> <span data-ttu-id="b7af3-105">Le librerie di buon livello si evolvono nel tempo, con l'aggiunta di funzionalità, la correzione di bug e il miglioramento delle prestazioni.</span><span class="sxs-lookup"><span data-stu-id="b7af3-105">Good libraries evolve over time, adding features, fixing bugs and improving performance.</span></span> <span data-ttu-id="b7af3-106">È importante poter rilasciare nuove versioni di una libreria .NET, offrendo valore aggiuntivo con ogni versione, ma senza interrompere le funzionalità per gli utenti esistenti.</span><span class="sxs-lookup"><span data-stu-id="b7af3-106">It is important that you can release new versions of a .NET library, providing additional value with each version, without breaking existing users.</span></span>

## <a name="breaking-changes"></a><span data-ttu-id="b7af3-107">Modifiche che causano un'interruzione</span><span class="sxs-lookup"><span data-stu-id="b7af3-107">Breaking changes</span></span>

<span data-ttu-id="b7af3-108">Per informazioni sulla gestione delle modifiche tra versioni che causano un'interruzione, vedere [Modifiche che causano un'interruzione](./breaking-changes.md).</span><span class="sxs-lookup"><span data-stu-id="b7af3-108">For information about handling breaking changes between versions, see [Breaking changes](./breaking-changes.md).</span></span>

## <a name="version-numbers"></a><span data-ttu-id="b7af3-109">Numeri di versione</span><span class="sxs-lookup"><span data-stu-id="b7af3-109">Version numbers</span></span>

<span data-ttu-id="b7af3-110">Una libreria .NET dispone di molti modi per specificare una versione.</span><span class="sxs-lookup"><span data-stu-id="b7af3-110">A .NET library has many ways to specify a version.</span></span> <span data-ttu-id="b7af3-111">Le versioni più importanti sono le seguenti:</span><span class="sxs-lookup"><span data-stu-id="b7af3-111">These versions are the most important:</span></span>

### <a name="nuget-package-version"></a><span data-ttu-id="b7af3-112">Versione del pacchetto NuGet</span><span class="sxs-lookup"><span data-stu-id="b7af3-112">NuGet package version</span></span>

<span data-ttu-id="b7af3-113">La [versione del pacchetto NuGet](/nuget/reference/package-versioning) viene visualizzata in NuGet.org, lo strumento di gestione pacchetti NuGet di Visual Studio, e viene aggiunta al codice sorgente quando il pacchetto viene usato.</span><span class="sxs-lookup"><span data-stu-id="b7af3-113">The [NuGet package version](/nuget/reference/package-versioning) is displayed on NuGet.org, the Visual Studio NuGet package manager, and is added to source code when the package is used.</span></span> <span data-ttu-id="b7af3-114">La versione del pacchetto NuGet è il numero di versione che gli utenti generalmente vedono e a cui fanno riferimento quando parlano della versione di una libreria che stanno usando.</span><span class="sxs-lookup"><span data-stu-id="b7af3-114">The NuGet package version is the version number users will commonly see, and they'll refer to it when they talk about the version of a library they're using.</span></span> <span data-ttu-id="b7af3-115">La versione del pacchetto NuGet viene usata da NuGet e non ha alcun effetto sul comportamento in fase di esecuzione.</span><span class="sxs-lookup"><span data-stu-id="b7af3-115">The NuGet package version is used by NuGet and has no effect on runtime behavior.</span></span>

```xml
<PackageVersion>1.0.0-alpha1</PackageVersion>
```

<span data-ttu-id="b7af3-116">L'identificatore del pacchetto NuGet combinato con la versione del pacchetto NuGet consente di identificare un pacchetto in NuGet.</span><span class="sxs-lookup"><span data-stu-id="b7af3-116">The NuGet package identifier combined with the NuGet package version is used to identify a package in NuGet.</span></span> <span data-ttu-id="b7af3-117">Ad esempio: `Newtonsoft.Json` + `11.0.2`.</span><span class="sxs-lookup"><span data-stu-id="b7af3-117">For example, `Newtonsoft.Json` + `11.0.2`.</span></span> <span data-ttu-id="b7af3-118">Un pacchetto con un suffisso è un versione non definitiva del pacchetto e ha un comportamento speciale che lo rende ideale per i test.</span><span class="sxs-lookup"><span data-stu-id="b7af3-118">A package with a suffix is a pre-release package and has special behavior that makes it ideal for testing.</span></span> <span data-ttu-id="b7af3-119">Per altre informazioni, vedere [Pacchetti in versione non definitiva](./nuget.md#pre-release-packages).</span><span class="sxs-lookup"><span data-stu-id="b7af3-119">For more information, see [Pre-release packages](./nuget.md#pre-release-packages).</span></span>

<span data-ttu-id="b7af3-120">Poiché la versione del pacchetto NuGet è la versione più visibile agli sviluppatori, è consigliabile aggiornarla tramite [Versionamento semantico (SemVer)](https://semver.org/).</span><span class="sxs-lookup"><span data-stu-id="b7af3-120">Because the NuGet package version is the most visible version to developers, it's a good idea to update it using [Semantic Versioning (SemVer)](https://semver.org/).</span></span> <span data-ttu-id="b7af3-121">SemVer indica la rilevanza delle modifiche tra versioni e aiuta agli sviluppatori a prendere decisioni basate su informazioni aggiornate nella scelta della versione da usare.</span><span class="sxs-lookup"><span data-stu-id="b7af3-121">SemVer indicates the significance of changes between release and helps developers make an informed decision when choosing what version to use.</span></span> <span data-ttu-id="b7af3-122">Il passaggio da `1.0` a `2.0`, ad esempio, indica la potenziale presenza di modifiche che causano un'interruzione.</span><span class="sxs-lookup"><span data-stu-id="b7af3-122">For example, going from `1.0` to `2.0` indicates that there are potentially breaking changes.</span></span>

<span data-ttu-id="b7af3-123">✔️ CONSIDERARE l'uso di [SemVer 2.0.0](https://semver.org/) per la versione del pacchetto NuGet.</span><span class="sxs-lookup"><span data-stu-id="b7af3-123">✔️ CONSIDER using [SemVer 2.0.0](https://semver.org/) to version your NuGet package.</span></span>

<span data-ttu-id="b7af3-124">✔️ usare la versione del pacchetto NuGet nella documentazione pubblica, perché si tratta del numero di versione che verrà visualizzato in genere dagli utenti.</span><span class="sxs-lookup"><span data-stu-id="b7af3-124">✔️ DO use the NuGet package version in public documentation as it's the version number that users will commonly see.</span></span>

<span data-ttu-id="b7af3-125">✔️ includere un suffisso di versione non definitiva quando si rilascia un pacchetto non stabile.</span><span class="sxs-lookup"><span data-stu-id="b7af3-125">✔️ DO include a pre-release suffix when releasing a non-stable package.</span></span>

> <span data-ttu-id="b7af3-126">Gli utenti devono scegliere esplicitamente di ottenere i pacchetti in versione non definitiva, quindi saranno consapevoli del fatto che il pacchetto non è completo.</span><span class="sxs-lookup"><span data-stu-id="b7af3-126">Users must opt in to getting pre-release packages, so they will understand that the package is not complete.</span></span>

### <a name="assembly-version"></a><span data-ttu-id="b7af3-127">Versione assembly</span><span class="sxs-lookup"><span data-stu-id="b7af3-127">Assembly version</span></span>

<span data-ttu-id="b7af3-128">La versione dell'assembly viene usata da CLR in fase di esecuzione per scegliere la versione di un assembly da caricare.</span><span class="sxs-lookup"><span data-stu-id="b7af3-128">The assembly version is what the CLR uses at runtime to select which version of an assembly to load.</span></span> <span data-ttu-id="b7af3-129">La selezione di un assembly tramite il controllo delle versioni si applica solo agli assembly con nome sicuro.</span><span class="sxs-lookup"><span data-stu-id="b7af3-129">Selecting an assembly using versioning only applies to assemblies with a strong name.</span></span>

```xml
<AssemblyVersion>1.0.0.0</AssemblyVersion>
```

<span data-ttu-id="b7af3-130">CLR .NET Framework in Windows richiede una corrispondenza esatta per il caricamento di un assembly con nome sicuro.</span><span class="sxs-lookup"><span data-stu-id="b7af3-130">The Windows .NET Framework CLR demands an exact match to load a strong named assembly.</span></span> <span data-ttu-id="b7af3-131">Ad esempio, la compilazione di `Libary1, Version=1.0.0.0` è stata eseguita con un riferimento a `Newtonsoft.Json, Version=11.0.0.0`.</span><span class="sxs-lookup"><span data-stu-id="b7af3-131">For example, `Libary1, Version=1.0.0.0` was compiled with a reference to `Newtonsoft.Json, Version=11.0.0.0`.</span></span> <span data-ttu-id="b7af3-132">.NET Framework caricherà solo la versione esatta `11.0.0.0`.</span><span class="sxs-lookup"><span data-stu-id="b7af3-132">The .NET Framework will only load that exact version `11.0.0.0`.</span></span> <span data-ttu-id="b7af3-133">Per caricare una versione diversa in fase di esecuzione, è necessario aggiungere un reindirizzamento di binding al file di configurazione dell'applicazione .NET.</span><span class="sxs-lookup"><span data-stu-id="b7af3-133">To load a different version at runtime, a binding redirect must be added to the .NET application's config file.</span></span>

<span data-ttu-id="b7af3-134">La combinazione di nome sicuro e versione dell'assembly permette il [caricamento della versione dell'assembly in modalità strict](../assembly/versioning.md).</span><span class="sxs-lookup"><span data-stu-id="b7af3-134">Strong naming combined with assembly version enables [strict assembly version loading](../assembly/versioning.md).</span></span> <span data-ttu-id="b7af3-135">Sebbene l'uso di un nome sicuro per una libreria offra diversi vantaggi, spesso comporta eccezioni in fase di esecuzione quando un assembly non viene trovato e sono necessari [reindirizzamenti di binding](../../framework/configure-apps/redirect-assembly-versions.md) in `app.config`/`web.config` per risolvere il problema.</span><span class="sxs-lookup"><span data-stu-id="b7af3-135">While strong naming a library has a number of benefits, it often results in runtime exceptions that an assembly can't be found and [requires binding redirects](../../framework/configure-apps/redirect-assembly-versions.md) in `app.config`/`web.config` to be fixed.</span></span> <span data-ttu-id="b7af3-136">Il caricamento di assembly .NET Core è meno rigido e CLR .NET Core carica automaticamente gli assembly in fase di esecuzione con una versione più recente.</span><span class="sxs-lookup"><span data-stu-id="b7af3-136">.NET Core assembly loading has been relaxed, and the .NET Core CLR will automatically load assemblies at runtime with a higher version.</span></span>

<span data-ttu-id="b7af3-137">✔️ CONSIDERARE solo l'inclusione di una versione principale nella AssemblyVersion.</span><span class="sxs-lookup"><span data-stu-id="b7af3-137">✔️ CONSIDER only including a major version in the AssemblyVersion.</span></span>

> <span data-ttu-id="b7af3-138">Ad esempio, sia per Library 1.0 che per Library 1.0.1 il valore di AssemblyVersion è `1.0.0.0`, mentre il valore di AssemblyVersion per Library 2.0 è `2.0.0.0`.</span><span class="sxs-lookup"><span data-stu-id="b7af3-138">e.g. Library 1.0 and Library 1.0.1 both have an AssemblyVersion of `1.0.0.0`, while Library 2.0 has AssemblyVersion of `2.0.0.0`.</span></span> <span data-ttu-id="b7af3-139">Quando la versione dell'assembly viene modificata con meno frequenza, si riducono i reindirizzamenti di binding.</span><span class="sxs-lookup"><span data-stu-id="b7af3-139">When the assembly version changes less often, it reduces binding redirects.</span></span>

<span data-ttu-id="b7af3-140">✔️ si consiglia di mantenere sincronizzati il numero di versione principale di AssemblyVersion e la versione del pacchetto NuGet.</span><span class="sxs-lookup"><span data-stu-id="b7af3-140">✔️ CONSIDER keeping the major version number of the AssemblyVersion and the NuGet package version in sync.</span></span>

> <span data-ttu-id="b7af3-141">Il valore di AssemblyVersion è incluso in alcuni messaggi informativi visualizzati per l'utente, ad esempio, il nome dell'assembly e i nomi di tipi qualificati dall'assembly nei messaggi di eccezione.</span><span class="sxs-lookup"><span data-stu-id="b7af3-141">The AssemblyVersion is included in some informational messages displayed to the user, e.g. the assembly name and assembly qualified type names in exception messages.</span></span> <span data-ttu-id="b7af3-142">Mantenendo una relazione tra le versioni è possibile fornire agli sviluppatori più informazioni sulla versione in uso.</span><span class="sxs-lookup"><span data-stu-id="b7af3-142">Maintaining a relationship between the versions provides more information to developers about which version they are using.</span></span>

<span data-ttu-id="b7af3-143">❌ non dispongono di un AssemblyVersion fisso.</span><span class="sxs-lookup"><span data-stu-id="b7af3-143">❌ DO NOT have a fixed AssemblyVersion.</span></span>

> <span data-ttu-id="b7af3-144">Sebbene evitando di modificare AssemblyVersion si eviti l'esigenza di reindirizzamenti di binding, ciò significa che è possibile installare solo un'unica versione dell'assembly nella Global Assembly Cache (GAC).</span><span class="sxs-lookup"><span data-stu-id="b7af3-144">While an unchanging AssemblyVersion avoids the need for binding redirects, it means that only a single version of the assembly can be installed in the Global Assembly Cache (GAC).</span></span> <span data-ttu-id="b7af3-145">Inoltre, le applicazioni che fanno riferimento all'assembly nella Global Assembly Cache non funzioneranno più se un'altra applicazione aggiorna l'assembly nella GAC con modifiche che causano un'interruzione.</span><span class="sxs-lookup"><span data-stu-id="b7af3-145">Also, the applications that reference the assembly in the GAC will break if another application updates the GAC assembly with breaking changes.</span></span>

### <a name="assembly-file-version"></a><span data-ttu-id="b7af3-146">Versione del file di assembly</span><span class="sxs-lookup"><span data-stu-id="b7af3-146">Assembly file version</span></span>

<span data-ttu-id="b7af3-147">La versione del file di assembly viene usata per visualizzare una versione di file in Windows e non ha alcun effetto sul comportamento in fase di esecuzione.</span><span class="sxs-lookup"><span data-stu-id="b7af3-147">The assembly file version is used to display a file version in Windows and has no effect on runtime behavior.</span></span> <span data-ttu-id="b7af3-148">L'impostazione di questa versione è facoltativa.</span><span class="sxs-lookup"><span data-stu-id="b7af3-148">Setting this version is optional.</span></span> <span data-ttu-id="b7af3-149">Il valore è visibile nella finestra di dialogo Proprietà file in Esplora risorse:</span><span class="sxs-lookup"><span data-stu-id="b7af3-149">It's visible in the File Properties dialog in Windows Explorer:</span></span>

```xml
<FileVersion>11.0.2.21924</FileVersion>
```

<span data-ttu-id="b7af3-150">![Esplora risorse](./media/versioning/win-properties.png "Esplora risorse")</span><span class="sxs-lookup"><span data-stu-id="b7af3-150">![Windows Explorer](./media/versioning/win-properties.png "Windows Explorer")</span></span>

<span data-ttu-id="b7af3-151">✔️ CONSIDERARE l'inclusione di un numero di build di integrazione continua come revisione AssemblyFileVersion.</span><span class="sxs-lookup"><span data-stu-id="b7af3-151">✔️ CONSIDER including a continuous integration build number as the AssemblyFileVersion revision.</span></span>

> <span data-ttu-id="b7af3-152">Ad esempio, se si sta compilando la versione 1.0.0 del progetto e il numero di build di integrazione continua è 99, il valore di AssemblyFileVersion sarà 1.0.0.99.</span><span class="sxs-lookup"><span data-stu-id="b7af3-152">For example, you are building version 1.0.0 of your project, and the continuous integration build number is 99 so your AssemblyFileVersion is 1.0.0.99.</span></span>

<span data-ttu-id="b7af3-153">✔️ utilizzare il formato `Major.Minor.Build.Revision` per la versione del file.</span><span class="sxs-lookup"><span data-stu-id="b7af3-153">✔️ DO use the format `Major.Minor.Build.Revision` for file version.</span></span>

> <span data-ttu-id="b7af3-154">La versione del file non viene mai usata da .NET, ma [Windows prevede che la versione del file](/windows/desktop/menurc/versioninfo-resource) usi il formato `Major.Minor.Build.Revision`.</span><span class="sxs-lookup"><span data-stu-id="b7af3-154">While the file version is never used by .NET, [Windows expects the file version](/windows/desktop/menurc/versioninfo-resource) to be in the `Major.Minor.Build.Revision` format.</span></span> <span data-ttu-id="b7af3-155">Se la versione non usa questo formato, viene generato un avviso.</span><span class="sxs-lookup"><span data-stu-id="b7af3-155">A warning is raised if the version doesn't follow this format.</span></span>

### <a name="assembly-informational-version"></a><span data-ttu-id="b7af3-156">Versione informativa dell'assembly</span><span class="sxs-lookup"><span data-stu-id="b7af3-156">Assembly informational version</span></span>

<span data-ttu-id="b7af3-157">La versione informativa dell'assembly viene usata per registrare informazioni aggiuntive sulla versione e non ha alcun effetto sul comportamento in fase di esecuzione.</span><span class="sxs-lookup"><span data-stu-id="b7af3-157">The assembly informational version is used to record additional version information and has no effect on runtime behavior.</span></span> <span data-ttu-id="b7af3-158">L'impostazione di questa versione è facoltativa.</span><span class="sxs-lookup"><span data-stu-id="b7af3-158">Setting this version is optional.</span></span> <span data-ttu-id="b7af3-159">Se si usa un collegamento all'origine, questa versione verrà impostata in fase di compilazione con la versione del pacchetto NuGet più una versione del controllo del codice sorgente.</span><span class="sxs-lookup"><span data-stu-id="b7af3-159">If you're using Source Link, this version will be set on build with the NuGet package version plus a source control version.</span></span> <span data-ttu-id="b7af3-160">Ad esempio, `1.0.0-beta1+204ff0a` include l'hash commit del codice sorgente da cui è stato compilato l'assembly.</span><span class="sxs-lookup"><span data-stu-id="b7af3-160">For example, `1.0.0-beta1+204ff0a` includes the commit hash of the source code the assembly was built from.</span></span> <span data-ttu-id="b7af3-161">Per altre informazioni, vedere [Collegamento all'origine](./sourcelink.md).</span><span class="sxs-lookup"><span data-stu-id="b7af3-161">For more information, see [Source Link](./sourcelink.md).</span></span>

```xml
<AssemblyInformationalVersion>The quick brown fox jumped over the lazy dog.</AssemblyInformationalVersion>
```

> [!NOTE]
> <span data-ttu-id="b7af3-162">Le versioni precedenti di Visual Studio generano un avviso di compilazione se questa versione non usa il formato `Major.Minor.Build.Revision`.</span><span class="sxs-lookup"><span data-stu-id="b7af3-162">Older versions of Visual Studio raise a build warning if this version doesn't follow the format `Major.Minor.Build.Revision`.</span></span> <span data-ttu-id="b7af3-163">L'avviso può essere ignorato senza problemi.</span><span class="sxs-lookup"><span data-stu-id="b7af3-163">The warning can be safely ignored.</span></span>

<span data-ttu-id="b7af3-164">❌ evitare di impostare manualmente la versione informativa dell'assembly.</span><span class="sxs-lookup"><span data-stu-id="b7af3-164">❌ AVOID setting the assembly informational version yourself.</span></span>

> <span data-ttu-id="b7af3-165">Lasciare che SourceLink generi automaticamente la versione contenente i metadati del controllo del codice sorgente e NuGet.</span><span class="sxs-lookup"><span data-stu-id="b7af3-165">Allow SourceLink to automatically generate the version containing NuGet and source control metadata.</span></span>

>[!div class="step-by-step"]
><span data-ttu-id="b7af3-166">[Precedente](publish-nuget-package.md)
>[Successivo](breaking-changes.md)</span><span class="sxs-lookup"><span data-stu-id="b7af3-166">[Previous](publish-nuget-package.md)
[Next](breaking-changes.md)</span></span>
