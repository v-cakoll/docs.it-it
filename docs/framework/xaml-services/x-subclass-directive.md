---
title: Direttiva x:Subclass
ms.date: 03/30/2017
f1_keywords:
- Subclass
- xSubclass
- x:Subclass
helpviewer_keywords:
- x:Subclass attribute [XAML Services]
- XAML [XAML Services], x:Subclass attribute
- Subclass attribute in XAML [XAML Services]
ms.assetid: 99f66072-8107-4362-ab99-8171dc83b469
ms.openlocfilehash: 850fe8acf9e47149bd385e78b30e04ba77d7a8b2
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 04/23/2019
ms.locfileid: "61938866"
---
# <a name="xsubclass-directive"></a><span data-ttu-id="197f4-102">Direttiva x:Subclass</span><span class="sxs-lookup"><span data-stu-id="197f4-102">x:Subclass Directive</span></span>
<span data-ttu-id="197f4-103">Modifica il comportamento di compilazione XAML markup quando `x:Class` viene anche fornito.</span><span class="sxs-lookup"><span data-stu-id="197f4-103">Modifies XAML markup compile behavior when `x:Class` is also provided.</span></span> <span data-ttu-id="197f4-104">Anziché creare una classe parziale che si basa sul `x:Class`, l'oggetto fornito `x:Class` viene creata come una classe intermedia, e quindi è previsto che la classe derivata specificata in base a `x:Class`.</span><span class="sxs-lookup"><span data-stu-id="197f4-104">Instead of creating a partial class that is based on `x:Class`, the provided `x:Class` is created as an intermediate class, and then your provided derived class is expected to be based on `x:Class`.</span></span>  
  
## <a name="xaml-attribute-usage"></a><span data-ttu-id="197f4-105">Uso della sintassi XAML per gli attributi</span><span class="sxs-lookup"><span data-stu-id="197f4-105">XAML Attribute Usage</span></span>  
  
```  
<object x:Class="namespace.classname" x:Subclass="subclassNamespace.subclassName">  
   ...  
</object>  
```  
  
## <a name="xaml-values"></a><span data-ttu-id="197f4-106">Valori XAML</span><span class="sxs-lookup"><span data-stu-id="197f4-106">XAML Values</span></span>  
  
|||  
|-|-|  
|`namespace`|<span data-ttu-id="197f4-107">Facoltativo.</span><span class="sxs-lookup"><span data-stu-id="197f4-107">Optional.</span></span> <span data-ttu-id="197f4-108">Specifica uno spazio dei nomi CLR contenente `classname`.</span><span class="sxs-lookup"><span data-stu-id="197f4-108">Specifies a CLR namespace that contains `classname`.</span></span> <span data-ttu-id="197f4-109">Se `namespace` viene specificato, un punto (.) separa `namespace` e `classname`.</span><span class="sxs-lookup"><span data-stu-id="197f4-109">If `namespace` is specified, a dot (.) separates `namespace` and `classname`.</span></span>|  
|`classname`|<span data-ttu-id="197f4-110">Obbligatorio.</span><span class="sxs-lookup"><span data-stu-id="197f4-110">Required.</span></span> <span data-ttu-id="197f4-111">Specifica il nome CLR della classe parziale che si connette il caricamento XAML e il code-behind per tale XAML.</span><span class="sxs-lookup"><span data-stu-id="197f4-111">Specifies the CLR name of the partial class that connects the loaded XAML and your code-behind for that XAML.</span></span> <span data-ttu-id="197f4-112">Vedere la sezione Osservazioni.</span><span class="sxs-lookup"><span data-stu-id="197f4-112">See Remarks.</span></span>|  
|`subclassNamespace`|<span data-ttu-id="197f4-113">Facoltativo.</span><span class="sxs-lookup"><span data-stu-id="197f4-113">Optional.</span></span> <span data-ttu-id="197f4-114">Può essere diversa dalla `namespace` se ogni spazio dei nomi può risolvere l'altra.</span><span class="sxs-lookup"><span data-stu-id="197f4-114">Can be different from `namespace` if each namespace can resolve the other.</span></span> <span data-ttu-id="197f4-115">Specifica uno spazio dei nomi CLR contenente `subclassName`.</span><span class="sxs-lookup"><span data-stu-id="197f4-115">Specifies a CLR namespace that contains `subclassName`.</span></span> <span data-ttu-id="197f4-116">Se `subclassName` viene specificato, un punto (.) separa `subclassNamespace` e `subclassName`.</span><span class="sxs-lookup"><span data-stu-id="197f4-116">If `subclassName` is specified, a dot (.) separates `subclassNamespace` and `subclassName`.</span></span>|  
|`subclassName`|<span data-ttu-id="197f4-117">Obbligatorio.</span><span class="sxs-lookup"><span data-stu-id="197f4-117">Required.</span></span> <span data-ttu-id="197f4-118">Specifica il nome CLR della sottoclasse.</span><span class="sxs-lookup"><span data-stu-id="197f4-118">Specifies the CLR name of the subclass.</span></span>|  
  
## <a name="dependencies"></a><span data-ttu-id="197f4-119">Dipendenze</span><span class="sxs-lookup"><span data-stu-id="197f4-119">Dependencies</span></span>  
 <span data-ttu-id="197f4-120">[Direttiva X:Class](x-class-directive.md) deve anche essere specificato nello stesso oggetto, che deve essere l'elemento radice della produzione XAML.</span><span class="sxs-lookup"><span data-stu-id="197f4-120">[x:Class Directive](x-class-directive.md) must also be provided on the same object, and that object must be the root element of the XAML production.</span></span>  
  
## <a name="remarks"></a><span data-ttu-id="197f4-121">Note</span><span class="sxs-lookup"><span data-stu-id="197f4-121">Remarks</span></span>  
 <span data-ttu-id="197f4-122">`x:Subclass` utilizzo è destinato principalmente in linguaggi che non supportano le dichiarazioni di classe parziale.</span><span class="sxs-lookup"><span data-stu-id="197f4-122">`x:Subclass` usage is primarily intended for languages that do not support partial class declarations.</span></span>  
  
 <span data-ttu-id="197f4-123">La classe utilizzata come le `x:Subclass` non può essere una classe annidata, e `x:Subclass` deve fare riferimento all'oggetto radice, come illustrato nella sezione "Dipendenze".</span><span class="sxs-lookup"><span data-stu-id="197f4-123">The class used as the `x:Subclass` cannot be a nested class, and `x:Subclass` must refer to the root object as explained in the "Dependencies" section.</span></span>  
  
 <span data-ttu-id="197f4-124">In caso contrario, il significato di concettuale `x:Subclass` non è definito da un'implementazione di servizi XAML di .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="197f4-124">Otherwise, the conceptual meaning of `x:Subclass` is undefined by a .NET Framework XAML Services implementation.</span></span> <span data-ttu-id="197f4-125">Questo avviene perché il comportamento dei servizi XAML di .NET Framework non specifica il modello di programmazione generale da cui XAML markup e il codice di supporto sono connessi.</span><span class="sxs-lookup"><span data-stu-id="197f4-125">This is because .NET Framework XAML Services behavior does not specify the overall programming model by which XAML markup and backing code are connected.</span></span> <span data-ttu-id="197f4-126">Le implementazioni di ulteriormente i concetti correlati a `x:Class` e `x:Subclass` vengono eseguite dal framework specifici che utilizzano modelli di programmazione o i modelli di applicazione per definire la modalità di connessione di markup XAML compilato markup e code-behind basati su CLR.</span><span class="sxs-lookup"><span data-stu-id="197f4-126">Implementations of further concepts related to `x:Class` and `x:Subclass` are performed by specific frameworks that use programming models or application models to define how to connect XAML markup, compiled markup, and CLR-based code-behind.</span></span> <span data-ttu-id="197f4-127">Ogni framework potrebbe essere operazioni di compilazione che consentono alcuni dei comportamenti o componenti specifici che devono essere inclusi nell'ambiente di compilazione.</span><span class="sxs-lookup"><span data-stu-id="197f4-127">Each framework might have its own build actions that enable some of the behavior, or specific components that must be included in the build environment.</span></span> <span data-ttu-id="197f4-128">All'interno di un framework, le azioni di compilazione possono variare anche in base al linguaggio specifico di CLR che viene usato per il code-behind.</span><span class="sxs-lookup"><span data-stu-id="197f4-128">Within a framework, build actions can also vary based on the specific CLR language that is used for the code-behind.</span></span>  
  
## <a name="wpf-usage-notes"></a><span data-ttu-id="197f4-129">Note sull'utilizzo WPF</span><span class="sxs-lookup"><span data-stu-id="197f4-129">WPF Usage Notes</span></span>  
 <span data-ttu-id="197f4-130">`x:Subclass` può essere in una radice della pagina o nel database di <xref:System.Windows.Application> radice nella definizione di applicazione, che ha già `x:Class`.</span><span class="sxs-lookup"><span data-stu-id="197f4-130">`x:Subclass` can be on a page root or on the <xref:System.Windows.Application> root in the application definition, which already has `x:Class`.</span></span> <span data-ttu-id="197f4-131">La dichiarazione `x:Subclass` su qualsiasi elemento diverso da una radice di pagina o un'applicazione o la specifica, in cui non `x:Class` esiste, provoca un errore in fase di compilazione.</span><span class="sxs-lookup"><span data-stu-id="197f4-131">Declaring `x:Subclass` on any element other than a page or application root, or specifying it where no `x:Class` exists, causes a compile-time error.</span></span>  
  
 <span data-ttu-id="197f4-132">Creazione di derivate le classi che funzionano correttamente per il `x:Subclass` scenario è piuttosto complesso.</span><span class="sxs-lookup"><span data-stu-id="197f4-132">Creating derived classes that work correctly for the `x:Subclass` scenario is fairly complex.</span></span> <span data-ttu-id="197f4-133">Potrebbe essere necessario esaminare i file intermedi (i file g nella cartella obj del progetto generati dalla compilazione del markup, con nomi che includono i nomi dei file con estensione XAML).</span><span class="sxs-lookup"><span data-stu-id="197f4-133">You might need to examine the intermediate files (the .g files produced in the obj folder of your project by markup compile, with names that incorporate the .xaml file names).</span></span> <span data-ttu-id="197f4-134">Questi file intermedi consente di determinare l'origine di determinati costrutti di programmazione in classi parziali unite nell'applicazione compilata.</span><span class="sxs-lookup"><span data-stu-id="197f4-134">These intermediate files can help you determine the origin of certain programming constructs in the joined partial classes in the compiled application.</span></span>  
  
 <span data-ttu-id="197f4-135">I gestori di eventi nella classe derivata devono essere `internal override` (`Friend Overrides` in Microsoft Visual Basic) per eseguire l'override gli stub per i gestori nella classe intermedia creati durante la compilazione.</span><span class="sxs-lookup"><span data-stu-id="197f4-135">Event handlers in the derived class must be `internal override` (`Friend Overrides` in Microsoft Visual Basic) in order to override the stubs for the handlers as created in the intermediate class during compilation.</span></span> <span data-ttu-id="197f4-136">In caso contrario, le implementazioni della classe derivata nascondono (ombreggiatura molto sfumata) dell'implementazione della classe intermedi e non vengono richiamati i gestori classi intermedia.</span><span class="sxs-lookup"><span data-stu-id="197f4-136">Otherwise, the derived class implementations hide (shadow) the intermediate class implementation and the intermediate class handlers are not invoked.</span></span>  
  
 <span data-ttu-id="197f4-137">Quando si definiscono entrambi `x:Class` e `x:Subclass`, non è necessario fornire qualsiasi implementazione della classe che fa riferimento `x:Class`.</span><span class="sxs-lookup"><span data-stu-id="197f4-137">When you define both `x:Class` and `x:Subclass`, you do not need to provide any implementation for the class that is referenced by `x:Class`.</span></span> <span data-ttu-id="197f4-138">È sufficiente assegnarle un nome tramite la `x:Class` attributo in modo che il compilatore contiene alcune indicazioni per la classe che crea i file intermedi (il compilatore non seleziona un nome predefinito in questo caso).</span><span class="sxs-lookup"><span data-stu-id="197f4-138">You only need to give it a name via the `x:Class` attribute so that the compiler has some guidance for the class that it creates in the intermediate files (the compiler does not select a default name in this case).</span></span> <span data-ttu-id="197f4-139">È possibile assegnare il `x:Class` un'implementazione della classe; tuttavia, ciò non lo scenario tipico per l'utilizzo di entrambe `x:Class` e `x:Subclass`.</span><span class="sxs-lookup"><span data-stu-id="197f4-139">You can give the `x:Class` class an implementation; however, this is not the typical scenario for using both `x:Class` and `x:Subclass`.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="197f4-140">Vedere anche</span><span class="sxs-lookup"><span data-stu-id="197f4-140">See also</span></span>

- [<span data-ttu-id="197f4-141">Direttiva x:Class</span><span class="sxs-lookup"><span data-stu-id="197f4-141">x:Class Directive</span></span>](x-class-directive.md)
- [<span data-ttu-id="197f4-142">Classi XAML e personalizzate per WPF</span><span class="sxs-lookup"><span data-stu-id="197f4-142">XAML and Custom Classes for WPF</span></span>](../wpf/advanced/xaml-and-custom-classes-for-wpf.md)
