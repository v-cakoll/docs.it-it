---
title: Data binding con LINQ to XML
ms.date: 10/22/2019
ms.topic: conceptual
ms.openlocfilehash: 65e1524a88f1920c037b2747b0bbe30386951635
ms.sourcegitcommit: 700ea803fb06c5ce98de017c7f76463ba33ff4a9
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 02/19/2020
ms.locfileid: "77452734"
---
# <a name="overview-of-wpf-data-binding-with-linq-to-xml"></a><span data-ttu-id="cd037-102">Panoramica di data binding WPF con LINQ to XML</span><span class="sxs-lookup"><span data-stu-id="cd037-102">Overview of WPF data binding with LINQ to XML</span></span>

<span data-ttu-id="cd037-103">Questo articolo presenta le funzionalità di data binding dinamiche nello spazio dei nomi <xref:System.Xml.Linq>.</span><span class="sxs-lookup"><span data-stu-id="cd037-103">This article introduces the dynamic data binding features in the <xref:System.Xml.Linq> namespace.</span></span> <span data-ttu-id="cd037-104">Queste funzionalità possono essere usate come origine dati per gli elementi dell'interfaccia utente nelle applicazioni WPF (Windows Presentation Foundation).</span><span class="sxs-lookup"><span data-stu-id="cd037-104">These features can be used as a data source for user interface (UI) elements in Windows Presentation Foundation (WPF) apps.</span></span> <span data-ttu-id="cd037-105">Questo scenario si basa su *proprietà dinamiche* speciali di <xref:System.Xml.Linq.XAttribute?displayProperty=fullName> e <xref:System.Xml.Linq.XElement?displayProperty=fullName>.</span><span class="sxs-lookup"><span data-stu-id="cd037-105">This scenario relies upon special *dynamic properties* of <xref:System.Xml.Linq.XAttribute?displayProperty=fullName> and <xref:System.Xml.Linq.XElement?displayProperty=fullName>.</span></span>

## <a name="xaml-and-linq-to-xml"></a><span data-ttu-id="cd037-106">XAML e LINQ to XML</span><span class="sxs-lookup"><span data-stu-id="cd037-106">XAML and LINQ to XML</span></span>

<span data-ttu-id="cd037-107">XAML (Extensible Application Markup Language) è un sottolinguaggio XML creato da Microsoft per supportare le tecnologie .NET.</span><span class="sxs-lookup"><span data-stu-id="cd037-107">The Extensible Application Markup Language (XAML) is an XML dialect created by Microsoft to support .NET technologies.</span></span> <span data-ttu-id="cd037-108">Viene usato in WPF per rappresentare elementi dell'interfaccia utente e funzionalità correlate, ad esempio eventi e data binding.</span><span class="sxs-lookup"><span data-stu-id="cd037-108">It is used in WPF to represent user interface elements and related features, such as events and data binding.</span></span> <span data-ttu-id="cd037-109">In Windows Workflow Foundation, XAML viene usato per rappresentare la struttura del programma, ad esempio il controllo del programma (*flussi di lavoro*).</span><span class="sxs-lookup"><span data-stu-id="cd037-109">In Windows Workflow Foundation, XAML is used to represent program structure, such as program control (*workflows*).</span></span> <span data-ttu-id="cd037-110">XAML consente la separazione di aspetti dichiarativi di una tecnologia dal codice procedurale correlato che definisce il comportamento più personalizzato di un programma.</span><span class="sxs-lookup"><span data-stu-id="cd037-110">XAML enables the declarative aspects of a technology to be separated from the related procedural code that defines the more individualized behavior of a program.</span></span>

<span data-ttu-id="cd037-111">XAML e LINQ to XML possono interagire in due modi:</span><span class="sxs-lookup"><span data-stu-id="cd037-111">There are two broad ways that XAML and LINQ to XML can interact:</span></span>

- <span data-ttu-id="cd037-112">Poiché i file XAML corrispondono a codice XML ben formato, è possibile eseguirvi query e modificarli tramite tecnologie XML quale LINQ to XML.</span><span class="sxs-lookup"><span data-stu-id="cd037-112">Because XAML files are well-formed XML, they can be queried and manipulated through XML technologies such as LINQ to XML.</span></span>

- <span data-ttu-id="cd037-113">Poiché le query LINQ to XML rappresentano un'origine dati, possono essere usate come origine dati per l'associazione dati per gli elementi dell'interfaccia utente WPF.</span><span class="sxs-lookup"><span data-stu-id="cd037-113">Because LINQ to XML queries represent a source of data, these queries can be used as a data source for data binding for WPF UI elements.</span></span>

<span data-ttu-id="cd037-114">In questa documentazione viene descritto il secondo scenario.</span><span class="sxs-lookup"><span data-stu-id="cd037-114">This documentation describes the second scenario.</span></span>

## <a name="data-binding-in-the-windows-presentation-foundation"></a><span data-ttu-id="cd037-115">Data Binding nel Windows Presentation Foundation</span><span class="sxs-lookup"><span data-stu-id="cd037-115">Data binding in the Windows Presentation Foundation</span></span>

<span data-ttu-id="cd037-116">L'associazione dati WPF consente a un elemento dell'interfaccia utente di associare una delle proprietà a un'origine dati.</span><span class="sxs-lookup"><span data-stu-id="cd037-116">WPF data binding enables a UI element to associate one of its properties with a data source.</span></span> <span data-ttu-id="cd037-117">Un semplice esempio di questo comportamento è <xref:System.Windows.Controls.Label> il cui testo presenta il valore di una proprietà pubblica in un oggetto definito dall'utente.</span><span class="sxs-lookup"><span data-stu-id="cd037-117">A simple example of this is a <xref:System.Windows.Controls.Label> whose text presents the value of a public property in a user-defined object.</span></span> <span data-ttu-id="cd037-118">L'associazione dati WPF si basa sui componenti seguenti:</span><span class="sxs-lookup"><span data-stu-id="cd037-118">WPF data binding relies on the following components:</span></span>

|<span data-ttu-id="cd037-119">Componente</span><span class="sxs-lookup"><span data-stu-id="cd037-119">Component</span></span>|<span data-ttu-id="cd037-120">Descrizione</span><span class="sxs-lookup"><span data-stu-id="cd037-120">Description</span></span>|
|---------------|-----------------|
|<span data-ttu-id="cd037-121">Destinazione di associazione</span><span class="sxs-lookup"><span data-stu-id="cd037-121">Binding target</span></span>|<span data-ttu-id="cd037-122">Elemento dell'interfaccia utente da associare all'origine dati.</span><span class="sxs-lookup"><span data-stu-id="cd037-122">The UI element to be associated with the data source.</span></span> <span data-ttu-id="cd037-123">Gli elementi visivi di WPF sono derivati dalla classe <xref:System.Windows.UIElement>.</span><span class="sxs-lookup"><span data-stu-id="cd037-123">Visual elements in WPF are derived from the <xref:System.Windows.UIElement> class.</span></span>|
|<span data-ttu-id="cd037-124">Proprietà di destinazione</span><span class="sxs-lookup"><span data-stu-id="cd037-124">Target property</span></span>|<span data-ttu-id="cd037-125">*Proprietà di dipendenza* della destinazione di associazione che riflette il valore dell'origine del data binding.</span><span class="sxs-lookup"><span data-stu-id="cd037-125">The *dependency property* of the binding target that reflects the value of the data-binding source.</span></span> <span data-ttu-id="cd037-126">Le proprietà della dipendenza sono supportate direttamente dalla classe <xref:System.Windows.DependencyObject>, da cui deriva <xref:System.Windows.UIElement>.</span><span class="sxs-lookup"><span data-stu-id="cd037-126">Dependency properties are directly supported by the <xref:System.Windows.DependencyObject> class, which <xref:System.Windows.UIElement> derives from.</span></span>|
|<span data-ttu-id="cd037-127">Origine di associazione</span><span class="sxs-lookup"><span data-stu-id="cd037-127">Binding source</span></span>|<span data-ttu-id="cd037-128">Oggetto di origine per uno o più valori forniti all'elemento dell'interfaccia utente per la presentazione.</span><span class="sxs-lookup"><span data-stu-id="cd037-128">The source object for one or more values that are supplied to the UI element for presentation.</span></span> <span data-ttu-id="cd037-129">In WPF sono automaticamente supportati i seguenti tipi di origini di associazione: oggetti CLR, oggetti dati ADO.NET, dati XML (provenienti da query XPath o LINQ to XML) o un altro oggetto <xref:System.Windows.DependencyObject>.</span><span class="sxs-lookup"><span data-stu-id="cd037-129">WPF automatically supports the following types as binding sources: CLR objects, ADO.NET data objects, XML data (from XPath or LINQ to XML queries), or another <xref:System.Windows.DependencyObject>.</span></span>|
|<span data-ttu-id="cd037-130">Percorso di origine</span><span class="sxs-lookup"><span data-stu-id="cd037-130">Source path</span></span>|<span data-ttu-id="cd037-131">Proprietà dell'origine di associazione che si risolve nel valore o set di valori a cui deve essere associata.</span><span class="sxs-lookup"><span data-stu-id="cd037-131">The property of the binding source that resolves to the value or set of values that is to be bound.</span></span>|

<span data-ttu-id="cd037-132">Quello della proprietà della dipendenza è un concetto specifico di WPF che rappresenta una proprietà di un elemento dell'interfaccia utente elaborato dinamicamente.</span><span class="sxs-lookup"><span data-stu-id="cd037-132">A dependency property is a concept specific to WPF that represent a dynamically computed property of a UI element.</span></span> <span data-ttu-id="cd037-133">Ad esempio, le proprietà di dipendenza includono spesso valori predefiniti o specificati da un elemento padre.</span><span class="sxs-lookup"><span data-stu-id="cd037-133">For example, dependency properties often have default values or values that are provided by a parent element.</span></span> <span data-ttu-id="cd037-134">Queste proprietà speciali sono supportate da istanze della classe <xref:System.Windows.DependencyProperty> e non da campi come con proprietà standard.</span><span class="sxs-lookup"><span data-stu-id="cd037-134">These special properties are backed by instances of the <xref:System.Windows.DependencyProperty> class (and not fields as with standard properties).</span></span> <span data-ttu-id="cd037-135">Per altre informazioni, vedere [Panoramica sulle proprietà di dipendenza](../advanced/dependency-properties-overview.md).</span><span class="sxs-lookup"><span data-stu-id="cd037-135">For more information, see [Dependency Properties Overview](../advanced/dependency-properties-overview.md).</span></span>

### <a name="dynamic-data-binding-in-wpf"></a><span data-ttu-id="cd037-136">data binding dinamici in WPF</span><span class="sxs-lookup"><span data-stu-id="cd037-136">Dynamic data binding in WPF</span></span>

<span data-ttu-id="cd037-137">Per impostazione predefinita, il data binding si verifica solo quando viene inizializzato l'elemento dell'interfaccia utente di destinazione.</span><span class="sxs-lookup"><span data-stu-id="cd037-137">By default, data binding occurs only when the target UI element is initialized.</span></span> <span data-ttu-id="cd037-138">Questo comportamento è denominato associazione *unica*.</span><span class="sxs-lookup"><span data-stu-id="cd037-138">This is called *one-time* binding.</span></span> <span data-ttu-id="cd037-139">Nella maggior parte dei casi, si tratta di una soluzione insufficiente. In genere una soluzione di data binding richiede la propagazione dinamica delle modifiche in fase di esecuzione in una delle seguenti modalità:</span><span class="sxs-lookup"><span data-stu-id="cd037-139">For most purposes, this is insufficient; typically, a data-binding solution requires that the changes be dynamically propagated at run time using one of the following:</span></span>

- <span data-ttu-id="cd037-140">L'associazione *unidirezionale* determina la propagazione automatica delle modifiche apportate a un lato.</span><span class="sxs-lookup"><span data-stu-id="cd037-140">*One-way* binding causes the changes to one side to be propagated automatically.</span></span> <span data-ttu-id="cd037-141">In generale, le modifiche apportate all'origine si riflettono nella destinazione, ma può essere utile anche l'inverso.</span><span class="sxs-lookup"><span data-stu-id="cd037-141">Most commonly, changes to the source are reflected in the target, but the reverse can sometimes be useful.</span></span>

- <span data-ttu-id="cd037-142">Nell'associazione *bidirezionale* le modifiche apportate all'origine vengono automaticamente propagate alla destinazione e viceversa.</span><span class="sxs-lookup"><span data-stu-id="cd037-142">In *two-way* binding, changes to the source are automatically propagated to the target, and changes to the target are automatically propagated to the source.</span></span>

<span data-ttu-id="cd037-143">Affinché si verifichi l'associazione unidirezionale o bidirezionale, è necessario che l'origine implementi un meccanismo di notifica delle modifiche, ad esempio tramite l'implementazione dell'interfaccia <xref:System.ComponentModel.INotifyPropertyChanged> o l'uso di un modello *PropertyNameChanged* per ogni proprietà supportata.</span><span class="sxs-lookup"><span data-stu-id="cd037-143">For one-way or two-way binding to occur, the source must implement a change notification mechanism, for example by implementing the <xref:System.ComponentModel.INotifyPropertyChanged> interface or by using a *PropertyNameChanged* pattern for each property supported.</span></span>

<span data-ttu-id="cd037-144">Per altre informazioni sul data binding in WPF, vedere [Associazione dati (WPF)](/dotnet/framework/wpf/data/data-binding-wpf).</span><span class="sxs-lookup"><span data-stu-id="cd037-144">For more information about data binding in WPF, see [Data Binding (WPF)](/dotnet/framework/wpf/data/data-binding-wpf).</span></span>

## <a name="dynamic-properties-in-linq-to-xml-classes"></a><span data-ttu-id="cd037-145">Proprietà dinamiche nelle classi LINQ to XML</span><span class="sxs-lookup"><span data-stu-id="cd037-145">Dynamic properties in LINQ to XML classes</span></span>

<span data-ttu-id="cd037-146">La maggior parte delle classi LINQ to XML non sono qualificate come origini di dati dinamici WPF appropriate.</span><span class="sxs-lookup"><span data-stu-id="cd037-146">Most LINQ to XML classes do not qualify as proper WPF dynamic data sources.</span></span> <span data-ttu-id="cd037-147">Alcune delle informazioni più utili sono disponibili solo tramite metodi, non tramite proprietà, e le proprietà di queste classi non implementano le notifiche delle modifiche.</span><span class="sxs-lookup"><span data-stu-id="cd037-147">Some of the most useful information is available only through methods, not properties, and properties in these classes do not implement change notifications.</span></span> <span data-ttu-id="cd037-148">Per supportare il data binding WPF, in LINQ to XML viene esposto un set di *proprietà dinamiche*.</span><span class="sxs-lookup"><span data-stu-id="cd037-148">To support WPF data binding, LINQ to XML exposes a set of *dynamic properties*.</span></span>

<span data-ttu-id="cd037-149">Queste proprietà dinamiche sono proprietà speciali di runtime che duplicano la funzionalità dei metodi e delle proprietà esistenti nelle classi <xref:System.Xml.Linq.XAttribute> e <xref:System.Xml.Linq.XElement>.</span><span class="sxs-lookup"><span data-stu-id="cd037-149">These dynamic properties are special run-time properties that duplicate the functionality of existing methods and properties in the <xref:System.Xml.Linq.XAttribute> and <xref:System.Xml.Linq.XElement> classes.</span></span> <span data-ttu-id="cd037-150">Sono state aggiunte a queste classi unicamente per consentirne l'uso come origini dati dinamiche per WPF.</span><span class="sxs-lookup"><span data-stu-id="cd037-150">They were added to these classes solely to enable them to act as dynamic data sources for WPF.</span></span> <span data-ttu-id="cd037-151">Per soddisfare questa esigenza, tutte queste proprietà dinamiche implementano le notifiche delle modifiche.</span><span class="sxs-lookup"><span data-stu-id="cd037-151">To meet this need, all these dynamic properties implement change notifications.</span></span> <span data-ttu-id="cd037-152">Un riferimento dettagliato per queste proprietà dinamiche è presentato nella sezione successiva, [Proprietà dinamiche di LINQ to XML](linq-to-xml-dynamic-properties.md).</span><span class="sxs-lookup"><span data-stu-id="cd037-152">A detailed reference for these dynamic properties is provided in the next section, [LINQ to XML Dynamic Properties](linq-to-xml-dynamic-properties.md).</span></span>

> [!NOTE]
> <span data-ttu-id="cd037-153">Molte delle proprietà pubbliche standard, disponibili nelle varie classi nello spazio dei nomi <xref:System.Xml.Linq>, possono essere usate per l'associazione dati unica.</span><span class="sxs-lookup"><span data-stu-id="cd037-153">Many of the standard public properties, found in the various classes in the <xref:System.Xml.Linq> namespace, can be used for one-time data binding.</span></span> <span data-ttu-id="cd037-154">Tenere presente, tuttavia, in questo schema non verrà aggiornata né l'origine né la destinazione.</span><span class="sxs-lookup"><span data-stu-id="cd037-154">However, remember that neither the source nor the target will be dynamically updated under this scheme.</span></span>

### <a name="access-dynamic-properties"></a><span data-ttu-id="cd037-155">Accedere alle proprietà dinamiche</span><span class="sxs-lookup"><span data-stu-id="cd037-155">Access dynamic properties</span></span>

<span data-ttu-id="cd037-156">Non è possibile accedere alle proprietà dinamiche delle classi <xref:System.Xml.Linq.XAttribute> e <xref:System.Xml.Linq.XElement> come alle proprietà standard.</span><span class="sxs-lookup"><span data-stu-id="cd037-156">The dynamic properties in the <xref:System.Xml.Linq.XAttribute> and <xref:System.Xml.Linq.XElement> classes cannot be accessed like standard properties.</span></span> <span data-ttu-id="cd037-157">Ad esempio, nei linguaggi conformi a CLR quale C# non è possibile:</span><span class="sxs-lookup"><span data-stu-id="cd037-157">For example, in CLR-compliant languages such as C#, they cannot be:</span></span>

- <span data-ttu-id="cd037-158">Accedere direttamente a queste proprietà in fase di compilazione.</span><span class="sxs-lookup"><span data-stu-id="cd037-158">Accessed directly at compile time.</span></span> <span data-ttu-id="cd037-159">Le proprietà dinamiche sono invisibili per il compilatore e per la funzionalità IntelliSense di Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="cd037-159">Dynamic properties are invisible to the compiler and to Visual Studio IntelliSense.</span></span>

- <span data-ttu-id="cd037-160">Individuare o accedere a queste proprietà tramite reflection .NET.</span><span class="sxs-lookup"><span data-stu-id="cd037-160">Discovered or accessed at run time using .NET reflection.</span></span> <span data-ttu-id="cd037-161">Anche in fase di esecuzione, non si tratta di proprietà nel senso CLR di base.</span><span class="sxs-lookup"><span data-stu-id="cd037-161">Even at run time, they are not properties in the basic CLR sense.</span></span>

<span data-ttu-id="cd037-162">In C# è possibile accedere alle proprietà dinamiche solo in fase di esecuzione tramite le funzionalità rese disponibili dallo spazio dei nomi <xref:System.ComponentModel>.</span><span class="sxs-lookup"><span data-stu-id="cd037-162">In C#, dynamic properties can only be accessed at run time through facilities provided by the <xref:System.ComponentModel> namespace.</span></span>

<span data-ttu-id="cd037-163">Al contrario, in un'origine XML è possibile accedere alle proprietà dinamiche tramite una notazione diretta nel seguente formato:</span><span class="sxs-lookup"><span data-stu-id="cd037-163">In contrast, however, in an XML source dynamic properties can be accessed through a straightforward notation in the following form:</span></span>

```xml
<object>.<dynamic-property>
```

<span data-ttu-id="cd037-164">Le proprietà dinamiche per queste due classi si risolvono in un valore che può essere usato direttamente o in un indicizzatore che deve essere fornito con un indice per ottenere il valore o la raccolta di valori risultante.</span><span class="sxs-lookup"><span data-stu-id="cd037-164">The dynamic properties for these two classes either resolve to a value that can be used directly, or to an indexer that must be supplied with an index to obtain the resulting value or collection of values.</span></span> <span data-ttu-id="cd037-165">Nell'ultimo caso la sintassi ha il seguente formato:</span><span class="sxs-lookup"><span data-stu-id="cd037-165">The latter syntax takes the form:</span></span>

```xml
<object>.<dynamic-property>[<index-value>]
```

<span data-ttu-id="cd037-166">Per altre informazioni, vedere [Proprietà dinamiche di LINQ to XML](linq-to-xml-dynamic-properties.md).</span><span class="sxs-lookup"><span data-stu-id="cd037-166">For more information, see [LINQ to XML Dynamic Properties](linq-to-xml-dynamic-properties.md).</span></span>

<span data-ttu-id="cd037-167">Per implementare l'associazione dinamica WPF, le proprietà dinamiche verranno usate con le funzionalità fornite dallo spazio dei nomi <xref:System.Windows.Data>, in particolare la classe <xref:System.Windows.Data.Binding>.</span><span class="sxs-lookup"><span data-stu-id="cd037-167">To implement WPF dynamic binding, dynamic properties will be used with facilities provided by the <xref:System.Windows.Data> namespace, most notably the <xref:System.Windows.Data.Binding> class.</span></span>

## <a name="see-also"></a><span data-ttu-id="cd037-168">Vedere anche</span><span class="sxs-lookup"><span data-stu-id="cd037-168">See also</span></span>

- [<span data-ttu-id="cd037-169">Data binding WPF con LINQ to XML</span><span class="sxs-lookup"><span data-stu-id="cd037-169">WPF Data Binding with LINQ to XML</span></span>](wpf-data-binding-with-linq-to-xml-overview.md)
- [<span data-ttu-id="cd037-170">Proprietà dinamiche in LINQ to XML</span><span class="sxs-lookup"><span data-stu-id="cd037-170">LINQ to XML Dynamic Properties</span></span>](linq-to-xml-dynamic-properties.md)
- [<span data-ttu-id="cd037-171">XAML in WPF</span><span class="sxs-lookup"><span data-stu-id="cd037-171">XAML in WPF</span></span>](../advanced/xaml-in-wpf.md)
- [<span data-ttu-id="cd037-172">Associazione dati (WPF)</span><span class="sxs-lookup"><span data-stu-id="cd037-172">Data Binding (WPF)</span></span>](/dotnet/framework/wpf/data/data-binding-wpf)
- <span data-ttu-id="cd037-173">[Utilizzo dei markup del flusso di lavoro](https://docs.microsoft.com/previous-versions/dotnet/netframework-3.5/ms735921(v=vs.90))</span><span class="sxs-lookup"><span data-stu-id="cd037-173">[Using Workflow Markup](https://docs.microsoft.com/previous-versions/dotnet/netframework-3.5/ms735921(v=vs.90))</span></span>
