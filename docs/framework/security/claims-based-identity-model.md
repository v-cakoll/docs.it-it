---
title: Modello di identità basata sulle attestazioni
ms.date: 03/30/2017
ms.assetid: 4a96a9af-d980-43be-bf91-341a23401431
author: BrucePerlerMS
ms.openlocfilehash: 7248cf51946d4bf798209c047ec2c7ed3bb04ebe
ms.sourcegitcommit: fb78d8abbdb87144a3872cf154930157090dd933
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 09/26/2018
ms.locfileid: "47197942"
---
# <a name="claims-based-identity-model"></a><span data-ttu-id="9c9bb-102">Modello di identità basata sulle attestazioni</span><span class="sxs-lookup"><span data-stu-id="9c9bb-102">Claims-Based Identity Model</span></span>
<span data-ttu-id="9c9bb-103">Quando si compilano applicazioni in grado di riconoscere attestazioni, l'identità dell'utente è rappresentata come un set di attestazioni nell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-103">When you build claims-aware applications, the user identity is represented in your application as a set of claims.</span></span> <span data-ttu-id="9c9bb-104">Un'attestazione può essere il nome dell'utente, un altro potrebbe essere un indirizzo di posta elettronica.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-104">One claim could be the user’s name, another might be an email address.</span></span> <span data-ttu-id="9c9bb-105">L'idea è che viene configurato un sistema di identità esterno per fornire all'applicazione tutti gli elementi necessari per ottenere informazioni sull'utente con ogni richiesta effettuata, con la garanzia crittografica che i dati di identità ricevuti provengono da un'origine attendibile.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-105">The idea is that an external identity system is configured to give your application everything it needs to know about the user with each request she makes, along with cryptographic assurance that the identity data you receive comes from a trusted source.</span></span>  
  
 <span data-ttu-id="9c9bb-106">In questo modello, un punto di accesso singolo è molto più semplice da ottenere e l'applicazione non viene più utilizzata per le operazioni seguenti:</span><span class="sxs-lookup"><span data-stu-id="9c9bb-106">Under this model, single sign-on is much easier to achieve, and your application is no longer responsible for the following:</span></span>  
  
-   <span data-ttu-id="9c9bb-107">Autenticazione degli utenti.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-107">Authenticating users.</span></span>  
  
-   <span data-ttu-id="9c9bb-108">Archiviazione di account utente e password.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-108">Storing user accounts and passwords.</span></span>  
  
-   <span data-ttu-id="9c9bb-109">Chiamata alle directory dell'organizzazione per trovare i dettagli dell'identità utente.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-109">Calling to enterprise directories to look up user identity details.</span></span>  
  
-   <span data-ttu-id="9c9bb-110">Integrazione con sistemi di identità da altre piattaforme o società.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-110">Integrating with identity systems from other platforms or companies.</span></span>  
  
 <span data-ttu-id="9c9bb-111">In questo modello, le decisioni correlate all'identità basate sulle attestazioni fornite dal sistema tramite cui è stato autenticato l'utente vengono prese dall'applicazione.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-111">Under this model, your application makes identity-related decisions based on claims supplied by the system that authenticated your user.</span></span> <span data-ttu-id="9c9bb-112">Può trattarsi di un elemento che va da una semplice personalizzazione dell'applicazione con il nome dell'utente, fino all'autorizzazione dell'utente per l'accesso a funzionalità e risorse di valore più elevato nell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-112">This could be anything from simple application personalization with the user’s first name, to authorizing the user to access higher valued features and resources in your application.</span></span>  
  
 <span data-ttu-id="9c9bb-113">In questo argomento vengono fornite le seguenti informazioni:</span><span class="sxs-lookup"><span data-stu-id="9c9bb-113">This topic provides the following information:</span></span>  
  
-   [<span data-ttu-id="9c9bb-114">Introduzione all'identità basata sulle attestazioni</span><span class="sxs-lookup"><span data-stu-id="9c9bb-114">Introduction to Claims-Based Identity</span></span>](../../../docs/framework/security/claims-based-identity-model.md#BKMK_1)  
  
-   [<span data-ttu-id="9c9bb-115">Scenario di base per un modello di identità basato sulle attestazioni</span><span class="sxs-lookup"><span data-stu-id="9c9bb-115">Basic Scenario for a Claims-Based Identity Model</span></span>](../../../docs/framework/security/claims-based-identity-model.md#BKMK_2)  
  
<a name="BKMK_1"></a>   
## <a name="introduction-to-claims-based-identity"></a><span data-ttu-id="9c9bb-116">Introduzione all'identità basata sulle attestazioni</span><span class="sxs-lookup"><span data-stu-id="9c9bb-116">Introduction to Claims-Based Identity</span></span>  
 <span data-ttu-id="9c9bb-117">La terminologia e i concetti seguenti sono utili per comprendere la nuova architettura dell'identità.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-117">The following terminology and concepts can help you understand this new architecture for identity.</span></span>  
  
### <a name="identity"></a><span data-ttu-id="9c9bb-118">identità</span><span class="sxs-lookup"><span data-stu-id="9c9bb-118">Identity</span></span>  
 <span data-ttu-id="9c9bb-119">Ai fini della descrizione del modello di programmazione in Windows Identity Foundation (WIF), il termine "identità" verrà usato per rappresentare un set di attributi che descrivono un utente o un'altra entità in un sistema che si vuole proteggere.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-119">For the purposes of describing the programming model in Windows Identity Foundation (WIF), we will use the term "identity" to represent a set of attributes that describe a user or some other entity in a system that you want to secure.</span></span>  
  
### <a name="claim"></a><span data-ttu-id="9c9bb-120">Attestazione</span><span class="sxs-lookup"><span data-stu-id="9c9bb-120">Claim</span></span>  
 <span data-ttu-id="9c9bb-121">Pensare a un'attestazione come una parte delle informazioni di identità, ad esempio nome, indirizzo di posta elettronica, age, l'appartenenza al ruolo Sales.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-121">Think of a claim as a piece of identity information such as name, email address, age, membership in the Sales role.</span></span> <span data-ttu-id="9c9bb-122">Più sono le attestazioni ricevute dall'applicazione, maggiori saranno le informazioni di cui si disporrà sull'utente.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-122">The more claims your application receives, the more you’ll know about your user.</span></span> <span data-ttu-id="9c9bb-123">Ci si potrebbe chiedere per quale motivo vengono definite "attestazioni" anziché "attributi", come avviene comunemente nella descrizione delle directory di un'organizzazione.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-123">You may be wondering why these are called "claims," rather than "attributes," as is commonly used in describing enterprise directories.</span></span> <span data-ttu-id="9c9bb-124">Il motivo è correlato al metodo di recapito.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-124">The reason has to do with the delivery method.</span></span> <span data-ttu-id="9c9bb-125">In questo modello, gli attributi dell'utente in una directory non vengono trovati dall'applicazione.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-125">In this model, your application doesn’t look up user attributes in a directory.</span></span> <span data-ttu-id="9c9bb-126">L'utente recapita le attestazioni all'applicazione tramite cui vengono esaminate.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-126">Instead, the user delivers claims to your application, and your application examines them.</span></span> <span data-ttu-id="9c9bb-127">Tutte le attestazioni vengono eseguite da un'autorità di certificazione e vengono considerate attendibili dall'utente solo se anche l'autorità di certificazione è altrettanto attendibile.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-127">Each claim is made by an issuer, and you trust the claim only as much as you trust the issuer.</span></span> <span data-ttu-id="9c9bb-128">Ad esempio, un'attestazione effettuata da un controller di dominio della società viene considerata più attendibile di una effettuata dall'utente stesso.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-128">For example, you trust a claim made by your company’s domain controller more than you trust a claim made by the user herself.</span></span> <span data-ttu-id="9c9bb-129">In WIF le attestazioni vengono rappresentate con un tipo <xref:System.Security.Claims.Claim>, che dispone di una proprietà <xref:System.Security.Claims.Claim.Issuer%2A> che consente all'utente di scoprire l'autore del rilascio dell'attestazione.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-129">WIF represents claims with a <xref:System.Security.Claims.Claim> type, which has an <xref:System.Security.Claims.Claim.Issuer%2A> property that allows you to find out who issued the claim.</span></span>  
  
### <a name="security-token"></a><span data-ttu-id="9c9bb-130">Token di sicurezza</span><span class="sxs-lookup"><span data-stu-id="9c9bb-130">Security Token</span></span>  
 <span data-ttu-id="9c9bb-131">L'utente fornisce un set di attestazioni all'applicazione con una richiesta.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-131">The user delivers a set of claims to your application along with a request.</span></span> <span data-ttu-id="9c9bb-132">In un servizio Web, queste attestazioni vengono eseguite nell'intestazione di sicurezza della busta SOAP.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-132">In a Web service, these claims are carried in the security header of the SOAP envelope.</span></span> <span data-ttu-id="9c9bb-133">In un'applicazione Web basata sul browser, le attestazioni arrivano tramite HTTP POST dal browser dell'utente e, successivamente, possono essere memorizzate nella cache in un cookie se si desidera una sessione.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-133">In a browser-based Web application, the claims arrive through an HTTP POST from the user’s browser, and may later be cached in a cookie if a session is desired.</span></span> <span data-ttu-id="9c9bb-134">Indipendentemente dalla modalità di arrivo, queste attestazioni devono essere serializzate, operazione durante la quale vengono utilizzati i token di sicurezza.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-134">Regardless of how these claims arrive, they must be serialized, which is where security tokens come in.</span></span> <span data-ttu-id="9c9bb-135">Un token di sicurezza è un set serializzato di attestazioni con firma digitale di un'autorità emittente.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-135">A security token is a serialized set of claims that is digitally signed by the issuing authority.</span></span> <span data-ttu-id="9c9bb-136">La firma è importante: garantisce che l'utente non ha semplicemente costituito un gruppo di attestazioni che ha poi inviato.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-136">The signature is important: it gives you assurance that the user didn’t just make up a bunch of claims and send them to you.</span></span> <span data-ttu-id="9c9bb-137">In situazioni a bassa sicurezza in cui la crittografia non è necessaria o desiderata, è possibile utilizzare token senza firma, ma uno scenario simile non è descritto in questo argomento.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-137">In low security situations where cryptography isn’t necessary or desired, you can use unsigned tokens, but that scenario is not described in this topic.</span></span>  
  
 <span data-ttu-id="9c9bb-138">Una delle funzionalità principali disponibile in WIF è la possibilità di creare e leggere i token di sicurezza.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-138">One of the core features in WIF is the ability to create and read security tokens.</span></span> <span data-ttu-id="9c9bb-139">Tramite WIF e .NET Framework vengono gestite tutte le operazioni di crittografia e viene presentata l'applicazione con un set di attestazioni che è possibile leggere.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-139">WIF and the .NET Framework handle all of the cryptographic work, and present your application with a set of claims that you can read.</span></span>  
  
### <a name="issuing-authority"></a><span data-ttu-id="9c9bb-140">Autorità emittente</span><span class="sxs-lookup"><span data-stu-id="9c9bb-140">Issuing Authority</span></span>  
 <span data-ttu-id="9c9bb-141">Esistono molti tipi diversi di autorità emittente, dal controller di dominio tramite cui vengono emessi ticket Kerberos, alle autorità di certificazione che emettono certificati X.509, ma il tipo specifico di autorità illustrato in questo argomento emette token di sicurezza contenenti attestazioni.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-141">There are lots of different types of issuing authorities, from domain controllers that issue Kerberos tickets, to certification authorities that issue X.509 certificates, but the specific type of authority discussed in this topic issues security tokens that contain claims.</span></span> <span data-ttu-id="9c9bb-142">Questa autorità emittente è un'applicazione Web o un servizio Web a cui è nota la modalità di emissione dei token di sicurezza.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-142">This issuing authority is a Web application or Web service that knows how to issue security tokens.</span></span> <span data-ttu-id="9c9bb-143">Deve disporre di informazioni sufficienti per poter emettere le attestazioni appropriate in base al relying party di destinazione e all'utente che effettua la richiesta e può essere responsabile dell'interazione con gli archivi dell'utente per trovare le attestazioni e autenticare gli utenti stessi.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-143">It must have enough knowledge to be able to issue the proper claims given the target relying party and the user making the request, and might be responsible for interacting with user stores to look up claims and authenticate the users themselves.</span></span>  
  
 <span data-ttu-id="9c9bb-144">Qualunque sia l'autorità emittente scelta, ha un ruolo centrale nella soluzione delle identità.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-144">Whatever issuing authority you choose, it plays a central role in your identity solution.</span></span> <span data-ttu-id="9c9bb-145">Quando si suddivide l'autenticazione in fattori dall'applicazione basandosi sulle attestazioni, la responsabilità viene passata all'autorità a cui viene richiesto di autenticare gli utenti per proprio conto.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-145">When you factor authentication out of your application by relying on claims, you’re passing responsibility to that authority and asking it to authenticate users on your behalf.</span></span>  
  
### <a name="security-token-service-sts"></a><span data-ttu-id="9c9bb-146">Servizio Token di sicurezza (STS, Security Token Service)</span><span class="sxs-lookup"><span data-stu-id="9c9bb-146">Security Token Service (STS)</span></span>  
 <span data-ttu-id="9c9bb-147">Un servizio token di sicurezza (STS) è il componente del servizio tramite cui i token di sicurezza vengono compilati, firmati ed emessi in base ai protocolli WS-Trust e WS-Federation.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-147">A security token service (STS) is the service component that builds, signs, and issues security tokens according to the WS-Trust and WS-Federation protocols.</span></span> <span data-ttu-id="9c9bb-148">L'implementazione di questi protocolli richiede molte operazioni che possono essere eseguite automaticamente da WIF, consentendo in questo modo a un utente non esperto sui protocolli di ottenere un servizio token di sicurezza operativo con facilità.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-148">There’s a lot of work that goes into implementing these protocols, but WIF does all of this work for you, making it feasible for someone who isn’t an expert in the protocols to get an STS up and running with very little effort.</span></span> <span data-ttu-id="9c9bb-149">È possibile usare un servizio token di sicurezza predefinito come [Active Directory® Federation Services (AD FS) 2.0](https://go.microsoft.com/fwlink/?LinkID=247516), un servizio token di sicurezza per cloud come [Servizio di controllo di accesso di Microsoft Azure](https://go.microsoft.com/fwlink/?LinkID=247517) oppure, se si vogliono emettere token personalizzati o implementare funzionalità di autenticazione o autorizzazione personalizzate, è possibile creare un proprio servizio token di sicurezza personalizzato usando WIF.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-149">You can use a pre-built STS such as [Active Directory® Federation Services (AD FS) 2.0](https://go.microsoft.com/fwlink/?LinkID=247516), a cloud STS such as a [Windows Azure Access Control Service (ACS)](https://go.microsoft.com/fwlink/?LinkID=247517), or, if you want to issue custom tokens or provide custom authentication or authorization, you can build your own custom STS using WIF.</span></span> <span data-ttu-id="9c9bb-150">WIF facilita la compilazione di un proprio servizio token di sicurezza.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-150">WIF makes it easy to build your own STS.</span></span>  
  
### <a name="relying-party-application"></a><span data-ttu-id="9c9bb-151">Applicazione relying party</span><span class="sxs-lookup"><span data-stu-id="9c9bb-151">Relying Party Application</span></span>  
 <span data-ttu-id="9c9bb-152">Quando si sviluppa un'applicazione basata sulle attestazioni, si compila un'applicazione relying party (RP).</span><span class="sxs-lookup"><span data-stu-id="9c9bb-152">When you build an application that relies on claims, you are building a relying party (RP) application.</span></span> <span data-ttu-id="9c9bb-153">Il termine "applicazione relying party" è sinonimo di "applicazione in grado di riconoscere attestazioni" e "applicazione basata sulle attestazioni".</span><span class="sxs-lookup"><span data-stu-id="9c9bb-153">Synonyms for an RP include "claims-aware application" and "claims-based application".</span></span> <span data-ttu-id="9c9bb-154">Le applicazioni Web e i servizi Web possono essere entrambi esempi di relying party.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-154">Web applications and Web services can both be RPs.</span></span> <span data-ttu-id="9c9bb-155">Tramite un'applicazione relying party vengono utilizzati i token emessi da un servizio token di sicurezza e vengono estratte le attestazioni dai token per utilizzarle per attività correlate all'identità.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-155">A RP application consumes the tokens issued by a STS and extracts the claims from tokens to use them for identity related tasks.</span></span> <span data-ttu-id="9c9bb-156">WIF offre funzionalità di supporto per la compilazione di applicazioni relying party.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-156">WIF offers functionalities to help you build RP applications.</span></span>  
  
### <a name="standards"></a><span data-ttu-id="9c9bb-157">Standard</span><span class="sxs-lookup"><span data-stu-id="9c9bb-157">Standards</span></span>  
 <span data-ttu-id="9c9bb-158">Per rendere interoperabile tutte le operazioni, nello scenario precedente sono stati utilizzati diversi standard WS-\*.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-158">In order to make all of this interoperable, several WS-\* standards are used in the previous scenario.</span></span> <span data-ttu-id="9c9bb-159">I criteri vengono recuperati tramite WS-MetadataExchange e i criteri stessi sono strutturati in base alla specifica WS-Policy.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-159">Policy is retrieved using WS-MetadataExchange, and the policy itself is structured according to the WS-Policy specification.</span></span> <span data-ttu-id="9c9bb-160">Tramite il servizio token di sicurezza vengono esposti gli endpoint con cui viene implementata la specifica WS-Trust, in cui viene illustrato come richiedere e ricevere i token di sicurezza.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-160">The STS exposes endpoints that implement the WS-Trust specification, which describes how to request and receive security tokens.</span></span> <span data-ttu-id="9c9bb-161">La maggior parte dei servizi token di sicurezza attuali è stata formattata con SAML (Security Assertion Markup Language),</span><span class="sxs-lookup"><span data-stu-id="9c9bb-161">Most STSs today issue tokens formatted with Security Assertion Markup Langauge (SAML).</span></span> <span data-ttu-id="9c9bb-162">un vocabolario XML riconosciuto dal settore utilizzabile per rappresentare le attestazioni in una modalità interoperabile.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-162">SAML is an industry-recognized XML vocabulary that can be used to represent claims in an interoperable way.</span></span> <span data-ttu-id="9c9bb-163">In alternativa, in una situazione con più piattaforme, questo consente di comunicare con un servizio token di sicurezza in una piattaforma completamente diversa e ottenere un punto di accesso singolo in tutte le applicazioni, indipendentemente dalla piattaforma.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-163">Or, in a multi-platform situation, this allows you to communicate with an STS on an entirely different platform and achieve single sign-on across all of your applications, regardless of platform.</span></span>  
  
### <a name="browser-based-applications"></a><span data-ttu-id="9c9bb-164">Applicazioni basate su browser</span><span class="sxs-lookup"><span data-stu-id="9c9bb-164">Browser-Based Applications</span></span>  
 <span data-ttu-id="9c9bb-165">Gli smart client non sono gli unici a poter utilizzare il modello di identità basato sulle attestazioni.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-165">Smart clients aren’t the only ones who can use the claims-based identity model.</span></span> <span data-ttu-id="9c9bb-166">Questo modello può inoltre essere utilizzato dalle applicazioni basate sul browser (definite anche client passivi).</span><span class="sxs-lookup"><span data-stu-id="9c9bb-166">Browser-based applications (also referred to as passive clients) can use it as well.</span></span> <span data-ttu-id="9c9bb-167">Il funzionamento è descritto nello scenario seguente:</span><span class="sxs-lookup"><span data-stu-id="9c9bb-167">The following scenario describes how this works.</span></span>  
  
 <span data-ttu-id="9c9bb-168">Innanzitutto, l'utente punta un browser in corrispondenza di un'applicazione Web in grado di riconoscere attestazioni (applicazione relying party).</span><span class="sxs-lookup"><span data-stu-id="9c9bb-168">First, the user points a browser at a claims-aware Web application (the relying party application).</span></span> <span data-ttu-id="9c9bb-169">Tramite l'applicazione Web il browser viene reindirizzato al servizio token di sicurezza in modo che l'utente possa essere autenticato.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-169">The Web application redirects the browser to the STS so the user can be authenticated.</span></span> <span data-ttu-id="9c9bb-170">Il servizio token di sicurezza è ospitato in un'applicazione Web semplice tramite cui viene letta la richiesta in ingresso, viene autenticato l'utente mediante i meccanismi HTTP standard e, successivamente, viene creato un token SAML e inviata una risposta con una parte di codice JavaScript che comporta l'inizializzazione da parte del browser di un HTTP POST tramite cui il token SAML viene reinviato al relying party.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-170">The STS is hosted in a simple web application that reads the incoming request, authenticates the user using standard HTTP mechanisms, and then creates a SAML token and replies with a piece of JavaScript code that causes the browser to initiate an HTTP POST that sends the SAML token back to the RP.</span></span> <span data-ttu-id="9c9bb-171">Nel corpo di questo POST sono contenute le attestazioni richieste dal relying party.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-171">The body of this POST contains the claims that the RP requested.</span></span> <span data-ttu-id="9c9bb-172">A questo punto, è normale per il relying party comprimere le attestazioni in un cookie in modo che l'utente non debba eseguire un reindirizzamento per ogni richiesta.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-172">At this point, it is common for the RP to package the claims into a cookie so that the user doesn’t have to be redirected for each request.</span></span>  
  
<a name="BKMK_2"></a>   
## <a name="basic-scenario-for-a-claims-based-identity-model"></a><span data-ttu-id="9c9bb-173">Scenario di base per un modello di identità basato sulle attestazioni</span><span class="sxs-lookup"><span data-stu-id="9c9bb-173">Basic Scenario for a Claims-Based Identity Model</span></span>  
 <span data-ttu-id="9c9bb-174">Di seguito è riportato un esempio di un sistema basato sulle attestazioni.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-174">The following is an example of a claims-based system.</span></span>  
  
 <span data-ttu-id="9c9bb-175">![Flusso di autenticazione di relying partner](../../../docs/framework/security/media/conc-relying-partner-processc.png "conc_relying_partner_processc")</span><span class="sxs-lookup"><span data-stu-id="9c9bb-175">![Relying Partner Authentication Flow](../../../docs/framework/security/media/conc-relying-partner-processc.png "conc_relying_partner_processc")</span></span>  
  
 <span data-ttu-id="9c9bb-176">In questo diagramma viene mostrato un sito Web (l'applicazione relying party, RP) configurato per utilizzare WIF per l'autenticazione e un client, un Web browser, tramite cui si intende utilizzare il sito in questione.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-176">This diagram shows a Web site (the relying party application, RP) that has been configured to use WIF for authentication and a client, a web browser, that wants to use that site.</span></span>  
  
1.  <span data-ttu-id="9c9bb-177">Quando un utente non autenticato richiede una pagina, il browser viene reindirizzato alle pagine del provider di identità (IP).</span><span class="sxs-lookup"><span data-stu-id="9c9bb-177">When an unauthenticated user requests a page their browser is redirected to the identity provider (IP) pages.</span></span>  
  
2.  <span data-ttu-id="9c9bb-178">Il provider di identità richiede all'utente di presentare le credenziali, ad esempio nome utente/password, Kerberos e così via.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-178">The IP requires the user to present their credentials, e.g. username/password, Kerberos, etc.</span></span>  
  
3.  <span data-ttu-id="9c9bb-179">Il provider di identità emette di nuovo un token che viene restituito al browser.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-179">The IP issues a token back to that is returned to the browser.</span></span>  
  
4.  <span data-ttu-id="9c9bb-180">Il browser viene ora di nuovo reindirizzato alla pagina richiesta originariamente in cui WIF determina se il token soddisfa i requisiti di accesso alla pagina.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-180">The browser is now redirected back to the originally requested page where WIF determines if the token satisfies the requirements to access the page.</span></span> <span data-ttu-id="9c9bb-181">In questo caso viene emesso un cookie per stabilire una sessione in modo che l'autenticazione deve essere eseguita una sola volta e il controllo viene passato all'applicazione.</span><span class="sxs-lookup"><span data-stu-id="9c9bb-181">If so a cookie is issued to establish a session so the authentication only needs to occur once, and control is passed to the application.</span></span>
