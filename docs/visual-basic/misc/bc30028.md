---
title: '&#39;#Else&#39; deve essere preceduto da un oggetto corrispondente &#39;#If&#39; o &#39;#ElseIf&#39;'
ms.date: 07/20/2015
f1_keywords:
- vbc30028
- bc30028
helpviewer_keywords:
- BC30028
ms.assetid: c6ed34de-d6ed-4227-9880-538055aff20a
ms.openlocfilehash: 62e454a9a480008e96ff1feb07c3654d43d09320
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 05/04/2018
ms.locfileid: "33603795"
---
# <a name="39else39-must-be-preceded-by-a-matching-39if39-or-39elseif39"></a><span data-ttu-id="45364-102">&#39;#Else&#39; deve essere preceduto da un oggetto corrispondente &#39;#If&#39; o &#39;#ElseIf&#39;</span><span class="sxs-lookup"><span data-stu-id="45364-102">&#39;#Else&#39; must be preceded by a matching &#39;#If&#39; or &#39;#ElseIf&#39;</span></span>
<span data-ttu-id="45364-103">`#Else` è una direttiva di compilazione condizionale.</span><span class="sxs-lookup"><span data-stu-id="45364-103">`#Else` is a conditional compilation directive.</span></span> <span data-ttu-id="45364-104">Una direttiva `#Else` non è preceduta da una direttiva `#If` o `#ElseIf` corrispondente.</span><span class="sxs-lookup"><span data-stu-id="45364-104">An `#Else` directive is not preceded by a corresponding `#If` or `#ElseIf` directive.</span></span>  
  
 <span data-ttu-id="45364-105">**ID errore:** BC30028</span><span class="sxs-lookup"><span data-stu-id="45364-105">**Error ID:** BC30028</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="45364-106">Per correggere l'errore</span><span class="sxs-lookup"><span data-stu-id="45364-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="45364-107">Verificare che un oggetto `#If` o `#ElseIf` precedente non sia separato da questo oggetto `#Else` mediante un blocco di compilazione condizionale o un oggetto `#End If`posizionato in modo non corretto.</span><span class="sxs-lookup"><span data-stu-id="45364-107">Check that a preceding `#If` or `#ElseIf` is not separated from this `#Else` by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
2.  <span data-ttu-id="45364-108">Verificare che `#Else` non sia preceduta da un'altra direttiva `#Else` .</span><span class="sxs-lookup"><span data-stu-id="45364-108">Check that `#Else` is preceded by another `#Else` directive.</span></span> <span data-ttu-id="45364-109">In caso affermativo, rimuovere `#Else` o modificarla in `#ElseIf`.</span><span class="sxs-lookup"><span data-stu-id="45364-109">If it is, either remove `#Else` or change it to an `#ElseIf`.</span></span>  
  
3.  <span data-ttu-id="45364-110">Se tutti gli altri elementi sono in ordine, aggiungere una direttiva `#If` all'inizio del blocco di compilazione condizionale.</span><span class="sxs-lookup"><span data-stu-id="45364-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="45364-111">Vedere anche</span><span class="sxs-lookup"><span data-stu-id="45364-111">See Also</span></span>  
 [<span data-ttu-id="45364-112">Direttive #If...Then...#Else</span><span class="sxs-lookup"><span data-stu-id="45364-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
