---
title: "Della classe&lt;NomeClasse&gt;' deve dichiarare un 'Sub New' perché il '&lt;nomecostruttore&gt;'nella relativa classe base'&lt;nomeclassebase&gt;' è contrassegnato come obsoleto: '&lt; messaggio di errore&gt;'"
ms.date: 07/20/2015
f1_keywords:
- vbc41002
- bc41002
helpviewer_keywords:
- BC41002
ms.assetid: 6d034bbe-ab6a-433a-ae31-8c4a42faf7f8
ms.openlocfilehash: dbe57a843e67193fb150fde21df51ac17f595d44
ms.sourcegitcommit: 0888d7b24f475c346a3f444de8d83ec1ca7cd234
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 12/22/2018
ms.locfileid: "53767114"
---
# <a name="class-ltclassnamegt-should-declare-a-sub-new-because-the-ltconstructornamegt-in-its-base-class-ltbaseclassnamegt-is-marked-obsolete-lterrormessagegt"></a><span data-ttu-id="d08a2-102">Della classe&lt;NomeClasse&gt;' deve dichiarare un 'Sub New' perché il '&lt;nomecostruttore&gt;'nella relativa classe base'&lt;nomeclassebase&gt;' è contrassegnato come obsoleto: '&lt; messaggio di errore&gt;'</span><span class="sxs-lookup"><span data-stu-id="d08a2-102">Class '&lt;classname&gt;' should declare a 'Sub New' because the '&lt;constructorname&gt;' in its base class '&lt;baseclassname&gt;' is marked obsolete: '&lt;errormessage&gt;'</span></span>
<span data-ttu-id="d08a2-103">Una dichiarazione di classe non include un costruttore e il costruttore della classe base è contrassegnato con l'attributo <xref:System.ObsoleteAttribute> e la direttiva di considerarlo come un avviso.</span><span class="sxs-lookup"><span data-stu-id="d08a2-103">A class declaration does not include a constructor, and the base class constructor is marked with the <xref:System.ObsoleteAttribute> attribute and the directive to treat it as a warning.</span></span>  
  
 <span data-ttu-id="d08a2-104">Quando una classe derivata non dichiara un costruttore, Visual Basic tenta di generare un costruttore senza parametri che chiama `MyBase.New()`.</span><span class="sxs-lookup"><span data-stu-id="d08a2-104">When a derived class does not declare a constructor, Visual Basic attempts to generate an implicit parameterless constructor that calls `MyBase.New()`.</span></span> <span data-ttu-id="d08a2-105">Se non è presente alcun costruttore accessibile nella classe di base che può essere chiamata senza argomenti, Visual Basic non è possibile generare un costruttore implicito.</span><span class="sxs-lookup"><span data-stu-id="d08a2-105">If there is no accessible constructor in the base class that can be called without arguments, Visual Basic cannot generate an implicit constructor.</span></span> <span data-ttu-id="d08a2-106">In questo caso, il costruttore obbligatorio è contrassegnato con il <xref:System.ObsoleteAttribute> attributo, in modo che Visual Basic non può chiamarlo.</span><span class="sxs-lookup"><span data-stu-id="d08a2-106">In this case, the required constructor is marked with the <xref:System.ObsoleteAttribute> attribute, so Visual Basic cannot call it.</span></span>  
  
 <span data-ttu-id="d08a2-107">È possibile contrassegnare qualsiasi elemento di programmazione come non più in uso applicando <xref:System.ObsoleteAttribute> a tale elemento.</span><span class="sxs-lookup"><span data-stu-id="d08a2-107">You can mark any programming element as being no longer in use by applying <xref:System.ObsoleteAttribute> to it.</span></span> <span data-ttu-id="d08a2-108">In questo caso, è possibile impostare la proprietà <xref:System.ObsoleteAttribute.IsError%2A> dell'attributo su `True` o `False`.</span><span class="sxs-lookup"><span data-stu-id="d08a2-108">If you do this, you can set the attribute's <xref:System.ObsoleteAttribute.IsError%2A> property to either `True` or `False`.</span></span> <span data-ttu-id="d08a2-109">Se si imposta la proprietà su `True`, il compilatore considera il tentativo di usare l'elemento come un errore.</span><span class="sxs-lookup"><span data-stu-id="d08a2-109">If you set it to `True`, the compiler treats an attempt to use the element as an error.</span></span> <span data-ttu-id="d08a2-110">Se si imposta la proprietà su `False`, o si lascia l'impostazione predefinita `False`, il compilatore genera un avviso se si prova a usare l'elemento.</span><span class="sxs-lookup"><span data-stu-id="d08a2-110">If you set it to `False`, or let it default to `False`, the compiler issues a warning if there is an attempt to use the element.</span></span>  
  
 <span data-ttu-id="d08a2-111">Per impostazione predefinita, si tratta di un messaggio di avviso perché la proprietà <xref:System.ObsoleteAttribute.IsError%2A> di <xref:System.ObsoleteAttribute> è `False`.</span><span class="sxs-lookup"><span data-stu-id="d08a2-111">By default, this message is a warning, because the <xref:System.ObsoleteAttribute.IsError%2A> property of <xref:System.ObsoleteAttribute> is `False`.</span></span> <span data-ttu-id="d08a2-112">Per informazioni su come nascondere gli avvisi o considerarli come errori, vedere [Configuring Warnings in Visual Basic](/visualstudio/ide/configuring-warnings-in-visual-basic).</span><span class="sxs-lookup"><span data-stu-id="d08a2-112">For information on hiding warnings or treating warnings as errors, see [Configuring Warnings in Visual Basic](/visualstudio/ide/configuring-warnings-in-visual-basic).</span></span>  
  
 <span data-ttu-id="d08a2-113">**ID errore:** BC41002</span><span class="sxs-lookup"><span data-stu-id="d08a2-113">**Error ID:** BC41002</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="d08a2-114">Per correggere l'errore</span><span class="sxs-lookup"><span data-stu-id="d08a2-114">To correct this error</span></span>  
  
1.  <span data-ttu-id="d08a2-115">Esaminare il messaggio di errore tra virgolette e intraprendere l'azione appropriata.</span><span class="sxs-lookup"><span data-stu-id="d08a2-115">Examine the quoted error message and take appropriate action.</span></span>  
  
2.  <span data-ttu-id="d08a2-116">Usare `Sub New` per dichiarare un costruttore nella classe derivata.</span><span class="sxs-lookup"><span data-stu-id="d08a2-116">Use `Sub New` to declare a constructor in the derived class.</span></span>