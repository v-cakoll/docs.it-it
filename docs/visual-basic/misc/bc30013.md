---
title: "'#ElseIf', '#Else' o '#End If' deve essere preceduto da un '#If' corrispondente"
ms.date: 07/20/2015
f1_keywords:
- vbc30013
- bc30013
helpviewer_keywords:
- BC30013
ms.assetid: 8fe2d23c-8b8f-46d8-90f2-7f8857ea43bb
ms.openlocfilehash: e59ef5ab7b296182fdfa9324961683b48c416be9
ms.sourcegitcommit: 6b308cf6d627d78ee36dbbae8972a310ac7fd6c8
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 01/23/2019
ms.locfileid: "54662205"
---
# <a name="elseif-else-or-end-if-must-be-preceded-by-a-matching-if"></a><span data-ttu-id="62e2e-102">'#ElseIf', '#Else' o '#End If' deve essere preceduto da un '#If' corrispondente</span><span class="sxs-lookup"><span data-stu-id="62e2e-102">'#ElseIf', '#Else', or '#End If' must be preceded by a matching '#If'</span></span>
<span data-ttu-id="62e2e-103">`#ElseIf`, `#Else`e `#End If` sono direttive di compilazione condizionale.</span><span class="sxs-lookup"><span data-stu-id="62e2e-103">`#ElseIf`, `#Else`, and `#End If` are conditional compilation directives.</span></span> <span data-ttu-id="62e2e-104">L'oggetto `#ElseIf`, `#Else`o `#End If` non è preceduto da una direttiva `#If` corrispondente.</span><span class="sxs-lookup"><span data-stu-id="62e2e-104">The `#ElseIf`, `#Else`, or `#End If` is not preceded by a corresponding `#If` directive.</span></span>  
  
 <span data-ttu-id="62e2e-105">**ID errore:** BC30013</span><span class="sxs-lookup"><span data-stu-id="62e2e-105">**Error ID:** BC30013</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="62e2e-106">Per correggere l'errore</span><span class="sxs-lookup"><span data-stu-id="62e2e-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="62e2e-107">Verificare che l'oggetto `#If` previsto non sia separato dalla clausola interessata mediante un blocco di compilazione condizionale o un oggetto `#End If`posizionato in modo non corretto.</span><span class="sxs-lookup"><span data-stu-id="62e2e-107">Check that the intended `#If` is not separated from the clause in question by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="62e2e-108">È consentito un solo oggetto `#Else` in ogni blocco `#If` , quindi due direttive `#Else` successive causano questo errore.</span><span class="sxs-lookup"><span data-stu-id="62e2e-108">Only one `#Else` is permitted in each `#If` block, so two successive `#Else` directives cause this error.</span></span>  
  
2.  <span data-ttu-id="62e2e-109">Verificare che una direttiva `#` precedente sia preceduta da `#If` .</span><span class="sxs-lookup"><span data-stu-id="62e2e-109">Check that the leading `#` is not missing from an earlier `#If` directive.</span></span>  
  
3.  <span data-ttu-id="62e2e-110">Se tutti gli altri elementi sono in ordine, aggiungere una direttiva `#If` all'inizio del blocco di compilazione condizionale.</span><span class="sxs-lookup"><span data-stu-id="62e2e-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="62e2e-111">Vedere anche</span><span class="sxs-lookup"><span data-stu-id="62e2e-111">See also</span></span>
- [<span data-ttu-id="62e2e-112">Direttive #If...Then...#Else</span><span class="sxs-lookup"><span data-stu-id="62e2e-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
