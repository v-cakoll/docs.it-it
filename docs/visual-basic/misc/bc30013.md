---
title: '&#39;#ElseIf&#39;, &#39;#Else&#39;, o &#39;#End End If&#39; deve essere preceduto da un oggetto corrispondente &#39;#If&#39;'
ms.date: 07/20/2015
f1_keywords:
- vbc30013
- bc30013
helpviewer_keywords:
- BC30013
ms.assetid: 8fe2d23c-8b8f-46d8-90f2-7f8857ea43bb
ms.openlocfilehash: 4571710097e84af1bb50bbc26a22e24c83daf908
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 05/04/2018
ms.locfileid: "33603444"
---
# <a name="39elseif39-39else39-or-39end-if39-must-be-preceded-by-a-matching-39if39"></a><span data-ttu-id="ae5bb-102">&#39;#ElseIf&#39;, &#39;#Else&#39;, o &#39;#End End If&#39; deve essere preceduto da un oggetto corrispondente &#39;#If&#39;</span><span class="sxs-lookup"><span data-stu-id="ae5bb-102">&#39;#ElseIf&#39;, &#39;#Else&#39;, or &#39;#End If&#39; must be preceded by a matching &#39;#If&#39;</span></span>
<span data-ttu-id="ae5bb-103">`#ElseIf`, `#Else`e `#End If` sono direttive di compilazione condizionale.</span><span class="sxs-lookup"><span data-stu-id="ae5bb-103">`#ElseIf`, `#Else`, and `#End If` are conditional compilation directives.</span></span> <span data-ttu-id="ae5bb-104">L'oggetto `#ElseIf`, `#Else`o `#End If` non è preceduto da una direttiva `#If` corrispondente.</span><span class="sxs-lookup"><span data-stu-id="ae5bb-104">The `#ElseIf`, `#Else`, or `#End If` is not preceded by a corresponding `#If` directive.</span></span>  
  
 <span data-ttu-id="ae5bb-105">**ID errore:** BC30013</span><span class="sxs-lookup"><span data-stu-id="ae5bb-105">**Error ID:** BC30013</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="ae5bb-106">Per correggere l'errore</span><span class="sxs-lookup"><span data-stu-id="ae5bb-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="ae5bb-107">Verificare che l'oggetto `#If` previsto non sia separato dalla clausola interessata mediante un blocco di compilazione condizionale o un oggetto `#End If`posizionato in modo non corretto.</span><span class="sxs-lookup"><span data-stu-id="ae5bb-107">Check that the intended `#If` is not separated from the clause in question by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="ae5bb-108">È consentito un solo oggetto `#Else` in ogni blocco `#If` , quindi due direttive `#Else` successive causano questo errore.</span><span class="sxs-lookup"><span data-stu-id="ae5bb-108">Only one `#Else` is permitted in each `#If` block, so two successive `#Else` directives cause this error.</span></span>  
  
2.  <span data-ttu-id="ae5bb-109">Verificare che una direttiva `#` precedente sia preceduta da `#If` .</span><span class="sxs-lookup"><span data-stu-id="ae5bb-109">Check that the leading `#` is not missing from an earlier `#If` directive.</span></span>  
  
3.  <span data-ttu-id="ae5bb-110">Se tutti gli altri elementi sono in ordine, aggiungere una direttiva `#If` all'inizio del blocco di compilazione condizionale.</span><span class="sxs-lookup"><span data-stu-id="ae5bb-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="ae5bb-111">Vedere anche</span><span class="sxs-lookup"><span data-stu-id="ae5bb-111">See Also</span></span>  
 [<span data-ttu-id="ae5bb-112">Direttive #If...Then...#Else</span><span class="sxs-lookup"><span data-stu-id="ae5bb-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
