---
title: Risoluzione dei problemi relativi alle routine (Visual Basic)
ms.date: 07/20/2015
helpviewer_keywords:
- troubleshooting Visual Basic, procedures
- procedures [Visual Basic], troubleshooting
- Visual Basic code, procedures
- troubleshooting procedures
- procedures [Visual Basic], about procedures
ms.assetid: 525721e8-2e02-4f75-b5d8-6b893462cf2b
ms.openlocfilehash: e29e4a3b216657b398407701530ad9bfe975dbf6
ms.sourcegitcommit: 40364ded04fa6cdcb2b6beca7f68412e2e12f633
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 02/28/2019
ms.locfileid: "56972001"
---
# <a name="troubleshooting-procedures-visual-basic"></a><span data-ttu-id="a2349-102">Risoluzione dei problemi relativi alle routine (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="a2349-102">Troubleshooting Procedures (Visual Basic)</span></span>
<span data-ttu-id="a2349-103">Questa pagina elenca alcuni problemi comuni che possono verificarsi quando si lavora con le procedure.</span><span class="sxs-lookup"><span data-stu-id="a2349-103">This page lists some common problems that can occur when working with procedures.</span></span>  
  
## <a name="returning-an-array-type-from-a-function-procedure"></a><span data-ttu-id="a2349-104">Restituzione di un tipo di matrice da una routine Function</span><span class="sxs-lookup"><span data-stu-id="a2349-104">Returning an Array Type from a Function Procedure</span></span>  
 <span data-ttu-id="a2349-105">Se un `Function` routine restituisce un tipo di dati di matrice, non è possibile usare il `Function` nome in cui archiviare i valori negli elementi della matrice.</span><span class="sxs-lookup"><span data-stu-id="a2349-105">If a `Function` procedure returns an array data type, you cannot use the `Function` name to store values in the elements of the array.</span></span> <span data-ttu-id="a2349-106">Se si prova a eseguire questa operazione, il compilatore interpreta come una chiamata al `Function`.</span><span class="sxs-lookup"><span data-stu-id="a2349-106">If you attempt to do this, the compiler interprets it as a call to the `Function`.</span></span> <span data-ttu-id="a2349-107">Nell'esempio seguente genera errori del compilatore.</span><span class="sxs-lookup"><span data-stu-id="a2349-107">The following example generates compiler errors.</span></span>  
  
 `Function allOnes(ByVal n As Integer) As Integer()`  
  
 `For i As Integer = 1 To n - 1`  
  
 <span data-ttu-id="a2349-108">`' The following statement generates a`   `COMPILER ERROR`  `.`</span><span class="sxs-lookup"><span data-stu-id="a2349-108">`' The following statement generates a`   `COMPILER ERROR`  `.`</span></span>  
  
 `allOnes(i) = 1`  
  
 `Next i`  
  
 <span data-ttu-id="a2349-109">`' The following statement generates a`   `COMPILER ERROR`  `.`</span><span class="sxs-lookup"><span data-stu-id="a2349-109">`' The following statement generates a`   `COMPILER ERROR`  `.`</span></span>  
  
 `Return allOnes()`  
  
 `End Function`  
  
 <span data-ttu-id="a2349-110">L'istruzione `allOnes(i) = 1` genera un errore del compilatore perché viene visualizzato per chiamare `allOnes` con un argomento di tipo di dati errato (un singleton `Integer` anziché un `Integer` matrice).</span><span class="sxs-lookup"><span data-stu-id="a2349-110">The statement `allOnes(i) = 1` generates a compiler error because it appears to call `allOnes` with an argument of the wrong data type (a singleton `Integer` instead of an `Integer` array).</span></span> <span data-ttu-id="a2349-111">L'istruzione `Return allOnes()` genera un errore del compilatore perché viene visualizzato per chiamare `allOnes` con nessun argomento.</span><span class="sxs-lookup"><span data-stu-id="a2349-111">The statement `Return allOnes()` generates a compiler error because it appears to call `allOnes` with no argument.</span></span>  
  
 <span data-ttu-id="a2349-112">**Approccio corretto:** Per essere in grado di modificare gli elementi della matrice che deve essere restituita, definire una matrice interna come una variabile locale.</span><span class="sxs-lookup"><span data-stu-id="a2349-112">**Correct Approach:** To be able to modify the elements of an array that is to be returned, define an internal array as a local variable.</span></span> <span data-ttu-id="a2349-113">L'esempio seguente viene compilato senza errori.</span><span class="sxs-lookup"><span data-stu-id="a2349-113">The following example compiles without error.</span></span>  
  
 [!code-vb[VbVbcnProcedures#66](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnProcedures/VB/Class1.vb#66)]  
  
## <a name="argument-not-being-modified-by-procedure-call"></a><span data-ttu-id="a2349-114">Argomento non viene modificato dalla chiamata di routine</span><span class="sxs-lookup"><span data-stu-id="a2349-114">Argument Not Being Modified by Procedure Call</span></span>  
 <span data-ttu-id="a2349-115">Se si prevede di consentire una procedura per modificare un elemento di programmazione sottostante a un argomento nel codice chiamante, è necessario passarlo per riferimento.</span><span class="sxs-lookup"><span data-stu-id="a2349-115">If you intend to allow a procedure to change a programming element underlying an argument in the calling code, you must pass it by reference.</span></span> <span data-ttu-id="a2349-116">Ma una procedura può accedere agli elementi di un argomento di tipo riferimento anche se viene passato per valore.</span><span class="sxs-lookup"><span data-stu-id="a2349-116">But a procedure can access the elements of a reference type argument even if you pass it by value.</span></span>  
  
-   <span data-ttu-id="a2349-117">**Variabile sottostante**.</span><span class="sxs-lookup"><span data-stu-id="a2349-117">**Underlying Variable**.</span></span> <span data-ttu-id="a2349-118">Per consentire la procedura sostituire il valore dell'elemento variabile sottostante stesso, la procedura è necessario dichiarare il parametro [ByRef](../../../../visual-basic/language-reference/modifiers/byref.md).</span><span class="sxs-lookup"><span data-stu-id="a2349-118">To allow the procedure to replace the value of the underlying variable element itself, the procedure must declare the parameter [ByRef](../../../../visual-basic/language-reference/modifiers/byref.md).</span></span> <span data-ttu-id="a2349-119">Inoltre, il codice chiamante non deve racchiudere l'argomento racchiuso tra parentesi, perché che sostituirebbe il `ByRef` meccanismo di trasferimento.</span><span class="sxs-lookup"><span data-stu-id="a2349-119">Also, the calling code must not enclose the argument in parentheses, because that would override the `ByRef` passing mechanism.</span></span>  
  
-   <span data-ttu-id="a2349-120">**Fare riferimento agli elementi di tipo**.</span><span class="sxs-lookup"><span data-stu-id="a2349-120">**Reference Type Elements**.</span></span> <span data-ttu-id="a2349-121">Se si dichiara un parametro [ByVal](../../../../visual-basic/language-reference/modifiers/byval.md), la procedura non è possibile modificare l'elemento variabile sottostante.</span><span class="sxs-lookup"><span data-stu-id="a2349-121">If you declare a parameter [ByVal](../../../../visual-basic/language-reference/modifiers/byval.md), the procedure cannot modify the underlying variable element itself.</span></span> <span data-ttu-id="a2349-122">Tuttavia, se l'argomento è un tipo riferimento, la procedura può modificare i membri dell'oggetto a cui fa riferimento, anche se Impossibile sostituire il valore della variabile.</span><span class="sxs-lookup"><span data-stu-id="a2349-122">However, if the argument is a reference type, the procedure can modify the members of the object to which it points, even though it cannot replace the variable's value.</span></span> <span data-ttu-id="a2349-123">Ad esempio, se l'argomento è una variabile di matrice, la procedura non è possibile assegnare una nuova matrice a esso, ma è possibile modificare uno o più dei relativi elementi.</span><span class="sxs-lookup"><span data-stu-id="a2349-123">For example, if the argument is an array variable, the procedure cannot assign a new array to it, but it can change one or more of its elements.</span></span> <span data-ttu-id="a2349-124">Gli elementi modificati vengono riflesse nella variabile di matrice sottostante nel codice chiamante.</span><span class="sxs-lookup"><span data-stu-id="a2349-124">The changed elements are reflected in the underlying array variable in the calling code.</span></span>  
  
 <span data-ttu-id="a2349-125">L'esempio seguente definisce due procedure che accettano una variabile di matrice per valore e operano sui relativi elementi.</span><span class="sxs-lookup"><span data-stu-id="a2349-125">The following example defines two procedures that take an array variable by value and operate on its elements.</span></span> <span data-ttu-id="a2349-126">Procedure `increase` aggiunge semplicemente uno per ogni elemento.</span><span class="sxs-lookup"><span data-stu-id="a2349-126">Procedure `increase` simply adds one to each element.</span></span> <span data-ttu-id="a2349-127">Routine `replace` assegna una nuova matrice al parametro `a()` , quindi aggiunge uno per ogni elemento.</span><span class="sxs-lookup"><span data-stu-id="a2349-127">Procedure `replace` assigns a new array to the parameter `a()` and then adds one to each element.</span></span> <span data-ttu-id="a2349-128">Tuttavia, la riassegnazione non influiscono sulla variabile di matrice sottostante nel codice chiamante perché `a()` viene dichiarato `ByVal`.</span><span class="sxs-lookup"><span data-stu-id="a2349-128">However, the reassignment does not affect the underlying array variable in the calling code because `a()` is declared `ByVal`.</span></span>  
  
 [!code-vb[VbVbcnProcedures#35](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnProcedures/VB/Class1.vb#35)]  
  
 [!code-vb[VbVbcnProcedures#38](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnProcedures/VB/Class1.vb#38)]  
  
 <span data-ttu-id="a2349-129">Nell'esempio seguente effettua chiamate a `increase` e `replace`.</span><span class="sxs-lookup"><span data-stu-id="a2349-129">The following example makes calls to `increase` and `replace`.</span></span>  
  
 [!code-vb[VbVbcnProcedures#37](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnProcedures/VB/Class1.vb#37)]  
  
 <span data-ttu-id="a2349-130">Il primo `MsgBox` chiamata viene visualizzato "dopo aver Increase (n): 11, 21, 31, 41".</span><span class="sxs-lookup"><span data-stu-id="a2349-130">The first `MsgBox` call displays "After increase(n): 11, 21, 31, 41".</span></span> <span data-ttu-id="a2349-131">In quanto `n` è un tipo di riferimento `increase` possono modificare i relativi membri, anche se viene passato `ByVal`.</span><span class="sxs-lookup"><span data-stu-id="a2349-131">Because `n` is a reference type, `increase` can change its members, even though it is passed `ByVal`.</span></span>  
  
 <span data-ttu-id="a2349-132">Il secondo `MsgBox` chiamata viene visualizzato "dopo Replace (n): 11, 21, 31, 41".</span><span class="sxs-lookup"><span data-stu-id="a2349-132">The second `MsgBox` call displays "After replace(n): 11, 21, 31, 41".</span></span> <span data-ttu-id="a2349-133">In quanto `n` viene passato `ByVal`, `replace` non è possibile modificare la variabile `n` assegnandole una nuova matrice.</span><span class="sxs-lookup"><span data-stu-id="a2349-133">Because `n` is passed `ByVal`, `replace` cannot modify the variable `n` by assigning a new array to it.</span></span> <span data-ttu-id="a2349-134">Quando `replace` crea la nuova istanza della matrice `k` e la assegna alla variabile locale `a`, perde il riferimento a `n` passato dal codice chiamante.</span><span class="sxs-lookup"><span data-stu-id="a2349-134">When `replace` creates the new array instance `k` and assigns it to the local variable `a`, it loses the reference to `n` passed in by the calling code.</span></span> <span data-ttu-id="a2349-135">Quando incrementa i membri del `a`, solo la matrice locale `k` è interessato.</span><span class="sxs-lookup"><span data-stu-id="a2349-135">When it increments the members of `a`, only the local array `k` is affected.</span></span>  
  
 <span data-ttu-id="a2349-136">**Approccio corretto:** Per essere in grado di modificare un elemento variabile sottostante, è necessario passarlo per riferimento.</span><span class="sxs-lookup"><span data-stu-id="a2349-136">**Correct Approach:** To be able to modify an underlying variable element itself, pass it by reference.</span></span> <span data-ttu-id="a2349-137">Nell'esempio seguente mostra la modifica nella dichiarazione di `replace` che consente di sostituire una matrice con un oggetto nel codice chiamante.</span><span class="sxs-lookup"><span data-stu-id="a2349-137">The following example shows the change in the declaration of `replace` that allows it to replace one array with another in the calling code.</span></span>  
  
 [!code-vb[VbVbcnProcedures#64](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnProcedures/VB/Class1.vb#64)]  
  
## <a name="unable-to-define-an-overload"></a><span data-ttu-id="a2349-138">Non è possibile definire un Overload</span><span class="sxs-lookup"><span data-stu-id="a2349-138">Unable to Define an Overload</span></span>  
 <span data-ttu-id="a2349-139">Se si desidera definire una versione di overload di una stored procedure, è necessario usare lo stesso nome e una firma diversa.</span><span class="sxs-lookup"><span data-stu-id="a2349-139">If you want to define an overloaded version of a procedure, you must use the same name but a different signature.</span></span> <span data-ttu-id="a2349-140">Se il compilatore non può distinguere la dichiarazione di un overload con la stessa firma, viene generato un errore.</span><span class="sxs-lookup"><span data-stu-id="a2349-140">If the compiler cannot differentiate your declaration from an overload with the same signature, it generates an error.</span></span>  
  
 <span data-ttu-id="a2349-141">Il *firma* di una stored procedure viene determinato tramite il nome della routine e l'elenco di parametri.</span><span class="sxs-lookup"><span data-stu-id="a2349-141">The *signature* of a procedure is determined by the procedure name and the parameter list.</span></span> <span data-ttu-id="a2349-142">Ogni overload deve avere lo stesso nome di tutti gli altri overload, ma deve essere diverso da tutti gli elementi in almeno uno degli altri componenti della firma.</span><span class="sxs-lookup"><span data-stu-id="a2349-142">Each overload must have the same name as all the other overloads but must differ from all of them in at least one of the other components of the signature.</span></span> <span data-ttu-id="a2349-143">Per altre informazioni, vedere [Procedure Overloading](./procedure-overloading.md).</span><span class="sxs-lookup"><span data-stu-id="a2349-143">For more information, see [Procedure Overloading](./procedure-overloading.md).</span></span>  
  
 <span data-ttu-id="a2349-144">Gli elementi seguenti, anche se si riferiscono all'elenco di parametri, non sono componenti della firma di una routine:</span><span class="sxs-lookup"><span data-stu-id="a2349-144">The following items, even though they pertain to the parameter list, are not components of a procedure's signature:</span></span>  
  
-   <span data-ttu-id="a2349-145">Parole chiave di modificatore di routine, ad esempio `Public`, `Shared`, e `Static`</span><span class="sxs-lookup"><span data-stu-id="a2349-145">Procedure modifier keywords, such as `Public`, `Shared`, and `Static`</span></span>  
  
-   <span data-ttu-id="a2349-146">Nomi dei parametri</span><span class="sxs-lookup"><span data-stu-id="a2349-146">Parameter names</span></span>  
  
-   <span data-ttu-id="a2349-147">Parole chiave di modificatori di parametro, ad esempio `ByRef` e `Optional`</span><span class="sxs-lookup"><span data-stu-id="a2349-147">Parameter modifier keywords, such as `ByRef` and `Optional`</span></span>  
  
-   <span data-ttu-id="a2349-148">il tipo di dati del valore restituito (ad eccezione di un operatore di conversione)</span><span class="sxs-lookup"><span data-stu-id="a2349-148">The data type of the return value (except for a conversion operator)</span></span>  
  
 <span data-ttu-id="a2349-149">È possibile eseguire l'overload di una procedura variando solo uno o più degli elementi precedenti.</span><span class="sxs-lookup"><span data-stu-id="a2349-149">You cannot overload a procedure by varying only one or more of the preceding items.</span></span>  
  
 <span data-ttu-id="a2349-150">**Approccio corretto:** Per poter definire un overload di routine, è necessario modificare la firma.</span><span class="sxs-lookup"><span data-stu-id="a2349-150">**Correct Approach:** To be able to define a procedure overload, you must vary the signature.</span></span> <span data-ttu-id="a2349-151">Perché è necessario utilizzare lo stesso nome, è necessario modificare il numero, ordine o tipi di dati dei parametri.</span><span class="sxs-lookup"><span data-stu-id="a2349-151">Because you must use the same name, you must vary the number, order, or data types of the parameters.</span></span> <span data-ttu-id="a2349-152">In una routine generica, è possibile variare il numero di parametri di tipo.</span><span class="sxs-lookup"><span data-stu-id="a2349-152">In a generic procedure, you can vary the number of type parameters.</span></span> <span data-ttu-id="a2349-153">In un operatore di conversione ([CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md)), è possibile variare il tipo restituito.</span><span class="sxs-lookup"><span data-stu-id="a2349-153">In a conversion operator ([CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md)), you can vary the return type.</span></span>  
  
### <a name="overload-resolution-with-optional-and-paramarray-arguments"></a><span data-ttu-id="a2349-154">Risoluzione dell'overload con facoltativa e argomenti ParamArray</span><span class="sxs-lookup"><span data-stu-id="a2349-154">Overload Resolution with Optional and ParamArray Arguments</span></span>  
 <span data-ttu-id="a2349-155">Se si esegue l'overload di una procedura con una o più [Optional](../../../../visual-basic/language-reference/modifiers/optional.md) i parametri o una [ParamArray](../../../../visual-basic/language-reference/modifiers/paramarray.md) parametro, è necessario evitare la duplicazione del *overload impliciti*.</span><span class="sxs-lookup"><span data-stu-id="a2349-155">If you are overloading a procedure with one or more [Optional](../../../../visual-basic/language-reference/modifiers/optional.md) parameters or a [ParamArray](../../../../visual-basic/language-reference/modifiers/paramarray.md) parameter, you must avoid duplicating any of the *implicit overloads*.</span></span> <span data-ttu-id="a2349-156">Per informazioni, vedere [considerazioni sull'overload di routine](./considerations-in-overloading-procedures.md).</span><span class="sxs-lookup"><span data-stu-id="a2349-156">For information, see [Considerations in Overloading Procedures](./considerations-in-overloading-procedures.md).</span></span>  
  
## <a name="calling-a-wrong-version-of-an-overloaded-procedure"></a><span data-ttu-id="a2349-157">La chiamata a una versione errata di una routine di overload</span><span class="sxs-lookup"><span data-stu-id="a2349-157">Calling a Wrong Version of an Overloaded Procedure</span></span>  
 <span data-ttu-id="a2349-158">Se una procedura dispone di diverse versioni di overload, è necessario avere familiarità con tutti i relativi elenchi di parametri e comprendere come Visual Basic consente di risolvere le chiamate tra gli overload.</span><span class="sxs-lookup"><span data-stu-id="a2349-158">If a procedure has several overloaded versions, you should be familiar with all their parameter lists and understand how Visual Basic resolves calls among the overloads.</span></span> <span data-ttu-id="a2349-159">In caso contrario, è possibile chiamare un overload diverso da quello previsto.</span><span class="sxs-lookup"><span data-stu-id="a2349-159">Otherwise you could call an overload other than the intended one.</span></span>  
  
 <span data-ttu-id="a2349-160">Dopo aver stabilito quale eseguire l'overload che si desidera chiamare, prestare attenzione a osservare le regole seguenti:</span><span class="sxs-lookup"><span data-stu-id="a2349-160">When you have determined which overload you want to call, be careful to observe the following rules:</span></span>  
  
-   <span data-ttu-id="a2349-161">Specificare il numero corretto di argomenti e nell'ordine corretto.</span><span class="sxs-lookup"><span data-stu-id="a2349-161">Supply the correct number of arguments, and in the correct order.</span></span>  
  
-   <span data-ttu-id="a2349-162">In teoria, gli argomenti devono avere esattamente gli stessi tipi di dati dei parametri corrispondenti.</span><span class="sxs-lookup"><span data-stu-id="a2349-162">Ideally, your arguments should have the exact same data types as the corresponding parameters.</span></span> <span data-ttu-id="a2349-163">In ogni caso, il tipo di dati di ogni argomento deve ampliarsi a quello del parametro corrispondente.</span><span class="sxs-lookup"><span data-stu-id="a2349-163">In any case, the data type of each argument must widen to that of its corresponding parameter.</span></span> <span data-ttu-id="a2349-164">Questo vale anche con il [Option Strict Statement](../../../../visual-basic/language-reference/statements/option-strict-statement.md) impostato su `Off`.</span><span class="sxs-lookup"><span data-stu-id="a2349-164">This is true even with the [Option Strict Statement](../../../../visual-basic/language-reference/statements/option-strict-statement.md) set to `Off`.</span></span> <span data-ttu-id="a2349-165">Se un overload richiede qualsiasi conversione di narrowing dall'elenco di argomenti, che eseguono l'overload non è idoneo a essere chiamato.</span><span class="sxs-lookup"><span data-stu-id="a2349-165">If an overload requires any narrowing conversion from your argument list, that overload is not eligible to be called.</span></span>  
  
-   <span data-ttu-id="a2349-166">Se si forniscono argomenti che richiedono un ampliamento, rendere i relativi tipi di dati più vicino possibile ai tipi di dati di parametro corrispondente.</span><span class="sxs-lookup"><span data-stu-id="a2349-166">If you supply arguments that require widening, make their data types as close as possible to the corresponding parameter data types.</span></span> <span data-ttu-id="a2349-167">Se due o più overload accettano i tipi di dati di argomento, il compilatore risolve la chiamata all'overload che richiede la quantità minima di ampliamento.</span><span class="sxs-lookup"><span data-stu-id="a2349-167">If two or more overloads accept your argument data types, the compiler resolves your call to the overload that calls for the least amount of widening.</span></span>  
  
 <span data-ttu-id="a2349-168">È possibile ridurre il rischio di mancate corrispondenze tra tipi di dati usando il [CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md) parola chiave di conversione durante la preparazione degli argomenti.</span><span class="sxs-lookup"><span data-stu-id="a2349-168">You can reduce the chance of data type mismatches by using the [CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md) conversion keyword when preparing your arguments.</span></span>  
  
### <a name="overload-resolution-failure"></a><span data-ttu-id="a2349-169">Errore di risoluzione dell'overload</span><span class="sxs-lookup"><span data-stu-id="a2349-169">Overload Resolution Failure</span></span>  
 <span data-ttu-id="a2349-170">Quando si chiama una routine di overload, il compilatore prova a eliminare tutto tranne uno degli overload.</span><span class="sxs-lookup"><span data-stu-id="a2349-170">When you call an overloaded procedure, the compiler attempts to eliminate all but one of the overloads.</span></span> <span data-ttu-id="a2349-171">Caso di esito positivo, la chiamata a tale overload viene risolta.</span><span class="sxs-lookup"><span data-stu-id="a2349-171">If it succeeds, it resolves the call to that overload.</span></span> <span data-ttu-id="a2349-172">Se vengono eliminati tutti gli overload o se non è possibile ridurre gli overload idonei a uno solo, viene generato un errore.</span><span class="sxs-lookup"><span data-stu-id="a2349-172">If it eliminates all the overloads, or if it cannot reduce the eligible overloads to a single candidate, it generates an error.</span></span>  
  
 <span data-ttu-id="a2349-173">L'esempio seguente illustra il processo di risoluzione dell'overload.</span><span class="sxs-lookup"><span data-stu-id="a2349-173">The following example illustrates the overload resolution process.</span></span>  
  
 [!code-vb[VbVbcnProcedures#62](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnProcedures/VB/Class1.vb#62)]  
  
 [!code-vb[VbVbcnProcedures#63](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnProcedures/VB/Class1.vb#63)]  
  
 <span data-ttu-id="a2349-174">Nella prima chiamata, il compilatore elimina il primo overload, perché il tipo del primo argomento (`Short`) viene convertito nel tipo del parametro corrispondente (`Byte`).</span><span class="sxs-lookup"><span data-stu-id="a2349-174">In the first call, the compiler eliminates the first overload because the type of the first argument (`Short`) narrows to the type of the corresponding parameter (`Byte`).</span></span> <span data-ttu-id="a2349-175">Il terzo overload viene quindi eliminato poiché ogni tipo di argomento nel secondo overload (`Short` e `Single`) viene ampliato al tipo del terzo overload corrispondente (`Integer` e `Single`).</span><span class="sxs-lookup"><span data-stu-id="a2349-175">It then eliminates the third overload because each argument type in the second overload (`Short` and `Single`) widens to the corresponding type in the third overload (`Integer` and `Single`).</span></span> <span data-ttu-id="a2349-176">Il secondo overload richiede un ampliamento minore, in modo che il compilatore lo usa per la chiamata.</span><span class="sxs-lookup"><span data-stu-id="a2349-176">The second overload requires less widening, so the compiler uses it for the call.</span></span>  
  
 <span data-ttu-id="a2349-177">Nella seconda chiamata, il compilatore non è possibile eliminare uno degli overload sulla base di restrizione.</span><span class="sxs-lookup"><span data-stu-id="a2349-177">In the second call, the compiler cannot eliminate any of the overloads on the basis of narrowing.</span></span> <span data-ttu-id="a2349-178">Elimina il terzo overload per lo stesso motivo della prima chiamata, perché può chiamare il secondo overload con meno di ampliamento dei tipi di argomento.</span><span class="sxs-lookup"><span data-stu-id="a2349-178">It eliminates the third overload for the same reason as in the first call, because it can call the second overload with less widening of the argument types.</span></span> <span data-ttu-id="a2349-179">Tuttavia, il compilatore non può risolvere tra i primi e il secondo overload.</span><span class="sxs-lookup"><span data-stu-id="a2349-179">However, the compiler cannot resolve between the first and second overloads.</span></span> <span data-ttu-id="a2349-180">Ognuno ha un tipo di parametro definito che viene ampliato al tipo corrispondente in altro (`Byte` al `Short`, ma `Single` a `Double`).</span><span class="sxs-lookup"><span data-stu-id="a2349-180">Each has one defined parameter type that widens to the corresponding type in the other (`Byte` to `Short`, but `Single` to `Double`).</span></span> <span data-ttu-id="a2349-181">Di conseguenza, il compilatore genera un errore di risoluzione dell'overload.</span><span class="sxs-lookup"><span data-stu-id="a2349-181">The compiler therefore generates an overload resolution error.</span></span>  
  
 <span data-ttu-id="a2349-182">**Approccio corretto:** Per essere in grado di chiamare una routine di overload senza ambiguità, usare [CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md) in modo che corrispondano ai tipi di dati di argomenti per i tipi di parametro.</span><span class="sxs-lookup"><span data-stu-id="a2349-182">**Correct Approach:** To be able to call an overloaded procedure without ambiguity, use [CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md) to match the argument data types to the parameter types.</span></span> <span data-ttu-id="a2349-183">Nell'esempio seguente viene illustrata una chiamata a `z` che forza la risoluzione per il secondo overload.</span><span class="sxs-lookup"><span data-stu-id="a2349-183">The following example shows a call to `z` that forces resolution to the second overload.</span></span>  
  
 [!code-vb[VbVbcnProcedures#65](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnProcedures/VB/Class1.vb#65)]  
  
### <a name="overload-resolution-with-optional-and-paramarray-arguments"></a><span data-ttu-id="a2349-184">Risoluzione dell'overload con facoltativa e argomenti ParamArray</span><span class="sxs-lookup"><span data-stu-id="a2349-184">Overload Resolution with Optional and ParamArray Arguments</span></span>  
 <span data-ttu-id="a2349-185">Se due overload di una stored procedure hanno firme identiche ad eccezione del fatto che l'ultimo parametro viene dichiarato [Optional](../../../../visual-basic/language-reference/modifiers/optional.md) in uno e [ParamArray](../../../../visual-basic/language-reference/modifiers/paramarray.md) in altro, il compilatore risolve una chiamata a tale procedura in base alla corrispondenza più vicina.</span><span class="sxs-lookup"><span data-stu-id="a2349-185">If two overloads of a procedure have identical signatures except that the last parameter is declared [Optional](../../../../visual-basic/language-reference/modifiers/optional.md) in one and [ParamArray](../../../../visual-basic/language-reference/modifiers/paramarray.md) in the other, the compiler resolves a call to that procedure according to the closest match.</span></span> <span data-ttu-id="a2349-186">Per altre informazioni, vedere [Overload Resolution](./overload-resolution.md).</span><span class="sxs-lookup"><span data-stu-id="a2349-186">For more information, see [Overload Resolution](./overload-resolution.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="a2349-187">Vedere anche</span><span class="sxs-lookup"><span data-stu-id="a2349-187">See also</span></span>
- [<span data-ttu-id="a2349-188">Routine</span><span class="sxs-lookup"><span data-stu-id="a2349-188">Procedures</span></span>](./index.md)
- [<span data-ttu-id="a2349-189">Routine Sub</span><span class="sxs-lookup"><span data-stu-id="a2349-189">Sub Procedures</span></span>](./sub-procedures.md)
- [<span data-ttu-id="a2349-190">Routine Function</span><span class="sxs-lookup"><span data-stu-id="a2349-190">Function Procedures</span></span>](./function-procedures.md)
- [<span data-ttu-id="a2349-191">Routine Property</span><span class="sxs-lookup"><span data-stu-id="a2349-191">Property Procedures</span></span>](./property-procedures.md)
- [<span data-ttu-id="a2349-192">Routine di operatore</span><span class="sxs-lookup"><span data-stu-id="a2349-192">Operator Procedures</span></span>](./operator-procedures.md)
- [<span data-ttu-id="a2349-193">Parametri e argomenti delle routine</span><span class="sxs-lookup"><span data-stu-id="a2349-193">Procedure Parameters and Arguments</span></span>](./procedure-parameters-and-arguments.md)
- [<span data-ttu-id="a2349-194">Overload della routine</span><span class="sxs-lookup"><span data-stu-id="a2349-194">Procedure Overloading</span></span>](./procedure-overloading.md)
- [<span data-ttu-id="a2349-195">Considerazioni sull'overload di routine</span><span class="sxs-lookup"><span data-stu-id="a2349-195">Considerations in Overloading Procedures</span></span>](./considerations-in-overloading-procedures.md)
- [<span data-ttu-id="a2349-196">Risoluzione dell'overload</span><span class="sxs-lookup"><span data-stu-id="a2349-196">Overload Resolution</span></span>](./overload-resolution.md)
