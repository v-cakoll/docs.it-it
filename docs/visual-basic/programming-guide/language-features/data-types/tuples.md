---
title: Tuple in Visual Basic
ms.date: 04/23/2017
helpviewer_keywords:
- tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
ms.openlocfilehash: 146e9c2360cea153d2f487769d5b983516861e8d
ms.sourcegitcommit: 6b308cf6d627d78ee36dbbae8972a310ac7fd6c8
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 01/23/2019
ms.locfileid: "54694727"
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="d772a-102">Tuple (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="d772a-102">Tuples (Visual Basic)</span></span>

<span data-ttu-id="d772a-103">A partire da Visual Basic 2017, il linguaggio Visual Basic offre supporto predefinito per le tuple che rende la creazione di tuple e l'accesso a elementi delle tuple più semplice.</span><span class="sxs-lookup"><span data-stu-id="d772a-103">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="d772a-104">Una tupla è una struttura dei dati leggero con un numero e una sequenza di valori.</span><span class="sxs-lookup"><span data-stu-id="d772a-104">A tuple is a light-weight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="d772a-105">Quando si crea un'istanza di tupla, si definisce il numero e il tipo di dati di ogni valore (o elemento).</span><span class="sxs-lookup"><span data-stu-id="d772a-105">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="d772a-106">Ad esempio, una tupla con 2 elementi (o coppia) dispone di due elementi.</span><span class="sxs-lookup"><span data-stu-id="d772a-106">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="d772a-107">Il primo potrebbe essere un' `Boolean` valore, mentre il secondo è un `String`.</span><span class="sxs-lookup"><span data-stu-id="d772a-107">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="d772a-108">Poiché le tuple rendono più semplice archiviare più valori in un singolo oggetto, vengono spesso usati come un modo semplice per restituire più valori da un metodo.</span><span class="sxs-lookup"><span data-stu-id="d772a-108">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="d772a-109">Supporto per le tuple richiede il <xref:System.ValueTuple> tipo.</span><span class="sxs-lookup"><span data-stu-id="d772a-109">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="d772a-110">Se non è installato .NET Framework 4.7, è necessario aggiungere il pacchetto NuGet `System.ValueTuple`, che è disponibile nella raccolta NuGet.</span><span class="sxs-lookup"><span data-stu-id="d772a-110">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="d772a-111">Senza questo pacchetto, è possibile ottenere un errore di compilazione simile a "Tipo predefinito 'ValueTuple(Of,,,)' non è definito né importato."</span><span class="sxs-lookup"><span data-stu-id="d772a-111">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="d772a-112">Creazione e uso di una tupla</span><span class="sxs-lookup"><span data-stu-id="d772a-112">Instantiating and using a tuple</span></span>

<span data-ttu-id="d772a-113">Si crea un'istanza di una tupla racchiudendo le sue parentesi messaggistica immediata di valori delimitato da virgole.</span><span class="sxs-lookup"><span data-stu-id="d772a-113">You instantiate a tuple by enclosing its comma-delimited values im parentheses.</span></span> <span data-ttu-id="d772a-114">Tali valori diventa quindi un campo di tupla.</span><span class="sxs-lookup"><span data-stu-id="d772a-114">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="d772a-115">Ad esempio, il codice seguente definisce una tripla (o 3 tuple) con un `Date` come primo valore, una `String` come il secondo e un `Boolean` come il rispettivo terzo.</span><span class="sxs-lookup"><span data-stu-id="d772a-115">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="d772a-116">Per impostazione predefinita, il nome di ogni campo in una tupla è costituita da stringa `Item` insieme a posizione del campo in base uno della tupla.</span><span class="sxs-lookup"><span data-stu-id="d772a-116">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="d772a-117">Per questa tupla con 3 elementi, il `Date` campo viene `Item1`, il `String` campo è `Item2`e il `Boolean` campo è `Item3`.</span><span class="sxs-lookup"><span data-stu-id="d772a-117">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="d772a-118">L'esempio seguente mostra i valori dei campi della tupla di creare un'istanza nella riga di codice precedente</span><span class="sxs-lookup"><span data-stu-id="d772a-118">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="d772a-119">I campi di una tupla di Visual Basic sono in lettura / scrittura; Dopo aver creato l'istanza di una tupla, è possibile modificare i relativi valori.</span><span class="sxs-lookup"><span data-stu-id="d772a-119">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="d772a-120">Nell'esempio seguente modifica due dei tre campi della tupla creato nell'esempio precedente e visualizza il risultato.</span><span class="sxs-lookup"><span data-stu-id="d772a-120">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="d772a-121">Creazione di un'istanza e l'utilizzo di una tupla con nome</span><span class="sxs-lookup"><span data-stu-id="d772a-121">Instantiating and using a named tuple</span></span>

<span data-ttu-id="d772a-122">Invece di usare i nomi predefiniti per i campi della tupla di valori, è possibile creare un'istanza di un *tupla denominata* assegnando nomi personalizzati agli elementi della tupla.</span><span class="sxs-lookup"><span data-stu-id="d772a-122">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="d772a-123">I campi della tupla è accessibile tramite i nomi assegnati *o* con i relativi nomi predefiniti.</span><span class="sxs-lookup"><span data-stu-id="d772a-123">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="d772a-124">Nell'esempio seguente crea un'istanza di 3 tuple stesso come in precedenza, ad eccezione del fatto che specifica esplicitamente il primo campo `EventDate`, il secondo `Name`e il terzo `IsHoliday`.</span><span class="sxs-lookup"><span data-stu-id="d772a-124">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="d772a-125">Quindi Visualizza i valori dei campi, li modifica e visualizza i valori dei campi nuovamente.</span><span class="sxs-lookup"><span data-stu-id="d772a-125">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="inferred-tuple-element-names"></a><span data-ttu-id="d772a-126">Nomi di elemento di tupla dedotti</span><span class="sxs-lookup"><span data-stu-id="d772a-126">Inferred tuple element names</span></span>

<span data-ttu-id="d772a-127">A partire da Visual Basic 15.3, Visual Basic in grado di dedurre i nomi di elementi di tupla. non è necessario assegnare loro in modo esplicito.</span><span class="sxs-lookup"><span data-stu-id="d772a-127">Starting with Visual Basic 15.3, Visual Basic can infer the names of tuple elements; you do not have to assign them explicitly.</span></span> <span data-ttu-id="d772a-128">I nomi di tupla dedotti sono utili quando si Inizializza una tupla da un set di variabili e si desidera che il nome dell'elemento tupla da corrispondere al nome della variabile.</span><span class="sxs-lookup"><span data-stu-id="d772a-128">Inferred tuple names are useful when you initialize a tuple from a set of variables, and you want the tuple element name to be the same as the variable name.</span></span> 

<span data-ttu-id="d772a-129">L'esempio seguente crea una `stateInfo` elementi, denominati tupla che contiene tre in modo esplicito `state`, `stateName`, e `capital`.</span><span class="sxs-lookup"><span data-stu-id="d772a-129">The following example creates a `stateInfo` tuple that contains three explicitly named elements, `state`, `stateName`, and `capital`.</span></span> <span data-ttu-id="d772a-130">Si noti che, nei nomi di elementi, l'istruzione di inizializzazione della tupla assegna semplicemente gli elementi denominati i valori delle variabili con nome identico.</span><span class="sxs-lookup"><span data-stu-id="d772a-130">Note that, in naming the elements, the tuple initialization statement simply assigns the named elements the values of the identically named variables.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#1)]
 
<span data-ttu-id="d772a-131">Poiché gli elementi e le variabili hanno lo stesso nome, il compilatore Visual Basic può dedurre i nomi dei campi, come illustrato nell'esempio seguente.</span><span class="sxs-lookup"><span data-stu-id="d772a-131">Because elements and variables have the same name, the Visual Basic compiler can infer the names of the fields, as the following example shows.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#2)]

<span data-ttu-id="d772a-132">Per abilitare i nomi degli elementi di tupla dedotti, è necessario definire la versione del compilatore Visual Basic da utilizzare nel progetto Visual Basic (\*vbproj) file:</span><span class="sxs-lookup"><span data-stu-id="d772a-132">To enable inferred tuple element names, you must define the version of the Visual Basic compiler to use in your Visual Basic project (\*.vbproj) file:</span></span> 

```xml 
<PropertyGroup> 
  <LangVersion>15.3</LangVersion> 
</PropertyGroup> 
```

<span data-ttu-id="d772a-133">Il numero di versione può essere qualsiasi versione del compilatore Visual Basic 15.3 a partire.</span><span class="sxs-lookup"><span data-stu-id="d772a-133">The version number can be any version of the Visual Basic compiler starting with 15.3.</span></span> <span data-ttu-id="d772a-134">Anziché impostare come hardcoded una versione del compilatore specifici, è possibile specificare anche "Latest" come valore di `LangVersion` per la compilazione con la versione più recente del compilatore Visual Basic installato nel sistema.</span><span class="sxs-lookup"><span data-stu-id="d772a-134">Rather than hard-coding a specific compiler version, you can also specify "Latest" as the value of `LangVersion` to compile with the most recent version of the Visual Basic compiler installed on your system.</span></span>

<span data-ttu-id="d772a-135">Per altre informazioni, vedere [impostando la versione del linguaggio Visual Basic](../../../language-reference/configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="d772a-135">For more information, see [setting the Visual Basic language version](../../../language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="d772a-136">In alcuni casi, il compilatore Visual Basic non è possibile dedurre il nome dell'elemento tupla dal nome candidato e il campo di tupla può fare riferimento solo utilizzando il nome predefinito, ad esempio `Item1`, `Item2`e così via. Sono inclusi:</span><span class="sxs-lookup"><span data-stu-id="d772a-136">In some cases, the Visual Basic compiler cannot infer the tuple element name from the candidate name, and the tuple field can only be referenced using its default name, such as `Item1`, `Item2`, etc. These include:</span></span>

- <span data-ttu-id="d772a-137">Il nome candidato è identico al nome di un membro, tupla, ad esempio `Item3`, `Rest`, o `ToString`.</span><span class="sxs-lookup"><span data-stu-id="d772a-137">The candidate name is the same as the name of a tuple member, such as `Item3`, `Rest`, or `ToString`.</span></span>

- <span data-ttu-id="d772a-138">Il nome candidato è duplicato nella tupla.</span><span class="sxs-lookup"><span data-stu-id="d772a-138">The candidate name is duplicated in the tuple.</span></span>
 
<span data-ttu-id="d772a-139">Quando l'inferenza del nome di campo non riesce, non genera un errore del compilatore Visual Basic, né è un'eccezione generata in fase di esecuzione.</span><span class="sxs-lookup"><span data-stu-id="d772a-139">When field name inference fails, Visual Basic does not generate a compiler error, nor is an exception thrown at runtime.</span></span> <span data-ttu-id="d772a-140">Al contrario, campi della tupla devono fare riferimento i relativi nomi predefiniti, ad esempio `Item1` e `Item2`.</span><span class="sxs-lookup"><span data-stu-id="d772a-140">Instead, tuple fields must be referenced by their predefined names, such as `Item1` and `Item2`.</span></span> 
  
## <a name="tuples-versus-structures"></a><span data-ttu-id="d772a-141">Tuple e strutture</span><span class="sxs-lookup"><span data-stu-id="d772a-141">Tuples versus structures</span></span>

<span data-ttu-id="d772a-142">Una tupla di Visual Basic è un tipo di valore che rappresenta un'istanza di uno di un **System. valuetuple** tipi generici.</span><span class="sxs-lookup"><span data-stu-id="d772a-142">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="d772a-143">Ad esempio, il `holiday` tupla definita nell'esempio precedente è un'istanza del <xref:System.ValueTuple%603> struttura.</span><span class="sxs-lookup"><span data-stu-id="d772a-143">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="d772a-144">È progettato per essere utilizzato come contenitore leggero per i dati.</span><span class="sxs-lookup"><span data-stu-id="d772a-144">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="d772a-145">Poiché la tupla mira a semplificare creare un oggetto con più elementi di dati, alcune delle funzionalità che potrebbe avere una struttura personalizzata manca.</span><span class="sxs-lookup"><span data-stu-id="d772a-145">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="d772a-146">Sono inclusi:</span><span class="sxs-lookup"><span data-stu-id="d772a-146">These include:</span></span>

- <span data-ttu-id="d772a-147">Membri personalizzati.</span><span class="sxs-lookup"><span data-stu-id="d772a-147">Custom members.</span></span> <span data-ttu-id="d772a-148">È possibile definire il proprio le proprietà, metodi o eventi da una tupla.</span><span class="sxs-lookup"><span data-stu-id="d772a-148">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="d772a-149">Convalida.</span><span class="sxs-lookup"><span data-stu-id="d772a-149">Validation.</span></span> <span data-ttu-id="d772a-150">È possibile convalidare i dati assegnati ai campi.</span><span class="sxs-lookup"><span data-stu-id="d772a-150">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="d772a-151">Immutabilità.</span><span class="sxs-lookup"><span data-stu-id="d772a-151">Immutability.</span></span> <span data-ttu-id="d772a-152">Le tuple di Visual Basic sono modificabili.</span><span class="sxs-lookup"><span data-stu-id="d772a-152">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="d772a-153">Al contrario, una struttura personalizzata consente di controllare se un'istanza è modificabile o non modificabili.</span><span class="sxs-lookup"><span data-stu-id="d772a-153">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="d772a-154">Se membri personalizzati, proprietà e la convalida del campo o immutabilità è importante, è consigliabile usare Visual Basic [struttura](../../../language-reference/statements/structure-statement.md) istruzione per definire un tipo di valore personalizzato.</span><span class="sxs-lookup"><span data-stu-id="d772a-154">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="d772a-155">Una tupla di Visual Basic ereditano i membri del relativo **ValueTuple** tipo.</span><span class="sxs-lookup"><span data-stu-id="d772a-155">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="d772a-156">Oltre ai relativi campi, tra i metodi seguenti:</span><span class="sxs-lookup"><span data-stu-id="d772a-156">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="d772a-157">Member</span><span class="sxs-lookup"><span data-stu-id="d772a-157">Member</span></span> | <span data-ttu-id="d772a-158">Descrizione</span><span class="sxs-lookup"><span data-stu-id="d772a-158">Description</span></span> |
| ---|---|
| <span data-ttu-id="d772a-159">CompareTo</span><span class="sxs-lookup"><span data-stu-id="d772a-159">CompareTo</span></span> | <span data-ttu-id="d772a-160">Confronta la tupla corrente da un'altra tupla con lo stesso numero di elementi.</span><span class="sxs-lookup"><span data-stu-id="d772a-160">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="d772a-161">Equals</span><span class="sxs-lookup"><span data-stu-id="d772a-161">Equals</span></span> | <span data-ttu-id="d772a-162">Determina se la tupla corrente è uguale a un altro oggetto o di tupla.</span><span class="sxs-lookup"><span data-stu-id="d772a-162">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="d772a-163">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="d772a-163">GetHashCode</span></span> | <span data-ttu-id="d772a-164">Calcola il codice hash per l'istanza corrente.</span><span class="sxs-lookup"><span data-stu-id="d772a-164">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="d772a-165">ToString</span><span class="sxs-lookup"><span data-stu-id="d772a-165">ToString</span></span> | <span data-ttu-id="d772a-166">Restituisce la rappresentazione di stringa di questa tupla, che assume la forma `(Item1, Item2...)`, dove `Item1` e `Item2` rappresentano i valori dei campi della tupla.</span><span class="sxs-lookup"><span data-stu-id="d772a-166">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="d772a-167">Inoltre, il **ValueTuple** tipi implementano <xref:System.Collections.IStructuralComparable> e <xref:System.Collections.IStructuralEquatable> interfacce che consentono di definire operatori di confronto dei clienti.</span><span class="sxs-lookup"><span data-stu-id="d772a-167">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define customer comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="d772a-168">Assegnazione e tuple</span><span class="sxs-lookup"><span data-stu-id="d772a-168">Assignment and tuples</span></span>

<span data-ttu-id="d772a-169">Visual Basic supporta l'assegnazione tra tipi di tupla che hanno lo stesso numero di campi.</span><span class="sxs-lookup"><span data-stu-id="d772a-169">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="d772a-170">I tipi di campo possono essere convertiti se viene soddisfatta una delle operazioni seguenti:</span><span class="sxs-lookup"><span data-stu-id="d772a-170">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="d772a-171">Il campo di origine e destinazione sono dello stesso tipo.</span><span class="sxs-lookup"><span data-stu-id="d772a-171">The source and target field are of the same type.</span></span>

- <span data-ttu-id="d772a-172">Viene definita una conversione widening (o implicita) del tipo di origine al tipo di destinazione.</span><span class="sxs-lookup"><span data-stu-id="d772a-172">A widening (or implicit) conversion of the source type to the target type is defined.</span></span> 

- <span data-ttu-id="d772a-173">`Option Strict` è `On`, e viene definita una conversione narrowing (o esplicita) del tipo di origine al tipo di destinazione.</span><span class="sxs-lookup"><span data-stu-id="d772a-173">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="d772a-174">Questa conversione può generare un'eccezione se il valore di origine è compreso nell'intervallo del tipo di destinazione.</span><span class="sxs-lookup"><span data-stu-id="d772a-174">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="d772a-175">Non sono considerate altre conversioni per le assegnazioni.</span><span class="sxs-lookup"><span data-stu-id="d772a-175">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="d772a-176">Esaminiamo i tipi di assegnazioni consentiti tra i tipi di tupla.</span><span class="sxs-lookup"><span data-stu-id="d772a-176">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="d772a-177">Considerare le variabili usate negli esempi seguenti:</span><span class="sxs-lookup"><span data-stu-id="d772a-177">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="d772a-178">Le prime due variabili `unnamed` e `anonymous`, non hanno nomi semantici forniti per i campi.</span><span class="sxs-lookup"><span data-stu-id="d772a-178">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="d772a-179">I nomi di campo sono il valore predefinito `Item1` e `Item2`.</span><span class="sxs-lookup"><span data-stu-id="d772a-179">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="d772a-180">Le ultime due variabili `named` e `differentName` hanno nomi semantici campo.</span><span class="sxs-lookup"><span data-stu-id="d772a-180">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="d772a-181">Si noti che queste due tuple presentano nomi diversi per i campi.</span><span class="sxs-lookup"><span data-stu-id="d772a-181">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="d772a-182">Tutte e quattro le tuple hanno lo stesso numero di campi (detto "grado") e i tipi di tali campi sono identici.</span><span class="sxs-lookup"><span data-stu-id="d772a-182">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="d772a-183">Pertanto, tutte queste assegnazioni funzionano:</span><span class="sxs-lookup"><span data-stu-id="d772a-183">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="d772a-184">Si noti che non sono assegnati i nomi delle tuple.</span><span class="sxs-lookup"><span data-stu-id="d772a-184">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="d772a-185">I valori dei campi vengono assegnati seguendo l'ordine dei campi nella tupla.</span><span class="sxs-lookup"><span data-stu-id="d772a-185">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="d772a-186">Infine, si noti che è possibile assegnare il `named` tupla per il `conversion` tupla, anche se il primo campo di `named` è un `Integer`e nel primo campo della `conversion` è un `Long`.</span><span class="sxs-lookup"><span data-stu-id="d772a-186">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="d772a-187">Questa assegnazione ha esito positivo perché la conversione di un `Integer` a un `Long` è una conversione verso un.</span><span class="sxs-lookup"><span data-stu-id="d772a-187">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="d772a-188">Le tuple con numeri diversi di campi non sono assegnabili:</span><span class="sxs-lookup"><span data-stu-id="d772a-188">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="d772a-189">Tuple come valori restituiti dal metodo</span><span class="sxs-lookup"><span data-stu-id="d772a-189">Tuples as method return values</span></span>

<span data-ttu-id="d772a-190">Un metodo può restituire un solo valore.</span><span class="sxs-lookup"><span data-stu-id="d772a-190">A method can return only a single value.</span></span> <span data-ttu-id="d772a-191">Spesso, tuttavia, si desidera che una chiamata al metodo per restituire più valori.</span><span class="sxs-lookup"><span data-stu-id="d772a-191">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="d772a-192">Esistono diversi modi per aggirare questa limitazione:</span><span class="sxs-lookup"><span data-stu-id="d772a-192">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="d772a-193">È possibile creare una classe personalizzata o una struttura le cui proprietà o campi rappresentano i valori restituiti dal metodo.</span><span class="sxs-lookup"><span data-stu-id="d772a-193">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="d772a-194">In questo modo è una soluzione ad alta densità. richiede la definizione di un tipo personalizzato di cui solo scopo consiste nel recuperare i valori da una chiamata al metodo.</span><span class="sxs-lookup"><span data-stu-id="d772a-194">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="d772a-195">È possibile restituire un solo valore dal metodo e restituire i valori rimanenti dal passaggio per riferimento al metodo.</span><span class="sxs-lookup"><span data-stu-id="d772a-195">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="d772a-196">Ciò comporta l'overhead di un'istanza di una variabile e i rischi di sovrascrivere inavvertitamente il valore della variabile che viene passato per riferimento.</span><span class="sxs-lookup"><span data-stu-id="d772a-196">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="d772a-197">È possibile usare una tupla, che offre una soluzione semplice per il recupero di più valori restituiti.</span><span class="sxs-lookup"><span data-stu-id="d772a-197">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="d772a-198">Ad esempio, il **TryParse** metodi Return .NET un `Boolean` valore che indica se l'operazione di analisi ha avuto esito positivo.</span><span class="sxs-lookup"><span data-stu-id="d772a-198">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="d772a-199">In una variabile passata per riferimento al metodo viene restituito il risultato dell'operazione di analisi.</span><span class="sxs-lookup"><span data-stu-id="d772a-199">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="d772a-200">In genere, una chiamata a di un metodo di analisi, ad esempio <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="d772a-200">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="d772a-201">È possibile restituire una tupla dall'operazione di analisi, se si esegue il wrapping della chiamata al <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> metodo nel nostro metodo.</span><span class="sxs-lookup"><span data-stu-id="d772a-201">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="d772a-202">Nell'esempio riportato di seguito `NumericLibrary.ParseInteger` chiama il <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> metodo e restituisce una tupla con nome con due elementi.</span><span class="sxs-lookup"><span data-stu-id="d772a-202">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span> 

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="d772a-203">È quindi possibile chiamare il metodo con codice simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="d772a-203">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="d772a-204">Le tuple di Visual Basic e le tuple in .NET Framework</span><span class="sxs-lookup"><span data-stu-id="d772a-204">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="d772a-205">Una tupla di Visual Basic è un'istanza di uno dei **System. valuetuple** tipi generici, che sono state introdotte in .NET Framework 4.7.</span><span class="sxs-lookup"><span data-stu-id="d772a-205">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="d772a-206">.NET Framework include anche un set del tipo generico **System. Tuple** classi.</span><span class="sxs-lookup"><span data-stu-id="d772a-206">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="d772a-207">Queste classi, tuttavia, è diverso da tuple di Visual Basic e il **System. valuetuple** tipi generici in diversi modi:</span><span class="sxs-lookup"><span data-stu-id="d772a-207">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="d772a-208">Gli elementi del **tupla** classi sono le proprietà denominate `Item1`, `Item2`e così via.</span><span class="sxs-lookup"><span data-stu-id="d772a-208">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="d772a-209">In Visual Basic le tuple e il **ValueTuple** tipi, gli elementi di tupla sono campi.</span><span class="sxs-lookup"><span data-stu-id="d772a-209">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="d772a-210">Non è possibile assegnare nomi significativi per gli elementi di un **tupla** istanza o di un **ValueTuple** istanza.</span><span class="sxs-lookup"><span data-stu-id="d772a-210">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="d772a-211">Visual Basic consente di assegnare nomi comunicano il significato dei campi.</span><span class="sxs-lookup"><span data-stu-id="d772a-211">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="d772a-212">Le proprietà di un **tupla** istanza sono di sola lettura, le tuple non sono modificabili.</span><span class="sxs-lookup"><span data-stu-id="d772a-212">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="d772a-213">In Visual Basic le tuple e il **ValueTuple** tipi, campi della tupla sono in lettura / scrittura, le tuple sono modificabili.</span><span class="sxs-lookup"><span data-stu-id="d772a-213">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="d772a-214">Il tipo generico **tupla** tipi sono tipi riferimento.</span><span class="sxs-lookup"><span data-stu-id="d772a-214">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="d772a-215">Usando queste **tupla** tipi traduce nell'allocazione di oggetti.</span><span class="sxs-lookup"><span data-stu-id="d772a-215">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="d772a-216">Nei percorsi critici, ciò può avere un impatto notevole sulle prestazioni dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="d772a-216">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="d772a-217">Le tuple di Visual Basic e il **ValueTuple** tipi sono tipi valore.</span><span class="sxs-lookup"><span data-stu-id="d772a-217">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="d772a-218">Metodi di estensione il <xref:System.TupleExtensions> classe rendono più semplice eseguire la conversione tra .NET e Visual Basic Tuple **tupla** oggetti.</span><span class="sxs-lookup"><span data-stu-id="d772a-218">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="d772a-219">Il **ToTuple** metodo converte una tupla di Visual Basic per .NET **tupla** oggetto e il **ToValueTuple** metodo converte .NET **tupla** oggetto da una tupla di Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="d772a-219">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="d772a-220">L'esempio seguente crea una tupla, lo converte in .NET **tupla** oggetto e converte di nuovo in una tupla di Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="d772a-220">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="d772a-221">L'esempio confronta quindi la tupla con quello originale per verificare che siano uguali.</span><span class="sxs-lookup"><span data-stu-id="d772a-221">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="d772a-222">Vedere anche</span><span class="sxs-lookup"><span data-stu-id="d772a-222">See also</span></span>

- [<span data-ttu-id="d772a-223">Riferimenti per il linguaggio Visual Basic</span><span class="sxs-lookup"><span data-stu-id="d772a-223">Visual Basic Language Reference</span></span>](index.md)
