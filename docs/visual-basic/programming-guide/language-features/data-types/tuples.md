---
title: Tuple
ms.date: 04/23/2017
helpviewer_keywords:
- tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
ms.openlocfilehash: 378ee4e7d3a3b106b719e5da819b09f336ff218e
ms.sourcegitcommit: 67cf756b033c6173a1bbd1cbd5aef1fccac99e34
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 07/10/2020
ms.locfileid: "86226660"
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="5d930-102">Tuple (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="5d930-102">Tuples (Visual Basic)</span></span>

<span data-ttu-id="5d930-103">A partire da Visual Basic 2017, il linguaggio Visual Basic offre supporto incorporato per le tuple che semplificano la creazione di tuple e l'accesso agli elementi delle tuple.</span><span class="sxs-lookup"><span data-stu-id="5d930-103">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="5d930-104">Una tupla è una struttura di dati leggera con un numero e una sequenza di valori specifici.</span><span class="sxs-lookup"><span data-stu-id="5d930-104">A tuple is a lightweight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="5d930-105">Quando si crea un'istanza della tupla, si definiscono il numero e il tipo di dati di ogni valore (o elemento).</span><span class="sxs-lookup"><span data-stu-id="5d930-105">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="5d930-106">Ad esempio, una tupla con 2 elementi (o coppia) dispone di due elementi.</span><span class="sxs-lookup"><span data-stu-id="5d930-106">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="5d930-107">Il primo può essere un `Boolean` valore, mentre il secondo è `String` .</span><span class="sxs-lookup"><span data-stu-id="5d930-107">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="5d930-108">Poiché le tuple semplificano l'archiviazione di più valori in un singolo oggetto, vengono spesso usati come metodo semplice per restituire più valori da un metodo.</span><span class="sxs-lookup"><span data-stu-id="5d930-108">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="5d930-109">Il supporto della tupla richiede il <xref:System.ValueTuple> tipo.</span><span class="sxs-lookup"><span data-stu-id="5d930-109">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="5d930-110">Se il .NET Framework 4,7 non è installato, è necessario aggiungere il pacchetto NuGet `System.ValueTuple` , disponibile nella raccolta NuGet.</span><span class="sxs-lookup"><span data-stu-id="5d930-110">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="5d930-111">Senza questo pacchetto, potrebbe essere presente un errore di compilazione simile a "il tipo predefinito ' ValueTuple (of,,,)' non è definito né importato".</span><span class="sxs-lookup"><span data-stu-id="5d930-111">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="5d930-112">Creazione di istanze e utilizzo di una tupla</span><span class="sxs-lookup"><span data-stu-id="5d930-112">Instantiating and using a tuple</span></span>

<span data-ttu-id="5d930-113">Si crea un'istanza di una tupla racchiudendo tra parentesi i valori delimitati da virgole.</span><span class="sxs-lookup"><span data-stu-id="5d930-113">You instantiate a tuple by enclosing its comma-delimited values in parentheses.</span></span> <span data-ttu-id="5d930-114">Ognuno di questi valori diventa quindi un campo della tupla.</span><span class="sxs-lookup"><span data-stu-id="5d930-114">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="5d930-115">Il codice seguente, ad esempio, definisce una tripla (o una tupla con 3 elementi) con un `Date` come primo valore, un `String` come secondo e un `Boolean` come terzo.</span><span class="sxs-lookup"><span data-stu-id="5d930-115">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="5d930-116">Per impostazione predefinita, il nome di ogni campo in una tupla è costituito dalla stringa `Item` insieme alla posizione in base 1 del campo nella tupla.</span><span class="sxs-lookup"><span data-stu-id="5d930-116">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="5d930-117">Per questa tupla con 3 elementi, il campo è `Date` `Item1` , il campo `String` è `Item2` e il `Boolean` campo è `Item3` .</span><span class="sxs-lookup"><span data-stu-id="5d930-117">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="5d930-118">Nell'esempio seguente vengono visualizzati i valori dei campi della tupla di cui è stata creata un'istanza nella riga di codice precedente</span><span class="sxs-lookup"><span data-stu-id="5d930-118">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="5d930-119">I campi di una tupla Visual Basic sono di lettura/scrittura; Dopo avere creato un'istanza di una tupla, è possibile modificarne i valori.</span><span class="sxs-lookup"><span data-stu-id="5d930-119">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="5d930-120">Nell'esempio seguente vengono modificati due dei tre campi della tupla creata nell'esempio precedente e viene visualizzato il risultato.</span><span class="sxs-lookup"><span data-stu-id="5d930-120">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="5d930-121">Creazione di istanze e utilizzo di una tupla denominata</span><span class="sxs-lookup"><span data-stu-id="5d930-121">Instantiating and using a named tuple</span></span>

<span data-ttu-id="5d930-122">Anziché utilizzare nomi predefiniti per i campi di una tupla, è possibile creare un'istanza di una *tupla denominata* assegnando nomi personalizzati agli elementi della tupla.</span><span class="sxs-lookup"><span data-stu-id="5d930-122">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="5d930-123">È possibile accedere ai campi della tupla in base ai nomi assegnati *o* ai nomi predefiniti.</span><span class="sxs-lookup"><span data-stu-id="5d930-123">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="5d930-124">Nell'esempio seguente viene creata un'istanza della stessa tupla con 3 elementi, ad eccezione del fatto che assegna in modo esplicito il nome del primo campo `EventDate` , il secondo `Name` e il terzo `IsHoliday` .</span><span class="sxs-lookup"><span data-stu-id="5d930-124">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="5d930-125">Visualizza quindi i valori dei campi, li modifica e visualizza di nuovo i valori dei campi.</span><span class="sxs-lookup"><span data-stu-id="5d930-125">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="inferred-tuple-element-names"></a><span data-ttu-id="5d930-126">Nomi di elemento di tupla dedotti</span><span class="sxs-lookup"><span data-stu-id="5d930-126">Inferred tuple element names</span></span>

<span data-ttu-id="5d930-127">A partire da Visual Basic 15,3, Visual Basic possibile dedurre i nomi degli elementi della tupla. non è necessario assegnarli in modo esplicito.</span><span class="sxs-lookup"><span data-stu-id="5d930-127">Starting with Visual Basic 15.3, Visual Basic can infer the names of tuple elements; you do not have to assign them explicitly.</span></span> <span data-ttu-id="5d930-128">I nomi di tupla dedotti sono utili quando si Inizializza una tupla da un set di variabili e si desidera che il nome dell'elemento di tupla corrisponda al nome della variabile.</span><span class="sxs-lookup"><span data-stu-id="5d930-128">Inferred tuple names are useful when you initialize a tuple from a set of variables, and you want the tuple element name to be the same as the variable name.</span></span>

<span data-ttu-id="5d930-129">Nell'esempio seguente viene creata una `stateInfo` tupla contenente tre elementi denominati in modo esplicito,, `state` `stateName` e `capital` .</span><span class="sxs-lookup"><span data-stu-id="5d930-129">The following example creates a `stateInfo` tuple that contains three explicitly named elements, `state`, `stateName`, and `capital`.</span></span> <span data-ttu-id="5d930-130">Si noti che, nella denominazione degli elementi, l'istruzione di inizializzazione della tupla assegna semplicemente gli elementi denominati ai valori delle variabili denominate in modo identico.</span><span class="sxs-lookup"><span data-stu-id="5d930-130">Note that, in naming the elements, the tuple initialization statement simply assigns the named elements the values of the identically named variables.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#1)]

<span data-ttu-id="5d930-131">Poiché gli elementi e le variabili hanno lo stesso nome, il Visual Basic compilatore può dedurre i nomi dei campi, come illustrato nell'esempio riportato di seguito.</span><span class="sxs-lookup"><span data-stu-id="5d930-131">Because elements and variables have the same name, the Visual Basic compiler can infer the names of the fields, as the following example shows.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#2)]

<span data-ttu-id="5d930-132">Per abilitare i nomi di elementi di tupla derivati, è necessario definire la versione del compilatore Visual Basic da usare nel file del progetto di Visual Basic ( \* . vbproj):</span><span class="sxs-lookup"><span data-stu-id="5d930-132">To enable inferred tuple element names, you must define the version of the Visual Basic compiler to use in your Visual Basic project (\*.vbproj) file:</span></span>

```xml
<PropertyGroup>
  <LangVersion>15.3</LangVersion>
</PropertyGroup>
```

<span data-ttu-id="5d930-133">Il numero di versione può essere qualsiasi versione del compilatore Visual Basic a partire da 15,3.</span><span class="sxs-lookup"><span data-stu-id="5d930-133">The version number can be any version of the Visual Basic compiler starting with 15.3.</span></span> <span data-ttu-id="5d930-134">Anziché impostare come hardcoded una versione specifica del compilatore, è anche possibile specificare "più recente" come valore di `LangVersion` per la compilazione con la versione più recente del compilatore di Visual Basic installato nel sistema.</span><span class="sxs-lookup"><span data-stu-id="5d930-134">Rather than hard-coding a specific compiler version, you can also specify "Latest" as the value of `LangVersion` to compile with the most recent version of the Visual Basic compiler installed on your system.</span></span>

<span data-ttu-id="5d930-135">Per ulteriori informazioni, vedere [impostazione della versione della lingua Visual Basic](../../../language-reference/configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="5d930-135">For more information, see [setting the Visual Basic language version](../../../language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="5d930-136">In alcuni casi, il compilatore Visual Basic non è in grado di dedurre il nome dell'elemento di tupla dal nome del candidato e il campo tupla può essere usato solo con il nome predefinito, ad esempio, e così `Item1` `Item2` via. Sono incluse le seguenti:</span><span class="sxs-lookup"><span data-stu-id="5d930-136">In some cases, the Visual Basic compiler cannot infer the tuple element name from the candidate name, and the tuple field can only be referenced using its default name, such as `Item1`, `Item2`, etc. These include:</span></span>

- <span data-ttu-id="5d930-137">Il nome del candidato corrisponde al nome di un membro di tupla, ad esempio `Item3` , `Rest` o `ToString` .</span><span class="sxs-lookup"><span data-stu-id="5d930-137">The candidate name is the same as the name of a tuple member, such as `Item3`, `Rest`, or `ToString`.</span></span>

- <span data-ttu-id="5d930-138">Il nome candidato viene duplicato nella tupla.</span><span class="sxs-lookup"><span data-stu-id="5d930-138">The candidate name is duplicated in the tuple.</span></span>

<span data-ttu-id="5d930-139">Quando l'inferenza del nome campo ha esito negativo, Visual Basic non genera un errore del compilatore, né un'eccezione generata in fase di esecuzione.</span><span class="sxs-lookup"><span data-stu-id="5d930-139">When field name inference fails, Visual Basic does not generate a compiler error, nor is an exception thrown at runtime.</span></span> <span data-ttu-id="5d930-140">Al contrario, è necessario fare riferimento ai campi di tupla con i relativi nomi predefiniti, ad esempio `Item1` e `Item2` .</span><span class="sxs-lookup"><span data-stu-id="5d930-140">Instead, tuple fields must be referenced by their predefined names, such as `Item1` and `Item2`.</span></span>

## <a name="tuples-versus-structures"></a><span data-ttu-id="5d930-141">Tuple e strutture</span><span class="sxs-lookup"><span data-stu-id="5d930-141">Tuples versus structures</span></span>

<span data-ttu-id="5d930-142">Una tupla Visual Basic è un tipo di valore che è un'istanza di uno dei tipi generici **System. ValueTuple** .</span><span class="sxs-lookup"><span data-stu-id="5d930-142">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="5d930-143">Ad esempio, la `holiday` tupla definita nell'esempio precedente è un'istanza della <xref:System.ValueTuple%603> struttura.</span><span class="sxs-lookup"><span data-stu-id="5d930-143">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="5d930-144">È progettato per essere un contenitore leggero per i dati.</span><span class="sxs-lookup"><span data-stu-id="5d930-144">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="5d930-145">Poiché la tupla ha lo scopo di semplificare la creazione di un oggetto con più elementi di dati, mancano alcune delle funzionalità che possono avere una struttura personalizzata.</span><span class="sxs-lookup"><span data-stu-id="5d930-145">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="5d930-146">Queste includono:</span><span class="sxs-lookup"><span data-stu-id="5d930-146">These include:</span></span>

- <span data-ttu-id="5d930-147">Membri personalizzati.</span><span class="sxs-lookup"><span data-stu-id="5d930-147">Custom members.</span></span> <span data-ttu-id="5d930-148">Non è possibile definire proprietà, metodi o eventi personalizzati per una tupla.</span><span class="sxs-lookup"><span data-stu-id="5d930-148">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="5d930-149">Convalida.</span><span class="sxs-lookup"><span data-stu-id="5d930-149">Validation.</span></span> <span data-ttu-id="5d930-150">Non è possibile convalidare i dati assegnati ai campi.</span><span class="sxs-lookup"><span data-stu-id="5d930-150">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="5d930-151">Immutabilità.</span><span class="sxs-lookup"><span data-stu-id="5d930-151">Immutability.</span></span> <span data-ttu-id="5d930-152">Visual Basic tuple sono modificabili.</span><span class="sxs-lookup"><span data-stu-id="5d930-152">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="5d930-153">Una struttura personalizzata, invece, consente di controllare se un'istanza è modificabile o non modificabile.</span><span class="sxs-lookup"><span data-stu-id="5d930-153">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="5d930-154">Se i membri personalizzati, la convalida di proprietà e campi o l'immutabilità sono importanti, è consigliabile utilizzare l'istruzione Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) per definire un tipo di valore personalizzato.</span><span class="sxs-lookup"><span data-stu-id="5d930-154">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="5d930-155">Una tupla Visual Basic eredita i membri del tipo **ValueTuple** .</span><span class="sxs-lookup"><span data-stu-id="5d930-155">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="5d930-156">Oltre ai relativi campi, includono i metodi seguenti:</span><span class="sxs-lookup"><span data-stu-id="5d930-156">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="5d930-157">Membro</span><span class="sxs-lookup"><span data-stu-id="5d930-157">Member</span></span> | <span data-ttu-id="5d930-158">Descrizione</span><span class="sxs-lookup"><span data-stu-id="5d930-158">Description</span></span> |
| ---|---|
| <span data-ttu-id="5d930-159">CompareTo</span><span class="sxs-lookup"><span data-stu-id="5d930-159">CompareTo</span></span> | <span data-ttu-id="5d930-160">Confronta la tupla corrente con un'altra tupla con lo stesso numero di elementi.</span><span class="sxs-lookup"><span data-stu-id="5d930-160">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="5d930-161">Uguale a</span><span class="sxs-lookup"><span data-stu-id="5d930-161">Equals</span></span> | <span data-ttu-id="5d930-162">Determina se la tupla corrente è uguale a un'altra tupla o a un altro oggetto.</span><span class="sxs-lookup"><span data-stu-id="5d930-162">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="5d930-163">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="5d930-163">GetHashCode</span></span> | <span data-ttu-id="5d930-164">Calcola il codice hash per l'istanza corrente.</span><span class="sxs-lookup"><span data-stu-id="5d930-164">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="5d930-165">ToString</span><span class="sxs-lookup"><span data-stu-id="5d930-165">ToString</span></span> | <span data-ttu-id="5d930-166">Restituisce la rappresentazione di stringa di questa tupla, che assume il formato `(Item1, Item2...)` , dove `Item1` e `Item2` rappresentano i valori dei campi della tupla.</span><span class="sxs-lookup"><span data-stu-id="5d930-166">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="5d930-167">Inoltre, i tipi **ValueTuple** implementano <xref:System.Collections.IStructuralComparable> le <xref:System.Collections.IStructuralEquatable> interfacce e, che consentono di definire gli operatori di confronto dei clienti.</span><span class="sxs-lookup"><span data-stu-id="5d930-167">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define customer comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="5d930-168">Assegnazione e tuple</span><span class="sxs-lookup"><span data-stu-id="5d930-168">Assignment and tuples</span></span>

<span data-ttu-id="5d930-169">Visual Basic supporta l'assegnazione tra tipi di tupla che hanno lo stesso numero di campi.</span><span class="sxs-lookup"><span data-stu-id="5d930-169">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="5d930-170">I tipi di campo possono essere convertiti se si verifica una delle condizioni seguenti:</span><span class="sxs-lookup"><span data-stu-id="5d930-170">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="5d930-171">Il campo di origine e di destinazione sono dello stesso tipo.</span><span class="sxs-lookup"><span data-stu-id="5d930-171">The source and target field are of the same type.</span></span>

- <span data-ttu-id="5d930-172">Viene definita una conversione verso un tipo di dati più ampio o implicito del tipo di origine nel tipo di destinazione.</span><span class="sxs-lookup"><span data-stu-id="5d930-172">A widening (or implicit) conversion of the source type to the target type is defined.</span></span>

- <span data-ttu-id="5d930-173">`Option Strict`è `On` e viene definita una conversione verso un tipo di dati più piccolo (o esplicito) del tipo di origine nel tipo di destinazione.</span><span class="sxs-lookup"><span data-stu-id="5d930-173">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="5d930-174">Questa conversione può generare un'eccezione se il valore di origine non è compreso nell'intervallo del tipo di destinazione.</span><span class="sxs-lookup"><span data-stu-id="5d930-174">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="5d930-175">Non sono considerate altre conversioni per le assegnazioni.</span><span class="sxs-lookup"><span data-stu-id="5d930-175">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="5d930-176">Esaminiamo i tipi di assegnazioni consentiti tra i tipi di tupla.</span><span class="sxs-lookup"><span data-stu-id="5d930-176">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="5d930-177">Considerare le variabili usate negli esempi seguenti:</span><span class="sxs-lookup"><span data-stu-id="5d930-177">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="5d930-178">Le prime due variabili, `unnamed` e `anonymous` , non hanno nomi semantici forniti per i campi.</span><span class="sxs-lookup"><span data-stu-id="5d930-178">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="5d930-179">I nomi dei campi sono i valori predefiniti `Item1` e `Item2` .</span><span class="sxs-lookup"><span data-stu-id="5d930-179">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="5d930-180">Le ultime due variabili `named` e `differentName` hanno nomi di campo semantico.</span><span class="sxs-lookup"><span data-stu-id="5d930-180">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="5d930-181">Si noti che queste due tuple presentano nomi diversi per i campi.</span><span class="sxs-lookup"><span data-stu-id="5d930-181">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="5d930-182">Tutte e quattro le tuple hanno lo stesso numero di campi (definito "grado") e i tipi di tali campi sono identici.</span><span class="sxs-lookup"><span data-stu-id="5d930-182">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="5d930-183">Pertanto, tutte queste assegnazioni funzionano:</span><span class="sxs-lookup"><span data-stu-id="5d930-183">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="5d930-184">Si noti che non sono assegnati i nomi delle tuple.</span><span class="sxs-lookup"><span data-stu-id="5d930-184">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="5d930-185">I valori dei campi vengono assegnati seguendo l'ordine dei campi nella tupla.</span><span class="sxs-lookup"><span data-stu-id="5d930-185">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="5d930-186">Si noti infine che è possibile assegnare la `named` tupla alla `conversion` tupla, anche se il primo campo di `named` è un `Integer` e il primo campo di `conversion` è `Long` .</span><span class="sxs-lookup"><span data-stu-id="5d930-186">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="5d930-187">Questa assegnazione ha esito positivo perché `Integer` la conversione di un oggetto in è una conversione verso un tipo di oggetto più `Long` ampio</span><span class="sxs-lookup"><span data-stu-id="5d930-187">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="5d930-188">Le tuple con numeri diversi di campi non possono essere assegnate:</span><span class="sxs-lookup"><span data-stu-id="5d930-188">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="5d930-189">Tuple come valori restituiti dal metodo</span><span class="sxs-lookup"><span data-stu-id="5d930-189">Tuples as method return values</span></span>

<span data-ttu-id="5d930-190">Un metodo può restituire solo un valore singolo.</span><span class="sxs-lookup"><span data-stu-id="5d930-190">A method can return only a single value.</span></span> <span data-ttu-id="5d930-191">Spesso, tuttavia, si vuole che una chiamata al metodo restituisca più valori.</span><span class="sxs-lookup"><span data-stu-id="5d930-191">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="5d930-192">Esistono diversi modi per ovviare a questa limitazione:</span><span class="sxs-lookup"><span data-stu-id="5d930-192">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="5d930-193">È possibile creare una classe o una struttura personalizzata le cui proprietà o campi rappresentano i valori restituiti dal metodo.</span><span class="sxs-lookup"><span data-stu-id="5d930-193">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="5d930-194">Quindi è una soluzione pesante; è necessario definire un tipo personalizzato il cui unico scopo è recuperare i valori da una chiamata al metodo.</span><span class="sxs-lookup"><span data-stu-id="5d930-194">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="5d930-195">È possibile restituire un singolo valore dal metodo e restituire i valori rimanenti passandoli per riferimento al metodo.</span><span class="sxs-lookup"><span data-stu-id="5d930-195">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="5d930-196">Questo implica il sovraccarico della creazione di un'istanza di una variabile e rischia di sovrascrivere inavvertitamente il valore della variabile passata per riferimento.</span><span class="sxs-lookup"><span data-stu-id="5d930-196">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="5d930-197">È possibile utilizzare una tupla, che fornisce una soluzione leggera per il recupero di più valori restituiti.</span><span class="sxs-lookup"><span data-stu-id="5d930-197">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="5d930-198">Ad esempio, i metodi **TryParse** in .NET restituiscono un `Boolean` valore che indica se l'operazione di analisi ha avuto esito positivo.</span><span class="sxs-lookup"><span data-stu-id="5d930-198">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="5d930-199">Il risultato dell'operazione di analisi viene restituito in una variabile passata per riferimento al metodo.</span><span class="sxs-lookup"><span data-stu-id="5d930-199">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="5d930-200">In genere, una chiamata al metodo di analisi, ad esempio, ha un <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> aspetto simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="5d930-200">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="5d930-201">Se si esegue il wrapping della chiamata al <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> metodo nel metodo personalizzato, è possibile restituire una tupla dall'operazione di analisi.</span><span class="sxs-lookup"><span data-stu-id="5d930-201">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="5d930-202">Nell'esempio seguente viene `NumericLibrary.ParseInteger` chiamato il <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> metodo e viene restituita una tupla denominata con due elementi.</span><span class="sxs-lookup"><span data-stu-id="5d930-202">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="5d930-203">È quindi possibile chiamare il metodo con codice simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="5d930-203">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="5d930-204">Visual Basic Tuple e Tuple nell'.NET Framework</span><span class="sxs-lookup"><span data-stu-id="5d930-204">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="5d930-205">Una tupla Visual Basic è un'istanza di uno dei tipi generici **System. ValueTuple** introdotti nella .NET Framework 4,7.</span><span class="sxs-lookup"><span data-stu-id="5d930-205">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="5d930-206">Il .NET Framework include anche un set di classi **System. Tuple** generiche.</span><span class="sxs-lookup"><span data-stu-id="5d930-206">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="5d930-207">Queste classi, tuttavia, differiscono da Visual Basic Tuple e dai tipi generici **System. ValueTuple** in diversi modi:</span><span class="sxs-lookup"><span data-stu-id="5d930-207">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="5d930-208">Gli elementi delle classi **Tuple** sono proprietà denominate `Item1` , `Item2` e così via.</span><span class="sxs-lookup"><span data-stu-id="5d930-208">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="5d930-209">In Visual Basic Tuple e i tipi **ValueTuple** , gli elementi della tupla sono campi.</span><span class="sxs-lookup"><span data-stu-id="5d930-209">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="5d930-210">Non è possibile assegnare nomi significativi agli elementi di un'istanza di **tupla** o di un'istanza di **ValueTuple** .</span><span class="sxs-lookup"><span data-stu-id="5d930-210">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="5d930-211">Visual Basic consente di assegnare nomi che comunicano il significato dei campi.</span><span class="sxs-lookup"><span data-stu-id="5d930-211">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="5d930-212">Le proprietà di un'istanza di **tupla** sono di sola lettura. le tuple non sono modificabili.</span><span class="sxs-lookup"><span data-stu-id="5d930-212">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="5d930-213">In Visual Basic Tuple e i tipi **ValueTuple** , i campi di tupla sono di lettura/scrittura; le tuple sono modificabili.</span><span class="sxs-lookup"><span data-stu-id="5d930-213">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="5d930-214">I tipi di **tupla** generici sono tipi di riferimento.</span><span class="sxs-lookup"><span data-stu-id="5d930-214">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="5d930-215">L'utilizzo di questi tipi di **tupla** indica l'allocazione di oggetti.</span><span class="sxs-lookup"><span data-stu-id="5d930-215">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="5d930-216">Nei percorsi critici, ciò può avere un impatto notevole sulle prestazioni dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="5d930-216">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="5d930-217">Visual Basic Tuple e i tipi **ValueTuple** sono tipi di valore.</span><span class="sxs-lookup"><span data-stu-id="5d930-217">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="5d930-218">I metodi di estensione nella <xref:System.TupleExtensions> classe facilitano la conversione tra Visual Basic Tuple e gli oggetti **tupla** .NET.</span><span class="sxs-lookup"><span data-stu-id="5d930-218">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="5d930-219">Il metodo **ToTuple** converte una tupla di Visual Basic in un oggetto **tupla** .NET e il metodo **ToValueTuple** converte un oggetto **tupla** .NET in una tupla di Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="5d930-219">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="5d930-220">Nell'esempio seguente viene creata una tupla, viene convertita in un oggetto **tupla** .NET e riconvertita in una tupla Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="5d930-220">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="5d930-221">Nell'esempio viene quindi confrontata la tupla con quella originale per verificare che siano uguali.</span><span class="sxs-lookup"><span data-stu-id="5d930-221">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="5d930-222">Vedi anche</span><span class="sxs-lookup"><span data-stu-id="5d930-222">See also</span></span>

- [<span data-ttu-id="5d930-223">Riferimenti al linguaggio Visual Basic</span><span class="sxs-lookup"><span data-stu-id="5d930-223">Visual Basic Language Reference</span></span>](index.md)
